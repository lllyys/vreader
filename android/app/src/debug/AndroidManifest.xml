<?xml version="1.0" encoding="utf-8"?>
<!-- DEBUG-only manifest additions (feature #114 WI-2): the backup/restore UI debug launcher.
     Excluded from the release APK. exported=true so it can be started via `adb shell am start`
     for emulator verification; it carries no production entry point. -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application>
        <activity
            android:name=".backup.BackupDebugActivity"
            android:exported="true" />
    </application>
</manifest>
