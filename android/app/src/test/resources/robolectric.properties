# Robolectric 4.13 ships SDK runtimes up to API 34; the app targetSdk is 36, so
# pin unit tests to the highest Robolectric-supported SDK (feature #106 WI-1).
# Unit tests exercise JVM logic + Android stubs — the simulated SDK runtime need
# not match targetSdk.
sdk=34
