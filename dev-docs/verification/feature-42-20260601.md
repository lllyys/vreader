---
kind: feature
id: 42
status_target: DONE
commit_sha: 9aba219c450d3f30513a741c234a9fee08af001a
app_version: 3.42.14 (build 777)
date: 2026-06-01
verifier: claude
device_or_simulator: iPhone 17 Pro Simulator
os_version: iOS 26.4
build_configuration: Debug
backend: OpenRouter (openrouter/owl-alpha, free tier) for the AI criterion
result: pass
---

# Feature #42 Phase-1 (Readium EPUB engine) — FULL acceptance pass (all 10 criteria CU-free)

Supersedes the 2026-05-29 `partial` pass. The three AI/gesture/photo partials
(#4, #6, #8) and the footnote live-tap (#10) are now all verified CU-free, using
harness capabilities built this session (idb HID-gesture selection, `--enable-ai`
gate completion, container-injected custom background, exact-rect link tapping).

## Acceptance criteria

| # | Criterion | Observed (CU-free) | Result |
|---|---|---|---|
| 1 / 1b | Render + RTL/CJK | querySelectorAll('p')=3; rtl dir=rtl; cjk ch2 writing-mode=vertical-rl | ✅ |
| 2 | Position save/restore | open ch1 → nav ch2 → reopen = ch2 | ✅ |
| 3 | Theme/font | theme=dark,fontSize=24 → body ≈23.9px | ✅ |
| 4 | **Highlight CREATE** | idb `ui tap --duration 0.8` long-press → SelectablePopover → Yellow → highlightCount 0→1 + rendered | ✅ NEW |
| 5 | Search-nav | search 'filler' → spine flips ch1→ch2 | ✅ |
| 6 | **Bilingual render** | provider configured + bilingual on → EN→中 interlinear renders (第二章的存在是为了…) | ✅ NEW |
| 7 | TTS follow | tts start → ttsState=speaking, offset advances; stop → idle | ✅ |
| 8 | **Photo full-transparency** | stored bg image + theme=photo → html:root + body rgba(0,0,0,0) | ✅ NEW |
| 9 | Navigation | cross-spine via .readerNavigateToLocator ch1↔ch2 | ✅ |
| 10 | **Footnote live-tap** | eval noteref rect (132,533) + 62pt inset → tap (132,595) → document.location.href ch1→ch2 (note target) | ✅ NEW |

## Commands run

```bash
# AI criterion: --enable-ai (now grants consent+aiAssistant, v3.42.14) + provider?action=add (OpenRouter)
# Highlight: idb ui tap --duration 0.8 <x> <y> → label "Yellow highlight"; snapshot highlightCount 0→1
# Photo: write JPG to <container>/Library/Application Support/ThemeBackgrounds/photo.jpg + -readerUseCustomBackground YES + theme?mode=photo; eval html/body backgroundColor
# Footnote: eval noteref getBoundingClientRect via bridge=epub → screen=(content + (screen.height-vh) inset) → idb tap → eval document.location.href
```

## Observations

- The prior `partial` reasons ("AI-provider-gated", "WKWebView selection not
  XCUITest-synthesizable", "image-picker gesture", "footnote tap requires CU")
  were all resolvable CU-free — idb HID events synthesize selection; `--enable-ai`
  needed the consent/flag gates (fixed); the bg image + the link rect can be set
  / read directly via the container + the eval bridge.

## Scope note

This verifies Feature #42 **Phase 1** (Readium EPUB engine). The feature row stays
**IN PROGRESS** because #42 is re-scoped to also include **Phase 2 — libmobi
convert-on-import** (Kindle AZW3/MOBI → EPUB at import), which is a separate large
undertaking and unstarted.

## Artifacts

- feature-42-bilingual-render-VERIFIED-20260601.png
- feature-42-highlight-create-VERIFIED-20260601.png
- feature-42-photo-transparency-VERIFIED-20260601.png
- feature-42-footnote-followed-VERIFIED-20260601.png
