---
kind: feature
id: 118
status_target: VERIFIED
commit_sha: 35de74db561f4ee602bbc609754f17d37253fd47
app_version: android/0.8.0 (versionCode 39)
date: 2026-06-22
verifier: claude
device_or_simulator: Pixel emulator (vreader-test AVD), emulator-5554
os_version: Android (API 35, compileSdk/targetSdk 36)
build_configuration: Debug
backend: python3 OpenAI-compatible SSE stub (throwaway, 127.0.0.1:<ephemeral>, reached from the emulator at 10.0.2.2)
result: pass
---

# Feature #118 — Android AI provider + chat/summary — VERIFIED

Implements the "VReader AI Provider & Chat" canvas (design import #1798): the provider gate
(list + editor + test-connection), the AI client (OpenAI-compatible + Anthropic, bounded SSE
streaming), and the chat/summary panel. Bilingual interlinear is the separate follow-on #119.

## Acceptance criteria

| # | Criterion | Observed | Result |
|---|---|---|---|
| 1 | Provider config persists (DataStore + Keystore); API key never in plaintext | `AiProviderStore` keeps the key only as a `KeystoreSecretCipher` token; 9 JVM `AiProviderStoreTest`/`AiProviderKindTest` (WI-1) | ✅ pass |
| 2 | Both providers stream a chat completion (SSE→Flow) + one-shot + test-connection, typed errors | 25 JVM AI tests (WI-2): OpenAI 7 + Anthropic 4 + SSE framer 5 + store 6 + kind 3 — `data:`/`[DONE]` + `content_block_delta`/`message_stop`/`error`, auth/429/offline mapping | ✅ pass |
| 3 | Provider list + editor (per the design) configure/test a provider | 7 instrumented Compose tests (WI-3) green on emulator-5554 (list 2 + editor 5) + 3 VM tests | ✅ pass |
| 4 | Chat panel streams an answer + renders a cached summary + gates when unconfigured | 4 instrumented `AiChatPanelTest` (WI-4) green + 5 markdown + 3 chat-VM tests; SHA-256 summary cache | ✅ pass |
| 5 | **Connected**: against a live local OpenAI-compatible SSE stub on the emulator, test-connection succeeds + a sent prompt streams an assembled answer | `AiRoundTripConnectedTest` (WI-5) green on emulator-5554: the REAL `OpenAiCompatibleProvider` did `testConnection()` (one-shot → Ok) and `streamChat()` (assembled "Hello from the vreader stub." over a real socket SSE stream) | ✅ pass |

## Commands run

```bash
# Live AI round-trip (stands up an OpenAI-compatible SSE stub, runs the connected test on the
# emulator, kills the stub by exact PID):
JAVA_HOME=.../openjdk@17 ANDROID_HOME=.../android-commandlinetools scripts/run-ai-roundtrip.sh
# → RUN-ANDROID-TESTS RESULT: SUCCEEDED
# → RUN-AI-ROUNDTRIP RESULT: SUCCEEDED (exit 0)

# Per-WI suites:
ANDROID_CMD="./gradlew :app:testDebugUnitTest --tests 'com.vreader.app.ai.*'" scripts/run-android-tests.sh
ANDROID_CMD="./gradlew :app:connectedDebugAndroidTest -Pandroid.testInstrumentationRunnerArguments.package=com.vreader.app.ai" scripts/run-android-verify.sh
```

## Observations

- The on-device SSE stream over the real `HttpURLConnection` works — the bounded `SseEventReader` +
  the prompt-cancellation handle behaved correctly against a real socket (JVM ServerSocket fakes
  prove the parse; this proves the Android transport + the loopback host alias 10.0.2.2 path).
- `KeystoreSecretCipher` (the on-device AES-256-GCM credential storage) is exercised via the
  provider editor/store path (also on-device verified in #116).
- The `https-or-loopback-only` key guard correctly allows the `10.0.2.2` emulator host for the test
  while refusing cleartext to any remote host.

## Artifacts

- Connected report: `android/app/build/reports/androidTests/connected/debug/com.vreader.app.ai.AiRoundTripConnectedTest.html`
- Harness: `scripts/run-ai-roundtrip.sh`
- Per-WI Gate-4 audits: `.claude/codex-audits/feat-feature-118-wi-*.md`
