---
kind: feature
id: 58
status_target: DONE
commit_sha: 6e9afe8f
app_version: 3.39.5 (build 626)
date: 2026-05-22
verifier: claude
device_or_simulator: iPhone 17 Pro Simulator (UDID 61149F0E-DC18-4BE2-BB37-52659F1F4F62)
os_version: iOS 26.x
build_configuration: Debug
backend: rclone WebDAV 127.0.0.1:8082 (test) — for criteria e/f path; integration test used in practice
result: partial
---

# Feature #58 — Reading-time + activity dashboard — Gate-5b verification (round 1)

Verify-cron Gate-5b attempt against merged `main` `6e9afe8f` (v3.39.5), after the feature-cron Gate-6 DONE flip (PR #1135). **Result: partial — row stays at `DONE`, NOT flipped to `VERIFIED`.** Criteria (a) + (e) + (f) verified; criteria (b)/(c)/(d) are harness-blocked CU-free and deferred behind newly-filed Bug #263 / GH #1138.

## Acceptance criteria

| # | Criterion | Method | Observed | Verdict |
|---|---|---|---|---|
| a | Dashboard reachable from Settings → profile card → Stats (D1=A) | CU-free — Feature #67's `settingsProfileStatsButton` → `.openReadingStatsRequested` → presents `ReadingDashboardView` | Confirmed via the existing Feature #67 WI-4 artifact `dev-docs/verification/artifacts/feature-67-wi-4-04-stats-sheet-20260520.png` — the dashboard presents with the 7-pill window bar + 5-column per-book table | **PASS** |
| b | All 7 time windows render correct totals on a fixture with seeded sessions | (intended) seed `ReadingSession` records → open dashboard → assert per-window totals | **BLOCKED** — no harness to seed `ReadingSession` rows (TestSeeder/DebugBridge seed books only). Dashboard renders all-zero data; correct-totals cannot be exercised CU-free | **DEFERRED (Bug #263)** |
| c | Per-book table shows time/notes/highlights counts, sortable on all columns | (intended) seeded-session device exercise of the table + sort | **BLOCKED** — same session-seeding gap; the table is empty without seeded sessions | **DEFERRED (Bug #263)** |
| d | Sort selection persists across app launches | (intended) set sort → relaunch → assert persisted | **BLOCKED** — depends on a populated table (b/c) to observe a meaningful sort | **DEFERRED (Bug #263)** |
| e | `BackupDataCollector` emits `reading-history.json`; restore reproduces totals | High-fidelity integration test `BackupReadingHistoryTests` (real `BackupDataCollector` → envelope → `BackupDataRestorer` boundary) | `xcodebuild test` clean build — 87 tests / 6 suites **passed** (incl. the reading-history collect→emit→restore round-trip) | **PASS** |
| f | Round-trip: backup → wipe → restore preserves `ReadingSession` + `ReadingStats` exactly | Same integration suite (the round-trip path) | Round-trip preservation asserted by the suite; WebDAV server (`rclone serve webdav 127.0.0.1:8082`) available but the device WebDAV-profile config is not CU-free drivable, so the integration test is the authoritative e/f evidence per the verify-skill's authorized path | **PASS** |

## Commands run

```bash
# Logic + backup round-trip (criteria e/f) — clean build per Bug #259
xcodebuild test -project vreader.xcodeproj -scheme vreader \
  -destination 'platform=iOS Simulator,id=61149F0E-DC18-4BE2-BB37-52659F1F4F62' \
  -only-testing:vreaderTests/BackupReadingHistoryTests \
  -parallel-testing-enabled NO -derivedDataPath build/verify-58
# → Test run with 87 tests in 6 suites passed. ** TEST SUCCEEDED **
```

```
# Criterion a (reachability) — confirmed via the committed Feature #67 WI-4 artifact
dev-docs/verification/artifacts/feature-67-wi-4-04-stats-sheet-20260520.png
```

## Observations

- Feature #58's implementation is complete on `main` (all WIs merged); the dashboard surface, the 7-window aggregator, the 5-column sortable table, and the WebDAV reading-history backup section all exist and compile.
- The **brief's premise was wrong**: there is no `vreader-debug://stats` entry and no session-seeding launch arg. `DebugCommand.swift` has no `stats` case; TestSeeder/VReaderApp launch args seed only books; `DebugSnapshot` has no stats capture. The dashboard is reachable only through Feature #67's profile-card Stats button.
- Criteria (a) reachability and (e/f) backup round-trip are both verifiable CU-free (artifact + integration test) and both PASS.
- Criteria (b/c/d) require the dashboard to render real per-window / per-book totals, which requires seeded `ReadingSession` records — and no harness exists to seed them. This is a genuine verification-harness gap, filed as **Bug #263 / GH #1138** (same class as the provider/present/ai-action/build-URL-scheme harness bugs fixed this session). Once #263 lands a session-seeding mechanism, a round-2 can exercise b/c/d and flip #58 → VERIFIED.

## Artifacts

- `dev-docs/verification/artifacts/feature-67-wi-4-04-stats-sheet-20260520.png` (dashboard reachability, criterion a — committed under Feature #67's WI-4 evidence).
- `BackupReadingHistoryTests` suite (criteria e/f) — green on `6e9afe8f`.
