---
kind: feature
id: 106
status_target: VERIFIED
commit_sha: 7c84fdbc65b0dd6658e217c4d84719ba8af3b820
app_version: android/0.3.0 (versionCode 9)
date: 2026-06-18
verifier: claude
device_or_simulator: Pixel emulator (vreader-test AVD), emulator-5554
os_version: Android (API 35, compileSdk/targetSdk 36)
build_configuration: Debug
backend: n/a (local EPUB)
result: pass
---

# Feature #106 — Android Phase 2 foundation bar — VERIFIED

The foundation bar (import → open → resume ONE EPUB end-to-end; the `contracts/`
conformance lane alive in Kotlin; release/version plumbing; an automatable
verification story) is complete and verified on a real Android emulator, including
a **real-book** end-to-end pass.

## Acceptance criteria

| # | Criterion | Observed | Result |
|---|---|---|---|
| 1 | `android/app` builds (pinned toolchain, JDK 17/Gradle 8.14.4) + `:app:testDebugUnitTest` green | 48 unit tests pass via `scripts/run-android-tests.sh` | ✅ |
| 2 | `contracts/` conformance lane runs against the shared `:identity` module the app depends on | `contracts/conformance/run.sh kotlin` → CONFORMANCE RESULT: PASS (drives `:identity:test`, the same module `:app` consumes) | ✅ |
| 3 | Import a **real** EPUB → app-private storage → keyed by the **exact-match fingerprint of the LOCAL artifact**; survives a process restart | **Real book** "The Half Second" (1.3MB): the stored artifact's on-device `sha256` == the host `sha256` ⇒ identical canonical key `epub:71fa…4a31:1302140` (exact-match identity holds for a real book). WI-4 `BookImporterTest.import_thenColdRestart_reopensFromLocalStorage_identityHolds` proves cold-restart identity. WI-8 `LibraryViewModelTest.import_epub_addsToLibrary` proves SAF-import → Library. | ✅ |
| 4 | Open in the Readium scroll reader; render; resume | Tapping the book opens `ReaderActivity`; Readium renders the **real** EPUB's own styled cover faithfully (`feature-106-realbook-render-20260618.png`) + the minimal `minimal.epub` ("Chapter 1 / Call me Ishmael."). `ReaderActivityTest.opensStoredEpub_rendersNavigator_andMarksOpened` (instrumented) confirms render + `markOpened`. Resume is precise-first / canonical-fallback (WI-6 `ResumeResolverTest`). | ✅ |
| 5 | Config-change / process-death recovers current book + last position | `ReaderActivityTest.backgrounding_flushesReadingPosition` (instrumented): backgrounding the reader flushes the current Readium position to Room via the `onStop` process-scope flush; `computeInitialLocator` restores it on reopen. The reader recreates fresh from the persisted intent + saved position. | ✅ |
| 6 | `android/version.properties` + an `android/vX.Y.Z` tag | `android/v0.1.0`…`android/v0.3.0` tags cut per WI (rule 40) | ✅ |
| 7 | Verifiable via `scripts/run-android-verify.sh` (instrumentation) | `:app:connectedDebugAndroidTest` on emulator-5554 → SUCCEEDED (4 instrumented tests) | ✅ |

## Commands run

```bash
# unit + conformance
scripts/run-android-tests.sh   # ANDROID_CMD=":app:testDebugUnitTest" → 48 tests pass
contracts/conformance/run.sh kotlin   # CONFORMANCE RESULT: PASS
# instrumented (emulator-5554)
scripts/run-android-verify.sh  # ANDROID_CMD=":app:connectedDebugAndroidTest" → 4 tests pass
# real-book identity + render
shasum -a 256 "The Half Second - Li Xiaolai.epub"   # == on-device sha256 of the stored artifact
adb shell am start … MainActivity ; input tap (book) → ReaderActivity renders the real EPUB
```

## Observations

- The design-gated UI surfaces (#1744 Library, #1745 reader host) were unblocked by
  **reusing the committed iOS `vreader-fidelity-v1` design bundle** as the Android
  design source (user-directed, rule-51-compliant — implementing a committed design,
  not inventing UI). Both shipped + emulator-verified (WI-8 / WI-9).
- Readium renders a real EPUB's own CSS/styling faithfully (the "The Half Second"
  cover with its serif title, typewriter illustration, and ruler graphic).

## Artifacts

- `dev-docs/verification/artifacts/feature-106-wi8-library-{empty,grid,list}-20260618.png`
- `dev-docs/verification/artifacts/feature-106-wi9-reader-render-20260618.png`
- `dev-docs/verification/artifacts/feature-106-realbook-render-20260618.png`

## Known limitations (Phase 3 — out of foundation-bar scope)

AZW3/PDF/TXT/MD readers, AI/TTS/book-sources/backup, the rich reader controls
(TOC/highlights/settings/search), grid/list genre filters, real-EPUB metadata
(author/cover/progress) extraction, and the canonical-progression resume fallback
(precise Readium-locator restore is implemented; the progression-only degraded path
is a documented follow-on). These are tracked via `docs/parity/README.md`.
