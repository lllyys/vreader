---
kind: feature
id: 73
status_target: VERIFIED
commit_sha: dcf14b24cdb0ada485b9b19d5ccb390c57ee1e1a
app_version: 3.41.8 (build 734)
date: 2026-05-31
verifier: claude
device_or_simulator: iPhone 17 Pro Simulator
os_version: iOS 26.4
build_configuration: Debug
backend: n/a
result: pass
---

## Acceptance criteria

| Criterion | Method | Observed | Result |
|---|---|---|---|
| AZW3/MOBI scroll mode crosses chapter boundaries smoothly (no D1/D2/D3 jump) | CU drag-scroll on mini-azw3 (7 sections) | Forward 0→1→2 + back: continuous prose across former section seams, chapter indicator tracks 1↔3, no stuck/blank/jump | pass |
| K-window mounts + slides + evicts | CU-free `vreader-debug://eval` (`foliate-view.renderer.getContents()`) | window mounts `[0,1,2]`, slides `[1,2,3]↔[3,4,5]` over 28 programmatic next/prev with no teardown | pass |
| DOM order matches reading order | CU-free eval of mounted iframes' `frameElement.top` after slide | `[1,2,3]` monotonic (the `[1,3,2]` insertion bug fixed) | pass |
| Bug #265 position restore preserved (section + intra-section) | CU + eval: read to mid-section, close, reopen | restored to the exact passage (window `[1,2,3]`, identical content) | pass |
| Selection / overlays wired on neighbour sections | CU-free eval | `getContents` overlayers `[true,true,true]` on mount + after slide | pass |
| Flag-off parity (paged + non-windowed unchanged) | `FoliatePaginatorScrollBoundaryTests` + `FoliateScrolledWindowMathTests` | GREEN on every commit | pass |

## Commands run

```bash
# flag-on build + install + open mini-azw3, then eval window/DOM state:
xcrun simctl openurl <SIM> "vreader-debug://eval?bridge=...&js=<base64>"
# regression on merged main:
xcodebuild test -only-testing:vreaderTests/FoliatePaginatorScrollBoundaryTests \
  -only-testing:vreaderTests/FoliateScrolledWindowMathTests   # TEST SUCCEEDED
```

## Observations

Shipped behind `#windowedScroll` (now default-ON for horizontal-writing scroll
mode). Two-round independent Codex audit (ship-as-is) caught + closed 5 device-found
mount bugs + a DOM-order bug + lifecycle/teardown gaps. CU smooth-crossing was
verified pre-round-2; round-2's only visual-affecting change (DOM order) was
re-verified correct CU-free. Vertical-writing + paged fall back to the proven path.

## Artifacts

Window-state eval JSON (`eval-feat73*.json`), the 2-round audit log
(`.claude/codex-audits/feat-feature-73-wi-1-windowing-math-audit.md`), and the plan
(`dev-docs/plans/20260530-feature-73-foliate-continuous-scroll.md`).
