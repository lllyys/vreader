---
kind: feature
id: 44
status_target: VERIFIED
commit_sha: 1a8968c2e35e7e6a61139d2fef116aac243e3fd3
app_version: 3.13.18 (build 95)
date: 2026-05-05
verifier: claude
device_or_simulator: iPhone 17 Pro Simulator (build artifact only — no install)
os_version: n/a (build-time gate)
build_configuration: Release
backend: n/a
result: partial
---

## Acceptance criteria

Mapping of feature #44 plan acceptance criteria to round-5 observations.
Round 5 targets criterion (b) — the release-build absence gate — which
round-4d (`feature-44-20260505d.md`) explicitly listed as "exists but
wasn't run this round." Other criteria are repeated here unchanged from
round 4d.

| # | Criterion (from plan) | Round 5 status | Notes |
|---|------------------------|----------------|-------|
| (a) | DEBUG build registers `vreader-debug://` and handles all 7 commands with correct success/error JSON | PASS (round 4d) | Re-verified end-to-end on merged-main v3.13.15 in round 4d — not re-exercised this round. |
| (b) | Release build has zero `vreader-debug` references (CI grep + Info.plist check) | **PASS (this round)** | `scripts/verify-release-no-debugbridge.sh` exits 0 against the Release `vreader.app` built from commit `1a8968c2`. All six sub-checks pass: Info.plist URL scheme absent, no DebugFixtures directory, no catalog fixture filenames in bundle, no DebugBridge-named files, no DebugBridge string references in resources, no DebugBridge symbols/strings in the main binary. |
| (c) | `xcrun simctl openurl ... vreader-debug://snapshot` produces valid JSON in app container | PASS (round 4d) | Re-verified round 4d; not re-exercised here. |
| (d) | `settle` reliably blocks until Foliate `relocate` + a frame committed | DEFERRED — EPUB fixture absent | Foliate happy-path requires an EPUB fixture in `DebugFixtureCatalog`; the catalog is TXT-only today. |
| (e) | All unit + integration tests pass under `xcodebuild test -only-testing:vreaderTests` and `-only-testing:vreaderUITests` | FAIL (round 4d) | 19 unrelated pre-existing failures (TXTChapterContentLoader/V1toV2Migration/etc.); not addressed this round. |
| (f) | `docs/subsystems/debug-bridge.md` documents URL grammar, JSON schema, fixture catalog, one repro per open bug | PASS (round 4d) | Doc shipped with feature in v3.13.x. |
| (g) | LOC under ~300 across new files; no file exceeds the project's 300-line guideline | FAIL (round 4d) | `RealDebugBridgeContext.swift` is 491 lines; split deferred per Codex. |

3 PASS (a, b, f) ✓ — including this round's (b)
1 still PASS from round 4d (c)
1 DEFERRED (d) — EPUB fixture
2 FAIL (e LOC, g tests)

## Commands run

```bash
git rev-parse HEAD
# 1a8968c2e35e7e6a61139d2fef116aac243e3fd3

DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer xcodebuild build \
    -configuration Release \
    -project vreader.xcodeproj \
    -scheme vreader \
    -destination 'platform=iOS Simulator,name=iPhone 17 Pro' \
    -derivedDataPath /tmp/vreader-release-build
# ** BUILD SUCCEEDED **

bash scripts/verify-release-no-debugbridge.sh
# OK: Info.plist has no vreader-debug entry
# OK: no DebugFixtures directory in bundle
# OK: no catalog fixture filenames in bundle
# OK: no DebugBridge-named files in bundle
# OK: no DebugBridge references in bundle resources
# OK: Release binary has no DebugBridge strings
# PASS: zero DebugBridge surface in Release build
```

## Observations

- The Release build emitted one warning unrelated to DebugBridge gating: `Run script build phase 'Inject DebugBridge URL types (DEBUG only)' will be run during every build because it does not specify any outputs.` That warning is benign for the absence gate (the script's effect is gated by `${CONFIGURATION} == Debug`); it's a build-perf nag, not a leakage. Deferred — separate from #44 acceptance.
- All 6 sub-checks of `verify-release-no-debugbridge.sh` exit OK on first run. No flake, no environmental dependency beyond Xcode.
- The script is self-driven — it auto-discovers fixture files from `vreader/Resources/DebugFixtures/` and rejects any leak by name, so adding new fixtures in future does not require editing the script.
- Round 4d listed (b) as the smallest discrete remaining gap. Closing it is mechanical: run the script. Worth doing per-round as a regression check, even if (d/e/g) are not addressed in the same round.
- VERIFIED is still gated on (d) EPUB fixture, (e) test suite cleanup, and (g) LOC split. None of those is closeable autonomously without scope creep.

## Artifacts

- Build product: `/tmp/vreader-release-build/Build/Products/Release-iphonesimulator/vreader.app` (transient — derivedData, not committed)
- Script: `scripts/verify-release-no-debugbridge.sh` (committed)
- Round 4d evidence: `dev-docs/verification/feature-44-20260505d.md`
