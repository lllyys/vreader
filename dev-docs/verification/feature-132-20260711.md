# Feature #132 — Android Reader Nav Chrome (box F root) — Gate-5 Acceptance Evidence

- **feature**: #132 (Android reader nav chrome — shell + Contents/TOC + annotations-review sheet + annotations.json backup; parity box F pt1 + box B annotations-review remainder)
- **date**: 2026-07-11
- **result**: pass
- **device_or_simulator**: Android emulator `emulator-5554` (AVD `vreader-test`, `sdk_gphone64_arm64`, API 35 / Android 15)
- **build**: `com.vreader.app` **v0.15.3** (versionCode 108), HEAD `0f007fdf` (WI-8 merge) — all 9 code WIs merged (`android/v0.14.x`→`v0.15.3`)
- **method**: read-only `verifier` agent on the leased emulator — `:app:installDebug` + connected instrumented suites (one FQCN per invocation, watchdog-wrapped per rule 52) + the live rclone-WebDAV annotation round-trip + CU-free `adb`-driven real-book screenshots — then orchestrator finalization (this evidence + the tracker VERIFIED flip).
- **GH**: #1924

## WIs (all merged)

WI-1 (TOC model/providers) · WI-2 (ReaderTopChrome) · WI-3 (TocContentsSheet) · WI-6b (annotation snapshot + restore seams) · WI-4 (AnnotationsReviewSheet + cards) · WI-5 (ReaderChromeScaffold + state) · WI-6 (TXT/MD host) · WI-7-hosts (PDF + AZW3 hosts) · WI-7-EPUB (EPUB host — persistent StateFlow chrome + native-locator TOC jump) · WI-8 (annotations.json backup/restore).

## Real corpus (real-books-first)

- `test-books/books/epub/道诡异仙 - 狐尾的笔.epub` — 19 MB CJK EPUB (P3 on-device chrome render).

## Acceptance criteria — all verified

| # | Criterion | Result | Evidence |
| --- | --- | --- | --- |
| A1 | Build + install debug app | pass | `:app:installDebug` → `RUN-ANDROID-TESTS RESULT: SUCCEEDED`; aapt badging confirms `com.vreader.app` v0.15.3 |
| B | **Live WebDAV annotation backup→restore round-trip, UUID-preserving** | pass | see P1 below |
| C1 | All-5-format chrome render (TXT/MD, PDF, AZW3, EPUB) | pass | connected suites P2 (Txt/Pdf/Azw3/EPUB) all green + EPUB visually confirmed |
| C2 | EPUB Contents (native TOC) jump | pass | `ReaderChromeConnectedTest` + `TocContentsSheetTest` green + end-to-end visual (native Readium TOC → tap chapter → chapter loads) |
| C3 | Notes → annotations-review sheet (highlights+notes, filter chips, Share) | pass | `AnnotationsReviewSheetTest` 14/0 + visual (Annotations sheet, All/Highlights/Notes chips, Share) |
| C4 | Tap-to-jump on applicable hosts (TXT/MD/PDF card→scroll/page; EPUB TOC) | pass | exercised in host suites; EPUB TOC jump visually confirmed |

### P1 — live WebDAV annotation round-trip (the headline device-only gate)

`com.vreader.app.backup.AnnotationBackupRoundTripConnectedTest` run over a throwaway `rclone serve webdav` on an ephemeral port (emulator → `10.0.2.2`), via an adapted copy of `scripts/run-webdav-roundtrip.sh` (scratchpad-only; the user's persistent `:8081` rclone server was never touched, the throwaway server was reaped by its EXIT trap).

- **`RUN-ANNOT-ROUNDTRIP RESULT: SUCCEEDED (exit 0)`** / `RUN-ANDROID-TESTS RESULT: SUCCEEDED`.
- Genuine run (not an assume-skip): `tests="1" failures="0" errors="0" skipped="0"`, testcase `annotations_survive_backup_then_restore_overLiveWebDav` (2.387 s).
- Observed path: import book + add highlight ("the whale") + note ("reminder") → live WebDAV connection asserted OK → back up → wipe book+annotations → list backups → **selective restore** → **highlight restored under its ORIGINAL UUID** (`highlight.id` + selectedText intact) and **note restored under its ORIGINAL UUID** (content intact). This is the #116/#127 device-only XML/crypto class the JVM tests can't reach — green. (WI-8 contract confirmed on device: `locatorJSON` is the PLAIN serialized `Locator`, round-trips through WI-6b's plain decoder.)

### P2 — connected chrome suites (one FQCN per invocation; no retries/cold-restart needed)

| Suite | RESULT | tests / failures |
| --- | --- | --- |
| `com.vreader.app.reader.TxtReaderChromeUiTest` (TXT/MD host) | SUCCEEDED | 6 / 0 |
| `com.vreader.app.reader.PdfReaderChromeUiTest` (PDF host) | SUCCEEDED | 5 / 0 |
| `com.vreader.app.reader.Azw3ReaderChromeUiTest` (AZW3 host) | SUCCEEDED | 5 / 0 |
| `com.vreader.app.reader.ReaderChromeConnectedTest` (EPUB host: TOC / native jump / touch-through / dismiss overlay) | SUCCEEDED | 6 / 0 |
| `com.vreader.app.reader.nav.TocContentsSheetTest` | SUCCEEDED | 11 / 0 |
| `com.vreader.app.annotations.AnnotationsReviewSheetTest` | SUCCEEDED | 14 / 0 |
| `com.vreader.app.reader.chrome.ReaderTopChromeTest` | SUCCEEDED | 14 / 0 |

**61 tests, 0 failures, 0 skipped, 0 flakes** — every suite passed cleanly on the first run (no cold-restart or retry). The live Compose runs deferred by WI-2..WI-8 (per the #128 WI-7 precedent) are now all confirmed on the real emulator.

### P3 — real-book chrome (CU-free `adb`, real 19 MB CJK EPUB)

Imported `道诡异仙 - 狐尾的笔.epub` via the SAF picker (`adb input tap`; no `ACTION_VIEW` intent-filter, per #128). Screenshots:

- **reader chrome on the real EPUB** — top bar "‹ Library" + centered title; Readium-rendered CJK cover; bottom chrome = progress slider + **Contents · Notes · Display**.
- **annotations-review sheet** — "Annotations" + Share; filter chips **All / Highlights / Notes**; empty state.
- **Contents (TOC) sheet** — populated from the EPUB's native Readium TOC (封面, 制作说明, … 第一章 师傅), current row accent-highlighted.
- **Contents jump confirmed** — tapping a chapter row navigated Readium to that chapter (body text shown), chrome preserved.

## Box status

- **Box F pt1** (reader chrome shell + Contents/TOC) — #132's half DONE+VERIFIED; box F fully checked only when #133/#134/#135 also VERIFIED.
- **Box B** (annotations review + backup) — #132's review-sheet + annotation backup/restore VERIFIED; the **bookmark** half stays with #135 (no BackupCollector change needed — WI-8 already wires `bookmarks`).

## Ghost hygiene

Clean at verifier exit: `pgrep -x codex` = 0; no stray `xcodebuild` / `connectedDebugAndroidTest` / `am instrument` / `http.server` / `adb logcat`; only the user's persistent `:8081` rclone server remains (throwaway server reaped by its EXIT trap). Pushed test books removed, app data cleared.

## Verdict

All gating acceptance criteria for feature #132 pass end-to-end on the real emulator, with the live WebDAV annotation round-trip (UUID-preserving) and all 5 formats' nav chrome confirmed. No genuine failures, no tooling-flakiness caveats. Row → **VERIFIED**.
