# Feature #128 — Android Library Search — Gate-5 Acceptance Evidence

- **feature**: #128 (Android library search — parity checklist item C, the *search* half)
- **date**: 2026-07-11
- **result**: pass
- **device_or_simulator**: Android emulator `emulator-5554` (vreader-test AVD, API 35)
- **build**: `android/v0.14.1` (WI-7 merge `e892fabb`); all 7 code WIs merged (`android/v0.13.17`→`v0.14.1`)
- **method**: read-only `verifier` agent on the leased emulator (adb + `run-as sqlite3` on the real `vreader.db`, connected instrumented suites, CU-free `adb input tap` UI drive) + orchestrator finalization (test-locator fix, this evidence).
- **GH**: #1901

## Real corpus (real-books-first)

- `test-books/books/epub/道诡异仙 - 狐尾的笔.epub` — 19.38 MB CJK EPUB
- `test-books/books/txt/黑暗血时代.txt` — 14.06 MB CJK TXT

Both imported into the live app via the SAF `OpenDocument` picker, driven CU-free by `adb input tap` (see "Named gaps" — there is no `ACTION_VIEW` intent-filter, so `am start -a VIEW` import is not available; the SAF path is the only import and it is fully adb-driveable).

## Acceptance criteria — all verified

| # | Criterion | Result | Evidence |
| --- | --- | --- | --- |
| A1 | Build + install debug app | pass | `:app:installDebug` → `RUN-ANDROID-TESTS RESULT: SUCCEEDED`; `run-as` works (DEBUG) |
| A2 | Import two real CJK books | pass | `books`: `epub:1f5aecb8…\|daoguiyixian\|epub`, `txt:04d60f6d…\|heianxueshidai\|txt` |
| A3 | Streaming index to completion | pass | live pipeline observed: EPUB staging→1044 then atomic publish (staging→0, sections→1044, state→indexed); TXT staging→254109 then published; both settled |
| A4 | Real DB on real SQLite | pass | see below |
| A5 | Relaunch-resume | pass | recorded `indexedAt` (EPUB 1783745469905, TXT 1783745759185); `am force-stop` + relaunch → both rows byte-identical, sections total 255153 unchanged, staging 0 — settled books not re-indexed |
| B | Live Compose UI suites | pass | `LibraryScreenSearchUiTest` 2/2; `SearchScreenUiTest` **17/17** after the locator fix (orchestrator-confirmed connected run `RUN-ANDROID-TESTS RESULT: SUCCEEDED`) |
| C | End-to-end UI search + no-OOM | pass | see below |

### A4 — real SQLite detail (`run-as com.vreader.app sqlite3 …/databases/vreader.db`)

- `search_sections`: EPUB **1044** + TXT **254109** = **255153** rows (real CJK content, e.g. `《道诡异仙》`, `诡异的天道，异常的仙佛`, `中央电视台`, `纽约市的皇后区`).
- `search_index_state`: both books **`indexed`**, indexerVersion 1.
- **FTS AFTER-INSERT trigger populated the content-table shadow**: `search_sections_fts` = **255153** = `search_sections` (Room recreates the trigger at open).
- **FTS `MATCH` real CJK hits** (app query shape — per-char quoted phrase): EPUB `"李 火 旺"` → **972**, `"道 诡 异 仙"` → **6**, `"诡 异"` → **90**; TXT `"中 央 电 视 台"` → **4**, `"纽 约"` → **6**; cross-book `李` → EPUB **1008** + TXT **264**.
- **FK CASCADE (#116-class)**: `PRAGMA foreign_keys=ON; DELETE FROM books WHERE <txt>` → TXT rows dropped to **0** across `search_sections`, `search_sections_fts`, `search_index_state`, `search_sections_staging`; EPUB's 1044 untouched; post-cascade `search_sections` == `search_sections_fts` == 1044 (no orphans), EPUB still matchable (972).
- **`books.author` backfilled** for the EPUB: **`狐尾的笔`** (from OPF metadata); TXT author null (no metadata) — correct.

### C — end-to-end UI + no-OOM

- Search pill → SearchScreen takeover ("Search title, author, or text…" / "Cancel"). Typed `dao` → "1 book", `daoguiyixian` result with matched-prefix highlight + author `狐尾的笔`; `heian` → "1 book" `heianxueshidai` (correct per-book disambiguation); `zzzznomatch` → no-results state renders "No matches for \"zzzznomatch\"" + the definitive helper copy. Screenshots captured in scratchpad.
- **EPUB streaming no-OOM**: bounded `logcat -d` grep found no OutOfMemory / FATAL / crash for the app pid during indexing of the 19 MB EPUB + 14 MB TXT.

## Named gaps (tooling, not product)

1. **No `ACTION_VIEW` intent-filter** (MainActivity is MAIN/LAUNCHER only) → the CU-free `am start -a VIEW` import path is unavailable; import driven via SAF + `adb input tap` instead (still fully CU-free).
2. **`adb input text` cannot type multibyte CJK** (no IME installed) → the *typed-CJK-in-UI keystroke* path is not adb-driveable; CJK search **logic** is proven at the FTS layer (A4, real `MATCH` hits) and the full UI→VM→query-builder→FTS→snippet-highlight stack is exercised CU-free with Latin queries.

## Finalization

- Test-locator defect found by the verifier (`SearchScreenUiTest:182` — the bare `thermodynamics` substring matched 2 nodes: the search input field AND the echoed no-results heading, so `assertIsDisplayed()`'s max-1 constraint threw). Product renders correctly (verified live). Fixed in this finalization by matching both nodes (`onAllNodesWithText(...).assertCountEquals(2)`) — suite now 17/17 green on `emulator-5554`.

## Verdict

All acceptance criteria for feature #128 pass end-to-end against real SQLite with the real CJK corpus, incl. the #116-class on-device FTS-trigger + FK-CASCADE checks. Row → **VERIFIED**.
