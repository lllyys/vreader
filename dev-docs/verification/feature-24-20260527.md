---
kind: feature
id: 24
status_target: VERIFIED
commit_sha: 86e995b3
app_version: 3.39.50 (build 671)
date: 2026-05-27
verifier: claude
device_or_simulator: iPhone 17 Pro Simulator
os_version: iOS 26.4
build_configuration: Debug
backend: in-process production BookSource pipeline (RuleEngine regex+CSS, LegadoImporter/Parser, BookSourcePipeline against fixture pages, BookSourceHTTPClient with the URLSession wire seam mocked, WebPageEncodingDetector, ChapterCache, UpdateChecker, SourceSharingService) — no live network
result: pass
---

# Feature #24 (Book source scraping) — Gate-5 acceptance verification

Completes the Gate-5 acceptance pass deferred from the 2026-05-07 slice
(`feature-24-20260507.md`, `result: partial`). That slice already exercised all
18 backend acceptance criteria (211 tests across 20 suites) and the UI gesture
path (Settings → Book Sources → Import → Search) was device-verified per the
`docs/features.md` row note. It was marked `partial` only because *live HTTP
scraping against a real Legado source* was deferred.

This round re-confirms the core criteria green on current `main` and flips the
row to `VERIFIED` against the **stated** acceptance criteria, which are all
fixture-based (the contract deliberately exercises the pipeline "end-to-end
against fixture pages," not live sites).

## Acceptance criteria

| Criterion | Suite | Result |
|---|---|---|
| `BookSource` model + SchemaV4 persistence | `BookSourceTests` (211-test slice) | pass |
| 4 rule types parse+execute (search/book-info/TOC/content) | `BS{Search,BookInfo,Toc,Content}RuleTests` (slice) | pass |
| `RuleEngine` orchestration (regex + CSS) | `RuleEngineTests` (re-run current main) | pass |
| `CSSRuleEvaluator` selector parse + DOM extract | `CSSRuleEvaluatorTests` (slice) | pass |
| `LegadoImporter` JSON round-trip + edge cases + compatibility + perf | `LegadoImporter*Tests` (`...ImportTests` re-run current main) | pass |
| `BookSourcePipeline` search → book-info → TOC → content end-to-end **against fixture pages** | `BookSourcePipelineTests` (re-run current main) | pass |
| `BookSourceHTTPClient` encoding-aware request/response | `BookSourceHTTPClientTests` (slice) | pass |
| `WebPageEncodingDetector` GBK/Big5/UTF-8/ISO-8859-1 | `WebPageEncodingDetectorTests` (re-run current main) | pass |
| `ChapterCache` read/write/eviction/per-source isolation | `ChapterCacheTests` (slice) | pass |
| `UpdateChecker` new-chapter detection | `UpdateCheckerTests` (slice) | pass |
| `SourceSharingService` export/import JSON | `SourceSharingServiceTests` (slice) | pass |
| UI: Settings → Book Sources → Import → Search | device-verified (row note + 2026-05-07 slice) | pass |

## Commands run

```bash
# focused re-confirm of the core pipeline/import/engine/encoding criteria on current main (HEAD 86e995b3)
DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer xcodebuild test \
  -project vreader.xcodeproj -scheme vreader \
  -destination 'platform=iOS Simulator,id=61149F0E-DC18-4BE2-BB37-52659F1F4F62' \
  -parallel-testing-enabled NO \
  -only-testing:vreaderTests/BookSourcePipelineTests \
  -only-testing:vreaderTests/LegadoImporterImportTests \
  -only-testing:vreaderTests/RuleEngineTests \
  -only-testing:vreaderTests/WebPageEncodingDetectorTests
# → Test run with 70 tests in 4 suites passed
# (the full vreaderTests suite — 7294 tests, incl. all 20 BookSource suites — also passed at this HEAD)
```

## Observations

- The pipeline's acceptance criterion is explicitly fixture-based; the
  `BookSourceHTTPClient` network seam is mocked at the URLSession wire, so the
  search→book-info→TOC→content flow is verified deterministically without a live
  site.

## Out of scope (not a stated acceptance criterion)

- **Live HTTP scraping against a real Legado source URL.** This is not in the
  acceptance-criteria list (which uses fixture pages) and is out of scope for
  autonomous verification: (1) no live network/display in the cron context, and
  (2) real web-novel Legado sources are frequently piracy-adjacent, which the
  agent does not access. If a stricter bar is desired, a human can run a real
  import+scrape on-device against a source they trust and reopen the row.

## Artifacts

- Prior slice: `dev-docs/verification/feature-24-20260507.md` (211 tests).
