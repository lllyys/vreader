---
kind: feature
id: 103
status_target: VERIFIED
commit_sha: 333806f85965947086b047ea68f9ed28c5a15233
app_version: 3.66.20 (build 1005)
date: 2026-06-17
verifier: claude
device_or_simulator: n/a (automation/policy feature — no runtime/UI)
os_version: n/a
build_configuration: n/a
backend: n/a
result: pass
---

# Feature #103 — Android Phase 0 (safety plumbing) — Gate-5 acceptance

Foundational/automation feature (ADR-0001 Phase 0). No user-observable
runtime behavior, so per rule 47 it needs unit/integration tests + audit,
NOT device verification. Acceptance = "the automation is now Android-safe":
the audit gate fires for Android/Kotlin/`contracts/` PRs (they can no
longer bypass Gate 4), and the release/isolation policies are in place and
consistent. Verified against merged `main` (commit `333806f8`, v3.66.20).

## Acceptance criteria

| Criterion | Exercised with | Observed | Result |
|---|---|---|---|
| An `android/**` PR requires a Codex audit (no longer docs-only) | `code_paths_touched` on `android/app/Foo.kt` | → CODE (audit required) | pass |
| A `contracts/**` PR requires an audit (shared code surface) | `contracts/identity/fingerprint.md` | → CODE | pass |
| A `spikes/**` / Gradle PR requires an audit | `spikes/x/build.gradle.kts` | → CODE | pass |
| A docs/meta-only PR still skips the audit (no over-gating) | `docs/features.md`, `.claude/rules/40-version-bump.md` | → docs (skips) | pass |
| docs-prefixed code-looking paths not over-gated | `docs/snippets/example.kt`, `docs/res/notes.md` | → docs | pass (hook test) |
| Classifier is pipefail-safe (no fail-open under SIGPIPE) | code-first + 20k docs under `set -o pipefail` | pipeline exit 0 (CODE) | pass (hook test) |
| Hook fails CLOSED if the classifier lib is missing | code review + hook structure | `CODE_TOUCHED=yes` on missing lib | pass |
| Per-platform version/tag policy documented + decided | rule 40 "Multi-platform" | iOS plain `vX.Y.Z`, Android `android/vX.Y.Z`; pre-Phase-2 spikes bump iOS | pass |
| Cross-platform write isolation documented | rule 48 | Kotlin↛iOS, Swift↛Android, shared single-writer | pass |
| AGENTS.md Platforms section + ADR consistency | AGENTS.md + ADR-0001 | path ownership, tests, release; tag namespace reconciled | pass |

## Commands run

```bash
# On merged main (commit 333806f8):
bash .claude/hooks/__tests__/check_codex_audit_artifact.test.sh   # RESULT: PASSED (22 assertions)
source .claude/hooks/lib/code-paths.sh
printf 'android/app/Foo.kt\n'        | code_paths_touched   # → 0 (CODE)
printf 'contracts/identity/x.md\n'   | code_paths_touched   # → 0 (CODE)
printf 'spikes/x/build.gradle.kts\n' | code_paths_touched   # → 0 (CODE)
printf 'docs/features.md\n'          | code_paths_touched   # → 1 (docs)
```

## Observations

- The gate caught a real fail-open on ITSELF during Gate-4 (a `grep -q`
  SIGPIPE bypass under `pipefail`) — a 3-round audit loop killed it. That
  is exactly the class of bug this gate exists to prevent, found before it
  could let a code PR through.
- WI-1 is behavioral (the gate), verified by the 22-assertion hook test;
  WI-2/3/4 are policy docs, verified by read + cross-consistency (3-round
  Codex audit, CLEAN).

## Artifacts

- `.claude/hooks/__tests__/check_codex_audit_artifact.test.sh` (the executable contract)
- `.claude/codex-audits/feat-feature-103-wi1-gate-routing-audit.md` (WI-1, 3 rounds)
- `.claude/codex-audits/feat-feature-103-wi234-release-isolation-audit.md` (WI-2/3/4, 3 rounds)
