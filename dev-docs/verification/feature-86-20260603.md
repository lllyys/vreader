---
kind: feature
id: 86
status_target: DONE
commit_sha: d838c72fe146da9cffbb8bd62b9cff66e2304630
app_version: 3.50.6 (build 849)
date: 2026-06-03
verifier: claude
device_or_simulator: iPhone 17 Pro Simulator
os_version: iOS 26.4
build_configuration: Debug
backend: n/a
result: partial
---

# Feature #86 — Expand AI reading scope & sources (Chat tab): acceptance

Implementation complete: all 6 work items merged (v3.50.1 → v3.50.6) through the
full 6-gate workflow. This records the acceptance pass. **result: partial** — the
CU-free-verifiable criteria are device-verified; the AI-*effect* criteria are
provider-key-blocked (no configured key in the headless harness, the same gate as
Feature #86 WI-1 and #14/#65) and require a keyed/manual pass before `VERIFIED`.

## Acceptance criteria

| Criterion | Status | Evidence |
|---|---|---|
| (1) Chat context covers at least the whole current chapter (not a fixed window) | ✅ device + unit | WI-1 slice smoke + `ReaderAICoordinatorScopedContextTests` (chapter scope) |
| Scope selector — Section / Chapter / Book-so-far chips + menu | ✅ device | WI-3: `feature-86-wi3-scope-menu-20260603.png` (3 rows) → select → `feature-86-wi3-scope-changed-20260603.png` (chip updates "Chapter"→"Book so far") |
| (2) Annotation sources — Notes / Highlights / Bookmarks foldable into context | ✅ device + unit | WI-4: `feature-86-wi4-sources-{chip,menu,toggled,badge-updated}-20260603.png` (chip "Sources 2" → popover 3 rows → toggle Highlights off → badge 2→1); `ChatAnnotationCacheTests`, `ChatAnnotationContextTests` |
| (3) Read-everywhere on demand — Whole-book scope + retrieval | ✅ UI device / ⚠️ effect keyed | WI-5b: `feature-86-wi5b-scope-menu-4rows-20260603.png` (Whole-book row + ON-DEMAND) → select → `feature-86-wi5b-armed-cluster-20260603.png` (Armed cluster "Reads on your next question"). The Reading%/Ready states + the actual map-reduce read **answering** are provider-key-blocked → keyed. `WholeBookReducerTests`, `WholeBookRetrievalViewModelTests`, `AIChatViewModelWholeBookTests` prove the logic. |
| Token-budget strategy (map-reduce) for over-limit spans | ✅ unit | `WholeBookReducerTests` (hierarchical reduce → ≤ budget; `maxChunks` overflow → bounded digest + non-complete coverage) |
| "Drew on" provenance row under each reply | ✅ unit / ⚠️ render keyed | `ChatCitationFactoryTests`, `ChatContextAssemblerSectionRetentionTests`. The row renders only under a real AI reply → keyed. |
| Mutation-complete annotation refresh (off the relocate path) | ✅ unit | `PersistenceActorAnnotationBusTests` (the `.readerAnnotationsDidChange` bus) |

## Commands run

```bash
# Per-WI Gate-5 device slices (iPhone 17 Pro Simulator, war-and-peace TXT, --enable-ai):
xcrun simctl launch <UDID> com.vreader.app --enable-ai
xcrun simctl openurl <UDID> "vreader-debug://present?sheet=ai&tab=chat"
scripts/sim-tap.sh xy <chip>            # open the scope / sources menus, select rows, toggle
# Unit gates:
scripts/run-tests.sh vreaderTests/<Suite>   # all WI suites GREEN
```

## Observations

- The scope/sources/whole-book-menu + the bar morphing (normal ↔ retrieval cluster)
  all render and respond exactly to design #1455 on-device.
- The single remaining gap to `VERIFIED` is the **AI-answer effect** (whole-book read
  producing an answer; the "Drew on" content), which needs a configured provider key.
  This is the standing provider-key harness gap, not a feature defect.

## Artifacts

- `dev-docs/verification/artifacts/feature-86-wi3-{scope-menu,scope-changed}-20260603.png`
- `dev-docs/verification/artifacts/feature-86-wi4-{sources-chip,sources-menu,sources-toggled,badge-updated}-20260603.png`
- `dev-docs/verification/artifacts/feature-86-wi5b-{scope-menu-4rows,armed-cluster}-20260603.png`
