---
kind: feature
id: 42
status_target: VERIFIED
commit_sha: b93e701b5df2237c63f994f2edbd208d6ed33e27
app_version: 3.51.1 (build 857)
date: 2026-06-03
verifier: claude
device_or_simulator: iPhone 17 Pro Simulator
os_version: iOS 26.4
build_configuration: Debug
backend: OpenRouter (free tier) for the AI criterion (Phase-1 evidence)
result: pass
---

# Feature #42 — VERIFIED reconciliation (both phases done + device-verified + no regression on current main)

This is the **Gate-5b VERIFIED roll-up** for Feature #42. The feature was
functionally complete and device-verified across BOTH phases by 2026-06-02
(v3.42.42); the `docs/features.md` row lagged at `IN PROGRESS` purely as a
tracker-state oversight. This file consolidates the prior phase acceptance passes
and records a no-regression confirmation on the current `main` (v3.51.1) so the
row can move to `VERIFIED`.

## Scope (re-scoped feature, two phases)

- **Phase 1** — Readium Swift Toolkit as the default reflowable EPUB engine
  (`readiumEPUBEngine`, default-ON since WI-14 G2, v3.42.0).
- **Phase 2** — libmobi (LGPL-3.0) Kindle **convert-on-import** (AZW3/MOBI/KF8 →
  EPUB at import, so converted books render through the Readium engine);
  `kindleConvertOnImport`, default-ON since the G2 flip (v3.42.42).

## Acceptance criteria

| Phase | Criterion | Evidence | Result |
|---|---|---|---|
| 1 | Render + RTL/CJK; position save/restore; theme/font; highlight CREATE; search-nav; bilingual interlinear render; TTS+follow; photo full-transparency; navigation; footnote live-tap — **all 10 criteria CU-free** | `dev-docs/verification/feature-42-20260601.md` (result: pass, v3.42.14, commit `9aba219c`) | ✅ |
| 2 | Real AZW3/MOBI converts to a valid `.epub` at import and renders via Readium (libmobi decode → EPUB assembly → BookImporter convert-on-import) | `dev-docs/verification/feature-42-p2-wi5-20260602.md` (result: pass, v3.42.24, `--enable-kindle-convert`) | ✅ |
| 2 | Default-ON flip: a real AZW3 imported with NO flag imports as EPUB (snapshot format=epub) + renders via Readium; existing native `.azw3` unaffected; revertable via override OFF | `dev-docs/verification/feature-42-p2-g2flip-20260602.md` (result: pass, v3.42.42, commit `563d5340`) | ✅ |

All acceptance criteria across both phases were exercised end-to-end on the
iPhone 17 Pro Simulator (Phase-1 AI criterion against a live OpenRouter provider).

## Commands run (no-regression confirmation on current main, v3.51.1)

`main` (v3.51.1, commit `b93e701b`) is 9 minor versions ahead of the last
phase-verification build (v3.42.42). To confirm no regression in #42's subsystems
since then, the #42 subsystem suites were re-run green on current `main`:

```bash
scripts/run-tests.sh \
  vreaderTests/LibmobiSmokeTests \
  vreaderTests/MobiEPUBConverterTests \
  vreaderTests/MobiEPUBAssemblerTests \
  vreaderTests/MobiDocumentTests \
  vreaderTests/BookImporterAZW3Tests \
  vreaderTests/BookImporterOriginalExtensionTests \
  vreaderTests/FeatureFlagsTests
# → RUN-TESTS RESULT: SUCCEEDED
```

Covers: the libmobi C-interop smoke + decode + EPUB-assembly + end-to-end
converter, the AZW3 convert-on-import path in `BookImporter`, the
original-extension retention, and the `kindleConvertOnImport` / `readiumEPUBEngine`
feature flags. All green — the converter + import wiring + engine flags are intact
on current `main`.

## Observations

- Both phases' code is merged on `main`: Phase-1 WI-1..14 (Readium engine,
  default-ON) + Phase-2 WI-1a..5 (libmobi convert-on-import) + the G2 default-ON
  flip (PR #1390). `MobiEPUBConverter` is wired into `BookImporter` and
  `ImportProvenance` records the conversion origin (#307 dedupe-origin fix).
- GH #1234 was closed 2026-06-02 after Phase-1 device verification, with the
  Phase-2 G2-flip device verification documented in a follow-up comment on the
  closed issue — so the GH side already records both phases. No GH action remains;
  this reconciliation flips only the `docs/features.md` row.
- No #42-subsystem regression was introduced by the features merged since v3.42.42
  (#74 locate-bloom, #77 bilingual loading shimmer, #80 test-connection, #86 AI
  context) — the suites above are green.

## Artifacts

- `dev-docs/verification/feature-42-20260601.md` (Phase-1 full acceptance, 4 PNGs)
- `dev-docs/verification/feature-42-p2-wi5-20260602.md` (Phase-2 libmobi device verify)
- `dev-docs/verification/feature-42-p2-g2flip-20260602.md` (Phase-2 default-ON flip verify)
