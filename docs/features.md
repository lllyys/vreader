# Feature Tracker

Track features to be implemented here. Must be planned before implementation.

## Rules

> **Binding for this file.** The rules, statuses, and plan template below govern every change made to `docs/features.md`. AGENTS.md treats them as the authoritative feature-tracker workflow.

- **Bugs vs features**: If something was implemented but doesn't work correctly, it is a **bug** — track it in `docs/bugs.md`. If something was never implemented, it is a **feature** — track it here. Never mix them.
- **Partial implementations**: If something is partially implemented, the broken part is a bug in `docs/bugs.md`; the missing capability is a feature here. Link them.
- **Cross-links**: When a bug fix resolves a feature, update the feature status to `DONE` with note `Resolved by bug #N`. When a feature depends on a bug fix, use `TODO` status with note `Blocked by bug #N`.
- **Plan before implementation**: Every feature must be planned before any code is written. Status must reach `PLANNED` before moving to `IN PROGRESS`. A plan requires the fields listed in the "Plan Template" section below.
- **Exception — resolved by bug fix**: If a bug fix incidentally delivers a feature, the feature may be set to `DONE` with `Resolved by bug #N` without a full plan. The bug's own cause/solution/lesson records serve as documentation.

## How to use

1. Add features as you identify them (fill in Summary and Area at minimum)
2. Plan the feature (fill in required plan fields above) → set status to `PLANNED`
3. Tell the agent: "implement feature #N" to start implementation
4. Agent updates Status when done

- **GitHub Issue closure** (post-merge finalizer — see `AGENTS.md` for full policy):
  - If the feature has a `GH: #N` in Notes, close the GitHub Issue only after:
    1. All acceptance criteria met and status is DONE in this file.
    2. Implementation is merged to `main`.
    3. Closure comment posted with commit SHA and acceptance result.
  - Partial delivery: keep GitHub Issue open; use checklist or split follow-ups.
  - PRs use `Refs #N`, not `Fixes #N` (prevents premature auto-close).

## Statuses

- `TODO` — not started
- `PLANNED` — plan complete (problem, scope, edge cases, tests, acceptance criteria), ready to implement
- `IN PROGRESS` — being worked on
- `DONE` — implemented; correctness not yet verified end-to-end
- `VERIFIED` — covered by an automated end-to-end test (XCUITest + DebugBridge) or by an explicit on-device manual verification log
- `DEFERRED` — postponed to a later milestone
- `WONT DO` — out of scope or rejected

## Plan Template

Before setting a feature to `PLANNED`, fill in these fields in a sub-section under the feature table (e.g., `### Feature #1 — Plan`):

- **Problem**: What user need does this address?
- **Scope**: What is included and excluded?
- **Edge cases**: Empty input, nil, boundary values, concurrent access, format-specific behavior.
- **Test plan**: What tests will verify the feature?
- **Acceptance criteria**: How do we know it's done?

## Features

| #  | Summary                                                                        | Area          | Priority | Status    | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| -- | ------------------------------------------------------------------------------ | ------------- | -------- | --------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1  | Edit and delete bookmarks                                                      | Reader/*     | High     | VERIFIED  | Rename via context menu (bug #42), delete via swipe + context menu. BookmarkListView has full CRUD UI. 2026-05-07 slice verify: 14 tests across 6 suites (BookmarkListViewModel loading/mutation/toggle/edge cases + PersistenceActor bookmark CRUD with in-memory container + BookmarkFeedback haptic). **2026-05-08 round-2 slice**: annotations panel UI leg now exercised end-to-end via `vreaderUITests/Annotations/AnnotationsPanelPlaceholderTests` + `vreaderUITests/Reader/ReaderAnnotationsPanelTests` (11 XCUITest methods, 188.1 s). **2026-05-09 round-3 device verify** (`dev-docs/verification/feature-1-20260509.md`, `result: pass`): closes the per-bookmark rename + swipe-delete gestures that round-2 deferred. On iPhone 17 Pro Sim (iOS 26.4) at v3.14.98 with `vreader-debug://seed?fixture=war-and-peace`, full CRUD round-trip driven end-to-end: tap bookmark toolbar icon → "Untitled Bookmark" appears in panel; long-press row → context menu with Rename + Delete; Rename → dialog → paste "Chapter 1 start" → Save → row updates; left-swipe row → red trash Delete revealed; tap Delete → row removed → "No Bookmarks" empty state. 6 screenshot artifacts capturing each transition. Combined with round-2's annotations-panel mechanics, BookmarkListView CRUD surface is fully verified. Original evidence at `dev-docs/verification/feature-1-20260507.md` + `feature-1-20260508.md`. GH: #383                                                          |
| 2  | Highlight search result at destination                                         | Search/*     | Medium   | DONE      | Resolved by bug #43 — yellow background highlight, auto-clears after 3s. 2026-05-07 cross-ref slice: no unique test surface; covered by SearchHighlightDismiss (#5), HighlightedSnippet (#3), per-format renderers (#11/#17). `dev-docs/verification/feature-2-20260507.md` (`result: partial` — UI gesture: search→tap result→scroll+highlight visible+auto-clear deferred, needs display). **2026-05-08 round-2 slice**: navigation half verified end-to-end on iPhone 17 Pro Sim (iOS 26.4) at v3.14.92 — search "Paragraph 50" → tap result → reader scrolls so the matched line lands inside viewport (bug #153 fix working). Render half **broken**: no yellow highlight appears on the matched range; root cause is an orphan `@State highlightRange` in `TXTReaderContainerView.swift` (notification handler writes `uiState.highlightRange`, bridge reads the orphan). Bug **#154** filed (GH #443) — distinct from #153, not fixed this round per verify-cron scope. Auto-clear half blocked-by-#154. `dev-docs/verification/feature-2-20260508.md` (`result: partial`). **2026-05-11 round-3** (`feature-2-20260511-round3.md`, result=**partial**) on merged-main fcb1ff5 (v3.14.145) against bug #154 PARTIAL FIX: navigation half re-verified (search "Pierre" → tap result → reader scrolls so "Pierre, who was alone among the guests" lands inside viewport, "1/1" pager + 0% scrubber render correctly). Render half **still blocked**: war-and-peace fixture triggers chapter mode (`hasChapterDisplay = true`, "Section 8" labels in search results), so the body dispatches to `chapterReaderContent` (`TXTReaderContainerView.swift:118` → line 496) where bug #154's WI-7 deferral keeps `highlightRange: nil`. No yellow highlight visible across t+0.4s / t+1.2s / t+4.2s captures. No new bug filed — same WI-7 deferral as bug #154 + bug #160 + feature #40 round-3. 8 screenshot artifacts. Driven via CU-substitute toolkit (`.claude/skills/sim-drive-fallback`) — CU MCP still returns `CU display unavailable`. Status stays DONE; flip to VERIFIED needs WI-7 OR addition of a chapter-less TXT fixture to `DebugFixtureCatalog.swift`. GH: #400                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| 3  | Manual text highlighting                                                       | Reader/*     | High     | VERIFIED  | **2026-05-12 round-2 EPUB** (`dev-docs/verification/feature-3-20260512.md`, v3.17.2, result=**pass**): CU recovery from SCContentFilter outage unlocked the EPUB device-verification leg deferred from round-1. Long-press at (130, 190) on word "emphasized" in mini-epub3 chapter 1 → custom UIMenu shows Highlight/Add Note/Copy → tap Highlight at (185, 380) → DebugBridge `highlightCount` 0 → 1; close (back chevron) + reopen via `vreader-debug://open` → `highlightCount` stayed at 1 AND yellow CSS Highlight overlay re-rendered automatically on first body render. EPUB was the largest cross-format risk (custom WKWebView bridge + JS highlight API + CSS Highlight overlay); TXT/MD/PDF paths covered by round-1's 84 unit tests. TXT chapter-mode failure (bug #160) is a separate scope-gap tracked in feature #48. Row → **VERIFIED**. 2 screenshot artifacts. **2026-05-09 round-3** (`dev-docs/verification/feature-3-20260509-round3.md`, result=**fail**): Now that feature #11 round-4 (this same session) verified EPUB highlighting end-to-end via the same long-press → custom-menu → tap-Highlight pattern, we can rule out CU/menu-wide failure modes and definitively name the TXT path as broken. Reproduced 2 more times (after round-2's 4) on words "Genoa" and "that" at distinct coords in war-and-peace.txt — 6/6 attempts produce identical outcome: menu shows + dismisses cleanly, but NO yellow paint AND NO entry in Highlights tab. Cross-format contrast: EPUB Highlight via same gesture works in this session (feature #11 round-4 PASS); Define sibling on same TXT menu works (round-2 cross-ref to #33). So failure is TXT-Highlight-action-specific, NOT a CU coordinate issue, NOT a menu-wide problem, DIFFERENT from bug #159 (which was a render gap with data persisting). Filed bug **#160 / GH #476** (Reader/* Medium). VERIFIED gated on #160 fix. 2 screenshot artifacts. Feature #4 (Add notes, same menu, same notification-post pattern) likely affected — separate verification needed. Resolved by bug #44 — Highlight action added to UITextView edit menu. 2026-05-07 slice verify: 84 tests across 14 suites — HighlightListViewModel (6 sub-suites: Loading/Mutation/Edit/Out-of-Bounds/Overlap/TXT-UTF-16), HighlightCoordinator + integration, HighlightableTextView (bug #44 lock), HighlightDedupe, HighlightRecordAnchor (Codable), HighlightAnchorStorage migration, HighlightedSnippet, PersistenceActor highlight CRUD. Per-format renderers covered in #17/#11. `dev-docs/verification/feature-3-20260507.md` (`result: partial` — UI gesture deferred). Round 2 (`feature-3-20260508.md`, v3.14.87) result=**partial**: CU confirmed the custom 4-item menu structure (Highlight / Add Note / Define / ▶) at runtime; Define sibling action verified separately in #33 round 2 (same menu) — proves CU CAN fire menu actions. The Highlight tap-to-persist gesture path inconclusive across 4 CU click attempts (menu dismisses but persistence layer reports "No Highlights"); not filed as a bug — `HighlightableTextViewTests` unit-tests the action dispatch and Define's success rules out a CU/menu-wide quirk. Recommend next round via XCUITest programmatic-tap or physical device. Status stays DONE. GH: #385                                |
| 4  | Add notes/annotations to text                                                  | Reader/*     | Medium   | DONE      | Resolved by bug #44 — Add Note action added to UITextView edit menu. 2026-05-07 slice verify: 130 tests across 10 suites — AnnotationListViewModel (4 sub-suites: Loading/Add-Remove/Edit/Edge Cases) + Annotation Models (highlight/bookmark/note unified) + AnnotationAnchor (EPUB/PDF/TXT round-trip + SHA stability) + PDFAnnotationBridge (PDFKit highlight + restore) + AnnotationExporter (JSON+Markdown) + VReaderAnnotationParser + AnnotationImporter end-to-end. Bug #44 entry-menu lock cross-ref'd via feature #3 evidence. `dev-docs/verification/feature-4-20260507.md` (`result: partial` — UI gesture: long-press text → Add Note modal; long-press existing note → Edit/Delete; export share-sheet; import file-picker — deferred, needs display). **2026-05-10 round-2** (`feature-4-20260510.md`, result=**partial**) on merged-main 41067e5 (v3.14.123): closes the **long-press → Add Note modal** entry-point slice end-to-end on iPhone 17 Sim with war-and-peace.txt. Long-press at mac (550, 500) brings up custom 4-item menu **Highlight \| Add Note \| Define \| ▶**; tap Add Note → modal slides up with `Cancel \| Add Note \| Save (greyed)` header and selected word `"how"` as italic attribution; empty-input Save-gating verified disabled; Cancel dismisses cleanly returning to reader with selection still rendered. 3 screenshot artifacts. **DEFERRED**: type note → Save → render highlight + panel entry; long-press existing note → Edit/Delete UI — both blocked by same external CU/iOS Simulator text-field input quirk previously documented in feature #34 round-3 (synthetic macOS keyboard events do not reach iOS Sim UITextView/UITextField inputs even with Connect Hardware Keyboard ✓ and Simulator frontmost). Tried `osascript keystroke`, `pbcopy + Cmd+V`, Simulator menu-bar `Edit → Paste` — all observable as no-op. Not a vreader defect. Status stays DONE; flip to VERIFIED needs an XCUITest-driven save-flow harness that bypasses macOS-side keyboard synthesis. **2026-05-13 round-3** (`feature-4-20260513.md`, result=**partial**) on HEAD (commit 6615c8b7, v3.21.8 build 285) on iPhone 17 Pro Simulator (iOS 26.5): closes the **save round-trip half** of round-2's deferred criterion 8. `mcp__computer-use__write_clipboard` + `key("cmd+v")` reaches the iOS Sim UITextView input layer (the round-2 quirk was osascript-side, not iOS-Sim-side). Note text pasted byte-for-byte → Save button enabled (gating flip confirmed) → tap Save → modal dismisses → Annotations panel → Notes tab shows row with attribution `"Genoa"`, body `"Genoa is a major port city in northwestern Italy."`, timestamp `May 13, 2026 at 16:16`. Save round-trip end-to-end PASS at the data layer. **Visual-highlight half (8c) still FAIL** — same root-cause class as bug #160 (chapter-mode visual paint deferred); even with note persisted, no yellow background renders on the annotated word. war-and-peace.txt CHAPTER I/II/III markers trigger chapter mode (the surface bug #160's note explicitly covers). No new bug filed — cross-ref bug #160. 4 screenshot artifacts (`feature-4-r3-*-20260513.png`). **Criterion 9 also newly closed this round**: long-press on the persisted note row in Annotations → Notes reveals the **Edit + Delete** context menu (default + destructive-red styles). Only criterion 8c (visual highlight on annotated text) remains FAIL — same bug #160 chapter-mode visual-paint gap. Status stays DONE; flip to VERIFIED gated on bug #160's chapter-mode visual-paint fix landing + device verify. GH: #388                                                                                                                                                                                                                                                                                                                |
| 5  | Search highlight auto-dismiss on next action                                   | Search/*     | Low      | DONE      | Mirror: no — pre-mirror TODO row, code shipped + tested but never given a GH issue; tracking inline only. WI-003 code committed + 25 unit tests in `SearchHighlightDismissTests` cover all dismiss triggers (tap/scroll/new search) + persistence preservation + idempotency at real `HighlightingLayoutManager`/`HighlightableTextView` boundaries. Sim verification 2026-05-04 was **partial** (`dev-docs/verification/feature-5-20260504.md`): search panel + result-tap navigation work, but search-result-tap navigation lands the matched text past the visible viewport so the visual highlight cannot be confirmed in the rendered region. Dismiss-on-tap exercised but inconclusive (chrome-toggle path also fired). **2026-05-08 round-2 device verification with CU**: confirmed the deferred-slice symptom empirically — landing region after search-result-tap shows only "reading and restore the scroll position when reopened." (the END of paragraph 100); "Paragraph 100: ..." is OFF-SCREEN above the viewport. By the time the user scrolls up to find the match, the 3-second highlight auto-clear has fired — yellow highlight is NEVER visible. Filed as **bug #153 / GH #433** (Reader/*, Medium severity). Result upgraded from "partial" (could not confirm) to **`fail`** (confirmed broken). Evidence: `dev-docs/verification/feature-5-20260508.md` + 2 PNG artifacts under `verification/artifacts/bug-153-*-20260508.png`. Status stays DONE pending bug #153 fix; will re-verify after fix lands. Suspect surface: `TXTTextViewBridge.scrollToGlobalOffset` / `scrollRangeToVisible` rounds to bottom of visible region, pushing the matched-text START above viewport. Possibly overlaps with bug #99 (#26) but separate root cause; both could share a centering-scroll-anchor fix path. **2026-05-13 round-3 device verification with CU on v3.21.7 build 284 (commit 6615c8b7, iPhone 17 Pro Sim iOS 26.5)**: bug #153 fix held — matched text now lands in viewport with correct headroom — but the temporary yellow highlight STILL does not render in TXT chapter mode. The bug #154 "FIXED" claim (chapter-mode wiring via feature #48 WI-3, commit a472a6d) was code-complete but runtime-incomplete. Sub-second post-tap simctl screenshot captured BEFORE the 3s auto-clear window expired — no yellow paint present. Bug #154 / GH #443 REOPENED. Evidence: `dev-docs/verification/feature-5-20260513.md` (`result: fail`) + 2 PNG artifacts (`feature-5-r3-immediately-after-search-tap-paragraph50-20260513.png`, `feature-5-r3-no-highlight-after-search-tap-20260513.png`). Status stays DONE pending bug #154 fix; will re-verify once the chapter-mode highlight render path is repaired. |
| 6  | Persist library view preferences across app restarts                           | Library/*    | Medium   | VERIFIED  | WI-001. PreferenceStore + UserDefaults. 2026-05-07 slice verify: 36 tests across 2 LibraryViewModel suites cover sort-order (title/addedAt/lastReadAt/totalReadingTime) + view-mode toggle + import flow + #20 reset path. `LibraryViewModelPersistenceTests.swift` is excluded from the test target by project.yml; persistence behavior is exercised via the ViewModel surface instead. **2026-05-08 round-2 slice**: UI-gesture leg now exercised end-to-end via `vreaderUITests/Library/LibraryPopulatedTests` (8 XCUITest methods, 56.8s) — view-mode toggle tap flips label, sort picker tap exposes all 4 options, grid/list bookCard/bookRow elements render. **2026-05-09 round-3 device verify** (`dev-docs/verification/feature-6-20260509.md`, `result: pass`): closes the relaunch-persistence leg that round-2 deferred. On iPhone 17 Pro Sim (iOS 26.4) at v3.14.96, sort changed from Title (default) → Date Added via UI, then `xcrun simctl terminate` + `xcrun simctl launch --uitesting --seed-books` (no `--reset-preferences`) — post-relaunch library re-displays Date Added order byte-identical to pre-terminate state, AND the sort picker shows ✓ on Date Added confirming structural persistence. UserDefaults survives terminate (independent of `--uitesting`'s in-memory SwiftData decision); `--reset-preferences` is the explicit opt-in for clearing it (per bug #152 fix). 3 screenshot artifacts. View-mode toggle relaunch persistence shares the same `PreferenceStore` plumbing — round-2's XCUITest coverage is sufficient there. Original evidence at `dev-docs/verification/feature-6-20260507.md` + `feature-6-20260508.md`. GH: #379                                                                  |
| 7  | Visual feedback when adding a bookmark                                         | Reader/*     | Low      | VERIFIED  | **2026-05-13 round-3** (`dev-docs/verification/feature-7-20260513.md`, result=**pass**, verification-exception path) on v3.21.5 build 282 (commit `727fdb3`): bug #172 FIXED via PR #568 (v3.18.1) wired haptic across all 5 reader paths — code-read confirms MD `MDReaderContainerView:245`, TXT `TXTReaderContainerView:479`, EPUB `EPUBReaderContainerView:177`, PDF `PDFReaderContainerView:179`, AZW3 `FoliateSpikeView:70`. `BookmarkFeedbackTests` (5) + `ReaderNotificationHandlerTests` (12, including 2 new haptic-fires-on-success + suppress-on-failure) PASS on iPhone 17 Pro Sim. Verification-exception path applies because haptic is a tactile signal with zero simulator observability; high-fidelity tests at real `NotificationCenter` + real `HapticFeedbackProvider` protocol seam drive the same production dispatch. **VERIFIED.** WI-002. UIImpactFeedbackGenerator(.light). 5 tests. 2026-05-07 dedicated slice (cross-ref to feature #1's run): BookmarkFeedback 5 tests pin haptic-fire on add (incl. pre-existing toggle), no-fire on error / nil provider, no debounce, intensity = .light. `dev-docs/verification/feature-7-20260507.md` (`result: partial` — physical haptic only verifiable on a real device; simulator does not vibrate). **2026-05-12 verify round-2 finding**: dispatch path is correct at the handler boundary (`ReaderNotificationHandlers.handleBookmarkRequest` → `deps.hapticFeedback?.triggerLightImpact()`, pinned by 5 BookmarkFeedbackTests with `MockHapticProvider`), but only 1 of 5 reader containers wires `HapticFeedbackProvider()` into its deps: `MDReaderContainerView.swift:245`. TXT's `makeNotificationDeps()` omits the parameter (defaults to nil → no-op via optional chaining); EPUB/PDF/Foliate observe `.readerBookmarkRequested` inline and bypass `ReaderNotificationHandlers` entirely. Filed as **bug #172 / GH #558** (Reader/* Low) — the broken cross-format wiring. Feature row stays DONE; VERIFIED flip is blocked until either (a) bug #172 fix lands the haptic across all 5 reader paths, or (b) feature scope is explicitly narrowed to MD-only. GH: #401                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| 8  | Reading position scrubber/progress bar                                         | Reader/*     | Medium   | VERIFIED  | WI-004a-d. ReadingProgressBar + per-format wiring (TXT/MD/PDF/EPUB). 2026-05-07 slice verify: 83 tests across 3 dedicated progress suites (ReadingProgressBar shared view + TXT/MD continuous + PDF page-based; EPUB chapter+progression via Foliate Locator covered cross-feature). `dev-docs/verification/feature-8-20260507.md` (`result: partial` — UI gesture deferred). Round 2 (`feature-8-20260508.md`, v3.14.90) result=**pass**: CU drove both deferred UI-gesture slices end-to-end on iPhone 17 Pro Sim — drag scrubber from left to right at y=580 → reader jumped from "Paragraph 1" / 0% to "Paragraph 91" / 90%; swipe-down on content → reader moved to "Paragraph 88" / 85%, scrubber thumb position + labels updated live. Bidirectional sync (scrubber↔scroll) live without observable latency. Status → VERIFIED. GH: #375 |
| 9  | Comprehensive book context menu in library                                     | Library/*    | Medium   | VERIFIED  | WI-006. Info/Share/Delete + BookInfoSheet. 2026-05-07 slice verify: 35 tests across 4 suites (FileSizeFormatter, BookInfoSheet metadata rendering, LibraryBookItem.resolvedFileURL, ShareSheet.shareItems). Set Cover + Delete actions verified cross-feature (Set Cover via #6 PhotosPicker, Delete via LibraryViewModel surface). **2026-05-08 round-2 slice**: long-press → context menu → Delete leg now exercised end-to-end via `vreaderUITests/Library/DeleteConfirmationTests` (6 XCUITest methods, 160.2 s) — long-press surfaces the menu, Delete option fires confirmation alert containing book title, Cancel dismisses, Confirm removes the row, swipe-to-delete shares the same path, deleting all 8 seeded books transitions to empty state. **2026-05-09 round-3 device verify** (`dev-docs/verification/feature-9-20260509.md`, `result: pass`): closes the Info + Share legs that round-2 deferred. On iPhone 17 Pro Sim (iOS 26.4) at v3.14.95 with `--seed-books`, long-press on "Test EPUB Book" surfaces the 6-item context menu (Info / Share / Set Cover / Remove Cover / Add to Collection / Delete with destructive coloring on the last two). Tapping Info presents `BookInfoSheet` with the seeded fixture's metadata (Title `Test EPUB Book`, Author `Test Author`, Format `EPUB`, File Size `102 KB`, Date Added `Mar 9, 2023 at 04:26`). Tapping Share presents `UIActivityViewController` with the EPUB file (sha256-named `epub_0000000000000000…`) and standard Copy / Save to Files actions. Combined with round-2's Delete coverage, every production menu item is now driven end-to-end. 3 screenshot artifacts. Original evidence at `dev-docs/verification/feature-9-20260507.md` + `feature-9-20260508.md`. GH: #377                                       |
| 10 | iCloud backup and restore                                                      | Settings/*   | —        | WONT DO   | Not needed. WebDAV (#29) covers backup needs.                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| 11 | EPUB text highlighting and note-taking                                         | EPUB/*       | High     | VERIFIED  | **2026-05-09 round-4** (`dev-docs/verification/feature-11-20260509-round4.md`, result=**pass**): VERIFIED. Round-3 gap closed by this session's bug #159 / GH #472 fix (XPath namespace rewrite + CSS Highlight API priority + foliate-bridge.js DOMContentLoaded timing fix). Re-verified end-to-end on iPhone 17 Pro Sim (iOS 26.4) at v3.14.118: long-press on word "testing" in mini-epub3 paragraph 1 → custom VReader menu (Highlight \| Add Note \| Copy) → tap Highlight → **yellow background visibly painted**; tap back → Library → reopen book → **yellow paint + Highlights tab entry both persist after close+reopen** (live probe confirms `cssHighlightCount: 1, styleEls: 1` post-reopen — restore path correctly re-injected style element AND re-registered the highlight). 3 screenshot artifacts. Combined with round-1's persistence + count slices, round-2's bridge-readiness probes, round-3's gesture + data-layer slice, feature #11 has full end-to-end coverage. AZW3 cross-format check remains scoped to feature #11's "AZW3 highlights — Selection capture + CFI anchoring shipped; overlay restoration deferred to WI-7" note. **2026-05-09 round-3** (`dev-docs/verification/feature-11-20260509.md`, result=**partial**): closed the long-deferred gesture-driven user-flow slice. Long-press text selection on word "some" in mini-epub3 Chapter One paragraph 3 → iOS shows custom VReader menu (Highlight \| Add Note \| Copy) alongside standard system menu → tap Highlight → selection dismisses → side panel Highlights tab shows row "🟡 some" with yellow color dot. **Data layer + gesture path PASS.** **Visual render FAIL**: word "some" appears with IDENTICAL styling to surrounding text — no yellow background painted on the EPUB page. Reproducible in Sepia AND Light theme; persists across book close+reopen. Filed as bug **#159 / GH #472** (EPUB/* Medium): CSS Highlight API paint pipeline gap — bridge accepts call + data persists, but `::highlight()` rule injection / `CSS.highlights.set` paint not firing. VERIFIED gated on #159 fix. 4 screenshot artifacts. Bonus regression: bug #158 picker shown for EPUB ✓, bug #128 banner copy update visible ✓, bug #128 native-mode no-op (Lorem ipsum verbatim) ✓. WI-C00 → WI-007. Bug #77 FIXED (JS buffering). Sim verification 2026-05-06 result=**partial** (`feature-11-20260506.md`): persistence + count-exposure slices PASS. Round 2 (`feature-11-20260506b.md`, post-bug-#126/v3.14.13): **bridge-readiness slice now PASSES via the new eval surface** — 6 probes against mini-epub3 confirm `__vreader_createHighlight` defined as function, `selectionChanged` message handler registered, CSS Highlight API supported, DOM ready, 3 `<p>` parsed, programmatic `CSS.highlights.set` round-trips with correct rangeText. **Still deferred**: gesture-driven user-flow (text selection → menu tap "Highlight" → bridge call) — requires UI driving which DebugBridge doesn't synthesize. Status stays DONE pending gesture-driven slice. GH: #404                                                                                                                                                                                                                                                                                                                                                                                                                |
| 12 | Auto-generate TOC for MD files                                                 | Reader/*     | Medium   | VERIFIED  | WI-005. Regex heading extraction, fenced code block skip, correct UTF-16 offsets. TOC builder slice = 25 tests; broader MD subsystem (parser, loader, types, renderer, metadata extractor, ViewModel, integration suites, unified-mode reflow, text extractor) brings 2026-05-07 verify total to 138 tests across 19 suites. `dev-docs/verification/feature-12-20260507.md` (`result: partial` — UI gesture scroll-to-heading deferred). **2026-05-13 round-2** (`dev-docs/verification/feature-12-20260513.md`, result=**pass**): closes deferred UI gesture leg end-to-end on iPhone 17 Pro Sim (iOS 26.5) at v3.21.1. `MDTOCVerificationTests` (14.4s): seed `--seed-md-toc` (5-heading MD fixture) → tap "Test Markdown TOC" → annotations panel → `tocEmptyState` absent + `tocRow-...:Introduction:0` found → tap → panel dismissed (`handleNavigate` fired `onNavigate + onDismiss`) → back button still present (no crash). All acceptance criteria now pass. GH: #373                                       |
| 13 | AI book/chapter summarization                                                  | AI/*         | High     | VERIFIED  | WI-D00 → WI-009 → WI-010. Bug #92 FIXED (encoding). Device verified: non-UTF-8 TXT → AI summarize shows real content. 2026-05-07 slice verify: 44 tests across 5 suites — AIAssistantViewModel (11 tests; idle/disabled/consent/success/error/empty/cache/reset + per-action dispatch summarize/translate/vocabulary/askQuestion), AIReaderIntegration (12 tests; end-to-end summarize call, panel display, error path, button hiding when feature disabled, content truncation, loading state, gates, **PDF + EPUB format-specific summarize**, reset), AIReaderAvailability (8 tests; live transition on consent revoke, hasAPIKey, gate composition), AIRequest.cacheKey (7 tests; determinism — context-text-included key, nil-fingerprint general prefix), AIConfiguration (6 tests; round-trip + corrupt-data tolerance + partial-config forward-compat). Bug #92 regression covered cross-ref via summarize tests. **2026-05-10 round-2** (`ai-features-end-to-end-20260510.md`, result=**pass**) on merged-main 41067e5 (v3.14.123): closes deferred live-endpoint slice. AI Assistant sheet → Summarize tab → blue Summarize button → real Ring 2.6 response with accurate book-content summary of mini-epub3: "The text is a minimal, synthetic EPUB used for automated testing. It contains two short chapters: the first includes a few paragraphs (the second uses Lorem-ipsum filler and some inline markup), while the second chapter is deliberately brief to allow chapter-navigation tests. The whole file ends with 'End of fixture.'" — structurally faithful to fixture. New Request affordance verified. Status → VERIFIED. GH: #394                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| 14 | AI chat — talk to the book                                                     | AI/*         | High     | VERIFIED  | WI-D00 → WI-009 → WI-010 → WI-011. Multi-turn chat with book context via AIChatViewModel. Chat tab in AIReaderPanel. 121 tests across 11 suites (chat VM, assistant VM, reader integration + availability, AIService, config, context extractor, request-cache key, response cache, consent). **2026-05-10 round-2** (`ai-features-end-to-end-20260510.md`, result=**pass**) on merged-main 41067e5 (v3.14.123): closes deferred UI gesture + real-endpoint slice. Reader chrome AI Assistant button → sheet → Chat tab shows book-aware "Ask questions about this book" empty state. Sent "Title only please" → Ring 2.6 returned "VReader DebugBridge Test" — correctly references mini-epub3's actual chapter heading (book-context injection working). Multi-turn history persists; rate-limit yellow banner with Dismiss error button verified on error path. Status → VERIFIED. GH: #363                                |
| 15 | AI chat interface (general)                                                    | AI/*         | Medium   | VERIFIED  | WI-013. General chat (nil bookFingerprint). Entry point in LibraryView toolbar. 2026-05-07 slice verify: 45 tests across 3 suites — AIChatViewModel (23 tests; book/general bifurcation, multi-turn, sliding-window, Unicode/CJK, error/loading, gating-rejection paths), AIChatGeneralMode (9 tests; Library-toolbar entry-point gating, switch between book↔general, ViewModel nil-fingerprint, distinct empty state), AIService (17 tests; gate ordering flag→consent→API-key, cache-key determinism, stream gating, live-flag toggle). **2026-05-10 round-2** (`ai-features-end-to-end-20260510.md`, result=**pass**) on merged-main 41067e5 (v3.14.123): closes deferred UI + real-endpoint slice. Library toolbar Chat icon (gated by AIReaderAvailability — feature flag + key + consent) → AI Chat sheet → "Start a conversation" empty state (vs book-aware "Ask questions about this book") → typed "Say hi in one word." → Ring 2.6 returned "Hi". Real network round-trip end-to-end. Multi-turn history persists across sheet close+reopen. Status → VERIFIED. GH: #391                                                                                                                                                                                                                                                                                                                                                                                                                          |
| 16 | Remote server integration (claude CLI / directory management)                  | Server/*     | High     | DEFERRED  | WI-014 (design only). Design doc at docs/codex-plans/remote-server-design.md                                                                                                                                                                                                                                                                                                                                                                                                            |
| 17 | PDF text highlighting, annotation, and theming                                 | PDF/*        | High     | DONE      | WI-C00 → WI-008. PDFAnnotationBridge + selection detection + persist/restore. 157 tests across 17 suites (annotation bridge, page navigator, ViewModel, position service, locator, session, lifecycle). 2026-05-07 slice verify: `dev-docs/verification/feature-17-20260507.md` (`result: partial` — UI gesture + PDFView native render deferred). GH: #361                                                                                                                          |
| 18 | AI-powered contextual translation with bilingual view                          | AI/*         | High     | VERIFIED  | WI-012. Bug #95 FIXED (initialTab). Device verified: Select word → Translate → opens Translate tab. 92 tests across 8 suites (translation VM, shared AIService + config + context + cache + consent + assistant orchestrator). **2026-05-10 round-2** (`ai-features-end-to-end-20260510.md`, result=**pass**) on merged-main 41067e5 (v3.14.123): closes deferred mid-sheet language change + real-endpoint slice. AI Assistant sheet → Translate tab → "Translate to: Chinese" picker reveals 9-language dropdown (Chinese/Japanese/Korean/Spanish/French/German/Portuguese/Russian/Arabic) → blue Translate button → Ring 2.6 returned bilingual view: Original (English Chapter One) + Chinese ("第一章 第一章 这是一段位于第一章节开头、用于 VReader DebugBridge 自主测试的小型合成 EPUB 的首段文字...") — accurate translation, structurally faithful. Mid-sheet language change verified by tapping Japanese in picker (label updates immediately). Translate panel rate-limit error renders cleanly with "Try Again" button (better UX than chat panel's persistent banner). Status → VERIFIED. GH: #365                                                                       |
| 19 | ~~Merged into feature #6~~                                                     | Library/*    | —        | DUPLICATE | Display mode persistence merged into feature #6 (library view preferences)                                                                                                                                                                                                                                                                                                                                                                                                              |
| 20 | Sort order reset/revert to default                                             | Library/*    | Low      | VERIFIED  | WI-001 (bundled with #6). 2026-05-07 cross-ref slice: reset path counted in feature #6's 36-test LibraryViewModel run; no separate test file. `dev-docs/verification/feature-20-20260507.md` (`result: partial` — UI gesture: sort menu→Default→list re-sorts deferred, needs display). **2026-05-09 round-2 device verify** (`dev-docs/verification/feature-20-20260509.md`, `result: pass`): on iPhone 17 Pro Sim (iOS 26.4) at v3.14.94 with `--seed-books --reset-preferences`, the round-trip Title (default) → Date Added (re-orders alphabetical → date-based) → Title (returns to original order) is verified end-to-end. 4 screenshot artifacts. **Documentation finding** — the row's "Default option in sort picker" wording was misleading: the actual `LibrarySortOrder.allCases` picker shows 4 named options (Title / Date Added / Last Read / Reading Time) without a separate "Default" item; "reset to default" is achieved by re-selecting Title (the source-of-truth default at `LibraryViewModel.swift:42`). The capability works as intended; only the description was off. GH: #402                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| 21 | Paginated reading mode with turnable pages                                     | Reader/*     | High     | VERIFIED  | B04-B13. Bug #82 FIXED (preserve navigator). Sim verification 2026-05-06 result=**partial** (`feature-21-20260506.md`): EPUB paged-mode CSS state verified active via the new bug #126 eval surface — `vreader-pagination` style element injected, `column-width: 362px` (matches `paginationCSS()` formula), `column-gap: 40px` (matches `EPUBPaginationHelper.columnGap`), `column-count: auto`, `body overflow: hidden`. Direct plist write of `readerEPUBLayout = paged` switched mode; reset+seed+open then eval probes confirmed CSS layout is real-render-correlated, not just configured. **Multi-page navigation slice was DEFERRED** at round-1: mini-epub3 chapter 1 fit on one column-page (scrollWidth == clientWidth), so `EPUBPaginationHelper.navigateToPageJS` wasn't observable on that fixture. **2026-05-11 round-2** (`feature-21-20260511-round2.md`, result=**pass**) on merged-main 639b5fa (v3.15.1, build 263): closes the deferred slice end-to-end against Alice's Adventures in Wonderland (137KB real-length EPUB, imported via OPDS during feature #36 round-5). Chapter 2 (Project Gutenberg license + Contents) produces `body.scrollWidth = 1458 > clientWidth = 980`. Setting `body.scrollLeft = 980` clamps to `maxScroll = 478` (browser-level, expected), and the rendered view scrolls horizontally from "license-left-col + Contents-right-col" to "Contents-left-col + blank-right" — bidirectional round-trip via `scrollLeft = 0` restores the original two-column view. `totalPagesJS` returns 2 for this chapter, matching `ceil(1458/980)`. EPUB Layout picker (Scroll/Paged) confirmed in AX tree at sheet pos (463, 985); Page Turn Animation section (None/Slide/Cover) rendering is runtime-conditional on `store.epubLayout == .paged` and IS rendered — runtime-confirming the active layout. CU-substitute toolkit observation: CGEventPost mouse-drag does NOT reach WKWebView's touch handler so swipe-gesture page-advance can't be verified that way; DebugBridge `eval` is the only verifier-driveable path for the navigation primitive — a verification-tooling gap, not a vreader defect. 6/6 acceptance criteria PASS. Status → VERIFIED. GH: #405 |
| 22 | Highlight matching text in search result list                                  | Search/*     | Medium   | VERIFIED  | Bold/highlight query term in search result row snippets. Sim verification 2026-05-07 result=**partial** (`feature-22-20260507.md`): 15 `HighlightedSnippetTests` PASS — single-word/multi-word/case-insensitive matching, adjacent-match coalescing into one bold run, non-adjacent matches as separate runs, FTS5 tag stripping, regex-special-char escaping (security gate against `NSRegularExpression` injection), empty query/whitespace/empty-snippet/no-match plain-text fallback, duplicate-word + extra-spaces edge cases. Production wiring at `SearchResultRow.swift` confirmed by code-read. Round 2 (`feature-22-20260508.md`, v3.14.89) result=**pass**: CU drove the deferred UI render slice end-to-end on iPhone 17 Pro Sim — search "100" produces 1 result with `100` rendered bold in "Paragraph **100**: This is test content for verifying rea..." snippet; search "Reading" produces 200+ results, each snippet shows lowercase "reading" runs in bold weight (case-insensitive match working at UI layer, separate bold runs per occurrence). Section label rendered in regular/secondary weight (not spuriously bolded). Status → VERIFIED. GH: #350 |
| 23 | Auto-generate TOC for TXT files                                                | Reader/*     | Medium   | VERIFIED  | **2026-05-13 round-4** (`dev-docs/verification/feature-23-20260513.md`, result=**pass**) on v3.21.4 build 281 (commit `9fa68c7`): closes acceptance criteria via XCUITest run of `Feature23TXTTocVerificationTests` against `.warAndPeace` seed. 2/2 tests pass on iPhone 17 Pro Sim (iOS 26.5) — TOC populates from `Chapter 1/2/3` markers (rule #3 enabled match), tap-to-navigate dismisses panel + keeps reader loaded. WI-2 seed-mismatch finding (verify cron 2026-05-13 06:24) closed by 1-line `.books → .warAndPeace` patch. **VERIFIED.** B01. Bug #83 FIXED (14/25 rules enabled). Sim verification 2026-05-05 result=**partial** (`dev-docs/verification/feature-23-20260505.md`): auto-TOC detection PASS — **1860 chapters** correctly extracted from 14 MB CJK web novel `《黑暗血时代》`, monotonic offsets, robust to multi-digit Chinese numerals (一→一千八百六十) and full-width / regular space variants, front-matter `前言` included. UI render in `TOCListView` and tap-to-navigate were not driven this iteration (MCP screenshot harness failed; DebugBridge URL scheme not registered in Info.plist — bug #121). Detection cache is durable on-device evidence; UI legs deferred until harness recovers. **2026-05-09 round-2 attempt** (`dev-docs/verification/feature-23-20260509.md`, `result: blocked`): DebugBridge URL-scheme harness IS recovered (per #121 fix), but the new blocker is fixture-content. Both available bundled TXT fixtures don't trigger TOC display in the UI: (a) `--seed-position-test` synthetic content is `Paragraph N` lines with no chapter markers (correct empty TOC); (b) `vreader-debug://seed?fixture=war-and-peace` (1708-byte stub with `CHAPTER I/II/III`) shows the `CHAPTER I` heading at the top of the reader but treats the whole file as 1 chapter (`1/1` pager), and the Contents tab still says `No Table of Contents` — likely because the English Roman-numeral chapter rule is among the 11/25 disabled Legado rules. UI render + tap-to-navigate verification BLOCKED on a chapter-marked TXT fixture matching an enabled rule (CJK `第N章` is the safest); 4 screenshot artifacts. GH: #238 |
| 24 | Book source scraping (web novels)                                              | BookSource/* | High     | DONE      | D01-D07. Bugs #100, #101 FIXED (modelContext.save + SchemaV4). Device verified: Import JSON → sources visible → search works. 2026-05-07 slice verify: 211 tests across 20 suites (BookSource model + 4 rule types + RuleEngine + CSS/regex evaluators + Legado importer 5 sub-suites + pipeline + HTTPClient + encoding detector + chapter cache + update checker + source sharing). `dev-docs/verification/feature-24-20260507.md` (`result: partial` — live HTTP scraping deferred). GH: #370 |
| 25 | Configurable tap zones                                                         | Reader/*     | High     | DONE      | A03. TapZone section in ReaderSettingsPanel — 3 Pickers (left/center/right → TapAction). TapZoneStore wired through settings sheet. Sim verification 2026-05-07 result=**partial** (`feature-25-20260507.md`): 26-test data-layer slice PASS — `TapZoneConfig.zone(atX:totalWidth:)` exhaustive boundary math (X = ⅓ / ⅔ / 0 / negative / exceeding-width), action mapping, Codable round-trip, 4 actions × 3 zones combinations, `TapZoneModifier` default 100pt bottom-inset (bug #63 fix), config preservation. `TapZoneDispatcher` (4-case switch) verified by code-read. **2026-05-08 round-2 slice**: Reader Settings Panel presentation + Tap Zones section presence leg now exercised end-to-end via `vreaderUITests/Reader/ReaderSettingsPanelTests` (4 XCUITest methods, 38.3 s) — `readerSettingsButton` tap presents `readerSettingsPanel`, Theme section header renders (panel populated, not stub), Tap Zones section header reachable after Form scroll (SwiftUI Form lazily renders below-fold sections), Reading Mode picker exposes Native option (cross-ref: gates #27/#28/#21), accessibility audit clean. Per-picker (Left/Center/Right Zone rows) deferred — iOS 26 SwiftUI Form Picker label rendering inconsistent. `dev-docs/verification/feature-25-20260508.md` (`result: partial` — real tap → handler dispatch, Settings UI driving, native-mode safety still deferred). **2026-05-10 round-3 BLOCKED** (`feature-25-20260510-round3-blocked.md`) + **round-4 FAIL** (`feature-25-20260510-round4.md`) → filed bug #162 (FIXED + verified). **2026-05-10 round-5** (`feature-25-20260510-round5.md`, result=**partial**) on merged-main 41067e5 (v3.14.123): closes the **center-zone real-tap dispatch** slice end-to-end on iPhone 17 Sim with mini-epub3 in Unified mode. Reading Settings → 3 zone pickers exposed at AX: Left=Previous Page (463,771), Center=Toggle Toolbar (463,821), Right=Next Page (463,871) — defaults match `TapZoneConfig.default`. After dismissing Settings, real tap at mac (625, 600) — center-third of viewport — toggled chrome OFF; second tap at same point toggled chrome ON. Bidirectional confirms `TapZoneOverlay` → `TapZoneConfig.zone(atX:)` → `TapZoneDispatcher.handle(.toggleToolbar)` firing on the live unified render path. **DEFERRED**: Left/Right zone page-turn dispatch — taps fire correctly but mini-epub3 fits in single paged column (`scrollWidth == clientWidth` per feature #21 finding), so `EPUBPaginationHelper.navigateToPageJS` has nothing to advance to. Same multi-page-fixture blocker as feature #21. 4 screenshot artifacts (Tap Zones section visible + 3-pickers + chrome-toggled-off + chrome-toggled-on). No bugs filed. Status stays DONE; flip to VERIFIED needs a multi-page EPUB fixture. Original evidence at `dev-docs/verification/feature-25-20260507.md`. GH: #342 |
| 26 | Text-to-Speech read aloud                                                      | Reader/*     | High     | DONE      | B03+E06. Bugs #96, #97 FIXED. Sim verification 2026-05-07 result=**partial** (`feature-26-20260507.md`): 101 tests PASS across 17 suites — TTSService state/speed/position/text-extraction(incl.CJK)/edge-cases (22 tests via MockSpeechSynthesizer); HTTPTTSProvider config/synthesis/chunking/cancellation/caching/position/Azure (43 tests via MockURLSession); FoliateTTSAdapter init/start/next/prev/setMark/parse/type (36 tests, JS-string generation). Three TTS providers integrate through one state machine. DEFERRED: audio playback + UI start/stop gestures + live HTTP TTS server + Foliate WebView render — all need audio session + UI driving. Status stays DONE pending UI-driven slice. **2026-05-09 round-2** (`dev-docs/verification/feature-26-20260509.md`, result=**pass**) closes the TXT UI gesture cycle on iPhone 17 Pro Sim against v3.14.111 (commit 239ac16). Full Pause/Resume/Stop cycle exercised end-to-end: tap speaker → TTS bar appears with ⏸/◾/1.2x + view auto-scrolls to CHAPTER I; tap Pause → bar shows ▶, auto-scroll halts mid-CHAPTER II; tap Resume → bar shows ⏸, auto-scroll continues through CHAPTER III; tap Stop → TTS bar removed, reader chrome restored at 56% / "2m read". State machine `.idle → .speaking → .paused → .speaking → .idle` traversed. Active audio playback inferred from observed auto-scroll progress (0% → 56%) which only fires on real `AVSpeechSynthesizerDelegate.willSpeakRange` callbacks. Cross-ref: feature #41 round-2's TTS-Start gesture evidence (same fixture). 4 screenshot artifacts. **DEFERRED**: live HTTP TTS server (third-party endpoint dep) + Foliate WebView TTS render (EPUB/AZW3 separate slice). Status stays DONE pending Foliate slice or capability-gate. GH: #359 |
| 27 | Content replacement rules                                                      | Reader/*     | Low      | VERIFIED  | **2026-05-09 round-3** (`dev-docs/verification/feature-27-20260509-round3.md`, result=**pass**): VERIFIED. Pivoted away from TXT (now correctly hidden from Unified per bug #158 fix) to mini-epub3.epub. Added "Lorem ipsum" → "REPLACED_LOREM" rule via Settings → Replacement Rules; banner copy confirmed live with post-#158 update ("currently EPUB, MD, AZW3 — not TXT or PDF"). Opened mini-epub3 → AA → Reading Mode picker IS shown for EPUB (capability-gated correctly post-#158). Switched to Unified, terminated + relaunched (rule pipeline only loads at `.task` time when readingMode == .unified, so mode-switch mid-session needs an app restart). Reopened mini-epub3 → reader renders Chapter One in Unified mode showing "Second paragraph. **REPLACED_LOREM** dolor sit amet, consectetur adipiscing elit. ..." instead of "Lorem ipsum dolor sit amet" — **rule applied at render time end-to-end**. Combined with round-1's data-layer slice (14 `ReplacementTransformTests`) + round-2's Native no-op confirmation, feature has full e2e coverage. Mode-switch UX caveat documented (not a bug — known design tied to `.task` load timing); could become a follow-up if user impact warrants. 1 screenshot artifact (`feature-27-r3-unified-epub-rule-applied-20260509.png`). E03. Bug #98 FIXED (sourceText + didSet re-apply). Sim verification 2026-05-06 (slice): `ReplacementTransform` unit tests pass (14 tests cover string/regex/CJK/disabled/invalid/offset-map/expansion/deletion). Production wiring confirmed by reading `ReaderContainerView.onAppear:195` + `loadReplacementRules` in `ReaderContainerView+Sheets.swift:97`. Discovered: native mode silently no-ops the transform pipeline → **bug #128** (GH #275) filed. Mitigation shipped this round: info banner in `ReplacementRulesView` ("Rules apply only when reading in Unified mode") so users see the limitation while configuring rules. Proper fix (wire native pipeline through transforms) tracked as follow-up. VERIFIED still gated on real-render verification in Unified mode (computer-use UI driving needed). 2026-05-07 evidence formalised at `dev-docs/verification/feature-27-20260507.md` (14 tests re-run + production wiring code-read confirmed; result still partial; native-mode no-op remains a known limitation per bug #128). bug #128 GH: #275 (since FIXED + CLOSED — banner shipped). **2026-05-09 round-2** (`dev-docs/verification/feature-27-20260509.md`, result=**blocked**): attempted real-render verification in Unified mode — added "Pierre" → "Peter" rule via Settings → Replacement Rules, opened war-and-peace.txt, switched to Unified mode, but Unified renderer truncates the fixture at end of CHAPTER II (Pierre on line 16 unreachable), missing bottom chrome, no TOC, and goes fully blank when toggled from Native at non-zero scroll position. Native mode behavior confirmed: at 47% scroll Pierre renders verbatim (rule does NOT apply per bug #128 — expected). Filed compound bug **#158 / GH #468** (Reader/* Medium, Unified TXT renderer + chrome + toggle issues). VERIFIED gated on bug #158 fix (either capability-gate Unified for TXT or wire UnifiedScrollView properly). 2 screenshot artifacts. bug #128 GH: #275. GH: #397 |
| 28 | Simplified/Traditional Chinese conversion                                      | Reader/*     | Medium   | VERIFIED  | E04. Bug #98 FIXED (sourceText + didSet re-apply). Sim verification 2026-05-05 result=**fail** (`dev-docs/verification/feature-28-20260505.md`): wiring was unified-mode-only. **WI-A (2026-05-12, PR #573, commit a9110a8, v3.21.0/build 277)**: TXT native-mode conversion wired — `attrStringKey` includes `chineseConversion`, `SimpTradTransform` applied before all three render paths. **2026-05-13 VERIFIED** (`dev-docs/verification/feature-28-20260513.md`, result=**pass**): XCUITest `ChineseConversionPickerGateTests` (2 tests, iPhone 17 Pro Simulator iOS 26.5) confirmed picker enabled for TXT native mode (enabled footer present, all 3 segments hittable). 13 unit gate tests + 4 attrStringKey rebuild tests all green. GH: #239 |
| 29 | WebDAV backup and restore                                                      | Settings/*   | Medium   | VERIFIED  | PR #128. Full data collector/restorer + BackupViewModel + WebDAVSettingsView backup section + Docker integration test. Manually verified end-to-end on iPhone 17 Pro simulator: credentials → Back Up Now → list → Restore → Delete all work against a real Bytemark/Apache WebDAV server. 2026-05-07 slice verify: 135 tests across 11 suites (ViewModel, collector, provider contract, WebDAVClient, WebDAVProvider [+manifest], BlobStore, ATS, ZIPWriter, BlobPath, manifest, projection). `dev-docs/verification/feature-29-20260507.md` (`result: partial` — gated live `WebDAVBackupIntegrationTests` deferred). **2026-05-11 round-2** (`feature-29-20260511-round2.md`, result=**pass**) closes the round-1 deferred slice on merged-main 187314e (v3.14.148): live `WebDAVBackupIntegrationSuite` ran end-to-end against a real WebDAV server (rclone serve webdav 127.0.0.1:8082, vreader:test123, fresh /tmp/vreader-webdav-verify-r2 data dir). Both gated tests pass — `webDAVClientUploadSucceeds()` 28ms (real `WebDAVClient.upload` over `URLSessionConfiguration.ephemeral`); `backupListRestoreDeleteRoundTrip()` 100ms (full BackupDataCollector → ZIPWriter → PUT → list → GET → BackupDataRestorer → DELETE through real `WebDAVProvider`). Pre-existing 135 unit/protocol tests still green. No new bugs filed. Status DONE → VERIFIED. GH: #367 |
| 30 | Custom book covers                                                             | Library/*    | Medium   | VERIFIED  | A01. CustomCoverStore + PhotosPicker in context menu. Sim verification 2026-05-07 result=**partial** (`feature-30-20260507.md`): 16 `CustomCoverStoreTests` PASS — unique-path-per-book, colon/slash sanitization, setCover/getCover/removeCover/hasCover round-trip, large-image resize + small-image no-upscale, empty-key safety, CustomCovers subdirectory layout. Plus live end-to-end via feature #43 round-3 (PR #341): AZW3 auto-save produced 341×512 baseline JPEG on disk. `PersistenceActor.deleteBook` cascade-removes cover (code-read). Round 2 (`feature-30-20260508.md`, v3.14.84) result=**pass**: CU drove the deferred UI gesture end-to-end on iPhone 17 Pro Sim — long-press card → context menu → Set Cover → `PhotosPicker` ("Private Access to Photos" scoped-access flow confirmed) → pick photo → grid re-renders with magenta flowers cover. JPEG (512×384, 117 KB, baseline JFIF+Exif) lands on disk at `CustomCovers/epub_<sha>_102400.jpg` with colons sanitized; cover persists across `terminate` + relaunch. Status → VERIFIED. GH: #347 |
| 31 | Auto page turning                                                              | Reader/*     | Low      | DONE      | B10. Unblocked by bug #82 fix. 2026-05-07 slice verify: 14 tests across 2 suites (AutoPageTurner timer state machine + interval reschedule + cancellation, PageTurnAnimator slide/cover-flip/instant transitions). `dev-docs/verification/feature-31-20260507.md` (`result: partial` — UI gesture for real-time advancement deferred). **2026-05-08 round-2 slice**: Reader Settings UI surface verified end-to-end — Paged segmented control reveals Auto Page Turn toggle + Page Turn Animation picker; sustained-press toggle reveals/hides Interval slider; settings persist across panel re-open. Live multi-page advancement still deferred — Position Test Book paginates to 1 page at 393×852 viewport with 18pt font, so `AutoPageTurner.scheduleTimer`'s at-last-page short-circuit correctly stops the timer without observable advancement. NOT a bug — fixture limit. `dev-docs/verification/feature-31-20260508.md` (`result: partial`). GH: #381                                                                                                                                       |
| 32 | Reading theme backgrounds                                                      | Reader/*     | Medium   | VERIFIED  | A04. PhotosPicker + opacity slider + remove button in ReaderSettingsPanel. ThemeBackgroundStore saves/loads per-theme. Sim verification 2026-05-07 result=**partial** (`feature-32-20260507.md`): 11 `ThemeBackgroundTests` PASS — save/load/remove round-trip, large-image + high-scale-image resize for memory cap, no-upscale for small images, overwrite-existing atomicity, unique path per theme, `.jpg` extension, no-throw on remove-when-missing. Production wiring at `ReaderSettingsPanel.swift:93/184` (PhotosPicker save + Remove call sites) confirmed by code-read. Round 2 (`feature-32-20260508.md`, v3.14.88) result=**pass**: CU drove the deferred UI flow end-to-end on iPhone 17 Pro Sim — Light/Sepia/Dark theme switch updates reader background immediately; Custom Background toggle (sustained press) reveals Opacity slider + Choose Image + Remove Background; tapping Choose Image opens `PhotosPicker` with "Private Access to Photos" scoped-access banner; pick photo → picker dismisses + panel state retained; reader renders the picked magenta-flowers image as a blended background at 15% opacity over the active theme. Status → VERIFIED. GH: #354 |
| 33 | Dictionary / define / translate-on-select                                      | Reader/*     | High     | VERIFIED  | B02. DictionaryLookup + UIReferenceLibraryViewController + AI translate. Sim verification 2026-05-07 result=**partial** (`feature-33-20260507.md`): 19 `DictionaryLookupTests` PASS — 13 selection-parsing tests (English / CJK / mixed / emoji / punctuation / whitespace / newlines / tabs / single-space, multi-word→first-word contract) + 3 Gate-5-class real-system tests hitting `UIReferenceLibraryViewController.dictionaryHasDefinition(forTerm:)` and `init(forTerm:)` directly (no mocks; real Apple system dictionary API) + 2 menu-title localization tests + 1 modal viewController construction. AI Translate path covered by feature #18 separately. Round 2 (`feature-33-20260508.md`, v3.14.86) result=**pass**: CU drove the deferred Define gesture flow end-to-end on iPhone 17 Pro Sim — double-tap word ("reader") → custom 4-item menu (Highlight / Add Note / Define / ▶) → tap Define → `UIReferenceLibraryViewController` modal slides up with term "reader" at top + "No Content Found" body (sim has no dictionaries downloaded, independent of vreader code) + "Search Web" / "Manage Dictionaries" buttons. Plumbing verified end-to-end. Status → VERIFIED. GH: #357 |
| 34 | Collections / tags / series organization                                       | Library/*    | Medium   | VERIFIED  | **2026-05-12 round-6** (`dev-docs/verification/feature-34-20260512.md`, result=**pass**): closed via **verification-exception** per AGENTS.md "Close gate". 17 of 19 acceptance criteria pass with concrete evidence accumulated across rounds 1-5: 30 unit tests at real `PersistenceActor` + `SchemaV6` boundaries (19 CollectionPersistenceTests + 11 model tests, Gate-5-class fidelity), 6 XCUITest methods (CollectionSidebarTests, 49.5s, full sidebar UI presentation + accessibility audit), 4 CU-driven device-verify rounds (create / swipe-delete / add-to-collection / tap-to-filter post-bug-#155 fix at v3.14.110). Remaining 2/19 criteria (rename inline editor + populated Tags/Series sections) are explicit fixture/harness gaps — NOT implementation gaps — tracked for follow-up via feature #45 verification-harness sweep or fixture-catalog extensions. No relevant code changed on feature #34's surface since round-5 (v3.14.110 → current v3.17.2). Host CoreSimulator framework out-of-date (1051.50.0 vs 1051.54.0), so a fresh round-6 sim run isn't physically possible this iteration; verification-exception path consolidates rounds 1-5 + cites all 5 evidence files. C01. Bugs #85, #86 FIXED. Sim verification 2026-05-07 result=**partial** (`feature-34-20260507.md`): Gate-5-class slice via real `PersistenceActor` + real `SchemaV6` (in-memory `ModelContainer`) — 19 `CollectionPersistenceTests` cover create/rename/delete/add/remove/multi-collection-membership/idempotency/cascade-on-deleteBook/Unicode-CJK/case-insensitive-duplicate-rejection; 11 model tests (`BookCollection Model` + `Book Series Fields`) cover init validation + defaults. **2026-05-08 round-2 slice**: Collections sidebar UI presentation leg now exercised end-to-end via `vreaderUITests/Library/CollectionSidebarTests` (6 XCUITest methods, 49.5 s) — sidebar opens from `collectionsToolbarButton` under `Filter` nav title, exposes `filterAllBooks` + `newCollectionButton`, inline TextField + Add button surface on tap with Add correctly disabled on empty input (whitespace-only-name gate), `filterDoneButton` dismisses cleanly, accessibility audit clean. **2026-05-08 round-3 slice**: create → swipe-delete UI round-trip closed via computer-use display capture (`feature-34-20260508b.md`, result=**pass**). CU drove the gesture flow on iPhone 17 Pro Sim: tap "+ New Collection" → text field reveals → type "My list" via per-character `key` actions (CU `type` action doesn't reach iOS Simulator text fields — observation noted, not filed as bug; external CU/Sim infra quirk) → Add button enables blue → tap Add → "My list" row appears with folder icon + count "0" + form returns to entry-point; swipe-left → red Delete affordance revealed; tap Delete → row removed, returns to empty state. 3 PNG artifacts saved (`feature-34-r3-{collection-created,swipe-delete,after-delete}-20260508.png`). **14/18 acceptance criteria pass** across rounds 1-3; 4 deferred (per-book context-menu Add-to-Collection, tap-to-filter, rename inline editor, populated Tags/Series sections — need pre-seeded data or harness extensions). **2026-05-09 round-4** (`dev-docs/verification/feature-34-20260509.md`, `result: partial`): closes 2 of 4 deferred legs, surfaces 1 bug, leaves 2 deferred. PASS — Add-to-Collection from book context menu (long-press → `Add to Collection ›` → submenu lists existing `Reading List` → tap adds book and increments `bookCount` from 0 to 1). FAIL — tap-to-filter from sidebar (tap `Reading List` in sidebar dismisses sheet but library still shows all 8 seeded books, not just the 1 member); root cause is view-layer wiring gap (`LibraryView.gridView/listView` iterate `viewModel.books` directly without consulting `activeFilter`); filed as **bug #155 / GH #451**. DEFERRED — rename inline editor + populated Tags/Series. **16/18 criteria pass post-round-4**; status stays DONE pending bug #155 fix + rename-editor exercise. 4 screenshot artifacts. **2026-05-09 round-5** (`dev-docs/verification/feature-34-20260509b.md`, result=**pass**): closes the deferred tap-to-filter leg now that bug #155 is FIXED + CLOSED in PR #454 (commit 37a17d1, on main since v3.14.95). End-to-end CU drive against v3.14.110 (commit 45ceed1) on iPhone 17 Pro Sim: created "Reading List" collection → added "The Masque of the Red Death" via long-press → Add to Collection submenu → re-opened sidebar (count badge → 1) → tapped Reading List row → **library grid filters from 3 books to 1**, the 2 non-member books correctly hidden. Bidirectional verified — tap All Books restores full 3-book grid. **17/18 criteria pass post-round-5**; only rename inline editor + populated Tags/Series remain deferred (harness/fixture-class gaps, same as round-4). 3 round-5 screenshot artifacts. Original evidence at `dev-docs/verification/feature-34-20260507.md` + `feature-34-20260508.md` + `feature-34-20260508b.md` + `feature-34-20260509.md` + `feature-34-20260509b.md`. GH: #336 |
| 35 | Export / import annotations                                                    | Reader/*     | Medium   | VERIFIED  | **2026-05-13 round-2** (`dev-docs/verification/feature-35-20260513.md`, result=**pass**) on v3.21.6 build 283 (commit `cdb007b`): WI-3's `Feature35AnnotationsExportVerificationTests` (2/2 verify_ methods) confirms `annotationsExportButton` + `annotationsImportButton` exist + are hittable in the `annotationsPanelSheet` toolbar after tapping `readerAnnotationsButton`. Combined with round-1's 47-test data-layer slice (Markdown/JSON export + VReaderAnnotationParser + AnnotationImporter round-trip), full acceptance criteria covered. System-modal share-sheet/document-picker observability is out of XCUITest scope by design. **VERIFIED.** C02+C03. Bug #88 FIXED (import highlight refresh). Sim verification 2026-05-07 result=**partial** (`feature-35-20260507.md`): JSON export PASS (10 tests), Markdown export PASS (8 tests + CJK + markdown-syntax-in-notes), exporter aggregator PASS (3 tests), JSON parse PASS (13 `VReaderAnnotationParser` tests inc. ISO8601/CJK/Unicode/malformed/forward-compat), Codable round-trip via `AnnotationImporter` PASS (13 tests; encoder settings byte-identical to production `JSONExportFormatter`). DEFERRED: real `PersistenceActor`-backed integration test (mock-store seam is mechanical), UI flow (panel → Export → share → save → file picker → import → panel refresh), bug #88 UI-level verification. Status stays DONE pending UI-driven slice. GH: #334 |
| 36 | OPDS catalog support                                                           | BookSource/* | Medium   | VERIFIED  | C04. Sim verification 2026-05-07 result=**partial** (`feature-36-20260507.md`): parser PASS (24 unit tests cover Atom/OPDS 1.x schema, dedup, navigation vs acquisition, cover-URL, Unicode/CJK); real-backend wire-format PASS via 3 live curl probes against Project Gutenberg (`/ebooks.opds/` root + `/search.opds/?sort_order=downloads` 25-entry listing + `/ebooks/84.opds` per-book detail with EPUB/EPUB3/MOBI acquisition links). **2026-05-08 round-2 slice**: catalog list sheet presentation leg now exercised end-to-end via `vreaderUITests/Library/OPDSCatalogListTests` (4 XCUITest methods, 31.9 s) — sheet opens from `opdsCatalogsToolbarButton` under `OPDS Catalogs` nav title, surfaces toolbar `opdsAddCatalog` (+) entry point, `opdsCatalogsDoneButton` dismisses cleanly, accessibility audit clean. Assertions are state-invariant (sheet shape, not catalog list contents) due to bug #152 (`--uitesting` doesn't reset UserDefaults — `opds.savedCatalogs` persists across launches). **2026-05-08 round-3 slice**: visual leg now closed via computer-use display capture (`feature-36-20260508b.md`, result=**pass**). CU drove the gesture flow on iPhone 17 Pro Sim: globe tap → OPDS sheet → empty-state shape (heading + description + Add Catalog button) all render correctly; toolbar `+` tap → Add Catalog modal with Catalog Info (Name + URL) + Authentication (Username + Password) sections, Save button visually disabled on empty input (whitespace-name gate confirmed); Cancel returns to list, Done returns to Library. Two screenshot artifacts saved: `feature-36-r3-empty-state-20260508.png`, `feature-36-r3-add-catalog-form-20260508.png`. **9/12 acceptance criteria pass; 3 deferred (per-catalog navigation, download → import round-trip, edit/delete)** — all need a real OPDS endpoint plus saved catalogs. Original evidence at `dev-docs/verification/feature-36-20260507.md` + `feature-36-20260508.md`. **2026-05-11 round-4** (`feature-36-20260511-round4.md`, result=**partial**) on merged-main de37ad4 (v3.14.149): attempted the 3 deferred legs against Project Gutenberg OPDS (`https://www.gutenberg.org/ebooks.opds/`, host curl probe returns 200 + valid Atom XML). Saved catalog persistence closes (Gutenberg row survives sheet dismiss/reopen). **Per-catalog navigation FAILS — bug #170 filed (GH #529)**: `OPDSBrowserView` destination renders blank with only back chevron (no spinner, no entries, no error) across t+0.5s / t+2.5s / t+10.5s / t+17s. Suspect `.task` lifecycle quirk in nested sheet→NavigationStack→NavigationLink presentation. **Download/import blocked-by-#170** (can't reach book entries when detail is blank). Edit/delete leg deferred (CGEventPost drag at 200ms is interpreted as tap; slower drag primitive needed). **10/12 criteria pass post-round-4**; status stays DONE pending bug #170 fix. 18 screenshot artifacts. **2026-05-11 round-5** (`feature-36-20260511-round5.md`, result=**pass**) on merged-main 263100a (v3.14.151, post bug #170 fix in PR #531): all 3 round-4 deferred legs now close end-to-end against Project Gutenberg OPDS. **Per-catalog navigation PASS** — saved Gutenberg row → spinner → "Project Gutenberg" feed with Popular/Latest/Random nav entries. **Sub-feed drill-down PASS** — Popular → "All Books" with 11+ real PG books (Hegel, Frankenstein, Moby Dick…); initial timeout triggered `errorState` correctly with Retry button that re-invoked `loadFeed`. **Acquisition entry → detail PASS** — Alice's Adventures in Wonderland → 2-variant sub-feed → `OPDSEntryView` with cover/author/rights/Download EPUB+MOBI buttons. **Download → import round-trip PASS** — tapped Download EPUB → "Downloaded! Book added to library." → backed out all the way to Library and confirmed Alice EPUB book row visible with cover thumbnail + author + EPUB tag (`feature-36-r5-15-library-after-dismiss-20260511.png`). **Swipe-to-delete PASS** — slower 30-step CGEventPost swipe (~750ms total) successfully deletes saved Gutenberg row; catalog list reverts to "No OPDS Catalogs" empty state. Round-4's 200ms drag was too fast (tap-classification); the slower swipe primitive is documented in round-5 evidence as a reusable pattern for `sim-drive-fallback`. **12/12 acceptance criteria PASS**. Status → VERIFIED. 15 round-5 screenshot artifacts. GH: #332 |
| 37 | Per-book reading settings                                                      | Reader/*     | Low      | VERIFIED  | A05. Bug #84 FIXED (applyResolvedSettings + suppressPersistence). Bugs #145 + #147 FIXED (bridge respects per-book; disable re-resolves from globals). Sim verification 2026-05-06 result=**pass for open path** (`feature-37-20260506.md`): two scenarios via direct per-book JSON write + eval probes. (A) Full override `{"themeName":"sepia","fontSize":26,"lineSpacing":1.8}` → body bg `rgb(244, 237, 221)`, color `rgb(58, 43, 22)`, fontSize `26px`, p line-height `46.8px` (= 26×1.8). (B) Partial override `{"fontSize":30}` → body fontSize `30px`, body bg `rgb(28, 28, 30)` (global dark inherited per nil-means-inherit contract). **2026-05-09 round-2 device verify** (`dev-docs/verification/feature-37-20260509.md`, `result: pass`): closes the save-via-UI gesture leg deferred from round-1. On iPhone 17 Pro Sim (iOS 26.4) at v3.14.101 with `--seed-position-test`: open Position Test Book → AA settings panel → scroll to per-book section → drag "Custom settings for this book" toggle ON → toggle flips green AND subtitle transitions to "Font, spacing, and theme changes apply only to this book." Drag toggle OFF → subtitle reverts to "All books share the same settings." Both transitions pin the gesture path that round-1 left unexercised. (CU+iOS Sim Toggle quirk: a 13-px `left_click_drag` is needed to flip SwiftUI Toggle reliably; documented as a verification-tooling quirk, not a vreader defect — same observation noted in feature #34's round-3 evidence.) 3 screenshot artifacts. Combined with round-1's open-path eval probes, the feature is end-to-end verified through the UI surface users actually drive. GH: #406                                                                                                                                                                                                                                                                                                                                                                                                                |
| 38 | Hierarchical/tree TOC display                                                  | Reader/*     | Low      | VERIFIED  | TOCListView indents by entry.level. PDF/MD builders populate nonzero levels. Not a disclosure tree but visual nesting works. Sim verification 2026-05-07 result=**partial** (`feature-38-20260507.md`): 69 tests pass across 4 TOC suites — `TOCBuilder.forMD` (h1→0..h6→5 parametric walk), `TOCBuilder.forTXT` (Legado regex-rule depth), `TOCChapterProgress` (reading position → current chapter), `FoliateTOCConverter` (Foliate-js TOC → flat `TOCEntry` array). View consumption verified by code-read at `TOCListView.swift:125-130` (level → leading padding + font/weight differentiation). DEFERRED: UI render of the indented hierarchy + tap-entry → navigate. **2026-05-09 round-2** (`feature-38-20260509.md`, result=**partial**): all 3 bundled DebugFixtures (mini-epub3, war-and-peace.txt, mini-azw3) yield flat or empty TOC — visual-hierarchy leg structurally blocked on fixture coverage, not implementation. **2026-05-10 round-3** (`feature-38-20260510-round3.md`, result=**partial**) on merged-main 41067e5 (v3.14.123): closes the **tap-to-navigate** slice end-to-end on iPhone 17 Sim with mini-epub3 — tapped Chapter Two row at Accessibility-derived mac coords (1427, 755); reader navigated Chapter 1 of 2 → Chapter 2 of 2, sheet auto-dismissed, bottom chrome flipped "Chapter One/9m read" → "Chapter Two/20m read", progress bar advanced 0% → ~50%. 2 screenshot artifacts. **Calibration technique** (reusable for future verify-cron iterations): query `tell process "Simulator" → entire contents of window 1 → position` for elements where `description contains "Chapter"`, use those mac-space coords directly with CGEventPost — bypasses sheet-presentation y-offset uncertainty. **2026-05-13 round-4** (`feature-38-20260513.md`, result=**pass**) on merged-main b51fcf2 (v3.21.11): closes the **visual-hierarchy** slice end-to-end on iPhone 17 Pro Sim (iOS 26.5) with the `--seed-md-toc` MD fixture which exposes 3 distinct heading levels. TOC panel renders 5 entries with 3 visible indentation tiers: "Introduction" (level 0 — H1) at left margin in body/medium/primary (accent blue + semibold as active row); "Chapter 1/2/3" (level 1 — H2) indented ~20pt in subheadline/regular/secondary; "Section 2.1: A Subsection" (level 2 — H3) indented ~40pt nested under Chapter 2. Matches `TOCRowView.indentPerLevel = 20pt × entry.level` and the body↔subheadline + primary↔secondary differentiation at `TOCRowView.swift:125-130`. Screenshot artifact: `dev-docs/verification/artifacts/feature-38-round4-md-toc-hierarchy-20260513.png`. GH: #352 |
| 39 | ~~Merged into feature #32~~                                                    | Reader/*     | —        | DUPLICATE | Same gap: background image picker UI needed. Merged into #32                                                                                                                                                                                                                                                                                                                                                                                                                            |
| 40 | TTS sentence highlighting                                                      | Reader/*     | Medium   | DONE      | TTSHighlightCoordinator: NLTokenizer sentences → binary search → uiState.highlightRange. TXT/MD wired via onChange(ttsService). Sim verification 2026-05-07 result=**partial** (`feature-40-20260507.md`): 9 `TTSHighlightCoordinatorTests` PASS — English/CJK(。)/mixed-language sentence tokenization via real NLTokenizer; binary-search range lookup + out-of-bounds safety; `updateHighlight` sets `highlightRange` when speaking; `clearHighlight` removes temporary but preserves user-persistent highlights (feature #11/#17 survive). DEFERRED: real TTS playback path (AVSpeechSynthesizer delegate → currentOffsetUTF16 → onChange → coordinator → HighlightableTextView render); HTTP cloud TTS driver path. **2026-05-09 round-2** (`feature-40-20260509.md`, result=**partial**): TTS audibly starts + auto-scroll fires (cross-ref feature #41), but no sentence highlight visible across 5+ screenshots — root cause inconclusive between (a) highlight not rendering, (b) sim audio session quirk, (c) screenshot timing. **2026-05-10 round-3** (`feature-40-20260510-round3.md`, result=**partial**) on merged-main 41067e5 (v3.14.123): **root cause definitively identified** via code-read. Producer side healthy (TTS auto-scroll advanced 0% → 84% over 30s of 1.2× playback, confirming `currentOffsetUTF16` flowing through). Consumer side gated by `TXTReaderContainerView.swift:496` which hardcodes `highlightRange: nil` in chapter-mode rendering branch — same WI-7 deferral as bug #160 (manual highlight chapter-mode). Inline comment at line 496 reads "Highlight offset translation is WI-7". `clearHighlight`-on-stop verified (no leftover band). 3 screenshot artifacts. **Decision: no new bug filed** — structural deferral already tracked by feature #48 WI-7 + bug #160; same fix unblocks both. Status stays DONE; flip to VERIFIED follows WI-7 OR addition of a non-chapter TXT fixture (current full-text branch at line 472 already passes `uiState.highlightRange` correctly — needs only a fixture without chapter markers to exercise). GH: #344 |
| 41 | TTS auto-scroll/paginate                                                       | Reader/*     | Medium   | VERIFIED  | TTSHighlightCoordinator sets uiState.scrollToOffset from sentence start. TXT/MD only via same onChange wiring. Sim verification 2026-05-07 result=**partial** (`feature-41-20260507.md`); round-2 verification 2026-05-09 result=**pass** (`feature-41-20260509.md`): drove TTS end-to-end against war-and-peace.txt on iPhone 17 Pro Sim — view auto-scrolled from 0% (CHAPTER I top, "Well, Prince, so Genoa and Lucca...") to 84% ("End of synthetic fixture excerpt.") over ~4s of 1.2× playback, with sentence-start anchor preventing word-level jitter. Auto-scroll target is sentence START (`scrollToOffset = range.location` at `TTSHighlightCoordinator.swift:66`), NOT word-level. TXT/MD wiring code-read confirmed; MD slice covered by cross-ref (identical seam, no MD fixture available). EPUB/AZW3/PDF intentionally out of scope (different scroll surfaces). Chunked-rendering behavior remains documented constraint (TXT >500K UTF-16 — `TXTReaderContainerView.swift:519` hardcodes `highlightRange: nil` for chunked path). Closes the deferred UI-gesture leg from round-1. GH: #345 |
| 42 | AZW3/KF8 + Foliate-js unified reader engine                                    | Reader/*     | High     | PLANNED   | Replace EPUB bridge with Foliate-js `<foliate-view>`. EPUB+AZW3/MOBI via one engine. PDF/TXT unchanged. GH: #113                                                                                                                                                                                                                                                                                                                                                                        |
| 43 | Extract and display cover images from EPUB/AZW3                                | Library/*    | Medium   | VERIFIED  | EPUB OPF + AZW3 MOBI header parsing. 55 cover-related tests across `EPUBCoverParsingTests` (9), `EPUBMetadataExtractorTests` (14), `MetadataExtractorTests` (18), `MOBICoverExtractorTests` (14). Bug #107 for white-edge padding (separate row, out of scope here). **Verification history**: (1) 2026-05-05 result=**fail** (`feature-43-20260505.md`) → split into bug #122 (EPUB redundant-prefix `href`, FIXED in PR #248 v3.13.12) and AZW3 cover gap for "被讨厌的勇气-1" (root cause inconclusive without deterministic re-import). (2) 2026-05-05 result=**partial** (`feature-43-20260505b.md`) — EPUB redundant-prefix path PASSES via synthetic-EPUB integration test. AZW3 BLOCKED on missing fixture. (3) 2026-05-07 result=**partial** (`feature-43-20260507.md`) — AZW3 cover extraction PASSES end-to-end on live iPhone 17 Pro Sim using freshly-bundled `mini-azw3` fixture (PG ebook 1064, Mobipocket v6, codepage 65001): seed → import → real `BookImporter` → `MOBICoverExtractor` → `CustomCoverStore.saveCover` produces a valid 341×512 baseline JPEG at `CustomCovers/azw3_<key>.jpg`. SwiftData record + on-disk JPEG verified. Filed bug GH #340 (since FIXED + CLOSED — AZW3 EXTH title now reads "The Masque of the Red Death" not the filename). DEFERRED: UI cover-grid render. (4) 2026-05-09 result=**pass** (`feature-43-20260509.md`) — round-4 closes the deferred UI render leg. Library grid on iPhone 17 Pro Sim (iOS 26.4) at v3.14.109 (commit 02de2dd) renders the extracted 341×512 JPEG byte-for-byte for `mini-azw3` ("The Masque of the Red Death" with the cyan/red Project Gutenberg artwork). EPUB-without-embedded-cover (`mini-epub3.epub`) correctly falls back to the synthetic blue/EPUB cover (verified by `unzip -l` showing no embedded cover image). TXT renders the generic icon. All acceptance criteria covered; both bugs filed during prior rounds (#122, #340) are FIXED + CLOSED. **VERIFIED**. GH: #121 |
| 44 | DebugBridge — debug-only URL scheme + state dumper for autonomous testing      | DevTools/*   | High     | VERIFIED    | **2026-05-13 round-15** (`dev-docs/verification/feature-44-20260513-round15.md`, result=**pass**): smoke pass after feature #45 WI-1 (PR #581, v3.21.3, commit `6ad6193`). All 7 handlers pass on iPhone 17 Pro Sim (iOS 26.5). `columnCount: "auto"` vs round-14's `"1"` is a WebKit 26.4→26.5 computed-style reporting difference; `scrollWidth === clientWidth === 980` confirms single-column paged mode is correct. Bug #141 SETTLE 100ms placeholder is tracked separately; settle handler writes the sentinel file correctly with documented schema — this is the contract feature #44 tests. **VERIFIED.** **2026-05-09 round-11** (`dev-docs/verification/feature-44-20260509-round11.md`, result=**pass**): closes device-verification of bug #141's eval-Foliate portion against bundled `mini-azw3.azw3` fixture. PR #311 (v3.14.15) shipped the keyed `DebugReaderRegistry.setActiveFoliateWebView/foliateWebView(for:)` binding for AZW3 but device-verify had been blocked on missing AZW3 fixture (bug #143). Bug #143 FIXED by bundling Project Gutenberg #1064. This round runs eval round-trip on merged-main v3.14.121 (commit `a458a69`): seed → open mini-azw3 → `vreader-debug://eval?bridge=foliate&js=<base64>` returns real DOM-state JSON (`docState: "complete"`, `docHTMLLen: 285987`, `relevantWindowKeys: ["FoliateHost","readerAPI"]`) keyed by fingerprintKey + format. No `unsupportedBridge` / `bridgeNotReady` errors. Snapshot post-open shows non-stub `currentBookId`/`format`. **Status stays DONE; VERIFIED still gated on criterion (d') = bug #141 SETTLE portion** (`awaitSettle` 100ms placeholder + per-bridge ready signal — separate work in bug #141 REMAINING). 1 screenshot artifact. DEBUG-only `vreader-debug://` handler with 7 commands (reset/seed/theme/open/settle/snapshot/eval). Active-reader registry, fixture catalog, stable error codes, release-gate script. 97 unit tests. Doc at `docs/subsystems/debug-bridge.md`. **Verification history**: (1) 2026-05-05 result=**fail** (`feature-44-20260505.md`) → bug #121 (URL scheme not registered, FIXED in v3.13.9). (2) 2026-05-05 result=**partial** (`feature-44-20260505b.md`) → bug #123 (`.onOpenURL` blocked by iOS scheme-approval prompt, FIXED in v3.13.11). (3) 2026-05-05 result=**partial** (`feature-44-20260505c.md`) → bugs #124 (FIXED v3.13.14) and #125 (FIXED v3.13.15). (4) 2026-05-05 result=**partial** (`feature-44-20260505d.md`) — all 7 URL handlers + their observable surfaces (logs, sentinel files, sqlite, snapshot JSON) work end-to-end on merged-main v3.13.15. **Implementation complete; row promoted IN PROGRESS → DONE.** VERIFIED still gated on 4 plan criteria not covered by round 4: (a) EPUB/Foliate coverage — both `settle`'s `relocate` happy-path and `eval`'s Foliate happy-path require an EPUB fixture which `DebugFixtureCatalog` doesn't carry today (TXT-only); (b) Release-build absence-gate — `scripts/verify-release-no-debugbridge.sh` exists but wasn't run this round; (c) all unit+integration tests pass — 19 unrelated pre-existing failures (TXTChapterContentLoader/V1toV2Migration/etc.); (d) LOC <300 — `RealDebugBridgeContext.swift` is 491 lines, split deferred per Codex. (5) 2026-05-05 result=**partial** (`feature-44-20260505e.md`) — round 5 closes acceptance criterion (b) Release-build absence-gate: clean Release build of v3.13.18 (commit `1a8968c2`); `scripts/verify-release-no-debugbridge.sh` exits 0 across all six sub-checks (Info.plist, DebugFixtures dir, fixture filenames, DebugBridge-named files, resource refs, binary strings). VERIFIED now gated on 3 remaining: (a) EPUB fixture, (c) test suite cleanup, (d) LOC split. (6) 2026-05-05 result=**partial** (`feature-44-20260505f.md`) — round 6 against the freshly-bundled `mini-epub3` fixture (PR #265, v3.13.20) extends criterion (a) operational coverage to EPUB end-to-end (seed → open → settle → snapshot → eval all produce documented JSON shapes; ZBOOK row inserted with correct title/fingerprintKey). Discovered structural gap that demotes criterion (d) from DEFERRED to **FAIL**: `DebugReaderProbeAdapter` has nil-defaulted `settleStrategy` (100ms placeholder) and `jsEvaluator` (throws unsupported), and no reader host plugs them in — round-4d's "EPUB fixture" framing was incomplete. Filed as **bug #126** (GH #266) for per-format probe-hook wiring at the reader-host level. VERIFIED now gated on: (d) bug #126 fix, (e) test suite cleanup, (g) LOC split. (7) 2026-05-05 — refactor PR #268 (v3.13.22) splits `RealDebugBridgeContext.swift` into per-command extensions (Settle / Snapshot / Eval). Pre-split: 506 lines. Post-split: 247 / 137 / 91 / 106 lines — every file under the 300-line guideline. **Closes acceptance criterion (g)**. Pure structural change; 37/38 DebugBridge tests still pass post-split (the 1 failure is pre-existing — see (e)). VERIFIED now gated on (d) bug #126 fix and (e) test suite cleanup. (8a) 2026-05-06 result=**partial** (`feature-44-20260506.md`) — first verification round on iOS 26.4 since bug #140 (LSD scheme-approval moved to SQLite) was closed in v3.14.10. Patched grant script (PR #302) writes both legacy plist + SQLite Election row. Full end-to-end command sequence verified on iPhone 17 Pro Sim: reset + seed both fixtures (war-and-peace.txt + mini-epub3.epub) + theme + open + settle + snapshot + eval. **First round with screenshot evidence** of both reader formats actually rendering (artifacts in `dev-docs/verification/artifacts/feature-44-r7-{epub,txt}-open-*.png`). Criterion (d) unchanged — bug #126 still open. No regressions across the 25-PR session. (8b) 2026-05-06 — chip away at criterion (e): pre-existing stale-test failures fixed across 3 test files. `SchemaV1Tests.migrationPlanHasSchemas` and `V1toV2MigrationTests.migrationPlanIncludesBothSchemas` switched from hardcoded `count == 2` (drifted to 6 after V3-V6 schemas shipped) to checking `SchemaV1`/`SchemaV2` presence by name — assertion now matches what the test name claims. `TXTChapterContentLoaderTests` helper `makeTestData` now populates `globalStartUTF16`/`textLengthUTF16` (production switched from byte-based to UTF-16-offset reads at some point; tests still passed byte-only init, leaving UTF-16 fields at `-1` default → every load threw `decodeFailed`). 16 tests in that suite + 3 ad-hoc cases all pass post-fix. Total ~19 of the (e) failure count addressed across rounds 6/8. (9) 2026-05-06 — **closes acceptance criterion (e)**. Final 6 stale-test failures fixed: `TXTOffsetTranslator.testToGlobalLocalBeyondLength` (production allows terminal offset == textLengthUTF16 per audit-fix comment; test asserted strict `<`); `TOCChapterProgress.beforeFirstEntry` (production clamps preamble to fraction=0; test wanted fractional preamble — filed as bug #127/GH #271, test updated to match current behavior with forward-link); `PerBookSettings.applyResolvedSettings_doesNotPollutedUserDefaults` (test asserted defaults["readingMode"] == "native" without ever explicitly setting `.native` — added explicit set so the assertion has meaning); `FoliateViewCoordinatorMessageRoutingTests.bridgeReadyTriggersOpenBook` + `bridgeReadyWithoutBase64ReportsError` (production rewrote bridge-ready to no-op since book opens via HTML-embedded base64; rewrote both tests to assert the new no-op contract); `HighlightIntegrationTests.handleRemovalRemovesAndRestores` (real flow has ViewModel call `persistence.removeHighlight` BEFORE `coordinator.handleRemoval` — coordinator only updates visual state; test now mirrors production sequence). **Full suite green: 716/716 tests pass.** VERIFIED now gated only on (d) bug #126 fix. Unblocks feature #45. (10) 2026-05-06 — partial closure of criterion (d). **Bug #126's EPUB-eval portion shipped in PR #305 (v3.14.13)**: `vreader-debug://eval?bridge=...&js=base64(...)` now returns real JS results for native-mode EPUB via the keyed `DebugReaderRegistry.epubWebView(for:)` binding (closure pulls webview at call-time, rejects stale-webview matches across books). 6 readiness probes against mini-epub3 via the new eval surface (artifacts: `feature-11-r2-*`): `__vreader_createHighlight` is function (true), `selectionChanged` handler registered (true), CSS Highlight API supported (true), DOM ready (true), `<p>` count (3), CSS Highlight roundtrip (`set+keys+rangeText`). **Settle's Foliate `relocate` portion** of original criterion (d) split to **bug #141** (GH #305) — settle 100ms placeholder is per-format, untouched by this fix. Same-book reopen race (Codex round-2 narrow Medium) split to **bug #142** (GH #306). Evidence: `feature-11-20260506b.md` (the bridge-readiness slice) + `bug-126-20260506.md` (the eval-portion fix) + `feature-44-20260506b.md` (round 10 — EPUB theme-CSS injection verified end-to-end via `getComputedStyle` against both dark and light modes; surfaced bug #144 — `vreader-debug://theme` doesn't propagate to live `ReaderSettingsStore`, fixed in PR #321). VERIFIED now gated on (d') bug #141 (settle Foliate wiring) — narrower than the original gate. GH: #244 |
| 45 | Verification harness sweep — retire the "Needs device verification" backlog    | DevTools/*   | High     | IN PROGRESS | XCUITest + DebugBridge recipes for 13 of 15 simulator-automatable backlog items. Adds VERIFIED status. Depends on #44. See plan below. Gate 1 plan: `dev-docs/plans/20260513-feature-45-verification-harness-sweep.md` (v2, 2026-05-13). Gate 2: manual-fallback audit complete (Codex unavailable), findings incorporated in plan v2. WI-1 (scaffold + AIDs) merged in v3.21.3 (PR #581). WI-2/3/4 pending. GH: #576 |
| 46 | WebDAV backup includes book files — materializing restore reconstructs library | Backup/*     | High     | VERIFIED  | Phase 1 of two-feature split. Adds `library-manifest.json` to the backup ZIP and uploads missing whole-file book blobs to `VReader/books/<format>/<sha256>_<byteCount>.<ext>` (content-addressed, deduped). Restore downloads every missing blob into the sandbox before applying metadata. **Shipped 2026-05-03** — `BackupDataCollector` emits the manifest, `BlobPath.make`+`WebDAVBlobStore.putBlobAtomically` handle the upload, `BookFileMaterializer` runs the download+import on restore. **VERIFIED 2026-05-03** via 4 curl probes against live rclone WebDAV + Swift Testing suite (`WebDAVProviderBackupWithManifestTests`). Evidence: `dev-docs/verification/feature-46-20260503.md`. Phase 2 = feature #47 (also VERIFIED 2026-05-04). GH: #144 (closed). |
| 47 | WebDAV restore — selective book picker + lazy-on-tap downloads                 | Backup/*     | Medium   | VERIFIED  | Phase 2 of feature #46. `BookFileState` (`local / remoteOnly / downloading / failed / missingRemote`), library rows with cloud icons, "Pick…" entry on each backup row → `SelectiveRestorePicker` against the manifest, on-tap blob fetch via background URLSession with `taskDescription` identity, Wi-Fi-only cellular policy via `NWPathMonitor`, "Clean unused remote files" GC carved out as feature #51. Depends on feature #46. **Shipped 2026-05-04** in v3.11.18..v3.12.0 (21 PRs). **VERIFIED 2026-05-04** in v3.12.9 after fixing 5 blocking bugs (#112, #113, #114, #115, #116) found during initial Gate-5 run. Evidence: `dev-docs/verification/feature-47-20260504.md` (initial partial) + `dev-docs/verification/feature-47-20260504b.md` (post-fix pass) + per-bug evidence files. GH: #407 (closed — VERIFIED). |
| 53 | Tap on highlighted text to get inline edit/delete options | Reader/* | Medium | TODO | **Problem**: Deleting a highlight requires navigating to the annotations panel (Highlights tab → swipe-to-delete). Tapping already-highlighted text in any reader format does nothing — there is no tap-on-highlight → inline popup/contextMenu. Users expect a Kindle-style inline popup with Delete (and Edit Color / Note) options. **Scope**: For each reader format — (1) EPUB: detect tap on a `::highlight(vreader-*)` range in JS, post message to Swift with the highlight ID, show UIMenu/action sheet with Delete; (2) TXT/MD: `HighlightableTextView` / `HighlightableLayoutManager` tracks painted NSRange regions — add UITapGestureRecognizer that hit-tests against those ranges and presents a contextual UIMenu with "Delete Highlight"; (3) AZW3/Foliate: `handleAnnotationShow(cfi:)` already fires on highlight tap (`FoliateReaderContainerView+Highlights.swift:39`) but only opens the panel — extend to also offer inline Delete; (4) PDF: `PDFAnnotationBridge` hit-test at tap location → action sheet. **Acceptance criteria**: (a) tapping a highlighted word shows a menu with at minimum a Delete option; (b) Delete removes the highlight visually and from persistence (`HighlightCoordinator.remove` / `.readerHighlightRemoved` pipeline); (c) consistent across all 5 formats; (d) tapping non-highlighted text preserves existing scroll/chrome-toggle behavior. Reported by user 2026-05-13. GH: #596 |
| 52 | Multiple WebDAV server profiles with active-server switching | Settings/Backup | Medium | TODO | **Problem**: WebDAV backup stores exactly one server's credentials in Keychain under fixed account keys (`com.vreader.webdav.serverURL/username/password`). Users with multiple WebDAV services (Nextcloud at home, Synology at work, Nutstore) must re-enter credentials every time they switch. **Scope**: (1) `WebDAVServerProfile` value type: `id: UUID`, `name: String`, `serverURL: String`, `username: String`, password Keychain-backed per `id`; (2) `WebDAVServerProfileStore` actor: persists a list of profiles (UserDefaults JSON) + `activeProfileID`; (3) Keychain: per-profile key `com.vreader.webdav.profile.<id>.password`; (4) Migration: on first launch, if legacy flat keys exist, migrate them into a profile named "Default" and set as active; (5) Settings UI: replace `WebDAVSettingsView`'s single-server form with a profile list (add/edit/delete/set-active) — mirrors `AIProviderListView` / `AIProviderEditSheet` pattern; (6) `WebDAVProviderFactory.make(...)` reads the active profile from the store instead of reading flat Keychain keys. **Edge cases**: (a) zero profiles — disable Back Up Now / restore with "Add a server in WebDAV Settings" prompt; (b) active profile deleted — fall back to first remaining or disable; (c) credential migration from legacy flat keys — silent, no re-entry required; (d) duplicate URLs are allowed (same server, different credentials); (e) profile name empty — default to serverURL hostname; (f) server URL malformed — validation before save. **Acceptance criteria**: (a) user can add two WebDAV profiles and switch the active one — only the active server is used for backup; (b) existing single-server users migrate without re-entering credentials; (c) deleting the active profile falls back to remaining or disables backup; (d) backup + restore round-trip works with the selected profile. Reported by user 2026-05-12. GH: #565 |
| 50 | Multi-provider AI support — Anthropic native API, multiple saved provider profiles, in-reader model switching | AI/*     | Medium | VERIFIED | **Problem**: AI is locked to one OpenAI-compatible profile. Anthropic's Messages API (`POST /v1/messages`, `x-api-key` header, `anthropic-version: 2023-06-01`, different response shape) cannot be used through the current `OpenAICompatibleProvider`. No multi-profile management exists — `AIConfigurationStore` stores exactly one `AIConfiguration` blob. **Scope**: (1) `ProviderProfile` model: `id`, `name`, `kind` (`.openAICompatible`/`.anthropicNative`), `baseURL`, `apiKey` (Keychain-backed), `model`, `isActive`; (2) `ProviderProfileStore` replaces `AIConfigurationStore` — persists a list, exposes `activeProfile`, migrates existing single-config on first launch; (3) `AnthropicProvider: AIProvider` — new concrete implementation for Anthropic Messages API (non-streaming + SSE `content_block_delta` streaming); (4) Settings UI: replace single-profile form with provider list (add/edit/delete/set-active, kind picker, test-connection button); (5) in-reader provider+model picker in settings panel. `AIProvider` protocol and all existing AI features unchanged — they call through `AIService` and are provider-agnostic. **Edge cases**: (a) zero configured providers — disable AI features with "Configure a provider in Settings" prompt; (b) active provider deleted — fall back to first remaining or disable; (c) API key migration from legacy `AIConfigurationStore` → `ProviderProfileStore` on first launch; (d) Anthropic streaming: `content_block_start` + `content_block_delta` SSE events, `message_stop` sentinel, error event shape (`type: "error"`); (e) Anthropic rate limits use the `retry-after` header in seconds (corrected from initial draft via Codex Gate-2 round-1 audit); (f) `anthropic-version` header must be pinned — bumping it could change response shape. **Acceptance criteria**: (a) user can add an Anthropic profile (API key + `claude-3-5-sonnet-20241022`) and use all AI features; (b) user can add multiple profiles and switch active provider from reader settings; (c) existing OpenAI users migrate without re-entering credentials; (d) wrong API key shows a clear per-provider error; (e) all existing AI feature tests pass with both providers. Reported by user 2026-05-10. **Gate 1+2 complete 2026-05-10**: implementation plan at `dev-docs/plans/20260510-feature-50-multi-provider-ai.md` (8 WIs, foundational/behavioral tier-marked). Codex audit thread `019e1269-16d8-70b2-80c6-f8d566de99cb` — 3 rounds, 12 findings (4H/4M/1L plus round-2 added 2H/1L plus round-3 added 1M), all fixed/accepted. **WI-1 shipped 2026-05-10 in v3.14.135** (PR #510, ProviderKind + ProviderProfile + KeychainService extension; Gate-4 audit thread `019e127d-a8c3-7450-98e1-a610487b9460` ship-as-is). **WI-2 shipped 2026-05-11** (ProviderProfileStore actor + commit-style migration; 25 tests; Gate-4 audit thread `019e12e7-7dc5-7182-85ef-8051d91cf35d` 3 rounds: 2H/1M then 1M/1L then 1L, all fixed; ship-as-is). **WI-3 shipped 2026-05-11** (AnthropicProvider non-streaming sendRequest; `x-api-key` + `anthropic-version: 2023-06-01` headers, top-level `system` field, required `max_tokens`, distinct 401/403 messages with `providerName` interpolation, max_tokens >= 1 validation pre-network; 21 tests; Gate-4 audit thread `019e1585-669a-74e0-85e9-b712a919ee83` 2 rounds: 2M/1L then ship-as-is). **WI-4 shipped 2026-05-11** (AnthropicProvider streaming via SSE — `content_block_delta` text deltas + `message_stop` sentinel + `error` event surface; partial-line buffering and UTF-8 mid-byte splits handled by `URLSession.AsyncBytes.lines`; **premature EOF detected and thrown** instead of silent success; streaming HTTP errors buffer up to 1KB of response body for the error excerpt; OSLog warning on malformed data lines; split into `AnthropicProvider+Streaming.swift` extension to stay under the 300-line convention; 16 streaming tests + WI-3 stub-assertion retired = 36/36 GREEN; Gate-4 audit thread `019e1612-4d44-7693-9b6f-ee20fecd3eed` 2 rounds: 1H/1M/3L then ship-as-is). **WI-5 shipped 2026-05-11 in v3.14.147** (PR #526): `AIService.resolveProvider()` switches from single-`AIConfiguration` factory to per-request snapshot resolution against `ProviderProfileStore`. Production dispatches on `snapshot.kind` → `OpenAICompatibleProvider(baseURL:apiKey:model:)` or `AnthropicProvider(baseURL:apiKey:model:maxTokens:)`. No active profile → `AIError.providerError("Configure a provider in Settings.")`; missing/empty per-profile key → `AIError.apiKeyMissing`. `streamRequest` becomes `async throws` (sole caller `AIChatViewModel` already awaited). Production callsites in `LibraryView.swift:697` + `ReaderAICoordinator.swift:76` switch to `ProviderProfileStore.shared`. `AIConfigurationStore` + `apiKeyAccount` retained for backward-compat (cleanup after WI-7). 9 new dispatch tests (`AIServiceProfileDispatchTests`) + 19 existing AIServiceTests + 51 AI VM tests all pass; full-suite SIGSEGV in unrelated AutoPageTurner/TTS suites matches WI-4 pre-existing flake. Gate-4 audit thread `019e16c7-5e87-72c2-b4d2-5f7aaf553e91` 2 rounds: 2L then ship-as-is. **WI-6a shipped 2026-05-11 in v3.15.0** (PR #533, commit 3a54513): Settings UI Phase A — provider list + active selection. `AISettingsViewModel` rewritten from single-profile to multi-profile (drops apiKeyInput/model/baseURL/temperature/maxTokens fields; adds `loadProfiles`/`setActive`/`deleteProfile`). New `AIProviderListView` (~180 LOC): radio-style row selection, per-row swipe-to-delete, empty state with globe icon + Add CTA, editor-placeholder sheet pointing to WI-6b. `AISettingsSection` shrunk to thin wrapper (~70 LOC) with toggle + NavigationLink + consent. Added `ProviderProfileStore.loadSnapshot() -> (profiles, activeID)` for atomic single-actor-hop read (round-1 Gate-4 finding [1] — closes split-read race in `loadProfiles`). `setActive` rejects unknown ids AND reads back authoritative active from store (round-1 finding [2]). Migration test strengthened to contract level: exactly one migrated profile, kind `.openAICompatible`, fields verbatim from legacy AIConfiguration, activeID set, legacy API key copied to per-profile keychain (round-1 finding [3]). `.isSelected` accessibility trait added for active row (round-1 finding [4]). 11 new `AISettingsViewModelMultiProfileTests` + 6 retained feature-flag/consent tests = 17 tests in scope; full AI-feature sweep 70/70 GREEN across AISettingsViewModelTests + AISettingsViewModelMultiProfileTests + ProviderProfileStoreTests + ProviderProfileMigratorTests + AIServiceTests + AIServiceProfileDispatchTests. Gate-4 audit thread `019e1763-2e46-7830-9f18-6160ca6f8887` 3 rounds: 1H/2M/1L → 1L → ship-as-is. **WI-6b shipped 2026-05-12**: Settings UI Phase B — profile editor sheet. New `AIProviderEditSheet` (add + edit modes), `AISettingsViewModel+Editor.swift` extension (addProfile/updateProfile/saveAPIKey/deleteAPIKey/testConnection/validateBaseURL), `KindResetPolicy` helper for value-based kind-picker defaults reset. Form covers: kind picker (segmented), name, baseURL + HTTPS validation (localhost / 127.0.0.1 exception; IPv6 loopback intentionally excluded to match provider preflight), model, temperature slider, maxTokens stepper, API key SecureField + Save/Delete (edit-mode only — add-mode is atomic via top-level Save). Test Connection sends a one-token "ping" against the LIVE form state (not the stored profile) so unsaved edits are exercised. `editorError` surfaces via `.alert` binding on the sheet; empty-state Add path clears stale `editingProfile`. Gate-4 audit thread `019e1808-81a2-7b00-a9f9-b302f00648b9` 3 rounds: 1H/5M → 1M/1L → 1M → ship-as-is. Round-1 fixes: (1) testConnection signature `forID:` → `profile:` so sheet form state drives the test, Anthropic uses profile.maxTokens not hardcoded 1; (2) kind-picker reset (iterated, see round 2); (3) validateBaseURL aligned with provider preflight (drop `::1`/`[::1]`); (4) add-mode Save Key disabled to prevent keychain orphans on Cancel — atomic write path via top-level Save only; (5) empty-state Add clears editingProfile + sheet binds editorError alert; (6) HTTP tests moved to nested `@Suite(.serialized)`, added URL/header shape assertions for OpenAI vs Anthropic dispatch. Round-2 fixes: replaced `isApplyingKindDefaults` flag (SwiftUI .onChange ordering risk) with pure-function `KindResetPolicy`; split AIProviderEditSheet (419→252 LOC) into main file + `AIProviderEditSheet+Sections.swift` extension; refreshed stale header comments. Round-3 fix: edit-mode prefill is sticky — added `inEditMode: Bool = false` parameter to KindResetPolicy so saved values aren't overwritten when they happen to equal defaults. 26 editor tests + 5 KindResetPolicy unit tests (including OpenAI→Anthropic→OpenAI round-trip regression) + 5 validateBaseURL unit tests = 26 in editor suite; 47 total in scope passing. Files: AIProviderEditSheet.swift (252 LOC) + AIProviderEditSheet+Sections.swift (202 LOC) + AISettingsViewModel+Editor.swift (222 LOC) + KindResetPolicy.swift (45 LOC) all under the 300-LOC guideline. **WI-7 shipped 2026-05-12**: in-reader AI provider picker — the FINAL WI of feature #50. New `AIProviderPickerViewModel` (`vreader/ViewModels/`, ~90 LOC) + `AIProviderPicker` SwiftUI Menu (`vreader/Views/Reader/`, ~85 LOC) wired into `AIReaderPanel.swift` toolbar (`topBarTrailing` slot, owned by `@State` so it survives tab changes). Toolbar Menu label shows the active profile's name + chevron.down; rows list every saved profile with the active one carrying both a checkmark glyph (sighted users) and the `.isSelected` accessibility trait (VoiceOver) — mirrors WI-6a's `AIProviderListView` pattern. Empty state renders a disabled "No providers — add one in Settings" row. Round-1 Codex audit finding [1] (thread `019e18a2`, 2 rounds: 1M/1L → ship-as-is): added `Notification.Name.providerProfilesDidChange` posted by `ProviderProfileStore` on every mutation (`upsert` / `remove` / `setActiveProfileID`); the picker VM observes it in init (via `installObserver()` to keep `[weak self]` capture valid) and resyncs via `loadSnapshot()` — so edits made from Settings while the AI panel stays open propagate live. Token stored as `nonisolated(unsafe) private var` so `deinit` can release it from any queue under Swift 6 strict (the plain `nonisolated` form is rejected on `@Observable` mutable stored properties — compiler warning is a known false positive). 9 picker tests + 73 cross-suite = 82/82 GREEN on iPhone 17 Sim iOS 26.4. **All 5 acceptance criteria are now implemented end-to-end**: (a) Anthropic profile + AI features; (b) multi-profile + active-switch from in-reader; (c) legacy migration without re-entry; (d) per-provider error surfacing; (e) all existing AI feature tests pass. Status flipped to **DONE**; VERIFIED gated on the full acceptance pass on a device. Audit log: `.claude/codex-audits/feat-issue-50-wi-7-in-reader-provider-picker-audit.md`. **2026-05-13 VERIFIED** (`dev-docs/verification/feature-50-20260513.md`, commit `f468934`, v3.21.0/build 277, result=**pass**): 220 tests in 18 suites GREEN on iPhone 17 Pro Simulator (iOS 26.5). All 5 acceptance criteria confirmed: (a) AnthropicProvider HTTP shape tests confirm `x-api-key` header, `anthropic-version: 2023-06-01`, top-level `system` field, SSE `content_block_delta`/`message_stop` streaming; (b) AIProviderPickerViewModel tests confirm multi-profile list, active-switch, live `providerProfilesDidChange` propagation; (c) ProviderProfileMigratorTests contract-level migration: exactly one migrated OpenAI profile, fields verbatim, legacy keychain key copied; (d) AnthropicProviderTests 401→`authenticationFailed("…Anthropic.")`, 403→`authenticationFailed("…Anthropic.")`, AIServiceTests no-profile→`providerError("Configure a provider in Settings.")`; (e) 220/220 AI test suite GREEN. GH: #501 |
| 49 | EPUB chapter top/bottom padding — add breathing room before and after each chapter document so chapter boundaries feel visually separated | EPUB/*   | Low    | VERIFIED | **Problem**: `ReaderTheme.epubOverrideCSS` injects `body { padding: 0 16px !important }` — zero vertical padding — so chapter content starts and ends flush against the viewport edge. Readers accustomed to Kindle/Moon+ expect a margin of whitespace at each chapter boundary. **Scope**: single CSS change in `vreader/Models/ReaderTheme.swift`: change `padding: 0 16px !important` to `padding: 2em 16px !important` (or `padding-top`/`padding-bottom` separately). No new types, no new files, no Swift logic. All three themes pick it up automatically because CSS is generated per-theme call. **Edge cases**: (a) EPUB book with its own body padding — overridden by `!important`, consistent with current behavior; (b) paged layout mode — top/bottom padding applies within each page, similar to a book margin; (c) full-bleed cover images that use `body { padding: 0 }` tricks — the `!important` override may add unwanted whitespace around cover art; mitigate by scoping padding to `p, div` or using the `:first-child` / `:last-child` selectors. **Test plan**: unit-test `ReaderTheme.epubOverrideCSS` to assert the padding value is non-zero vertical; snapshot/visual verify on `mini-epub3.epub` that chapter start has visible top breathing room. **Acceptance criteria**: (a) opening a chapter document shows ≥ 1em top padding before the first line of text; (b) bottom of chapter shows ≥ 1em padding after the last line; (c) no visual regression in mid-chapter reading; (d) all three themes (Light, Sepia, Dark) render consistently. Reported by user 2026-05-10. **Shipped 2026-05-12 in v3.17.1** (PR #546, commit `6521f22`). Single CSS literal change in `ReaderTheme.epubOverrideCSS`: `padding: 0 16px !important` → `padding: 2em 16px !important`. All three themes (Light/Sepia/Dark) inherit because CSS is generated per-theme. Plan: `dev-docs/plans/20260512-feature-49-epub-chapter-padding.md`. Gate 2 audit: thread `019e1949` (3 rounds, 2M+2L → 1M+1L → 1L cosmetic). Gate 4 audit: thread `019e198d` (2 rounds, 3L → ship-as-is; audit log `.claude/codex-audits/feat-issue-49-epub-chapter-padding-audit.md`). Three regression-guard tests in `ReaderThemeTests.swift` (28 total in suite, all GREEN). **Gate 5 device verification 2026-05-12 result: pass** — CU-free via DebugBridge on iPhone 17 Pro Sim: portrait paddingTop/Bottom=36px (2em × 18px font, exact), landscape paddingTop/Bottom=36px (intact in short viewport 525px high), columnCount="1" preserved, pagination math consistent. Evidence: `dev-docs/verification/feature-49-20260512.md`. GH: #496 |
| 48 | TXT chapter-mode highlight pipeline (WI-7 — global↔chapter-local UTF-16 offset translation for both search-tap rendering AND gesture-driven persisted highlights) | Reader/* | Medium | VERIFIED | **2026-05-13 VERIFIED** (`dev-docs/verification/feature-48-20260513.md`, v3.21.0/build 277, result=**pass**): `TXTChapterModeHighlightVerificationTests.testChapterModeHighlightCreatedAndPersistsAfterRelaunch` PASSED on iPhone 17 Pro Simulator (iOS 26.5) — long-press word in war-and-peace Chapter I → Highlight → `highlightEmptyState` gone → app.terminate() + relaunch keepExisting → Highlights tab still populated. Both acceptance criteria (a) gesture-create + Highlights tab entry, (b) persist across relaunch, confirmed end-to-end. iOS 26 accessibility flattening required encoding `chapterMode:true` in `accessibilityValue` of `txtReaderContainer`; relaunch approach used instead of navigation (AnnotationsPanelView has no close button — terminate is the stronger persistence assertion). GH: #554 closed. **2026-05-12 promoted TODO → PLANNED with `dev-docs/plans/20260512-feature-48-chapter-highlight-pipeline.md`** — formal Gate 1 plan covers Problem, Surface area (TXTReaderContainerView.swift:486+402, helper at TXTChapterHighlightHelper.swift already exists with 24 tests), 3 WIs (render / scroll / create), test catalogue, risks, BC. Gate 2 ran Codex MCP independent audit (thread `019e1a7f-0b81-7a02-8b2c-e452f76829fa`), 3 rounds; plan v4 clean (zero Critical/High/Medium findings). PR #553 merged. GH: #554. **Filed by bugfix cron 2026-05-09** during bug #154 investigation; **scope expanded 2026-05-09** during bug #160 fix to cover persisted highlights (gesture-creation + restore) in addition to the original search-tap rendering scope. **WI-1 PR #553, WI-2 PR #566, WI-3 PR #570 — all 3 WIs merged as of 2026-05-12 (v3.20.0/build 276). Feature DONE; awaiting device verification (VERIFIED pending close-gate).** GH: #554 |

### Feature #44 — DebugBridge — Plan

- **Problem**: Autonomous AI debug loop and XCUITest regression suite both need cheap, deterministic state setup and ground-truth state inspection. Currently every repro requires manual UI-driving (open library → tap book → scroll → set theme → ...). For WKWebView-heavy bugs (Foliate-js highlights, EPUB rendering, page navigation) there is no external way to read what the webview actually rendered. Vision-only inspection is too flaky for the hard bugs, which is the class that piles up in `docs/bugs.md`.
- **Scope**:
  - **Included**: A `#if DEBUG`-gated `DebugBridge` that handles the `vreader-debug://` URL scheme via `.onOpenURL` on `VReaderApp`. Commands: `reset`, `seed?fixture=<name>`, `open?bookId=<uuid>&cfi=<x>` (plus equivalent for TXT/MD/PDF positions), `theme?mode=<dark|light>&fontSize=<n>`, `settle?token=<id>` (writes ready-sentinel after Foliate `relocate` + native layout settles), `snapshot?dest=<file>` (semantic state JSON to app container), `eval?bridge=foliate&js=<base64>` (active WKWebView JS eval, result to container). Bundle a small set of fixture books in `vreader/Resources/DebugFixtures/` (alice.epub, war-and-peace.txt, sample.azw3, sample.pdf). Document one repro recipe per existing open bug (#107, #108, plus a few of the "Needs device verification" features) in `docs/subsystems/debug-bridge.md`.
  - **Excluded**: No release-build code path. No remote control over network. No authentication (device-local only, DEBUG-only). No fixture *editing* in the bridge — fixtures are read-only bundle resources. No replacement of XCUITest — DebugBridge is a state-setup peer, not a test runner. No screenshot capture from inside the bridge — caller uses `xcrun simctl io booted screenshot`. No mutation of SwiftData beyond the `reset` and `seed` commands.
  - **Out of scope, deferred**: Recording-and-replay of user gestures. Mocking the network layer. AI Genie state injection.
- **Edge cases**:
  - URL scheme called in non-DEBUG build → ignore silently (handler not registered).
  - `seed` called twice with the same fixture → idempotent (no duplicate library row).
  - `open` with unknown `bookId` → write error JSON to `lastError.json`, do not crash.
  - `eval` JS that throws → capture exception in result JSON; do not propagate to app.
  - `eval` called when no webview is active → return `{ "error": "no active webview" }`.
  - `settle` called when render never completes (e.g., corrupt EPUB) → timeout sentinel after 30s with `{ "error": "settle timeout", "phase": "<lastPhase>" }`.
  - Concurrent `vreader-debug://` calls → serialize via `MainActor` queue; later calls wait.
  - Snapshot during in-flight TTS or AI streaming → state JSON includes a `transientOps` array so caller can decide whether to retry.
  - Filesystem write failures on `Caches/` → log and continue; do not block app.
  - `reset` while a book is open → close reader first, dismiss sheets, then wipe.
  - Build configuration with `DEBUG` undefined but URL scheme registered (e.g., archive build): URL scheme registration also `#if DEBUG`-gated in Info.plist via Active Compilation Conditions check at build phase. Verify no `vreader-debug` entry leaks into Release.app.
  - `snapshot` JSON must exclude PII (no fingerprinting hashes of book content beyond what's already in SwiftData).
- **Test plan**:
  - Unit: `DebugBridgeRouterTests` — URL parsing for each command, error paths (malformed URL, missing required params, unknown command).
  - Unit: `DebugStateSnapshotterTests` — JSON shape stability against a fixed model (golden JSON), nil/optional handling, `transientOps` array population.
  - Unit: `DebugFixtureLoaderTests` — idempotent `seed`, missing fixture name → error, fixture path resolution.
  - Integration: `DebugBridgeIntegrationTests` (XCUITest) — launch with `vreader-debug://reset` then `seed` then `open` then `snapshot`, read back container JSON, assert state matches expected.
  - Integration: `DebugBridgeFoliateEvalTests` — open EPUB fixture, `eval` returns `document.querySelectorAll('.foliate-highlight').length` correctly after creating a highlight.
  - Build gate: a CI step asserts `vreader-debug` is **not** present in the Release bundle's Info.plist. Fail the build if found.
  - Manual verification: one repro recipe in `docs/subsystems/debug-bridge.md` exercised end-to-end against current open bugs.
- **Acceptance criteria**:
  - DEBUG build registers `vreader-debug://` and handles all six commands above with correct success/error JSON in the app container.
  - Release build has zero `vreader-debug` references (verified by CI grep + Info.plist check).
  - `xcrun simctl openurl booted vreader-debug://snapshot?dest=state.json` followed by `simctl get_app_container booted <bundle-id> data` produces a valid JSON file with the documented schema (currentBookId, format, cfi/page, theme, fontSize, selection, highlightCount, renderPhase, lastError).
  - `settle` reliably blocks until the Foliate `relocate` event has fired and a frame has been committed (no mid-frame screenshots in the documented happy-path repro).
  - All unit + integration tests pass under `xcodebuild test -only-testing:vreaderTests` and `-only-testing:vreaderUITests`.
  - `docs/subsystems/debug-bridge.md` documents the URL grammar, JSON schema, fixture catalogue, and one worked repro per currently-open bug.
  - LOC under \~300 across new files (router, snapshotter, fixture loader, Foliate eval adapter); no file exceeds the project's 300-line guideline.

### Feature #45 — Verification harness sweep — Plan

- **Problem**: 15 features in `docs/features.md` and many bugs in `docs/bugs.md` are marked "Needs device verification" or "Not verified on device". This backlog grows monotonically because every release adds new entries faster than humans can verify the old ones, and the verification work is repetitive UI-driving that humans hate. Result: tracker statuses lie (`DONE` actually means "code shipped, untested"), regressions ride to release, and pre-release manual passes balloon. Currently 16 occurrences in `docs/features.md` alone.
- **Scope**:
  - **Included**:
    - Add `VERIFIED` status to `docs/features.md` statuses (already done in this commit).
    - Build a verification harness in `vreaderUITests/Verification/` with one XCUITest file per backlog item, sharing helpers in `vreaderUITests/Verification/Helpers/` (DebugBridge URL builder, container JSON reader, settle-token waiter, fixture catalog).
    - Implement automated verifications for 13 simulator-automatable backlog items: features #11, #21, #23, #27, #28, #29 (with local WebDAV container), #31, #34, #35, #36 (with local OPDS feed fixture), #37, #40, #41.
    - Three proving-ground items implemented first to crystallize the pattern: **#37 Per-book reading settings** (simple UI-state assertion), **#34 Collections / tags** (SwiftData + UI), **#11 EPUB highlighting** (webview + race condition; previously bug #77).
    - For each item: at least one happy-path test plus the *specific* edge case that the original bug fix addressed (e.g., #11 must exercise the JS buffering race that bug #77 fixed; #28 must exercise the `didSet` re-apply path that bug #98 fixed).
    - Update `docs/manual-test-checklist.md`: each verified item is marked "Auto-verified by `<test-name>`" and removed from the human checklist.
    - CI: add a `xcodebuild test -only-testing:vreaderUITests/Verification` job to the existing test gate. Target completion under 8 min on CI hardware.
    - A short residual manual checklist (`docs/manual-test-checklist.md` "Real-device only" section) for the irreducible items: #26 TTS audio quality and any flow that requires real iCloud / real haptics / real Apple ID.
  - **Excluded**:
    - Verifying #26 audible TTS output (no programmatic way to QA voice quality on simulator).
    - Real iCloud sync paths, real Apple ID flows, real-device haptics — those stay manual.
    - Visual regression / snapshot diffing — separate concern, separate feature if/when it arrives.
    - Performance / load testing — separate concern.
    - Replacing existing unit tests in `vreaderTests/` — those continue to gate at the unit level; this harness is layered on top, not a replacement.
    - Full test fixture authoring tools (the harness uses fixed bundle resources from #44; new fixtures get added there, not here).
  - **Out of scope, deferred**:
    - Cross-device-type matrix runs (iPad layouts, multiple iOS versions).
    - Localization sweep across all supported languages — verifications run in the default locale.
    - Stress / fuzz testing of the bridge surface itself.
- **Edge cases**:
  - **Flaky tests**: Foliate render and SwiftData async save have inherent timing. The harness must use `settle` from #44 — never `sleep`. Any test that needs `sleep` to pass is rejected from merge.
  - **Fixture pollution**: each test runs `vreader-debug://reset` in `setUp` so the SwiftData container is fresh; tests that depend on a prior test's state are forbidden.
  - **WebDAV / OPDS containers**: tests that need a live server run *only* when the server is up; if it isn't, the test is skipped (not failed) with a clear log line. CI starts the containers as a job step before the test phase.
  - **Race conditions in concurrency-sensitive flows**: bug #77 (highlight buffering), bug #82 (paginate navigator preserve), bug #88 (highlight refresh on import) — each must have a verification that *would have caught the original bug* if run before the fix, not just a happy-path smoke. Verified by reverting the fix locally and confirming the test fails (RED check, then re-apply).
  - **Locale / dynamic type / dark mode**: verifications use a fixed default. A separate "appearance variations" sweep is out of scope.
  - **TTS callbacks without audio**: TTS verification asserts on `AVSpeechSynthesizerDelegate` callback events surfaced via DebugBridge `snapshot` — never requires audible output. Sentence boundary count + fired-callback count is the assertion.
  - **OPDS / WebDAV server flake**: harness uses local fixtures (a static OPDS XML feed served from the test bundle, a local WebDAV root in `/tmp`) to avoid external network dependence. CI never reaches the public internet.
  - **PhotosPicker (#32 theme backgrounds)**: simulator's photo library may be empty; the test seeds a known image into the simulator's Photos via `xcrun simctl addmedia` in setUp.
  - **DocumentPicker (import/export #35)**: handled via `vreader-debug://` import command that bypasses the OS picker (file path passed directly), since the picker is OS chrome and not vreader's responsibility. The test verifies *what happens after import succeeds*, not the picker UI.
  - **Test-name collisions**: each verification is named `verify_feature_<NN>_<short_name>` so they're greppable and the failure → tracker mapping is mechanical.
  - **Tracker drift**: when a verification passes, status moves to `VERIFIED`. When it later starts failing, it moves back to `DONE` and a bug is filed. CI prints a one-line status summary so the tracker can be updated mechanically.
- **Test plan**:
  - The deliverable *is* tests. The plan is what they cover and how they're structured.
  - **Helper unit tests** in `vreaderTests/Verification/`:
    - `DebugBridgeURLBuilderTests` — URL escaping, command serialization, fixture name validation.
    - `ContainerJSONReaderTests` — parses `simctl get_app_container` output, handles missing files, malformed JSON.
    - `SettleTokenWaiterTests` — timeout, success path, never returns false-positive.
    - `FixtureCatalogTests` — fixtures listed in catalog actually exist in the bundle.
  - **Verification tests** in `vreaderUITests/Verification/` — one file per feature in the 13-item list. Each file contains:
    1. A happy-path `verify_feature_<NN>_<name>` test.
    2. One regression test per linked bug (e.g., feature #11 has a test specifically targeting the bug #77 race).
  - **CI integration**: `verification` test plan added to the Xcode scheme; new CI step runs it after the unit-test phase. Failure of any verification → red CI.
- **Acceptance criteria**:
  - 13 of the 15 backlog items reach `VERIFIED` status, with each entry's Notes column citing the test name.
  - The 2 items that stay manual (#26 audio, anything real-device) are explicitly listed in `docs/manual-test-checklist.md` under a "Real-device only" section, and *removed* from the auto-verifiable list — the manual checklist for these gets shorter, not longer.
  - `xcodebuild test -only-testing:vreaderUITests/Verification` exits 0 and completes in under 8 minutes on the CI runner.
  - Each regression test, when run against the pre-fix commit of its linked bug, fails (RED proof) — recorded once in `dev-docs/verification-red-checks.md`.
  - `docs/manual-test-checklist.md` reflects the new state: items moved to "Auto-verified by `<test-name>`" or "Real-device only".
  - CI prints a summary line per verification (`PASS feature_11_epub_highlighting in 4.2s`) so tracker updates are mechanical.
  - No verification test uses `sleep` or `Thread.sleep` for synchronization — `settle` and explicit waiters only.
  - Total LOC for harness helpers under 500; per-feature tests average \~80 LOC. Files under 300-line guideline.

### Feature #46 — WebDAV materializing restore — Plan

- **Problem**: Feature #29 (WebDAV backup, PR #128) is metadata-only by design — the ZIP contains positions, annotations, settings, collections, book-sources, per-book-settings, and replacement-rules keyed by `bookFingerprintKey`, but **no book file bytes**. `BackupDataCollector.swift:12` documents the trade-off as "Books that no longer exist on the restoring device are silently skipped." The user-visible consequence: restoring a backup to a fresh device leaves the library empty, because every position/highlight/note references a fingerprint that doesn't match any local book. The backup is unusable for its primary purpose (recover my library after wipe / new device). Addresses the gap reported 2026-05-03.
- **Scope**:
  - **Included**:
    - New `library-manifest.json` section in the backup ZIP. Per book: title, author, format, fingerprint key (`{format}:{sha256}:{byteCount}`), SHA-256, byte count, original filename, detected encoding (TXT only), addedAt, lastOpenedAt, blob path. SchemaVersion field for forward compat.
    - Backup uploads missing whole-file book blobs to a content-addressed path on the WebDAV server: `VReader/books/<format>/<sha256>_<byteCount>.<ext>` (NOT `<fingerprintKey>` — colons in the key are awkward across servers/tools; canonical fingerprintKey lives only inside the manifest).
    - Atomic upload pattern: `PUT` to `VReader/uploads/tmp/<uuid>.part` → verify size via PROPFIND `getcontentlength` → `MOVE` into final blob path. Direct `PUT` to the final path can leave a corrupt blob on a partial upload; the temp+MOVE pattern guarantees blobs are either fully present or absent.
    - Existence check before upload: PROPFIND on the final blob path (NOT HEAD — some WebDAV servers don't implement HEAD reliably; PROPFIND is portable and `WebDAVClient` already supports it). If present and `getcontentlength` matches, skip upload (content-addressed dedupe — identical bytes converge across snapshots).
    - Restore order change: download every missing blob from the manifest into the app sandbox at the canonical `Documents/Books/<sanitized-original-filename>` path → run `BookImporter` to register `BookRecord` rows → run the existing metadata restore (positions/annotations/etc.). Metadata restore continues to silently skip un-resolvable fingerprints (defensive — the manifest *should* have provided everything, but a partial blob download must not crash restore).
    - Progress UI: per-blob download progress, total bytes, ETA. Pre-restore confirmation dialog when total missing bytes exceed a threshold (proposal: 100 MB) so the user can cancel before a slow transfer starts.
    - Backup progress UI gains a per-blob upload phase distinct from the metadata-ZIP phase.
    - Backward compat: a backup ZIP without `library-manifest.json` (i.e., a v1-format snapshot from PR #128) restores exactly as it does today (metadata-only, books skipped). The new format is purely additive.
  - **Excluded**:
    - No `BookFileState` enum, no remote-only library rows, no cloud-download icons, no on-tap downloads — those are feature #47. Phase 1's invariant: every `Book` row continues to mean "readable local file at fingerprint-derived sandbox path." This is what keeps the surface area small.
    - No selective restore picker — phase 1 always materializes everything in the chosen backup. Phase 2 adds the picker.
    - No GC of orphaned book blobs when a backup is deleted. Blobs accumulate; a future "Clean unused remote files" action (phase 2) walks every backup manifest and deletes unreferenced blobs.
    - No conditional `If-None-Match: *` create-if-absent — content-addressing makes "last writer wins" safe for identical bytes, and conditional WebDAV semantics aren't universally implemented. Use PROPFIND-then-PUT and tolerate the rare race.
    - No background download (`URLSessionConfiguration.isDiscretionary`) in phase 1 — restore is foreground-driven from the user's tap on "Restore". Phase 2's lazy load benefits from background, but a foreground restore with progress UI is acceptable for v1.
    - No Cellular vs Wi-Fi policy in phase 1 — restore is explicit user action; the user accepts the data charge. Phase 2's lazy-load needs the toggle.
    - No CloudKit / `CKAsset` / FileProvider / Background Assets path — those are different backends, not the same feature. Stay on WebDAV for the user's existing self-hosted setup.
  - **Out of scope, deferred**:
    - Chunked dedup (restic/borg-style) — overbuilt for personal libraries; whole-file SHA-256 is the right 80/20.
    - rsync-style block diffs — EPUB/AZW3/PDF are already compressed/containerized; small metadata changes reshuffle bytes and defeat block-diffs.
    - Cross-backup snapshot diffing (only ship blobs new since the last snapshot) — phase 1 already gets dedup-on-upload via PROPFIND existence checks; explicit diffs are an optimization for later.
    - Encryption at rest — WebDAV server is user-controlled; encryption is a separate cross-cutting feature.
- **Edge cases**:
  - **Empty library at backup time**: `library-manifest.json` is present with `books: []`; restore is a no-op (also empty). No crash.
  - **Backup with 0 books on device that have non-zero annotations** (orphan annotations from before): manifest lists 0 books, annotations restore continues to silently skip un-resolvable fingerprints. No regression vs current behavior.
  - **Partial upload failure mid-backup**: `.part` file remains in `uploads/tmp/`; never moved to final path → next backup's PROPFIND skips this blob (because final path doesn't exist) and re-uploads. No duplicate. No corruption. Tmp files older than 7 days swept by an idempotent housekeeping pass at the start of each backup.
  - **Partial download failure mid-restore**: BookRecord not yet registered for the failed blob; partial sandbox file deleted. Restore continues with the next blob and reports a per-book failure summary at the end (total succeeded / total failed, with retry option). User can re-run restore — already-downloaded blobs are skipped via local-file existence check.
  - **WebDAV server out of space mid-upload**: 507 → surface clear error, leave `.part` for retry, abort backup. Existing `WebDAVError.quotaExceeded` covers it.
  - **Two devices backing up to same WebDAV root with overlapping books**: content-addressed paths converge — both write the same `<sha256>_<byteCount>.<ext>` and PROPFIND skips the second upload. Safe by design.
  - **SHA-256 collision**: not a real risk for SHA-256, but the path also encodes `byteCount` so a hypothetical collision with different sizes would still be distinguishable. A collision with identical size is cryptographically negligible for the threat model (personal backup).
  - **Filename with characters illegal on the WebDAV server**: blob path uses only `[0-9a-f_.<format>]` so it's safe across servers. Original filename from manifest is sanitized for the sandbox at restore time (existing `BookImporter` rules apply).
  - **Same fingerprint, different filenames across devices** (same content imported with different original names): blob path is identical (content-addressed); manifest carries the original filename for the restoring device's sandbox. Each device gets the filename from its source backup.
  - **Books larger than available device storage at restore**: pre-restore confirmation dialog shows total bytes; if the user proceeds and storage runs out, restore aborts with a clear error and a "downloaded so far" summary.
  - **Manifest references blob path that doesn't exist on server** (server pruned manually): restore reports per-book failure, continues with remaining blobs. Manifest schema has version field so a future format change can carry an explicit `blobETag` for stronger integrity.
  - **User restores the same backup twice**: second restore's PROPFIND of local sandbox finds every blob already imported → all blob downloads skipped → metadata re-restore is idempotent (existing behavior). No duplicate library rows.
  - **Concurrent backup and restore on different devices**: blob writes are content-addressed and atomic via temp+MOVE; metadata ZIPs have unique timestamp+UUID names. No conflict.
  - **WebDAV server returns 301 redirect on ********`MOVE`**: existing `WebDAVClient` MKCOL trailing-slash bug (fixed in PR #128) taught us to normalize paths before MOVE. Apply the same normalization.
  - **Manifest > 1 MB** (large library, e.g., 10k books): keep using JSONEncoder — 10k entries with the documented schema is \~3 MB, still trivial. No streaming parser needed at this scale.
- **Test plan**:
  - **Unit — manifest**:
    - `LibraryManifestEncodingTests` — round-trip BookRecord array → JSON → array; schemaVersion present; all fields stable.
    - `LibraryManifestSchemaCompatTests` — v1 ZIP (no manifest) loads with empty book list and triggers metadata-only restore path.
  - **Unit — blob path**:
    - `BlobPathTests` — fingerprintKey → `<format>/<sha256>_<byteCount>.<ext>` mapping, parameterized over all 5 formats. Round-trip: parse blob path back to (sha256, byteCount, format).
  - **Unit — collector**:
    - `BackupDataCollectorBlobTests` — given a fixture library of 3 books, manifest contains 3 entries with correct paths; missing-blob list is computed correctly against a fake WebDAV state.
  - **Unit — restorer**:
    - `BackupDataRestorerMaterializeTests` — manifest with 3 entries → restorer requests 3 downloads via injected `WebDAVClient` mock → registers 3 BookRecords → runs metadata restore.
    - `BackupDataRestorerPartialFailureTests` — 1 of 3 downloads fails → 2 books imported, 1 reported in failure summary, restore overall succeeds with `restorePartiallyFailed` aggregate.
  - **Unit — WebDAVProvider**:
    - `WebDAVProviderBlobUploadTests` — temp+MOVE atomicity with mock client; abort mid-upload leaves `.part` and final path missing; retry succeeds.
    - `WebDAVProviderDedupeTests` — PROPFIND returns existing blob with matching size → skip PUT; PROPFIND 404 → PUT executes.
    - `WebDAVProviderTmpSweepTests` — `.part` files older than 7 days are deleted at backup start.
  - **Integration — Docker WebDAV** (mirroring PR #128's existing integration test):
    - `BackupBlobIntegrationTests` — backup library of 2 books → assert blobs at expected paths via raw curl probe; restore to empty library → assert 2 books present, positions/annotations re-attached.
    - `BackupBlobReuploadTest` — back up the same library twice; second backup uploads zero blobs (PROPFIND dedupe).
  - **Manual verification recipe** (recorded in `docs/manual-test-checklist.md`):
    - On simulator: import 3 books → backup → wipe library via `vreader-debug://reset` → restore → assert 3 books visible with original positions/highlights.
    - Destructive simulation: kill the app mid-upload → relaunch → re-run backup → verify no orphan `.part` files in `uploads/tmp/` after housekeeping sweep.
- **Acceptance criteria**:
  - A backup ZIP from feature #46 contains `library-manifest.json` plus the existing 8 metadata sections.
  - WebDAV server after a backup has `VReader/books/<format>/<sha256>_<byteCount>.<ext>` for every book in the library; second consecutive backup uploads zero new blobs.
  - Restoring a feature #46 backup to an **empty library** populates the library with every book referenced in the manifest, and positions/annotations re-attach correctly.
  - Restoring a feature #29 backup (v1 format, no manifest) continues to work exactly as today — metadata-only, books silently skipped.
  - Mid-upload kill leaves no corrupt blobs at the final path; the next backup attempt re-uploads cleanly.
  - All unit + integration tests pass under `xcodebuild test -only-testing:vreaderTests`.
  - LOC under \~300 per new file (`LibraryManifest.swift`, `BlobPath.swift`, additions to `BackupDataCollector.swift`, `BackupDataRestorer.swift`, `WebDAVProvider.swift`); no file exceeds the 300-line guideline.
  - Manual recipe in `docs/manual-test-checklist.md` passes on iPhone 17 Pro Simulator against `lllyys/vreader-webdav-host`.
  - `docs/architecture.md` updated: add `library-manifest.json` and the blob path layout to the Backup section; note the temp+MOVE upload pattern.

### Feature #47 — WebDAV restore — selective picker + lazy-on-tap — Plan

- **Problem**: Feature #46 ships materializing restore — the user gets their whole library back, but downloading every book up front can be slow or wasteful (large libraries, low-storage devices, secondary devices that only need a few books). Lazy-on-tap downloads and selective restore are the modern reading-app UX (Apple Books, Kindle, Readwise) and unblock multi-device usage. They also let users browse a backup's contents before committing storage.
- **Scope**:
  - **Included**:
    - `BookFileState` enum on `BookRecord`: `local`, `remoteOnly`, `downloading`, `failed`, `missingRemote`.
    - "Restore selectively…" mode in the WebDAV restore UI: lists every book from the chosen backup's `library-manifest.json` with title, author, size, format icon, last-opened date, checkbox. Default selection is none. "Restore all" remains a one-tap option that maps to phase-1 behavior.
    - Library rows for `remoteOnly` books: cloud-download icon, size label, opening attempts the download. Position/annotation sidecars restore alongside the row even before the file is local (so a tap-to-download book lands at the saved position).
    - Per-book download via `URLSessionConfiguration.isDiscretionary` background session — iOS schedules transfers when power/network are favorable.
    - "Wi-Fi only" toggle in WebDAV settings (default ON for cellular-aware behavior). Honored by both lazy downloads and "Restore all".
    - "Clean unused remote files" maintenance action in WebDAV settings: walks every backup manifest on the server, computes the union of referenced blob paths, deletes unreferenced blobs. Requires user confirmation with affected blob count + freed bytes estimate.
    - Reader entry-point checks `BookFileState`: opening a `remoteOnly` book routes to a download flow (progress sheet), not the reader, until the file is `local`.
    - Delete behavior: deleting a `remoteOnly` book removes the row only (no local file to delete); deleting a `local` book removes the file (existing behavior). Both leave the WebDAV blob untouched (GC is a separate explicit action).
  - **Excluded**:
    - No multi-device sync, no CRDT convergence — restore remains a one-shot user action, not continuous sync. Sync is a separate future feature.
    - No partial book downloads (range-requested first chapters) — granularity is whole-book.
    - No automatic retry of failed downloads — user retries via tap on the failed row. Surfacing transient vs permanent failure cleanly is enough; retry policy is out.
    - No CloudKit migration — phase 2 stays on WebDAV.
  - **Out of scope, deferred**:
    - Backup retention policy ("keep last N snapshots, delete older") — independent feature.
    - Per-book encryption — same cross-cutting concern as in feature #46.
    - Read-while-downloading streaming — opens the door to PDF/EPUB streaming complexity that doesn't pay off in the iOS reader context.
- **Edge cases**:
  - **Open ****************************************************************************************************************************************************************`remoteOnly`**************************************************************************************************************************************************************** book offline**: download flow shows a clear "no network" error; row stays `remoteOnly` (does not flip to `failed`).
  - **Cellular with Wi-Fi-only ON**: download is queued (not started); user sees a "Waiting for Wi-Fi" indicator. Background session resumes when Wi-Fi appears.
  - **Cellular with Wi-Fi-only OFF**: download proceeds; cellular byte counter visible.
  - **Selective restore picker on a backup with 10k books**: list is virtualized (`LazyVStack`) and supports filter/sort by title/size/date.
  - **Open a book whose blob no longer exists on server** (manually deleted): download flow returns 404; row flips to `missingRemote`; user can delete the orphan row.
  - **Concurrent downloads of multiple books**: serialized to 2-3 concurrent transfers (avoid saturating WebDAV); progress aggregated in a download tray.
  - **Background download completion when app suspended**: standard `URLSessionDownloadDelegate` background-session callback wakes the app; file moved into sandbox; row flips to `local`.
  - **"Clean unused remote files" run during a concurrent backup on another device**: GC reads manifests at start, deletes only blobs unreferenced at that moment; race window is acceptable because the next backup re-uploads any missing blobs (content-addressed convergence).
  - **Storage pressure during lazy download**: iOS may evict the downloaded file under memory pressure if marked `isExcludedFromBackup` improperly — set the right resource flags.
  - **Selective restore choice changes mid-download**: user can cancel an in-flight download from the row; row reverts to `remoteOnly`.
- **Test plan**:
  - **Unit**:
    - `BookFileStateTests` — every state transition is valid; opening reader for non-`local` routes to download flow.
    - `LazyDownloadCoordinatorTests` — concurrent download cap, retry on tap, Wi-Fi-only gating with mocked reachability.
    - `RemoteBlobGarbageCollectorTests` — given 3 manifests with overlapping book sets, computes correct unreferenced-blob set.
  - **ViewModel**:
    - `SelectiveRestoreViewModelTests` — manifest filtering, selection toggling, "select all" semantics, pre-restore size computation.
  - **Integration — Docker WebDAV**:
    - `LazyDownloadIntegrationTests` — restore selectively (1 of 3 books) → library has 1 `local` and 2 `remoteOnly` rows → tap on one `remoteOnly` → download completes → row flips to `local` and reader opens at saved position.
    - `RemoteGarbageCollectorIntegrationTests` — back up 2 libraries with one shared book → delete one backup's manifest → run GC → only the unique-to-that-backup blob is deleted; shared blob survives.
  - **Manual verification**:
    - On iPhone 17 Pro Simulator with `lllyys/vreader-webdav-host`: backup 5 books → wipe → selectively restore 2 → verify 5 rows visible (2 local, 3 remote) → tap a remote row → verify download + open at correct position. Repeat with Wi-Fi-only toggle and cellular reachability mocked via Network Link Conditioner.
- **Acceptance criteria**:
  - "Restore selectively…" picker correctly lists every book in the chosen backup's manifest and respects user selection.
  - `remoteOnly` rows appear in the library with cloud-download icons and remain visible after app relaunch.
  - Tap-to-download flips a row to `local` and opens at the saved position.
  - "Wi-Fi only" toggle correctly defers cellular downloads to Wi-Fi; visible "waiting" state.
  - "Clean unused remote files" deletes only blobs unreferenced by any backup manifest; concurrent backup on another device does not corrupt state.
  - All unit + integration + ViewModel tests pass.
  - LOC under \~300 per new file; reader entry-point change does not exceed 50 LOC delta in `ReaderContainerView`.
  - `docs/architecture.md` updated: add `BookFileState`, lazy-download coordinator, GC action.

