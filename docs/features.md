# Feature Tracker

Track features to be implemented here. Must be planned before implementation.

## Rules

> **Binding for this file.** The rules, statuses, and plan template below govern every change made to `docs/features.md`. AGENTS.md treats them as the authoritative feature-tracker workflow.

- **Bugs vs features**: If something was implemented but doesn't work correctly, it is a **bug** — track it in `docs/bugs.md`. If something was never implemented, it is a **feature** — track it here. Never mix them.
- **Partial implementations**: If something is partially implemented, the broken part is a bug in `docs/bugs.md`; the missing capability is a feature here. Link them.
- **Cross-links**: When a bug fix resolves a feature, update the feature status to `DONE` with note `Resolved by bug #N`. When a feature depends on a bug fix, use `TODO` status with note `Blocked by bug #N`.
- **Plan before implementation**: Every feature must be planned before any code is written. Status must reach `PLANNED` before moving to `IN PROGRESS`. A plan requires the fields listed in the "Plan Template" section below.
- **Exception — resolved by bug fix**: If a bug fix incidentally delivers a feature, the feature may be set to `DONE` with `Resolved by bug #N` without a full plan. The bug's own cause/solution/lesson records serve as documentation.

## How to use

1. Add features as you identify them (fill in Summary and Area at minimum)
2. Plan the feature (fill in required plan fields above) → set status to `PLANNED`
3. Tell the agent: "implement feature #N" to start implementation
4. Agent updates Status when done

- **GitHub Issue closure** (post-merge finalizer — see `AGENTS.md` for full policy):
  - If the feature has a `GH: #N` in Notes, close the GitHub Issue only after:
    1. All acceptance criteria met and status is DONE in this file.
    2. Implementation is merged to `main`.
    3. Closure comment posted with commit SHA and acceptance result.
  - Partial delivery: keep GitHub Issue open; use checklist or split follow-ups.
  - PRs use `Refs #N`, not `Fixes #N` (prevents premature auto-close).

## Statuses

- `TODO` — not started
- `PLANNED` — plan complete (problem, scope, edge cases, tests, acceptance criteria), ready to implement
- `IN PROGRESS` — being worked on
- `DONE` — implemented; correctness not yet verified end-to-end
- `VERIFIED` — covered by an automated end-to-end test (XCUITest + DebugBridge) or by an explicit on-device manual verification log
- `DEFERRED` — postponed to a later milestone
- `WONT DO` — out of scope or rejected

## Plan Template

Before setting a feature to `PLANNED`, fill in these fields in a sub-section under the feature table (e.g., `### Feature #1 — Plan`):

- **Problem**: What user need does this address?
- **Scope**: What is included and excluded?
- **Edge cases**: Empty input, nil, boundary values, concurrent access, format-specific behavior.
- **Test plan**: What tests will verify the feature?
- **Acceptance criteria**: How do we know it's done?

## Features

| #  | Summary                                                                        | Area          | Priority | Status    | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| -- | ------------------------------------------------------------------------------ | ------------- | -------- | --------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1  | Edit and delete bookmarks                                                      | Reader/*     | High     | VERIFIED  | Rename via context menu (bug #42), delete via swipe + context menu. BookmarkListView has full CRUD UI. 2026-05-07 slice verify: 14 tests across 6 suites (BookmarkListViewModel loading/mutation/toggle/edge cases + PersistenceActor bookmark CRUD with in-memory container + BookmarkFeedback haptic). **2026-05-08 round-2 slice**: annotations panel UI leg now exercised end-to-end via `vreaderUITests/Annotations/AnnotationsPanelPlaceholderTests` + `vreaderUITests/Reader/ReaderAnnotationsPanelTests` (11 XCUITest methods, 188.1 s). **2026-05-09 round-3 device verify** (`dev-docs/verification/feature-1-20260509.md`, `result: pass`): closes the per-bookmark rename + swipe-delete gestures that round-2 deferred. On iPhone 17 Pro Sim (iOS 26.4) at v3.14.98 with `vreader-debug://seed?fixture=war-and-peace`, full CRUD round-trip driven end-to-end: tap bookmark toolbar icon → "Untitled Bookmark" appears in panel; long-press row → context menu with Rename + Delete; Rename → dialog → paste "Chapter 1 start" → Save → row updates; left-swipe row → red trash Delete revealed; tap Delete → row removed → "No Bookmarks" empty state. 6 screenshot artifacts capturing each transition. Combined with round-2's annotations-panel mechanics, BookmarkListView CRUD surface is fully verified. Original evidence at `dev-docs/verification/feature-1-20260507.md` + `feature-1-20260508.md`. GH: #383                                                          |
| 2  | Highlight search result at destination                                         | Search/*     | Medium   | VERIFIED  | **2026-05-22 round-6** (`dev-docs/verification/feature-2-20260522-round6.md`, result=**pass**) on iPhone 17 Pro Sim (iOS 26.5) at v3.39.6 build 627 (HEAD `529c9723`): BOTH the round-5 EPUB fail leg AND the round-3 TXT partial leg now verify end-to-end CU-free via the `vreader-debug://search?query=…&index=N` driver (Bug #238). **EPUB** (mini-epub3, cross-chapter): `search?query=navigation&index=0` navigates Chapter One → Chapter Two; DOM probe of `.vreader_search_highlight` returns **count=1** at t≈1.0s/1.9s (texts=[" navigatio"], `inViewport:true`, `spanTop:148`), **count=0** at t≈3.0s/4.9s (auto-cleared) — the exact green lifecycle round-5's count=0-throughout fail recommended; visual yellow paint on "navigation" confirmed. Bug #182/GH #621's `searchHighlightJS` retry-loop fix is durable. **TXT** (war-and-peace, chapter mode): `search?query=Pierre&index=0` → `navigateToChapter idx=3` (position 0→1433, "Chapter 4 of 4"); yellow highlight on "Pierre" visible at t+0.7s/t+1.5s and **gone at t+4.5s** (auto-clear) — Bug #154/GH #443's `chapterLocalHighlightRanges` + `highlightNonce` fix makes the chapter-mode temp highlight render+clear (round-3's blocker resolved). All acceptance criteria PASS on both formats → **VERIFIED**. **Bug #264 / GH #1141 filed** (verification-tooling, NOT a Feature #2 defect): `vreader-debug://reset` doesn't clear the persistent FTS store, so a stale `search_metadata` row with empty `segment_base_offsets` makes `setup()` skip indexing + in-memory-mark → `isIndexed()` never flips → the `search` driver stalls at its 30s index-wait; worked around by clearing `Library/Application Support/SearchIndex/`. 6+ artifacts. — Resolved by bug #43 — yellow background highlight, auto-clears after 3s. 2026-05-07 cross-ref slice: no unique test surface; covered by SearchHighlightDismiss (#5), HighlightedSnippet (#3), per-format renderers (#11/#17). `dev-docs/verification/feature-2-20260507.md` (`result: partial` — UI gesture: search→tap result→scroll+highlight visible+auto-clear deferred, needs display). **2026-05-08 round-2 slice**: navigation half verified end-to-end on iPhone 17 Pro Sim (iOS 26.4) at v3.14.92 — search "Paragraph 50" → tap result → reader scrolls so the matched line lands inside viewport (bug #153 fix working). Render half **broken**: no yellow highlight appears on the matched range; root cause is an orphan `@State highlightRange` in `TXTReaderContainerView.swift` (notification handler writes `uiState.highlightRange`, bridge reads the orphan). Bug **#154** filed (GH #443) — distinct from #153, not fixed this round per verify-cron scope. Auto-clear half blocked-by-#154. `dev-docs/verification/feature-2-20260508.md` (`result: partial`). **2026-05-11 round-3** (`feature-2-20260511-round3.md`, result=**partial**) on merged-main fcb1ff5 (v3.14.145) against bug #154 PARTIAL FIX: navigation half re-verified (search "Pierre" → tap result → reader scrolls so "Pierre, who was alone among the guests" lands inside viewport, "1/1" pager + 0% scrubber render correctly). Render half **still blocked**: war-and-peace fixture triggers chapter mode (`hasChapterDisplay = true`, "Section 8" labels in search results), so the body dispatches to `chapterReaderContent` (`TXTReaderContainerView.swift:118` → line 496) where bug #154's WI-7 deferral keeps `highlightRange: nil`. No yellow highlight visible across t+0.4s / t+1.2s / t+4.2s captures. No new bug filed — same WI-7 deferral as bug #154 + bug #160 + feature #40 round-3. 8 screenshot artifacts. Driven via CU-substitute toolkit (`.claude/skills/sim-drive-fallback`) — CU MCP still returns `CU display unavailable`. Status stays DONE; flip to VERIFIED needs WI-7 OR addition of a chapter-less TXT fixture to `DebugFixtureCatalog.swift`. GH: #400                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| 3  | Manual text highlighting                                                       | Reader/*     | High     | VERIFIED  | **2026-05-12 round-2 EPUB** (`dev-docs/verification/feature-3-20260512.md`, v3.17.2, result=**pass**): CU recovery from SCContentFilter outage unlocked the EPUB device-verification leg deferred from round-1. Long-press at (130, 190) on word "emphasized" in mini-epub3 chapter 1 → custom UIMenu shows Highlight/Add Note/Copy → tap Highlight at (185, 380) → DebugBridge `highlightCount` 0 → 1; close (back chevron) + reopen via `vreader-debug://open` → `highlightCount` stayed at 1 AND yellow CSS Highlight overlay re-rendered automatically on first body render. EPUB was the largest cross-format risk (custom WKWebView bridge + JS highlight API + CSS Highlight overlay); TXT/MD/PDF paths covered by round-1's 84 unit tests. TXT chapter-mode failure (bug #160) is a separate scope-gap tracked in feature #48. Row → **VERIFIED**. 2 screenshot artifacts. **2026-05-09 round-3** (`dev-docs/verification/feature-3-20260509-round3.md`, result=**fail**): Now that feature #11 round-4 (this same session) verified EPUB highlighting end-to-end via the same long-press → custom-menu → tap-Highlight pattern, we can rule out CU/menu-wide failure modes and definitively name the TXT path as broken. Reproduced 2 more times (after round-2's 4) on words "Genoa" and "that" at distinct coords in war-and-peace.txt — 6/6 attempts produce identical outcome: menu shows + dismisses cleanly, but NO yellow paint AND NO entry in Highlights tab. Cross-format contrast: EPUB Highlight via same gesture works in this session (feature #11 round-4 PASS); Define sibling on same TXT menu works (round-2 cross-ref to #33). So failure is TXT-Highlight-action-specific, NOT a CU coordinate issue, NOT a menu-wide problem, DIFFERENT from bug #159 (which was a render gap with data persisting). Filed bug **#160 / GH #476** (Reader/* Medium). VERIFIED gated on #160 fix. 2 screenshot artifacts. Feature #4 (Add notes, same menu, same notification-post pattern) likely affected — separate verification needed. Resolved by bug #44 — Highlight action added to UITextView edit menu. 2026-05-07 slice verify: 84 tests across 14 suites — HighlightListViewModel (6 sub-suites: Loading/Mutation/Edit/Out-of-Bounds/Overlap/TXT-UTF-16), HighlightCoordinator + integration, HighlightableTextView (bug #44 lock), HighlightDedupe, HighlightRecordAnchor (Codable), HighlightAnchorStorage migration, HighlightedSnippet, PersistenceActor highlight CRUD. Per-format renderers covered in #17/#11. `dev-docs/verification/feature-3-20260507.md` (`result: partial` — UI gesture deferred). Round 2 (`feature-3-20260508.md`, v3.14.87) result=**partial**: CU confirmed the custom 4-item menu structure (Highlight / Add Note / Define / ▶) at runtime; Define sibling action verified separately in #33 round 2 (same menu) — proves CU CAN fire menu actions. The Highlight tap-to-persist gesture path inconclusive across 4 CU click attempts (menu dismisses but persistence layer reports "No Highlights"); not filed as a bug — `HighlightableTextViewTests` unit-tests the action dispatch and Define's success rules out a CU/menu-wide quirk. Recommend next round via XCUITest programmatic-tap or physical device. Status stays DONE. GH: #385                                |
| 4  | Add notes/annotations to text                                                  | Reader/*     | Medium   | VERIFIED  | **2026-05-14 round-5** (`feature-4-20260514-round5.md`, result=**pass**) on iPhone 17 Pro Sim (iOS 26.5) at v3.21.55 (commit `e66b15952b0248ba8d7dfb8416444cd1c4254984`, build 332) with bundled `war-and-peace.txt`: closes the last gating criterion. Bug #188 fix held under production sequence — long-press `Lucca` (Ch1 line 1) → custom menu → `Add Note` → modal opens with `"Lucca"` attribution + greyed Save → clipboard fast-path paste note text → Save flips enabled → tap Save → modal dismisses → **annotations panel → Notes tab shows row with `Lucca` chip + body + `May 14, 2026 at 21:51` timestamp**, SQLite confirms BOTH `ZANNOTATIONNOTE` (1 row) AND `ZHIGHLIGHT` (1 row, selectedText=`Lucca`, color=`yellow`, note matches), `DebugBridge` snapshot `highlightCount: 1`, AND **yellow background plainly visible on "Lucca" in chapter mode** with chrome restored + selection cleared. Criterion 8 PASS (save round-trip), criterion 8c PASS (visual highlight on annotated text). All 10 criteria now PASS. Status → **VERIFIED**. 3 screenshot artifacts (`feature-4-r5-{postsave,clean,notes-tab}-*-20260514.png`). Round-5 simultaneously unblocks Bug #181 (GH #616) close-gate device verification AND Bug #188 (GH #659) close-gate device verification (same production sequence exercises both fixes). Resolved by bug #44 — Add Note action added to UITextView edit menu. 2026-05-07 slice verify: 130 tests across 10 suites — AnnotationListViewModel (4 sub-suites: Loading/Add-Remove/Edit/Edge Cases) + Annotation Models (highlight/bookmark/note unified) + AnnotationAnchor (EPUB/PDF/TXT round-trip + SHA stability) + PDFAnnotationBridge (PDFKit highlight + restore) + AnnotationExporter (JSON+Markdown) + VReaderAnnotationParser + AnnotationImporter end-to-end. Bug #44 entry-menu lock cross-ref'd via feature #3 evidence. `dev-docs/verification/feature-4-20260507.md` (`result: partial` — UI gesture: long-press text → Add Note modal; long-press existing note → Edit/Delete; export share-sheet; import file-picker — deferred, needs display). **2026-05-10 round-2** (`feature-4-20260510.md`, result=**partial**) on merged-main 41067e5 (v3.14.123): closes the **long-press → Add Note modal** entry-point slice end-to-end on iPhone 17 Sim with war-and-peace.txt. Long-press at mac (550, 500) brings up custom 4-item menu **Highlight \| Add Note \| Define \| ▶**; tap Add Note → modal slides up with `Cancel \| Add Note \| Save (greyed)` header and selected word `"how"` as italic attribution; empty-input Save-gating verified disabled; Cancel dismisses cleanly returning to reader with selection still rendered. 3 screenshot artifacts. **DEFERRED**: type note → Save → render highlight + panel entry; long-press existing note → Edit/Delete UI — both blocked by same external CU/iOS Simulator text-field input quirk previously documented in feature #34 round-3 (synthetic macOS keyboard events do not reach iOS Sim UITextView/UITextField inputs even with Connect Hardware Keyboard ✓ and Simulator frontmost). Tried `osascript keystroke`, `pbcopy + Cmd+V`, Simulator menu-bar `Edit → Paste` — all observable as no-op. Not a vreader defect. Status stays DONE; flip to VERIFIED needs an XCUITest-driven save-flow harness that bypasses macOS-side keyboard synthesis. **2026-05-13 round-3** (`feature-4-20260513.md`, result=**partial**) on HEAD (commit 6615c8b7, v3.21.8 build 285) on iPhone 17 Pro Simulator (iOS 26.5): closes the **save round-trip half** of round-2's deferred criterion 8. `mcp__computer-use__write_clipboard` + `key("cmd+v")` reaches the iOS Sim UITextView input layer (the round-2 quirk was osascript-side, not iOS-Sim-side). Note text pasted byte-for-byte → Save button enabled (gating flip confirmed) → tap Save → modal dismisses → Annotations panel → Notes tab shows row with attribution `"Genoa"`, body `"Genoa is a major port city in northwestern Italy."`, timestamp `May 13, 2026 at 16:16`. Save round-trip end-to-end PASS at the data layer. **Visual-highlight half (8c) still FAIL** — same root-cause class as bug #160 (chapter-mode visual paint deferred); even with note persisted, no yellow background renders on the annotated word. war-and-peace.txt CHAPTER I/II/III markers trigger chapter mode (the surface bug #160's note explicitly covers). No new bug filed — cross-ref bug #160. 4 screenshot artifacts (`feature-4-r3-*-20260513.png`). **Criterion 9 also newly closed this round**: long-press on the persisted note row in Annotations → Notes reveals the **Edit + Delete** context menu (default + destructive-red styles). Only criterion 8c (visual highlight on annotated text) remains FAIL — same bug #160 chapter-mode visual-paint gap. Status stays DONE; flip to VERIFIED gated on bug #160's chapter-mode visual-paint fix landing + device verify. **2026-05-14 round-4** (`feature-4-20260514-round4.md`, result=**fail**) on iPhone 17 Pro Sim (iOS 26.5) at v3.21.53 (commit `177e3e35`, build 330) with bundled `war-and-peace.txt`: criterion 8c remains FAIL because criterion 8 (save round-trip) **REGRESSED** from round-3 PASS. Add Note Save at v3.21.53 persists NEITHER AnnotationRecord NOR HighlightRecord (SQLite both tables empty for the book; snapshot `highlightCount: 0`; Notes tab "No Annotations"). Control test (rules out broader regression): long-press same word → tap "Highlight" instead of "Add Note" on word "family" → HighlightRecord persists + yellow paint renders in chapter mode. So locatorFactory + chapter-mode visual paint (bug #160 fix) are healthy; the failure is specifically in `handleAnnotationSave` reading `state.pendingAnnotationInfo` AFTER AddNoteSheet's `dismiss()` has already cleared it (race introduced by bug #181 fix at PR #658). **Bug #188 filed** with full root-cause analysis + fix direction (`docs/bugs.md`, GH #659). Status stays DONE; flip to VERIFIED gated on Bug #188 fix landing + re-verify (this becomes round-5 once Bug #188 is FIXED). GH: #388                                                                                                                                                                                                                                                                                                                |
| 5  | Search highlight auto-dismiss on next action                                   | Search/*     | Low      | DONE      | Mirror: no — pre-mirror TODO row, code shipped + tested but never given a GH issue; tracking inline only. WI-003 code committed + 25 unit tests in `SearchHighlightDismissTests` cover all dismiss triggers (tap/scroll/new search) + persistence preservation + idempotency at real `HighlightingLayoutManager`/`HighlightableTextView` boundaries. Sim verification 2026-05-04 was **partial** (`dev-docs/verification/feature-5-20260504.md`): search panel + result-tap navigation work, but search-result-tap navigation lands the matched text past the visible viewport so the visual highlight cannot be confirmed in the rendered region. Dismiss-on-tap exercised but inconclusive (chrome-toggle path also fired). **2026-05-08 round-2 device verification with CU**: confirmed the deferred-slice symptom empirically — landing region after search-result-tap shows only "reading and restore the scroll position when reopened." (the END of paragraph 100); "Paragraph 100: ..." is OFF-SCREEN above the viewport. By the time the user scrolls up to find the match, the 3-second highlight auto-clear has fired — yellow highlight is NEVER visible. Filed as **bug #153 / GH #433** (Reader/*, Medium severity). Result upgraded from "partial" (could not confirm) to **`fail`** (confirmed broken). Evidence: `dev-docs/verification/feature-5-20260508.md` + 2 PNG artifacts under `verification/artifacts/bug-153-*-20260508.png`. Status stays DONE pending bug #153 fix; will re-verify after fix lands. Suspect surface: `TXTTextViewBridge.scrollToGlobalOffset` / `scrollRangeToVisible` rounds to bottom of visible region, pushing the matched-text START above viewport. Possibly overlaps with bug #99 (#26) but separate root cause; both could share a centering-scroll-anchor fix path. **2026-05-13 round-3 device verification with CU on v3.21.7 build 284 (commit 6615c8b7, iPhone 17 Pro Sim iOS 26.5)**: bug #153 fix held — matched text now lands in viewport with correct headroom — but the temporary yellow highlight STILL does not render in TXT chapter mode. The bug #154 "FIXED" claim (chapter-mode wiring via feature #48 WI-3, commit a472a6d) was code-complete but runtime-incomplete. Sub-second post-tap simctl screenshot captured BEFORE the 3s auto-clear window expired — no yellow paint present. Bug #154 / GH #443 REOPENED. Evidence: `dev-docs/verification/feature-5-20260513.md` (`result: fail`) + 2 PNG artifacts (`feature-5-r3-immediately-after-search-tap-paragraph50-20260513.png`, `feature-5-r3-no-highlight-after-search-tap-20260513.png`). Status stays DONE pending bug #154 fix; will re-verify once the chapter-mode highlight render path is repaired. |
| 6  | Persist library view preferences across app restarts                           | Library/*    | Medium   | VERIFIED  | WI-001. PreferenceStore + UserDefaults. 2026-05-07 slice verify: 36 tests across 2 LibraryViewModel suites cover sort-order (title/addedAt/lastReadAt/totalReadingTime) + view-mode toggle + import flow + #20 reset path. `LibraryViewModelPersistenceTests.swift` is excluded from the test target by project.yml; persistence behavior is exercised via the ViewModel surface instead. **2026-05-08 round-2 slice**: UI-gesture leg now exercised end-to-end via `vreaderUITests/Library/LibraryPopulatedTests` (8 XCUITest methods, 56.8s) — view-mode toggle tap flips label, sort picker tap exposes all 4 options, grid/list bookCard/bookRow elements render. **2026-05-09 round-3 device verify** (`dev-docs/verification/feature-6-20260509.md`, `result: pass`): closes the relaunch-persistence leg that round-2 deferred. On iPhone 17 Pro Sim (iOS 26.4) at v3.14.96, sort changed from Title (default) → Date Added via UI, then `xcrun simctl terminate` + `xcrun simctl launch --uitesting --seed-books` (no `--reset-preferences`) — post-relaunch library re-displays Date Added order byte-identical to pre-terminate state, AND the sort picker shows ✓ on Date Added confirming structural persistence. UserDefaults survives terminate (independent of `--uitesting`'s in-memory SwiftData decision); `--reset-preferences` is the explicit opt-in for clearing it (per bug #152 fix). 3 screenshot artifacts. View-mode toggle relaunch persistence shares the same `PreferenceStore` plumbing — round-2's XCUITest coverage is sufficient there. Original evidence at `dev-docs/verification/feature-6-20260507.md` + `feature-6-20260508.md`. GH: #379                                                                  |
| 7  | Visual feedback when adding a bookmark                                         | Reader/*     | Low      | VERIFIED  | **2026-05-13 round-3** (`dev-docs/verification/feature-7-20260513.md`, result=**pass**, verification-exception path) on v3.21.5 build 282 (commit `727fdb3`): bug #172 FIXED via PR #568 (v3.18.1) wired haptic across all 5 reader paths — code-read confirms MD `MDReaderContainerView:245`, TXT `TXTReaderContainerView:479`, EPUB `EPUBReaderContainerView:177`, PDF `PDFReaderContainerView:179`, AZW3 `FoliateSpikeView:70`. `BookmarkFeedbackTests` (5) + `ReaderNotificationHandlerTests` (12, including 2 new haptic-fires-on-success + suppress-on-failure) PASS on iPhone 17 Pro Sim. Verification-exception path applies because haptic is a tactile signal with zero simulator observability; high-fidelity tests at real `NotificationCenter` + real `HapticFeedbackProvider` protocol seam drive the same production dispatch. **VERIFIED.** WI-002. UIImpactFeedbackGenerator(.light). 5 tests. 2026-05-07 dedicated slice (cross-ref to feature #1's run): BookmarkFeedback 5 tests pin haptic-fire on add (incl. pre-existing toggle), no-fire on error / nil provider, no debounce, intensity = .light. `dev-docs/verification/feature-7-20260507.md` (`result: partial` — physical haptic only verifiable on a real device; simulator does not vibrate). **2026-05-12 verify round-2 finding**: dispatch path is correct at the handler boundary (`ReaderNotificationHandlers.handleBookmarkRequest` → `deps.hapticFeedback?.triggerLightImpact()`, pinned by 5 BookmarkFeedbackTests with `MockHapticProvider`), but only 1 of 5 reader containers wires `HapticFeedbackProvider()` into its deps: `MDReaderContainerView.swift:245`. TXT's `makeNotificationDeps()` omits the parameter (defaults to nil → no-op via optional chaining); EPUB/PDF/Foliate observe `.readerBookmarkRequested` inline and bypass `ReaderNotificationHandlers` entirely. Filed as **bug #172 / GH #558** (Reader/* Low) — the broken cross-format wiring. Feature row stays DONE; VERIFIED flip is blocked until either (a) bug #172 fix lands the haptic across all 5 reader paths, or (b) feature scope is explicitly narrowed to MD-only. GH: #401                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| 8  | Reading position scrubber/progress bar                                         | Reader/*     | Medium   | VERIFIED  | WI-004a-d. ReadingProgressBar + per-format wiring (TXT/MD/PDF/EPUB). 2026-05-07 slice verify: 83 tests across 3 dedicated progress suites (ReadingProgressBar shared view + TXT/MD continuous + PDF page-based; EPUB chapter+progression via Foliate Locator covered cross-feature). `dev-docs/verification/feature-8-20260507.md` (`result: partial` — UI gesture deferred). Round 2 (`feature-8-20260508.md`, v3.14.90) result=**pass**: CU drove both deferred UI-gesture slices end-to-end on iPhone 17 Pro Sim — drag scrubber from left to right at y=580 → reader jumped from "Paragraph 1" / 0% to "Paragraph 91" / 90%; swipe-down on content → reader moved to "Paragraph 88" / 85%, scrubber thumb position + labels updated live. Bidirectional sync (scrubber↔scroll) live without observable latency. Status → VERIFIED. GH: #375 |
| 9  | Comprehensive book context menu in library                                     | Library/*    | Medium   | VERIFIED  | WI-006. Info/Share/Delete + BookInfoSheet. 2026-05-07 slice verify: 35 tests across 4 suites (FileSizeFormatter, BookInfoSheet metadata rendering, LibraryBookItem.resolvedFileURL, ShareSheet.shareItems). Set Cover + Delete actions verified cross-feature (Set Cover via #6 PhotosPicker, Delete via LibraryViewModel surface). **2026-05-08 round-2 slice**: long-press → context menu → Delete leg now exercised end-to-end via `vreaderUITests/Library/DeleteConfirmationTests` (6 XCUITest methods, 160.2 s) — long-press surfaces the menu, Delete option fires confirmation alert containing book title, Cancel dismisses, Confirm removes the row, swipe-to-delete shares the same path, deleting all 8 seeded books transitions to empty state. **2026-05-09 round-3 device verify** (`dev-docs/verification/feature-9-20260509.md`, `result: pass`): closes the Info + Share legs that round-2 deferred. On iPhone 17 Pro Sim (iOS 26.4) at v3.14.95 with `--seed-books`, long-press on "Test EPUB Book" surfaces the 6-item context menu (Info / Share / Set Cover / Remove Cover / Add to Collection / Delete with destructive coloring on the last two). Tapping Info presents `BookInfoSheet` with the seeded fixture's metadata (Title `Test EPUB Book`, Author `Test Author`, Format `EPUB`, File Size `102 KB`, Date Added `Mar 9, 2023 at 04:26`). Tapping Share presents `UIActivityViewController` with the EPUB file (sha256-named `epub_0000000000000000…`) and standard Copy / Save to Files actions. Combined with round-2's Delete coverage, every production menu item is now driven end-to-end. 3 screenshot artifacts. Original evidence at `dev-docs/verification/feature-9-20260507.md` + `feature-9-20260508.md`. GH: #377                                       |
| 10 | iCloud backup and restore                                                      | Settings/*   | —        | WONT DO   | Not needed. WebDAV (#29) covers backup needs.                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| 11 | EPUB text highlighting and note-taking                                         | EPUB/*       | High     | VERIFIED  | **2026-05-09 round-4** (`dev-docs/verification/feature-11-20260509-round4.md`, result=**pass**): VERIFIED. Round-3 gap closed by this session's bug #159 / GH #472 fix (XPath namespace rewrite + CSS Highlight API priority + foliate-bridge.js DOMContentLoaded timing fix). Re-verified end-to-end on iPhone 17 Pro Sim (iOS 26.4) at v3.14.118: long-press on word "testing" in mini-epub3 paragraph 1 → custom VReader menu (Highlight \| Add Note \| Copy) → tap Highlight → **yellow background visibly painted**; tap back → Library → reopen book → **yellow paint + Highlights tab entry both persist after close+reopen** (live probe confirms `cssHighlightCount: 1, styleEls: 1` post-reopen — restore path correctly re-injected style element AND re-registered the highlight). 3 screenshot artifacts. Combined with round-1's persistence + count slices, round-2's bridge-readiness probes, round-3's gesture + data-layer slice, feature #11 has full end-to-end coverage. AZW3 cross-format check remains scoped to feature #11's "AZW3 highlights — Selection capture + CFI anchoring shipped; overlay restoration deferred to WI-7" note. **2026-05-09 round-3** (`dev-docs/verification/feature-11-20260509.md`, result=**partial**): closed the long-deferred gesture-driven user-flow slice. Long-press text selection on word "some" in mini-epub3 Chapter One paragraph 3 → iOS shows custom VReader menu (Highlight \| Add Note \| Copy) alongside standard system menu → tap Highlight → selection dismisses → side panel Highlights tab shows row "🟡 some" with yellow color dot. **Data layer + gesture path PASS.** **Visual render FAIL**: word "some" appears with IDENTICAL styling to surrounding text — no yellow background painted on the EPUB page. Reproducible in Sepia AND Light theme; persists across book close+reopen. Filed as bug **#159 / GH #472** (EPUB/* Medium): CSS Highlight API paint pipeline gap — bridge accepts call + data persists, but `::highlight()` rule injection / `CSS.highlights.set` paint not firing. VERIFIED gated on #159 fix. 4 screenshot artifacts. Bonus regression: bug #158 picker shown for EPUB ✓, bug #128 banner copy update visible ✓, bug #128 native-mode no-op (Lorem ipsum verbatim) ✓. WI-C00 → WI-007. Bug #77 FIXED (JS buffering). Sim verification 2026-05-06 result=**partial** (`feature-11-20260506.md`): persistence + count-exposure slices PASS. Round 2 (`feature-11-20260506b.md`, post-bug-#126/v3.14.13): **bridge-readiness slice now PASSES via the new eval surface** — 6 probes against mini-epub3 confirm `__vreader_createHighlight` defined as function, `selectionChanged` message handler registered, CSS Highlight API supported, DOM ready, 3 `<p>` parsed, programmatic `CSS.highlights.set` round-trips with correct rangeText. **Still deferred**: gesture-driven user-flow (text selection → menu tap "Highlight" → bridge call) — requires UI driving which DebugBridge doesn't synthesize. Status stays DONE pending gesture-driven slice. GH: #404                                                                                                                                                                                                                                                                                                                                                                                                                |
| 12 | Auto-generate TOC for MD files                                                 | Reader/*     | Medium   | VERIFIED  | WI-005. Regex heading extraction, fenced code block skip, correct UTF-16 offsets. TOC builder slice = 25 tests; broader MD subsystem (parser, loader, types, renderer, metadata extractor, ViewModel, integration suites, unified-mode reflow, text extractor) brings 2026-05-07 verify total to 138 tests across 19 suites. `dev-docs/verification/feature-12-20260507.md` (`result: partial` — UI gesture scroll-to-heading deferred). **2026-05-13 round-2** (`dev-docs/verification/feature-12-20260513.md`, result=**pass**): closes deferred UI gesture leg end-to-end on iPhone 17 Pro Sim (iOS 26.5) at v3.21.1. `MDTOCVerificationTests` (14.4s): seed `--seed-md-toc` (5-heading MD fixture) → tap "Test Markdown TOC" → annotations panel → `tocEmptyState` absent + `tocRow-...:Introduction:0` found → tap → panel dismissed (`handleNavigate` fired `onNavigate + onDismiss`) → back button still present (no crash). All acceptance criteria now pass. GH: #373                                       |
| 13 | AI book/chapter summarization                                                  | AI/*         | High     | VERIFIED  | WI-D00 → WI-009 → WI-010. Bug #92 FIXED (encoding). Device verified: non-UTF-8 TXT → AI summarize shows real content. 2026-05-07 slice verify: 44 tests across 5 suites — AIAssistantViewModel (11 tests; idle/disabled/consent/success/error/empty/cache/reset + per-action dispatch summarize/translate/vocabulary/askQuestion), AIReaderIntegration (12 tests; end-to-end summarize call, panel display, error path, button hiding when feature disabled, content truncation, loading state, gates, **PDF + EPUB format-specific summarize**, reset), AIReaderAvailability (8 tests; live transition on consent revoke, hasAPIKey, gate composition), AIRequest.cacheKey (7 tests; determinism — context-text-included key, nil-fingerprint general prefix), AIConfiguration (6 tests; round-trip + corrupt-data tolerance + partial-config forward-compat). Bug #92 regression covered cross-ref via summarize tests. **2026-05-10 round-2** (`ai-features-end-to-end-20260510.md`, result=**pass**) on merged-main 41067e5 (v3.14.123): closes deferred live-endpoint slice. AI Assistant sheet → Summarize tab → blue Summarize button → real Ring 2.6 response with accurate book-content summary of mini-epub3: "The text is a minimal, synthetic EPUB used for automated testing. It contains two short chapters: the first includes a few paragraphs (the second uses Lorem-ipsum filler and some inline markup), while the second chapter is deliberately brief to allow chapter-navigation tests. The whole file ends with 'End of fixture.'" — structurally faithful to fixture. New Request affordance verified. Status → VERIFIED. GH: #394                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| 14 | AI chat — talk to the book                                                     | AI/*         | High     | VERIFIED  | WI-D00 → WI-009 → WI-010 → WI-011. Multi-turn chat with book context via AIChatViewModel. Chat tab in AIReaderPanel. 121 tests across 11 suites (chat VM, assistant VM, reader integration + availability, AIService, config, context extractor, request-cache key, response cache, consent). **2026-05-10 round-2** (`ai-features-end-to-end-20260510.md`, result=**pass**) on merged-main 41067e5 (v3.14.123): closes deferred UI gesture + real-endpoint slice. Reader chrome AI Assistant button → sheet → Chat tab shows book-aware "Ask questions about this book" empty state. Sent "Title only please" → Ring 2.6 returned "VReader DebugBridge Test" — correctly references mini-epub3's actual chapter heading (book-context injection working). Multi-turn history persists; rate-limit yellow banner with Dismiss error button verified on error path. Status → VERIFIED. GH: #363                                |
| 15 | AI chat interface (general)                                                    | AI/*         | Medium   | VERIFIED  | WI-013. General chat (nil bookFingerprint). Entry point in LibraryView toolbar. 2026-05-07 slice verify: 45 tests across 3 suites — AIChatViewModel (23 tests; book/general bifurcation, multi-turn, sliding-window, Unicode/CJK, error/loading, gating-rejection paths), AIChatGeneralMode (9 tests; Library-toolbar entry-point gating, switch between book↔general, ViewModel nil-fingerprint, distinct empty state), AIService (17 tests; gate ordering flag→consent→API-key, cache-key determinism, stream gating, live-flag toggle). **2026-05-10 round-2** (`ai-features-end-to-end-20260510.md`, result=**pass**) on merged-main 41067e5 (v3.14.123): closes deferred UI + real-endpoint slice. Library toolbar Chat icon (gated by AIReaderAvailability — feature flag + key + consent) → AI Chat sheet → "Start a conversation" empty state (vs book-aware "Ask questions about this book") → typed "Say hi in one word." → Ring 2.6 returned "Hi". Real network round-trip end-to-end. Multi-turn history persists across sheet close+reopen. Status → VERIFIED. GH: #391                                                                                                                                                                                                                                                                                                                                                                                                                          |
| 16 | Remote server integration (claude CLI / directory management)                  | Server/*     | High     | DEFERRED  | WI-014 (design only). Design doc at docs/codex-plans/remote-server-design.md                                                                                                                                                                                                                                                                                                                                                                                                            |
| 17 | PDF text highlighting, annotation, and theming                                 | PDF/*        | High     | VERIFIED  | **VERIFIED 2026-05-28** (`dev-docs/verification/feature-17-20260528.md`, result=pass) — final residual cleared via computer-use (CU available again this session): a real long-press-drag over PDF text triggered vreader's highlight-action menu (Text Selection → Highlight/Add Note/Copy), and tapping Highlight created a persisted `HighlightRecord` (`page=0, color=yellow, format=pdf, hasAnchor=1, selectedText="Paragraph 1 of the ALPHA s"`, confirmed in the SwiftData `default.store`, ZHIGHLIGHT count 1 from clean reset). Combined with the prior pdf-highlight-driver verification (create→render→persist visible + survives reopen + theme gutter flip) the full acceptance is met. GH #361 closed. **2026-05-27 PDF-fixture render+theme device-verify (partial)** — `dev-docs/verification/feature-17-20260527.md` (result=partial). Added the `multi-page-pdf` DebugFixtureCatalog fixture (6-page text-layer PDF, the previously-missing harness piece) and device-verified CU-free on iPhone 17 Pro Sim: the PDF seeds+opens+renders (6 pages, "Page 1 of 6" indicator — criterion 7), and the theme **gutter** background flips light→dark via snapshot (criterion 8, as implemented per Bug #198 — page-color inversion is a separately-scoped feature, not #17). 7 backend criteria remain unit-verified (157-test slice). **Criterion 1 create→render→persist DEVICE-VERIFIED 2026-05-27** (`dev-docs/verification/feature-17-20260527-highlight.md`, result=partial) via the new `vreader-debug://pdf-highlight?page=N&rect=x,y,w,h[&color=]` driver (v3.39.58, PR #1214 — injects a highlight via the SAME production path the gesture uses: `handleHighlightAction`→`HighlightCoordinator.create`→`addHighlight`+`PDFAnnotationBridge.createHighlightFromAnchor`; byte-identical to a gesture highlight; Codex Gate-4 3 rounds ship-as-is): `pdf-highlight?page=0&rect=0.1,0.3,0.8,0.4` created a yellow highlight that **rendered** (visible over the text band) and **persisted** (survived close→reopen, restored from `HighlightRecord`); a rect over a margin/line-gap correctly **no-op'd** (mirrors the gesture's non-empty-selection gate). **Residual narrowed to the raw long-press-drag touch SELECTION gesture only** — PDFKit producing the `PDFSelection` that `selectionDidChange` consumes (everything downstream is now device-verified). That raw touch→selection is real-device/CU-only (analogous to #71's raw-scroll residual). Stays `DONE` (not flipped to VERIFIED) pending the raw selection gesture on a real device / with CU. **Round-2 device-verify 2026-05-16** (`dev-docs/verification/feature-17-20260516.md`, `result: partial`). 1 of 4 deferred UI slices closes: theme switch → PDF page background flips Dark → Light verified visually on `test.pdf` in vreader at v3.22.19 (PASS). 3 text-selection-dependent slices (long-press → Highlight menu; tap Highlight → annotation renders; tap existing highlight → change color) remain DEFERRED — the fixture available in this session (DeviceKit framebuffer mask, no selectable text) only yields the degraded iOS "Copy / Select All" popover. Long-press gesture wiring confirmed firing (popover appears). Unblocking those 3 slices needs either a bundled text-bearing PDF fixture in `DebugFixtureCatalog`, or Bug #197 fixed so a Share Sheet PDF import becomes visible in library without cold-restart friction. WI-C00 → WI-008. PDFAnnotationBridge + selection detection + persist/restore. 157 tests across 17 suites (annotation bridge, page navigator, ViewModel, position service, locator, session, lifecycle). 2026-05-07 slice verify: `dev-docs/verification/feature-17-20260507.md` (`result: partial` — UI gesture + PDFView native render deferred). GH: #361                                                                                                                          |
| 18 | AI-powered contextual translation with bilingual view                          | AI/*         | High     | VERIFIED  | WI-012. Bug #95 FIXED (initialTab). Device verified: Select word → Translate → opens Translate tab. 92 tests across 8 suites (translation VM, shared AIService + config + context + cache + consent + assistant orchestrator). **2026-05-10 round-2** (`ai-features-end-to-end-20260510.md`, result=**pass**) on merged-main 41067e5 (v3.14.123): closes deferred mid-sheet language change + real-endpoint slice. AI Assistant sheet → Translate tab → "Translate to: Chinese" picker reveals 9-language dropdown (Chinese/Japanese/Korean/Spanish/French/German/Portuguese/Russian/Arabic) → blue Translate button → Ring 2.6 returned bilingual view: Original (English Chapter One) + Chinese ("第一章 第一章 这是一段位于第一章节开头、用于 VReader DebugBridge 自主测试的小型合成 EPUB 的首段文字...") — accurate translation, structurally faithful. Mid-sheet language change verified by tapping Japanese in picker (label updates immediately). Translate panel rate-limit error renders cleanly with "Try Again" button (better UX than chat panel's persistent banner). Status → VERIFIED. GH: #365                                                                       |
| 19 | ~~Merged into feature #6~~                                                     | Library/*    | —        | DUPLICATE | Display mode persistence merged into feature #6 (library view preferences)                                                                                                                                                                                                                                                                                                                                                                                                              |
| 20 | Sort order reset/revert to default                                             | Library/*    | Low      | VERIFIED  | WI-001 (bundled with #6). 2026-05-07 cross-ref slice: reset path counted in feature #6's 36-test LibraryViewModel run; no separate test file. `dev-docs/verification/feature-20-20260507.md` (`result: partial` — UI gesture: sort menu→Default→list re-sorts deferred, needs display). **2026-05-09 round-2 device verify** (`dev-docs/verification/feature-20-20260509.md`, `result: pass`): on iPhone 17 Pro Sim (iOS 26.4) at v3.14.94 with `--seed-books --reset-preferences`, the round-trip Title (default) → Date Added (re-orders alphabetical → date-based) → Title (returns to original order) is verified end-to-end. 4 screenshot artifacts. **Documentation finding** — the row's "Default option in sort picker" wording was misleading: the actual `LibrarySortOrder.allCases` picker shows 4 named options (Title / Date Added / Last Read / Reading Time) without a separate "Default" item; "reset to default" is achieved by re-selecting Title (the source-of-truth default at `LibraryViewModel.swift:42`). The capability works as intended; only the description was off. GH: #402                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| 21 | Paginated reading mode with turnable pages                                     | Reader/*     | High     | VERIFIED  | B04-B13. Bug #82 FIXED (preserve navigator). Sim verification 2026-05-06 result=**partial** (`feature-21-20260506.md`): EPUB paged-mode CSS state verified active via the new bug #126 eval surface — `vreader-pagination` style element injected, `column-width: 362px` (matches `paginationCSS()` formula), `column-gap: 40px` (matches `EPUBPaginationHelper.columnGap`), `column-count: auto`, `body overflow: hidden`. Direct plist write of `readerEPUBLayout = paged` switched mode; reset+seed+open then eval probes confirmed CSS layout is real-render-correlated, not just configured. **Multi-page navigation slice was DEFERRED** at round-1: mini-epub3 chapter 1 fit on one column-page (scrollWidth == clientWidth), so `EPUBPaginationHelper.navigateToPageJS` wasn't observable on that fixture. **2026-05-11 round-2** (`feature-21-20260511-round2.md`, result=**pass**) on merged-main 639b5fa (v3.15.1, build 263): closes the deferred slice end-to-end against Alice's Adventures in Wonderland (137KB real-length EPUB, imported via OPDS during feature #36 round-5). Chapter 2 (Project Gutenberg license + Contents) produces `body.scrollWidth = 1458 > clientWidth = 980`. Setting `body.scrollLeft = 980` clamps to `maxScroll = 478` (browser-level, expected), and the rendered view scrolls horizontally from "license-left-col + Contents-right-col" to "Contents-left-col + blank-right" — bidirectional round-trip via `scrollLeft = 0` restores the original two-column view. `totalPagesJS` returns 2 for this chapter, matching `ceil(1458/980)`. EPUB Layout picker (Scroll/Paged) confirmed in AX tree at sheet pos (463, 985); Page Turn Animation section (None/Slide/Cover) rendering is runtime-conditional on `store.epubLayout == .paged` and IS rendered — runtime-confirming the active layout. CU-substitute toolkit observation: CGEventPost mouse-drag does NOT reach WKWebView's touch handler so swipe-gesture page-advance can't be verified that way; DebugBridge `eval` is the only verifier-driveable path for the navigation primitive — a verification-tooling gap, not a vreader defect. 6/6 acceptance criteria PASS. Status → VERIFIED. GH: #405 |
| 22 | Highlight matching text in search result list                                  | Search/*     | Medium   | VERIFIED  | Bold/highlight query term in search result row snippets. Sim verification 2026-05-07 result=**partial** (`feature-22-20260507.md`): 15 `HighlightedSnippetTests` PASS — single-word/multi-word/case-insensitive matching, adjacent-match coalescing into one bold run, non-adjacent matches as separate runs, FTS5 tag stripping, regex-special-char escaping (security gate against `NSRegularExpression` injection), empty query/whitespace/empty-snippet/no-match plain-text fallback, duplicate-word + extra-spaces edge cases. Production wiring at `SearchResultRow.swift` confirmed by code-read. Round 2 (`feature-22-20260508.md`, v3.14.89) result=**pass**: CU drove the deferred UI render slice end-to-end on iPhone 17 Pro Sim — search "100" produces 1 result with `100` rendered bold in "Paragraph **100**: This is test content for verifying rea..." snippet; search "Reading" produces 200+ results, each snippet shows lowercase "reading" runs in bold weight (case-insensitive match working at UI layer, separate bold runs per occurrence). Section label rendered in regular/secondary weight (not spuriously bolded). Status → VERIFIED. GH: #350 |
| 23 | Auto-generate TOC for TXT files                                                | Reader/*     | Medium   | VERIFIED  | **2026-05-13 round-4** (`dev-docs/verification/feature-23-20260513.md`, result=**pass**) on v3.21.4 build 281 (commit `9fa68c7`): closes acceptance criteria via XCUITest run of `Feature23TXTTocVerificationTests` against `.warAndPeace` seed. 2/2 tests pass on iPhone 17 Pro Sim (iOS 26.5) — TOC populates from `Chapter 1/2/3` markers (rule #3 enabled match), tap-to-navigate dismisses panel + keeps reader loaded. WI-2 seed-mismatch finding (verify cron 2026-05-13 06:24) closed by 1-line `.books → .warAndPeace` patch. **VERIFIED.** B01. Bug #83 FIXED (14/25 rules enabled). Sim verification 2026-05-05 result=**partial** (`dev-docs/verification/feature-23-20260505.md`): auto-TOC detection PASS — **1860 chapters** correctly extracted from 14 MB CJK web novel `《黑暗血时代》`, monotonic offsets, robust to multi-digit Chinese numerals (一→一千八百六十) and full-width / regular space variants, front-matter `前言` included. UI render in `TOCListView` and tap-to-navigate were not driven this iteration (MCP screenshot harness failed; DebugBridge URL scheme not registered in Info.plist — bug #121). Detection cache is durable on-device evidence; UI legs deferred until harness recovers. **2026-05-09 round-2 attempt** (`dev-docs/verification/feature-23-20260509.md`, `result: blocked`): DebugBridge URL-scheme harness IS recovered (per #121 fix), but the new blocker is fixture-content. Both available bundled TXT fixtures don't trigger TOC display in the UI: (a) `--seed-position-test` synthetic content is `Paragraph N` lines with no chapter markers (correct empty TOC); (b) `vreader-debug://seed?fixture=war-and-peace` (1708-byte stub with `CHAPTER I/II/III`) shows the `CHAPTER I` heading at the top of the reader but treats the whole file as 1 chapter (`1/1` pager), and the Contents tab still says `No Table of Contents` — likely because the English Roman-numeral chapter rule is among the 11/25 disabled Legado rules. UI render + tap-to-navigate verification BLOCKED on a chapter-marked TXT fixture matching an enabled rule (CJK `第N章` is the safest); 4 screenshot artifacts. GH: #238 |
| 24 | Book source scraping (web novels)                                              | BookSource/* | High     | VERIFIED  | **VERIFIED 2026-05-27 (Gate-5)** — `dev-docs/verification/feature-24-20260527.md` (result=pass). All stated (fixture-based) acceptance criteria confirmed: 211-test slice (2026-05-07) + core pipeline/import/engine/encoding suites re-run green on current main (HEAD `86e995b3`, 70 tests/4 suites) + UI path (Settings → Book Sources → Import → Search) device-verified per the 2026-05-07 slice. **Out of scope (not a stated criterion)**: live HTTP scraping against a real Legado source — the criteria deliberately use fixture pages; real web-novel sources are network-blocked in the cron context + piracy-adjacent, so unverifiable autonomously. GH #370 closed. D01-D07. Bugs #100, #101 FIXED (modelContext.save + SchemaV4). Device verified: Import JSON → sources visible → search works. 2026-05-07 slice verify: 211 tests across 20 suites (BookSource model + 4 rule types + RuleEngine + CSS/regex evaluators + Legado importer 5 sub-suites + pipeline + HTTPClient + encoding detector + chapter cache + update checker + source sharing). `dev-docs/verification/feature-24-20260507.md` (`result: partial` — live HTTP scraping deferred). GH: #370 |
| 25 | Configurable tap zones                                                         | Reader/*     | High     | VERIFIED  | **VERIFIED 2026-05-28** (`dev-docs/verification/feature-25-20260528.md`, result=pass) — tap-zone BEHAVIOR device-verified via CU on the paged PDF reader: right-zone tap → "Page 1 of 6"→"Page 2 of 6" (`nextPage`), left-zone tap → "Page 2"→"Page 1" (`previousPage`); `ReaderTapZoneRouter.dispatch` confirmed wired into every reader. Data-layer config + 26 unit tests pass. **Scope note**: the original "3-picker config UI in `ReaderSettingsPanel`" criterion was DELIBERATELY removed by feature #54 (Tap Zones settings section deleted; default `TapZoneConfig` left=prev/center=chrome/right=next retained) — that criterion is *superseded by a later design decision*, not failed, so it does not block VERIFIED. GH #342 closed. A03. TapZone section in ReaderSettingsPanel — 3 Pickers (left/center/right → TapAction). TapZoneStore wired through settings sheet. Sim verification 2026-05-07 result=**partial** (`feature-25-20260507.md`): 26-test data-layer slice PASS — `TapZoneConfig.zone(atX:totalWidth:)` exhaustive boundary math (X = ⅓ / ⅔ / 0 / negative / exceeding-width), action mapping, Codable round-trip, 4 actions × 3 zones combinations, `TapZoneModifier` default 100pt bottom-inset (bug #63 fix), config preservation. `TapZoneDispatcher` (4-case switch) verified by code-read. **2026-05-08 round-2 slice**: Reader Settings Panel presentation + Tap Zones section presence leg now exercised end-to-end via `vreaderUITests/Reader/ReaderSettingsPanelTests` (4 XCUITest methods, 38.3 s) — `readerSettingsButton` tap presents `readerSettingsPanel`, Theme section header renders (panel populated, not stub), Tap Zones section header reachable after Form scroll (SwiftUI Form lazily renders below-fold sections), Reading Mode picker exposes Native option (cross-ref: gates #27/#28/#21), accessibility audit clean. Per-picker (Left/Center/Right Zone rows) deferred — iOS 26 SwiftUI Form Picker label rendering inconsistent. `dev-docs/verification/feature-25-20260508.md` (`result: partial` — real tap → handler dispatch, Settings UI driving, native-mode safety still deferred). **2026-05-10 round-3 BLOCKED** (`feature-25-20260510-round3-blocked.md`) + **round-4 FAIL** (`feature-25-20260510-round4.md`) → filed bug #162 (FIXED + verified). **2026-05-10 round-5** (`feature-25-20260510-round5.md`, result=**partial**) on merged-main 41067e5 (v3.14.123): closes the **center-zone real-tap dispatch** slice end-to-end on iPhone 17 Sim with mini-epub3 in Unified mode. Reading Settings → 3 zone pickers exposed at AX: Left=Previous Page (463,771), Center=Toggle Toolbar (463,821), Right=Next Page (463,871) — defaults match `TapZoneConfig.default`. After dismissing Settings, real tap at mac (625, 600) — center-third of viewport — toggled chrome OFF; second tap at same point toggled chrome ON. Bidirectional confirms `TapZoneOverlay` → `TapZoneConfig.zone(atX:)` → `TapZoneDispatcher.handle(.toggleToolbar)` firing on the live unified render path. **DEFERRED**: Left/Right zone page-turn dispatch — taps fire correctly but mini-epub3 fits in single paged column (`scrollWidth == clientWidth` per feature #21 finding), so `EPUBPaginationHelper.navigateToPageJS` has nothing to advance to. Same multi-page-fixture blocker as feature #21. 4 screenshot artifacts (Tap Zones section visible + 3-pickers + chrome-toggled-off + chrome-toggled-on). No bugs filed. Status stays DONE; flip to VERIFIED needs a multi-page EPUB fixture. Original evidence at `dev-docs/verification/feature-25-20260507.md`. **2026-05-14 round-6** (`feature-25-20260514-round6.md`, result=**partial**): attempted to close the deferred Left/Right slice using `war-and-peace.txt` (4 chapters via bug #173 fixture fix) as a Unified-mode alternative to mini-epub3. **BLOCKED on a different invariant**: TXT was capability-gated OUT of `.unifiedReflow` by bug #158 fix, so the Reading Mode picker is hidden for TXT — user cannot reach Unified mode → `TapZoneOverlay` never installs → tap-right fires the bridge's content-tap (toggles chrome) instead of `.nextPage`. Confirmed by scrolling the full Settings sheet on the merged build (v3.21.32, commit 5519d2e) and observing no Reading Mode section. Three unblock paths (all feature-class, not verify-cron scope): (a) add a multi-page MD fixture to `DebugFixtureCatalog` — minimum-risk; (b) add a multi-page EPUB fixture; (c) wait for bug #158 proper Unified-TXT fix. 1 screenshot artifact. **2026-05-19 round-7** (`feature-25-20260519.md`, result=**partial — terminal**) on origin/main `dab2dc3` (v3.35.6 build 512): new CU-free XCUITest suite `vreaderUITests/Verification/Feature25TapZonesVerificationTests.swift` (4 tests, TXT/MD/EPUB, 4/4 green twice). The round-5/6 multi-page-fixture blocker is now moot — **feature #54 deliberately removed feature #25's user-facing capability**. #54 WI-3 (PR #883) deleted `ReaderUnifiedDispatch.swift`, the sole `tapZoneOverlay()` install site; #54 WI-4 (PR #886) removed the Tap Zones section + `TapZoneStore` wiring from `ReaderSettingsPanel`/`ReaderContainerView` (enforced by the `ReaderSettingsPanelTapZonesGateTests` regression suite). The suite confirms CU-free on iPhone 17 Pro/iOS 26.5: no Tap Zones config section is reachable in reader settings for any of TXT/MD/EPUB, and synthesized coordinate taps at the left-third and right-third of the EPUB reader produce the SAME chrome-toggle (taps are positionally uniform — no left/right page-turn zoning). The `TapZoneConfig`/`TapZoneStore`/`TapZoneModifier` types survive only as dead code (32 unit tests still green). **Blocker is now terminal — not fixture/tool-class**: a `VERIFIED` flip is impossible because the feature no longer ships. Row stays **DONE**. Recommended (maintainer triage decision, left to the user — out of verification scope): re-classify #25 — close GH #342 as superseded by feature #54, or open a fresh re-introduction feature (would need a `needs-design` issue per rule 51). No bug filed (the removal is intentional + tracked); no #25/#54 production code changed. GH: #342 |
| 26 | Text-to-Speech read aloud                                                      | Reader/*     | High     | VERIFIED  | **VERIFIED 2026-05-26 round-7** (`dev-docs/verification/feature-26-20260526.md`, result=pass, commit `827d37e9`, v3.39.34): the SOLE remaining gate — C6 (HTTP cloud TTS end-to-end) — is now satisfied by **Feature #72** (VERIFIED), which wired the orphaned `HTTPTTSProvider` into `TTSService` via `HTTPSpeechSynthesizer` and verified it end-to-end through the real stack (`Feature72CloudTTSIntegrationTests`, 3 tests green on main). C1–C5 + TXT/MD/EPUB UI cycles were device-verified in rounds 2–6; PDF-format TTS remains unexercised (no-PDF-fixture harness gap, not a #26 defect). All acceptance criteria met → DONE→VERIFIED. B03+E06. Bugs #96, #97 FIXED. Sim verification 2026-05-07 result=**partial** (`feature-26-20260507.md`): 101 tests PASS across 17 suites — TTSService state/speed/position/text-extraction(incl.CJK)/edge-cases (22 tests via MockSpeechSynthesizer); HTTPTTSProvider config/synthesis/chunking/cancellation/caching/position/Azure (43 tests via MockURLSession); FoliateTTSAdapter init/start/next/prev/setMark/parse/type (36 tests, JS-string generation). Three TTS providers integrate through one state machine. DEFERRED: audio playback + UI start/stop gestures + live HTTP TTS server + Foliate WebView render — all need audio session + UI driving. Status stays DONE pending UI-driven slice. **2026-05-09 round-2** (`dev-docs/verification/feature-26-20260509.md`, result=**pass**) closes the TXT UI gesture cycle on iPhone 17 Pro Sim against v3.14.111 (commit 239ac16). Full Pause/Resume/Stop cycle exercised end-to-end: tap speaker → TTS bar appears with ⏸/◾/1.2x + view auto-scrolls to CHAPTER I; tap Pause → bar shows ▶, auto-scroll halts mid-CHAPTER II; tap Resume → bar shows ⏸, auto-scroll continues through CHAPTER III; tap Stop → TTS bar removed, reader chrome restored at 56% / "2m read". State machine `.idle → .speaking → .paused → .speaking → .idle` traversed. Active audio playback inferred from observed auto-scroll progress (0% → 56%) which only fires on real `AVSpeechSynthesizerDelegate.willSpeakRange` callbacks. Cross-ref: feature #41 round-2's TTS-Start gesture evidence (same fixture). 4 screenshot artifacts. **DEFERRED**: live HTTP TTS server (third-party endpoint dep) + Foliate WebView TTS render (EPUB/AZW3 separate slice). **2026-05-13 round-3** (`feature-26-20260513-round3.md`, result=**partial**) attempted the Foliate slice on iPhone 17 Pro Sim (iOS 26.5) at v3.21.17 (commit `a0ec073`) against bundled `mini-azw3.azw3` (PG #1064). `vreader-debug://open` rendered the AZW3 Foliate webview correctly. **`vreader-debug://tts?action=start` had no effect** — snapshot post-start: `ttsState: "idle"`, `ttsOffsetUTF16: null`. Root cause code-read identified: `ReaderAICoordinator.loadBookTextContent(fileURL:format:)` has no `azw3`/`mobi` case, falls through to `default: return nil`. `startTTS()`'s guards skip the `ttsService.startSpeaking` call entirely. Capability is advertised (`FormatCapabilities.azw3` includes `.tts`) but wire-up missing — `FoliateTTSAdapter` exists with JS-side hooks but isn't evaluated by production. **Filed bug #176** (GH #602) per scope guardrail; status stays DONE; Foliate slice now blocked on bug #176 fix rather than missing test fixture. 2 screenshot artifacts (`feature-26-r3-01-azw3-open-20260513.png`, `feature-26-r3-02-azw3-tts-start-20260513.png`). Once #176 lands, re-run this recipe to confirm transition to "speaking". **2026-05-18 round-4 verify result=partial** (`dev-docs/verification/feature-26-20260518-round4.md`) on iPhone 17 Pro Sim (iOS 26.4.1) at v3.27.25 build 439 (== merged main `8cab12a`). Round-2's TXT UI cycle predates the feature-#60 chrome re-skin (TTS start moved from a dedicated speaker button into the `ReaderMorePopover` "Read aloud" row, bug #602) — this round regression-re-verifies TXT through the new control AND adds the never-covered EPUB slice. **TXT + EPUB UI-gesture cycles both PASS**: war-and-peace.txt + mini-epub3, More (⋯) → "Read aloud" → TTS bar (⏸/◾/speed) → full state machine `idle→speaking→paused→speaking→idle` traversed, confirmed via DebugBridge snapshots (`ttsState` + monotonic `ttsOffsetUTF16`: TXT 425→772→941, EPUB 198→394→599 — offsets advance only on real `AVSpeechSynthesizerDelegate.willSpeakRange` callbacks, so the synthesizer is genuinely speaking). AZW3/MOBI TTS is n/a (capability-removed by bug #176). **DEFERRED**: HTTP cloud TTS — needs a live third-party HTTP TTS server, none available. 4 screenshot artifacts. #26 stays DONE; VERIFIED gated only on the HTTP-cloud-TTS slice. No bug discovered, no code changed (verification scope). **2026-05-18 round-5** (`feature-26-20260518-round5.md`, result=**partial**, CU-free — DebugBridge `tts` command; CU MCP display unavailable, diagnosed as a Screen-Sharing-virtual-display issue): verifies the previously-uncovered **Markdown** TTS slice at the service level. Opened the `--seed-md-multi-page` MD book via `vreader-debug://open`; `vreader-debug://tts?action=start` → snapshot `ttsState: speaking`, `ttsOffsetUTF16: 0`; `ttsOffsetUTF16` then advances **monotonically 0→176→302** across the speaking phase (genuine `AVSpeechSynthesizerDelegate.willSpeakRange`-driven speech — round-4 criterion 4); `tts?action=stop` → `ttsState: idle`, `ttsOffsetUTF16: null`. MD TTS service start / genuine-speech / stop all **PASS**. The MD-specific More-popover "Read aloud" UI affordance was NOT device-verified (needs CU) — covered by round-4's shared-component finding (`ReaderMorePopover` + ⏸/◾/speed control bar identical across all readers). 1 artifact. Net TTS format coverage: TXT (round-2) + EPUB (round-4) + MD (round-5) device-verified; AZW3 `n/a` (capability-removed, bug #176); only **PDF** unexercised — blocked on the long-standing no-PDF-fixture harness gap. VERIFIED still gated only on the HTTP-cloud-TTS slice (needs an external HTTP TTS server). **2026-05-19 round-6** (`feature-26-20260519.md`, result=**partial**, CU-free XCUITest) closes the **pause/resume reachability gap** that kept #26 at DONE through rounds 1-5. New suite `vreaderUITests/Verification/Feature26TextToSpeechVerificationTests.swift` (3 tests, TXT/MD/EPUB) drives the app via the XCUITest accessibility API with synthesized taps — it CAN tap the `TTSControlBar` pause/resume button, which rounds 4-5's DebugBridge `tts` command (start/stop only) could not reach. All 3 tests **PASS** on iPhone 17 Pro Sim (iOS 26.4) at v3.35.1: tap ⋯ → "Read aloud" row → `TTSControlBar` mounts → tap play/pause (label flips "Pause"→"Resume", the `.speaking→.paused` transition) → tap again ("Resume"→"Pause", `.paused→.speaking`) → tap Stop (control bar torn down, `.idle`). The full `.idle→.speaking→.paused→.speaking→.idle` state machine is now automation-exercised through the production control bar, not inferred. Pure XCUITest — no DebugBridge snapshot (the `VerificationDebugBridgeHelper` `posix_spawn`-of-`simctl` does not work from the sandboxed XCUITest runner; the `TTSControlBar` play/pause `Button`'s accessibility label IS the state machine — "Pause" while `.speaking`, "Resume" while `.paused` — so it is read directly from the accessibility tree). Lesson reused from #54/#63 pilots: query by accessibility LABEL not identifier (`ReaderMorePopover` + `TTSControlBar` container `.accessibilityIdentifier`s clobber the per-element identifiers). One DEBUG-only test-harness change: `XCUITestMockSpeechSynthesizer` gained a suspendable synthetic timeline so `pauseSpeaking()` genuinely holds (its file header had flagged this as required for pause/resume test coverage) — feature #26 production code (`TTSService`/`TTSControlBar`/`startTTS()`) untouched. #26 **stays DONE** (not VERIFIED): acceptance criterion **HTTP cloud TTS end-to-end (C6)** still cannot be exercised — needs a live third-party HTTP TTS server, none available; covered at unit level by round-1's 43 `HTTPTTSProvider` tests but not end-to-end. VERIFIED now gated solely on a live-HTTP-TTS-server integration test (standing external-infrastructure follow-up). 3 screenshot artifacts + XCUITest log. No bug discovered, no feature-#26 product code changed. GH: #359 |
| 27 | Content replacement rules                                                      | Reader/*     | Low      | VERIFIED  | **2026-05-18 round-4 regression verify result=pass** (`dev-docs/verification/feature-27-20260518-round4.md`) on iPhone 17 Pro Sim (iOS 26.4.1) at v3.27.25 build 439 (== merged main `8cab12a`). Regression round triggered by GH #839 / Bug #217 — v3.27.25 removed the `DispatchQueue.global()` + 1s `DispatchSemaphore` timeout from `ReplacementTransform.applyRegexRule` and shipped via the verification-exception path (no device verify). Round-3 device-verified only a **string** rule; this round closes the **regex** gap. Converted the existing "Lorem ipsum" rule to regex mode (`regex` badge confirmed in the rules list), opened mini-epub3 in Unified mode after the required `.task`-reload restart: Chapter One renders "Second paragraph. **REPLACED_LOREM** dolor sit amet…" — the regex rule applies at render time through `applyRegexRule` (the #839-changed code path, now synchronous). Native mode still no-ops the pipeline verbatim (Bug #128, unchanged). **#839 did NOT regress #27** — stays VERIFIED. 2 screenshot artifacts. No bug discovered, no code changed (verification scope). **2026-05-09 round-3** (`dev-docs/verification/feature-27-20260509-round3.md`, result=**pass**): VERIFIED. Pivoted away from TXT (now correctly hidden from Unified per bug #158 fix) to mini-epub3.epub. Added "Lorem ipsum" → "REPLACED_LOREM" rule via Settings → Replacement Rules; banner copy confirmed live with post-#158 update ("currently EPUB, MD, AZW3 — not TXT or PDF"). Opened mini-epub3 → AA → Reading Mode picker IS shown for EPUB (capability-gated correctly post-#158). Switched to Unified, terminated + relaunched (rule pipeline only loads at `.task` time when readingMode == .unified, so mode-switch mid-session needs an app restart). Reopened mini-epub3 → reader renders Chapter One in Unified mode showing "Second paragraph. **REPLACED_LOREM** dolor sit amet, consectetur adipiscing elit. ..." instead of "Lorem ipsum dolor sit amet" — **rule applied at render time end-to-end**. Combined with round-1's data-layer slice (14 `ReplacementTransformTests`) + round-2's Native no-op confirmation, feature has full e2e coverage. Mode-switch UX caveat documented (not a bug — known design tied to `.task` load timing); could become a follow-up if user impact warrants. 1 screenshot artifact (`feature-27-r3-unified-epub-rule-applied-20260509.png`). E03. Bug #98 FIXED (sourceText + didSet re-apply). Sim verification 2026-05-06 (slice): `ReplacementTransform` unit tests pass (14 tests cover string/regex/CJK/disabled/invalid/offset-map/expansion/deletion). Production wiring confirmed by reading `ReaderContainerView.onAppear:195` + `loadReplacementRules` in `ReaderContainerView+Sheets.swift:97`. Discovered: native mode silently no-ops the transform pipeline → **bug #128** (GH #275) filed. Mitigation shipped this round: info banner in `ReplacementRulesView` ("Rules apply only when reading in Unified mode") so users see the limitation while configuring rules. Proper fix (wire native pipeline through transforms) tracked as follow-up. VERIFIED still gated on real-render verification in Unified mode (computer-use UI driving needed). 2026-05-07 evidence formalised at `dev-docs/verification/feature-27-20260507.md` (14 tests re-run + production wiring code-read confirmed; result still partial; native-mode no-op remains a known limitation per bug #128). bug #128 GH: #275 (since FIXED + CLOSED — banner shipped). **2026-05-09 round-2** (`dev-docs/verification/feature-27-20260509.md`, result=**blocked**): attempted real-render verification in Unified mode — added "Pierre" → "Peter" rule via Settings → Replacement Rules, opened war-and-peace.txt, switched to Unified mode, but Unified renderer truncates the fixture at end of CHAPTER II (Pierre on line 16 unreachable), missing bottom chrome, no TOC, and goes fully blank when toggled from Native at non-zero scroll position. Native mode behavior confirmed: at 47% scroll Pierre renders verbatim (rule does NOT apply per bug #128 — expected). Filed compound bug **#158 / GH #468** (Reader/* Medium, Unified TXT renderer + chrome + toggle issues). VERIFIED gated on bug #158 fix (either capability-gate Unified for TXT or wire UnifiedScrollView properly). 2 screenshot artifacts. bug #128 GH: #275. GH: #397 |
| 28 | Simplified/Traditional Chinese conversion                                      | Reader/*     | Medium   | VERIFIED  | E04. Bug #98 FIXED (sourceText + didSet re-apply). Sim verification 2026-05-05 result=**fail** (`dev-docs/verification/feature-28-20260505.md`): wiring was unified-mode-only. **WI-A (2026-05-12, PR #573, commit a9110a8, v3.21.0/build 277)**: TXT native-mode conversion wired — `attrStringKey` includes `chineseConversion`, `SimpTradTransform` applied before all three render paths. **2026-05-13 VERIFIED** (`dev-docs/verification/feature-28-20260513.md`, result=**pass**): XCUITest `ChineseConversionPickerGateTests` (2 tests, iPhone 17 Pro Simulator iOS 26.5) confirmed picker enabled for TXT native mode (enabled footer present, all 3 segments hittable). 13 unit gate tests + 4 attrStringKey rebuild tests all green. GH: #239 |
| 29 | WebDAV backup and restore                                                      | Settings/*   | Medium   | VERIFIED  | PR #128. Full data collector/restorer + BackupViewModel + WebDAVSettingsView backup section + Docker integration test. Manually verified end-to-end on iPhone 17 Pro simulator: credentials → Back Up Now → list → Restore → Delete all work against a real Bytemark/Apache WebDAV server. 2026-05-07 slice verify: 135 tests across 11 suites (ViewModel, collector, provider contract, WebDAVClient, WebDAVProvider [+manifest], BlobStore, ATS, ZIPWriter, BlobPath, manifest, projection). `dev-docs/verification/feature-29-20260507.md` (`result: partial` — gated live `WebDAVBackupIntegrationTests` deferred). **2026-05-11 round-2** (`feature-29-20260511-round2.md`, result=**pass**) closes the round-1 deferred slice on merged-main 187314e (v3.14.148): live `WebDAVBackupIntegrationSuite` ran end-to-end against a real WebDAV server (rclone serve webdav 127.0.0.1:8082, vreader:test123, fresh /tmp/vreader-webdav-verify-r2 data dir). Both gated tests pass — `webDAVClientUploadSucceeds()` 28ms (real `WebDAVClient.upload` over `URLSessionConfiguration.ephemeral`); `backupListRestoreDeleteRoundTrip()` 100ms (full BackupDataCollector → ZIPWriter → PUT → list → GET → BackupDataRestorer → DELETE through real `WebDAVProvider`). Pre-existing 135 unit/protocol tests still green. No new bugs filed. Status DONE → VERIFIED. GH: #367 |
| 30 | Custom book covers                                                             | Library/*    | Medium   | VERIFIED  | A01. CustomCoverStore + PhotosPicker in context menu. Sim verification 2026-05-07 result=**partial** (`feature-30-20260507.md`): 16 `CustomCoverStoreTests` PASS — unique-path-per-book, colon/slash sanitization, setCover/getCover/removeCover/hasCover round-trip, large-image resize + small-image no-upscale, empty-key safety, CustomCovers subdirectory layout. Plus live end-to-end via feature #43 round-3 (PR #341): AZW3 auto-save produced 341×512 baseline JPEG on disk. `PersistenceActor.deleteBook` cascade-removes cover (code-read). Round 2 (`feature-30-20260508.md`, v3.14.84) result=**pass**: CU drove the deferred UI gesture end-to-end on iPhone 17 Pro Sim — long-press card → context menu → Set Cover → `PhotosPicker` ("Private Access to Photos" scoped-access flow confirmed) → pick photo → grid re-renders with magenta flowers cover. JPEG (512×384, 117 KB, baseline JFIF+Exif) lands on disk at `CustomCovers/epub_<sha>_102400.jpg` with colons sanitized; cover persists across `terminate` + relaunch. Status → VERIFIED. GH: #347 |
| 31 | Auto page turning                                                              | Reader/*     | Low      | VERIFIED  | B10. Unblocked by bug #82 fix. 2026-05-07 slice verify: 14 tests across 2 suites (AutoPageTurner timer state machine + interval reschedule + cancellation, PageTurnAnimator slide/cover-flip/instant transitions). `dev-docs/verification/feature-31-20260507.md` (`result: partial` — UI gesture for real-time advancement deferred). **2026-05-08 round-2 slice**: Reader Settings UI surface verified end-to-end — Paged segmented control reveals Auto Page Turn toggle + Page Turn Animation picker; sustained-press toggle reveals/hides Interval slider; settings persist across panel re-open. Live multi-page advancement still deferred — Position Test Book paginates to 1 page at 393×852 viewport with 18pt font, so `AutoPageTurner.scheduleTimer`'s at-last-page short-circuit correctly stops the timer without observable advancement. NOT a bug — fixture limit. `dev-docs/verification/feature-31-20260508.md` (`result: partial`). **2026-05-14 round-3** (`feature-31-20260514-round3.md`, result=**partial**) on merged-main 75b6a27 (v3.21.21). Reattempted the deferred live-advancement slice with the just-merged WI-4c-c snapshot wiring + bug #175 fix in place. Found the same fixture-size blocker as round-2: mdTOC fixture (678 bytes) renders to 1 page at 18pt in MD scroll mode; bumping fontSize via `vreader-debug://theme?mode=light&fontSize=48` updates `UserDefaults` + `DebugSnapshot.fontSize` but doesn't visually re-render to multi-page. The `--reader-default-layout=paged` launch arg is EPUB-only (sets `readerEPUBLayout`); MD reads `readerReadingMode` which has no equivalent DebugBridge override today. Pre-launch UserDefaults injection (`xcrun simctl spawn defaults write com.vreader.app readerAutoPageTurn -bool true` + `readerAutoPageTurnInterval -float 3.0` BEFORE the `--uitesting --seed-md-toc` launch, without `--reset-preferences`) successfully persists the auto-page-turn enable through the seed flow — the recipe is documented for future iterations. 5 screenshot artifacts. **Three unblock paths** identified in the evidence file: (a) larger MD seed fixture (`--seed-md-multi-page` 5KB+), (b) new DebugBridge URL for MD `readerReadingMode=paged`, (c) further capability-gating per bug #157 option-b. None are verify-cron scope; (a) is the minimum-risk path. **2026-05-15 (Feature #45 WI-5)**: unblock path (a) shipped — `--seed-md-multi-page` now seeds a ~9.5 KB MD doc that paginates to ≥2 pages at 18pt on iPhone 17 Pro Sim (unit-tested via the full `MDAttributedStringRenderer.render` → `NativeTextPaginator.paginateAttributed` pipeline at `UIScreen.main.bounds.size`). Live-advancement XCUITest can now drive a multi-page MD reader without the at-last-page short-circuit firing. Status stays DONE pending the live-advancement XCUITest method that uses the new fixture. **2026-05-17 round-6** (`feature-31-20260517-round6.md`, result=**partial**): ran the round-5-suggested disambiguation recipe on v3.27.23 build 437 (== main `6936ccf`). Criteria 1-4 re-confirmed PASS; criterion 5 (live advancement) is now **FAIL** rather than deferred — the MD reader never enters paged mode. `readerEPUBLayout=paged` is set (so `MDReaderContainerView.isPagedMode` is true) but `uiState.pageNavigator` is nil, so the body falls through to scroll-content rendering ("0%" scrubber, no "Page X of Y", body clipped mid-line); passive auto-turn + manual right-tap + scroll/swipe are all inert while reader chrome stays responsive. Filed **Bug #215 / GH #837** — the MD instance Bug #157 predicted ("MD reader likely has the same shape") but left untested. VERIFIED flip blocked on Bug #215. **2026-05-18 round-7** (`feature-31-20260518-round7.md`, result=**partial**, CU-free — DebugBridge + `simctl io screenshot`, CU MCP display unavailable this iteration): settles the format-scope question round-6 left open. `FormatCapabilities.swift` is authoritative — `.autoPageTurn` (`rawValue 1<<9`) is unioned into the **`.md` branch only**; the `reflowableBase` doc comment states it verbatim — *".autoPageTurn is intentionally excluded — only MD has end-to-end AutoPageTurner wiring (bug #157 / GH #461)"*. TXT lost the capability via bug #157's gate (`TXTReaderContainerView.updatePaginationIfNeeded()` is defined-but-never-called, no TXT paged renderer); EPUB/PDF/AZW3 never had it; `ReaderSettingsPanel.swift:103` hides the Auto Page Turn toggle for any format without the capability. EPUB slice runtime-probed CU-free: `readerAutoPageTurn` forced ON via `defaults write` (bypassing the hidden toggle) + `--reader-default-layout=paged`, opened mini-epub3 at `fontSize=48` (paginated to ≥2 pages — chapter-1 page 1 clips mid-text, "Chapter 1 of 2"); across 90 `simctl io` frames / ~15 s steady state / ≈5× the 3 s interval the rendered page was **byte-identical (305460 B) — zero advancement**. The EPUB reader has **zero `AutoPageTurner` wiring** (grep-confirmed across `vreader/Views`+`vreader/Services`) — the capability gate is structural, not a cosmetic toggle-hide; intended by-design behaviour, NOT a defect (no new bug filed, same disposition shape as feature #26 round-4's AZW3-TTS-capability-gated-`n/a`). **Net: feature #31 is MD-only by design — there is NO non-MD slice that can contribute partial verification credit, so the `VERIFIED` flip is 100% gated on Bug #215 / GH #837** (MD paged mode, itself `BLOCKED: needs-design #842`). Round-7 sharpens round-6: not "MD is one of N formats blocked" but "MD is the entire feature." **2026-05-20 round-8** (`feature-31-20260520-round8.md`, result=**partial**, FIRST CU-restored round since 2026-05-09) on main `55dc855` (v3.38.9). CU MCP display capture functional again (UGREEN HDMI dummy attached); the round-7 verdict is re-confirmed and **gesture-strengthened**: round-7 inferred page-turn pathway dead from `simctl io` byte-identical screenshot loops; round-8 directly drove CU right-tap at (300, 450) and CU swipe-drag (300,450)→(80,450) on the MD paged reader and observed **both inert** — page indicator stays at "Page 1 of 8" across 18 s passive + active gestures. MD paged mode does engage (the "Page 1 of N" indicator is present, confirming round-7's corrected understanding that `pageNavigator` does build — round-6's surface diagnosis was retracted). Bug #215 Cause 2 (ReaderBottomChrome occlusion + missing TapZoneOverlay) is the active blocker, confirmed visually + via gesture inertness. **GH #842 (the design issue for Bug #215) closed 2026-05-18** — design landed at `dev-docs/designs/vreader-fidelity-v1/project/design-notes/reader-navigation.md` §3; Bug #215's row notes "this bug is unblocked for implementation." But the implementation has not shipped in the 48 h since (verified — no MDReaderContainerView commits since 2b04217 highlight-driver). The bugfix-cron's 2026-05-19 skip comment ("remains BLOCKED on needs-design #842") is now stale. **Bug #239** (sibling: TapZoneOverlay deletion across TXT/EPUB/AZW3/MD/PDF via feature #54 WI-3) is the wider regression; Bug #215's design covers the MD-paged-mode tap-zone affordance for that fix. No new bug filed (the failure is fully accounted for by existing Bugs #215 + #239). `VERIFIED` flip remains 100% gated on Bug #215. **2026-05-21 round-9** (`feature-31-20260521-round9.md`, result=**partial**, CU-free) on main `aedcee23` (v3.39.1): round-8's blockers Bug #215 (MD paged layout, FIXED PR #1100) + Bug #239 (side-tap producer, FIXED PR #1098) confirmed fixed — MD paged mode now engages cleanly (seeded book paginates to 6 pages, "Page 1 of 6", clean content above chrome). Criteria 1-4 PASS; criterion 5 (live advancement) is **FAIL** with a NEW root cause distinct from #215/#239: `AutoPageTurner.scheduleTimer` advances the navigator's internal page on its timer but never syncs the observable `uiState.pagedCurrentPage` that drives the `NativeTextPagedView` render param + `snapshot.position`, so the page never turns (`position` stuck at `0` across 20s + a 16s-untouched re-run). Filed **Bug #258 / GH #1125** (verification-only — not fixed in-round). VERIFIED flip narrowed to a single gate: Bug #258's code-only fix. **2026-05-21 round-10** (`feature-31-20260521-round10.md`, result=**pass** → **VERIFIED**, CU-free) on merged main `cd1549ef` (v3.39.4 build 625, incl. Bug #258 fix PR #1127 `4bd39177` / `bf19575f`). Bug #258 FIXED → re-ran the round-9 recipe: clean build (Bug #259 workaround — fresh DerivedData so the inject build-phase keeps the `vreader-debug` scheme), install, pre-launch `readerEPUBLayout=paged`+`readerAutoPageTurn=true`+interval=2.0, open the `--seed-md-multi-page` book, snapshot position UNTOUCHED over time. **Criterion 5 now PASS**: position climbs monotonically `0 → 3523 → 4758 → 5970 → 7258 → 8440` (1.5s fine cadence from page 1) then plateaus at the document-end `8440` (auto-stops at last page — `renderPhase: idle`, `lastError: null`, no wrap/crash); final screenshot "Page 8 of 8". Pre-fix (round-9) was `0/0/0`. Criteria 1-4 re-confirmed on merged main (capability gate `FormatCapabilities.swift:89` MD-only intact; persistence survived open + 2 relaunches; full `AutoPageTurnerTests` 27/27 green incl. the 3 Bug #258 bridge tests `timerAdvance_invokesOnAdvanceCallback`/`timerAtLastPage_doesNotInvokeOnAdvance`/`stop_preventsFurtherOnAdvance`). **All 5 acceptance criteria PASS → flipped DONE → VERIFIED.** No code changed in-round; no new bug. The deferred live-advancement thread open since round-1 (2026-05-07) is closed. GH: #381                                                                                                                                       |
| 32 | Reading theme backgrounds                                                      | Reader/*     | Medium   | VERIFIED  | A04. PhotosPicker + opacity slider + remove button in ReaderSettingsPanel. ThemeBackgroundStore saves/loads per-theme. Sim verification 2026-05-07 result=**partial** (`feature-32-20260507.md`): 11 `ThemeBackgroundTests` PASS — save/load/remove round-trip, large-image + high-scale-image resize for memory cap, no-upscale for small images, overwrite-existing atomicity, unique path per theme, `.jpg` extension, no-throw on remove-when-missing. Production wiring at `ReaderSettingsPanel.swift:93/184` (PhotosPicker save + Remove call sites) confirmed by code-read. Round 2 (`feature-32-20260508.md`, v3.14.88) result=**pass**: CU drove the deferred UI flow end-to-end on iPhone 17 Pro Sim — Light/Sepia/Dark theme switch updates reader background immediately; Custom Background toggle (sustained press) reveals Opacity slider + Choose Image + Remove Background; tapping Choose Image opens `PhotosPicker` with "Private Access to Photos" scoped-access banner; pick photo → picker dismisses + panel state retained; reader renders the picked magenta-flowers image as a blended background at 15% opacity over the active theme. Status → VERIFIED. GH: #354 |
| 33 | Dictionary / define / translate-on-select                                      | Reader/*     | High     | VERIFIED  | B02. DictionaryLookup + UIReferenceLibraryViewController + AI translate. Sim verification 2026-05-07 result=**partial** (`feature-33-20260507.md`): 19 `DictionaryLookupTests` PASS — 13 selection-parsing tests (English / CJK / mixed / emoji / punctuation / whitespace / newlines / tabs / single-space, multi-word→first-word contract) + 3 Gate-5-class real-system tests hitting `UIReferenceLibraryViewController.dictionaryHasDefinition(forTerm:)` and `init(forTerm:)` directly (no mocks; real Apple system dictionary API) + 2 menu-title localization tests + 1 modal viewController construction. AI Translate path covered by feature #18 separately. Round 2 (`feature-33-20260508.md`, v3.14.86) result=**pass**: CU drove the deferred Define gesture flow end-to-end on iPhone 17 Pro Sim — double-tap word ("reader") → custom 4-item menu (Highlight / Add Note / Define / ▶) → tap Define → `UIReferenceLibraryViewController` modal slides up with term "reader" at top + "No Content Found" body (sim has no dictionaries downloaded, independent of vreader code) + "Search Web" / "Manage Dictionaries" buttons. Plumbing verified end-to-end. Status → VERIFIED. GH: #357 |
| 34 | Collections / tags / series organization                                       | Library/*    | Medium   | VERIFIED  | **2026-05-12 round-6** (`dev-docs/verification/feature-34-20260512.md`, result=**pass**): closed via **verification-exception** per AGENTS.md "Close gate". 17 of 19 acceptance criteria pass with concrete evidence accumulated across rounds 1-5: 30 unit tests at real `PersistenceActor` + `SchemaV6` boundaries (19 CollectionPersistenceTests + 11 model tests, Gate-5-class fidelity), 6 XCUITest methods (CollectionSidebarTests, 49.5s, full sidebar UI presentation + accessibility audit), 4 CU-driven device-verify rounds (create / swipe-delete / add-to-collection / tap-to-filter post-bug-#155 fix at v3.14.110). Remaining 2/19 criteria (rename inline editor + populated Tags/Series sections) are explicit fixture/harness gaps — NOT implementation gaps — tracked for follow-up via feature #45 verification-harness sweep or fixture-catalog extensions. No relevant code changed on feature #34's surface since round-5 (v3.14.110 → current v3.17.2). Host CoreSimulator framework out-of-date (1051.50.0 vs 1051.54.0), so a fresh round-6 sim run isn't physically possible this iteration; verification-exception path consolidates rounds 1-5 + cites all 5 evidence files. C01. Bugs #85, #86 FIXED. Sim verification 2026-05-07 result=**partial** (`feature-34-20260507.md`): Gate-5-class slice via real `PersistenceActor` + real `SchemaV6` (in-memory `ModelContainer`) — 19 `CollectionPersistenceTests` cover create/rename/delete/add/remove/multi-collection-membership/idempotency/cascade-on-deleteBook/Unicode-CJK/case-insensitive-duplicate-rejection; 11 model tests (`BookCollection Model` + `Book Series Fields`) cover init validation + defaults. **2026-05-08 round-2 slice**: Collections sidebar UI presentation leg now exercised end-to-end via `vreaderUITests/Library/CollectionSidebarTests` (6 XCUITest methods, 49.5 s) — sidebar opens from `collectionsToolbarButton` under `Filter` nav title, exposes `filterAllBooks` + `newCollectionButton`, inline TextField + Add button surface on tap with Add correctly disabled on empty input (whitespace-only-name gate), `filterDoneButton` dismisses cleanly, accessibility audit clean. **2026-05-08 round-3 slice**: create → swipe-delete UI round-trip closed via computer-use display capture (`feature-34-20260508b.md`, result=**pass**). CU drove the gesture flow on iPhone 17 Pro Sim: tap "+ New Collection" → text field reveals → type "My list" via per-character `key` actions (CU `type` action doesn't reach iOS Simulator text fields — observation noted, not filed as bug; external CU/Sim infra quirk) → Add button enables blue → tap Add → "My list" row appears with folder icon + count "0" + form returns to entry-point; swipe-left → red Delete affordance revealed; tap Delete → row removed, returns to empty state. 3 PNG artifacts saved (`feature-34-r3-{collection-created,swipe-delete,after-delete}-20260508.png`). **14/18 acceptance criteria pass** across rounds 1-3; 4 deferred (per-book context-menu Add-to-Collection, tap-to-filter, rename inline editor, populated Tags/Series sections — need pre-seeded data or harness extensions). **2026-05-09 round-4** (`dev-docs/verification/feature-34-20260509.md`, `result: partial`): closes 2 of 4 deferred legs, surfaces 1 bug, leaves 2 deferred. PASS — Add-to-Collection from book context menu (long-press → `Add to Collection ›` → submenu lists existing `Reading List` → tap adds book and increments `bookCount` from 0 to 1). FAIL — tap-to-filter from sidebar (tap `Reading List` in sidebar dismisses sheet but library still shows all 8 seeded books, not just the 1 member); root cause is view-layer wiring gap (`LibraryView.gridView/listView` iterate `viewModel.books` directly without consulting `activeFilter`); filed as **bug #155 / GH #451**. DEFERRED — rename inline editor + populated Tags/Series. **16/18 criteria pass post-round-4**; status stays DONE pending bug #155 fix + rename-editor exercise. 4 screenshot artifacts. **2026-05-09 round-5** (`dev-docs/verification/feature-34-20260509b.md`, result=**pass**): closes the deferred tap-to-filter leg now that bug #155 is FIXED + CLOSED in PR #454 (commit 37a17d1, on main since v3.14.95). End-to-end CU drive against v3.14.110 (commit 45ceed1) on iPhone 17 Pro Sim: created "Reading List" collection → added "The Masque of the Red Death" via long-press → Add to Collection submenu → re-opened sidebar (count badge → 1) → tapped Reading List row → **library grid filters from 3 books to 1**, the 2 non-member books correctly hidden. Bidirectional verified — tap All Books restores full 3-book grid. **17/18 criteria pass post-round-5**; only rename inline editor + populated Tags/Series remain deferred (harness/fixture-class gaps, same as round-4). 3 round-5 screenshot artifacts. Original evidence at `dev-docs/verification/feature-34-20260507.md` + `feature-34-20260508.md` + `feature-34-20260508b.md` + `feature-34-20260509.md` + `feature-34-20260509b.md`. GH: #336 |
| 35 | Export / import annotations                                                    | Reader/*     | Medium   | VERIFIED  | **2026-05-13 round-2** (`dev-docs/verification/feature-35-20260513.md`, result=**pass**) on v3.21.6 build 283 (commit `cdb007b`): WI-3's `Feature35AnnotationsExportVerificationTests` (2/2 verify_ methods) confirms `annotationsExportButton` + `annotationsImportButton` exist + are hittable in the `annotationsPanelSheet` toolbar after tapping `readerAnnotationsButton`. Combined with round-1's 47-test data-layer slice (Markdown/JSON export + VReaderAnnotationParser + AnnotationImporter round-trip), full acceptance criteria covered. System-modal share-sheet/document-picker observability is out of XCUITest scope by design. **VERIFIED.** C02+C03. Bug #88 FIXED (import highlight refresh). Sim verification 2026-05-07 result=**partial** (`feature-35-20260507.md`): JSON export PASS (10 tests), Markdown export PASS (8 tests + CJK + markdown-syntax-in-notes), exporter aggregator PASS (3 tests), JSON parse PASS (13 `VReaderAnnotationParser` tests inc. ISO8601/CJK/Unicode/malformed/forward-compat), Codable round-trip via `AnnotationImporter` PASS (13 tests; encoder settings byte-identical to production `JSONExportFormatter`). DEFERRED: real `PersistenceActor`-backed integration test (mock-store seam is mechanical), UI flow (panel → Export → share → save → file picker → import → panel refresh), bug #88 UI-level verification. Status stays DONE pending UI-driven slice. GH: #334 |
| 36 | OPDS catalog support                                                           | BookSource/* | Medium   | VERIFIED  | C04. Sim verification 2026-05-07 result=**partial** (`feature-36-20260507.md`): parser PASS (24 unit tests cover Atom/OPDS 1.x schema, dedup, navigation vs acquisition, cover-URL, Unicode/CJK); real-backend wire-format PASS via 3 live curl probes against Project Gutenberg (`/ebooks.opds/` root + `/search.opds/?sort_order=downloads` 25-entry listing + `/ebooks/84.opds` per-book detail with EPUB/EPUB3/MOBI acquisition links). **2026-05-08 round-2 slice**: catalog list sheet presentation leg now exercised end-to-end via `vreaderUITests/Library/OPDSCatalogListTests` (4 XCUITest methods, 31.9 s) — sheet opens from `opdsCatalogsToolbarButton` under `OPDS Catalogs` nav title, surfaces toolbar `opdsAddCatalog` (+) entry point, `opdsCatalogsDoneButton` dismisses cleanly, accessibility audit clean. Assertions are state-invariant (sheet shape, not catalog list contents) due to bug #152 (`--uitesting` doesn't reset UserDefaults — `opds.savedCatalogs` persists across launches). **2026-05-08 round-3 slice**: visual leg now closed via computer-use display capture (`feature-36-20260508b.md`, result=**pass**). CU drove the gesture flow on iPhone 17 Pro Sim: globe tap → OPDS sheet → empty-state shape (heading + description + Add Catalog button) all render correctly; toolbar `+` tap → Add Catalog modal with Catalog Info (Name + URL) + Authentication (Username + Password) sections, Save button visually disabled on empty input (whitespace-name gate confirmed); Cancel returns to list, Done returns to Library. Two screenshot artifacts saved: `feature-36-r3-empty-state-20260508.png`, `feature-36-r3-add-catalog-form-20260508.png`. **9/12 acceptance criteria pass; 3 deferred (per-catalog navigation, download → import round-trip, edit/delete)** — all need a real OPDS endpoint plus saved catalogs. Original evidence at `dev-docs/verification/feature-36-20260507.md` + `feature-36-20260508.md`. **2026-05-11 round-4** (`feature-36-20260511-round4.md`, result=**partial**) on merged-main de37ad4 (v3.14.149): attempted the 3 deferred legs against Project Gutenberg OPDS (`https://www.gutenberg.org/ebooks.opds/`, host curl probe returns 200 + valid Atom XML). Saved catalog persistence closes (Gutenberg row survives sheet dismiss/reopen). **Per-catalog navigation FAILS — bug #170 filed (GH #529)**: `OPDSBrowserView` destination renders blank with only back chevron (no spinner, no entries, no error) across t+0.5s / t+2.5s / t+10.5s / t+17s. Suspect `.task` lifecycle quirk in nested sheet→NavigationStack→NavigationLink presentation. **Download/import blocked-by-#170** (can't reach book entries when detail is blank). Edit/delete leg deferred (CGEventPost drag at 200ms is interpreted as tap; slower drag primitive needed). **10/12 criteria pass post-round-4**; status stays DONE pending bug #170 fix. 18 screenshot artifacts. **2026-05-11 round-5** (`feature-36-20260511-round5.md`, result=**pass**) on merged-main 263100a (v3.14.151, post bug #170 fix in PR #531): all 3 round-4 deferred legs now close end-to-end against Project Gutenberg OPDS. **Per-catalog navigation PASS** — saved Gutenberg row → spinner → "Project Gutenberg" feed with Popular/Latest/Random nav entries. **Sub-feed drill-down PASS** — Popular → "All Books" with 11+ real PG books (Hegel, Frankenstein, Moby Dick…); initial timeout triggered `errorState` correctly with Retry button that re-invoked `loadFeed`. **Acquisition entry → detail PASS** — Alice's Adventures in Wonderland → 2-variant sub-feed → `OPDSEntryView` with cover/author/rights/Download EPUB+MOBI buttons. **Download → import round-trip PASS** — tapped Download EPUB → "Downloaded! Book added to library." → backed out all the way to Library and confirmed Alice EPUB book row visible with cover thumbnail + author + EPUB tag (`feature-36-r5-15-library-after-dismiss-20260511.png`). **Swipe-to-delete PASS** — slower 30-step CGEventPost swipe (~750ms total) successfully deletes saved Gutenberg row; catalog list reverts to "No OPDS Catalogs" empty state. Round-4's 200ms drag was too fast (tap-classification); the slower swipe primitive is documented in round-5 evidence as a reusable pattern for `sim-drive-fallback`. **12/12 acceptance criteria PASS**. Status → VERIFIED. 15 round-5 screenshot artifacts. GH: #332 |
| 37 | Per-book reading settings                                                      | Reader/*     | Low      | VERIFIED  | A05. Bug #84 FIXED (applyResolvedSettings + suppressPersistence). Bugs #145 + #147 FIXED (bridge respects per-book; disable re-resolves from globals). Sim verification 2026-05-06 result=**pass for open path** (`feature-37-20260506.md`): two scenarios via direct per-book JSON write + eval probes. (A) Full override `{"themeName":"sepia","fontSize":26,"lineSpacing":1.8}` → body bg `rgb(244, 237, 221)`, color `rgb(58, 43, 22)`, fontSize `26px`, p line-height `46.8px` (= 26×1.8). (B) Partial override `{"fontSize":30}` → body fontSize `30px`, body bg `rgb(28, 28, 30)` (global dark inherited per nil-means-inherit contract). **2026-05-09 round-2 device verify** (`dev-docs/verification/feature-37-20260509.md`, `result: pass`): closes the save-via-UI gesture leg deferred from round-1. On iPhone 17 Pro Sim (iOS 26.4) at v3.14.101 with `--seed-position-test`: open Position Test Book → AA settings panel → scroll to per-book section → drag "Custom settings for this book" toggle ON → toggle flips green AND subtitle transitions to "Font, spacing, and theme changes apply only to this book." Drag toggle OFF → subtitle reverts to "All books share the same settings." Both transitions pin the gesture path that round-1 left unexercised. (CU+iOS Sim Toggle quirk: a 13-px `left_click_drag` is needed to flip SwiftUI Toggle reliably; documented as a verification-tooling quirk, not a vreader defect — same observation noted in feature #34's round-3 evidence.) 3 screenshot artifacts. Combined with round-1's open-path eval probes, the feature is end-to-end verified through the UI surface users actually drive. GH: #406                                                                                                                                                                                                                                                                                                                                                                                                                |
| 38 | Hierarchical/tree TOC display                                                  | Reader/*     | Low      | VERIFIED  | TOCListView indents by entry.level. PDF/MD builders populate nonzero levels. Not a disclosure tree but visual nesting works. Sim verification 2026-05-07 result=**partial** (`feature-38-20260507.md`): 69 tests pass across 4 TOC suites — `TOCBuilder.forMD` (h1→0..h6→5 parametric walk), `TOCBuilder.forTXT` (Legado regex-rule depth), `TOCChapterProgress` (reading position → current chapter), `FoliateTOCConverter` (Foliate-js TOC → flat `TOCEntry` array). View consumption verified by code-read at `TOCListView.swift:125-130` (level → leading padding + font/weight differentiation). DEFERRED: UI render of the indented hierarchy + tap-entry → navigate. **2026-05-09 round-2** (`feature-38-20260509.md`, result=**partial**): all 3 bundled DebugFixtures (mini-epub3, war-and-peace.txt, mini-azw3) yield flat or empty TOC — visual-hierarchy leg structurally blocked on fixture coverage, not implementation. **2026-05-10 round-3** (`feature-38-20260510-round3.md`, result=**partial**) on merged-main 41067e5 (v3.14.123): closes the **tap-to-navigate** slice end-to-end on iPhone 17 Sim with mini-epub3 — tapped Chapter Two row at Accessibility-derived mac coords (1427, 755); reader navigated Chapter 1 of 2 → Chapter 2 of 2, sheet auto-dismissed, bottom chrome flipped "Chapter One/9m read" → "Chapter Two/20m read", progress bar advanced 0% → ~50%. 2 screenshot artifacts. **Calibration technique** (reusable for future verify-cron iterations): query `tell process "Simulator" → entire contents of window 1 → position` for elements where `description contains "Chapter"`, use those mac-space coords directly with CGEventPost — bypasses sheet-presentation y-offset uncertainty. **2026-05-13 round-4** (`feature-38-20260513.md`, result=**pass**) on merged-main b51fcf2 (v3.21.11): closes the **visual-hierarchy** slice end-to-end on iPhone 17 Pro Sim (iOS 26.5) with the `--seed-md-toc` MD fixture which exposes 3 distinct heading levels. TOC panel renders 5 entries with 3 visible indentation tiers: "Introduction" (level 0 — H1) at left margin in body/medium/primary (accent blue + semibold as active row); "Chapter 1/2/3" (level 1 — H2) indented ~20pt in subheadline/regular/secondary; "Section 2.1: A Subsection" (level 2 — H3) indented ~40pt nested under Chapter 2. Matches `TOCRowView.indentPerLevel = 20pt × entry.level` and the body↔subheadline + primary↔secondary differentiation at `TOCRowView.swift:125-130`. Screenshot artifact: `dev-docs/verification/artifacts/feature-38-round4-md-toc-hierarchy-20260513.png`. GH: #352 |
| 39 | ~~Merged into feature #32~~                                                    | Reader/*     | —        | DUPLICATE | Same gap: background image picker UI needed. Merged into #32                                                                                                                                                                                                                                                                                                                                                                                                                            |
| 40 | TTS sentence highlighting                                                      | Reader/*     | Medium   | VERIFIED  | TTSHighlightCoordinator: NLTokenizer sentences → binary search → uiState.highlightRange. TXT/MD wired via onChange(ttsService). **2026-05-13 round-4** (`feature-40-20260513.md`, result=**pass**) on merged-main bcf4205 (v3.21.16). Used Position Test fixture (`--seed-position-test`, 100 paragraphs, no chapter markers → exercises the full-text branch at `TXTReaderContainerView.swift:472`) combined with WI-4c-c's just-shipped `vreader-debug://snapshot` ttsState/ttsOffsetUTF16 wiring (PR #599, v3.21.15). Snapshot at t+2s: `ttsState: "speaking"`, `ttsOffsetUTF16: 26`. Screenshot at t+2s shows **yellow sentence highlight band painted on "reading position persistence."** — exact sentence boundary at offset 26 per NLTokenizer English tokenization. Offset advances 26→74 over 3s (1.2× speed). Stop → `ttsState: "idle"`, no band, TTS control bar removed, chrome restored. 4 screenshot artifacts. **VERIFIED.** Chapter-mode + chunked-mode deferrals stay tracked under feature #48 WI-7 and chunked-renderer scope (separate gates, not blocking #40's row contract). Prior rounds: **2026-05-07** (`feature-40-20260507.md`, partial — unit tests only); **2026-05-09 round-2** (`feature-40-20260509.md`, partial — TTS audibly starts + auto-scroll fires, but no sentence highlight visible, root cause inconclusive); **2026-05-10 round-3** (`feature-40-20260510-round3.md`, partial — root cause identified: `TXTReaderContainerView.swift:496` hardcodes `highlightRange: nil` in chapter-mode branch, same WI-7 deferral as bug #160; non-chapter full-text branch at line 472 confirmed to pass `uiState.highlightRange` correctly via code-read, awaiting fixture). GH: #344 |
| 41 | TTS auto-scroll/paginate                                                       | Reader/*     | Medium   | VERIFIED  | TTSHighlightCoordinator sets uiState.scrollToOffset from sentence start. TXT/MD only via same onChange wiring. Sim verification 2026-05-07 result=**partial** (`feature-41-20260507.md`); round-2 verification 2026-05-09 result=**pass** (`feature-41-20260509.md`): drove TTS end-to-end against war-and-peace.txt on iPhone 17 Pro Sim — view auto-scrolled from 0% (CHAPTER I top, "Well, Prince, so Genoa and Lucca...") to 84% ("End of synthetic fixture excerpt.") over ~4s of 1.2× playback, with sentence-start anchor preventing word-level jitter. Auto-scroll target is sentence START (`scrollToOffset = range.location` at `TTSHighlightCoordinator.swift:66`), NOT word-level. TXT/MD wiring code-read confirmed; MD slice covered by cross-ref (identical seam, no MD fixture available). EPUB/AZW3/PDF intentionally out of scope (different scroll surfaces). Chunked-rendering behavior remains documented constraint (TXT >500K UTF-16 — `TXTReaderContainerView.swift:519` hardcodes `highlightRange: nil` for chunked path). Closes the deferred UI-gesture leg from round-1. GH: #345 |
| 42 | Unified reader engine — RE-SCOPED to Readium + libmobi (convert-on-import)      | Reader/*     | High     | VERIFIED    | **VERIFIED 2026-06-03 (v3.51.1)** — both phases implemented + device-verified + no regression on current `main`. Consolidating evidence `dev-docs/verification/feature-42-20260603.md` (result=pass) rolls up the three phase acceptance passes — Phase-1 full 10/10 (`feature-42-20260601.md`, v3.42.14, commit `9aba219c`), Phase-2 libmobi convert-on-import device verify (`feature-42-p2-wi5-20260602.md`, v3.42.24), Phase-2 default-ON flip device verify (`feature-42-p2-g2flip-20260602.md`, v3.42.42, commit `563d5340`) — plus a green re-run of the libmobi (smoke/decode/assemble/convert) + BookImporter-AZW3-convert + FeatureFlags suites on `main` (`b93e701b`), confirming no #42-subsystem regression from the features merged since v3.42.42 (#74/#77/#80/#86). GH #1234 already CLOSED 2026-06-02 with BOTH phases documented (Phase-1 closure comment + a Phase-2 G2-flip follow-up comment). The row had lagged at IN PROGRESS as a tracker-state oversight only. **History below.** **IN PROGRESS 2026-05-29 (reconciled from stale PLANNED) — Phase-1 fully merged + WI-13 acceptance pass — now FULL `pass` 2026-06-01 (`dev-docs/verification/feature-42-20260601.md`): all 10 Phase-1 criteria verified CU-free at v3.42.14, incl. the 4 formerly-partial (#4 highlight-CREATE via idb HID long-press, #6 bilingual interlinear render, #8 photo full-transparency, #10 footnote tap-follow ch1→ch2). Row stays IN PROGRESS only because #42 is re-scoped to also include Phase-2 libmobi convert-on-import — **Phase-2 implementation + verification COMPLETE behind a default-OFF flag (2026-06-02): WI-1a..2c (MOBI→EPUB converter), WI-4a/4b (gated convert-on-import wiring in `BookImporter`), WI-5 (device verification, `dev-docs/verification/feature-42-p2-wi5-20260602.md`, result=pass at v3.42.24) + #307 (dedupe-origin fix) all merged behind `--enable-kindle-convert` (default OFF). The default-ON flip was HUMAN-GATED (symmetric with Phase-1's WI-14 G2) — **user ratified + flipped default-ON 2026-06-02 (v3.42.42): `kindleConvertOnImport` default ON + persisted-revertable; device-verified — a real AZW3 imported with no flag now imports as EPUB (snapshot format=epub) + renders via Readium (`dev-docs/verification/feature-42-p2-g2flip-20260602.md`, result=pass). NEW Kindle imports convert; existing native `.azw3` unaffected; revertable via override OFF.** Original WI-13 was `partial`.** All 12 Phase-1 WIs + 3 refinements (WI-7/8/10b) are merged at v3.40.30 (build 711, HEAD `87f7f54a`); the row had drifted at `PLANNED`. **WI-13 acceptance** (`dev-docs/verification/feature-42-20260529.md`, `result: partial`) — Readium engine FORCED ON (`-com.vreader.featureFlags.readiumEPUBEngine YES`), host-driven CU-free across an EPUB2/EPUB3/RTL/CJK corpus (new fixtures `mini-{epub2,rtl,cjk}.epub`). PASS: render (all corpus p-count=3), RTL `dir=rtl`, CJK vertical-rl, position save/restore (chapter2 round-trip), theme/font (dark+24pt), search-nav (cross-spine), TTS+follow (offset 8→246 while speaking), navigation (cross-spine via `.readerNavigateToLocator`), footnote noteref resolves. PARTIAL (environment-gated, unit-covered at the subsystem boundary): highlight CREATE (WKWebView selection gesture — not XCUITest-synthesizable; restore-adapter unit-covered), bilingual translation CONTENT (AI-provider-gated; enumerate pipeline unit-covered), photo full `html:root` transparency (custom-image picker gesture; compositing CSS unit-covered + body-layer transparency confirmed). **The criteria reachable CU-free pass with the flag forced ON; the default-ON flip (WI-14) is the pending human gate G2 — GH #1234 stays open.** Full unit bundle green (7579 tests). NOTE: `navigate?spine=N` is a legacy-engine-only bridge path; the Readium host observes the downstream `.readerNavigateToLocator` (a DEBUG-gated Readium `.debugBridgeNavigateCommand` observer is a possible follow-up, out of WI-13 scope). **PLANNED 2026-05-29 — Gate-1 COMPLETE + Gate-2 PASSED** (independent Codex audit, 3 rounds, zero open Critical/High/Medium). Plan: `dev-docs/plans/20260528-feature-42-readium-libmobi-reader-engine.md`. Phase-1 scope = Readium Swift Toolkit (BSD-3) renders EPUB behind a default-OFF `readiumEPUBEngine` flag; `EPUBWebViewBridge` stays live default until parity (14 WIs; bilingual #56 + continuous-scroll #71 gate only the default-ON flip). Kindle stays on Foliate in Phase 1; libmobi dropped from the critical path (licensing cleared — user's distribution posture; MIT Foliate-js reuse is the lighter Kindle-convert option for a later phase). **Phase-1 implementation (Gate-3) is gated on the G1 human go-ahead** — it replaces the VERIFIED EPUB engine + adds a third-party SPM dependency tree (reverses the repo's "no third-party packages" posture), so it needs an explicit proceed decision. GH: #1234 (historical Foliate-swap scope: #113, closed). **RE-SCOPED 2026-05-28** → drop the Foliate-js EPUB-swap; new target = **Readium Swift Toolkit** as the single runtime reflowable engine + **libmobi** AZW3/MOBI→EPUB convert-on-import + PDFKit; Foliate-js eliminated. Gate-1 plan DRAFT: `dev-docs/plans/20260528-feature-42-readium-libmobi-reader-engine.md`. Rationale: Readium ships the Locator/Decoration/search/TTS model out of the box (neutralises the old #1 risk of owning that abstraction); informed by web research + 4 Codex consults (migration cost off the table). **Stays DEFERRED — BLOCKERS before it leaves DEFERRED:** (a) libmobi LGPL-3.0 distribution decision (potential hard blocker), (b) Kindle conversion-fidelity corpus spike, (c) explicit go-ahead; Gate-2 audit of the new plan pending. **Prior (Option A / Foliate-swap) history below.** **DEFERRED 2026-05-19 (human decision)** — the EPUB-engine swap has no user-facing value and is a high-risk replacement of the VERIFIED, heavily-bug-fixed `EPUBWebViewBridge`; it sat Gate-3-blocked on human ratification with none given (GH #113 closed), and EPUB has accrued further investment since the plan (feature #60 v2 reskin, EPUB-highlight fixes), raising the swap cost. The low-risk half — consolidating the unwired duplicate Foliate path (`FoliateReaderHost` / `FoliateViewBridge` / `FoliateURLSchemeHandler` vs the live `FoliateSpikeView`, plan §10) — can be filed as a separate small cleanup feature. **Original plan note:** Replace EPUB bridge with Foliate-js `<foliate-view>`. EPUB+AZW3/MOBI via one engine. PDF/TXT unchanged. Old GH #113 (Foliate scope, closed — superseded by the 2026-05-28 Readium+libmobi re-scope; a fresh issue will be filed when #42 reaches PLANNED). **2026-05-18 (feature-cron Gate 1+2)** — implementation plan drafted: `dev-docs/plans/20260518-feature-42-foliate-unified-reader-engine.md` (v4, 17 WIs / 4 phases). Gate-2 independent audit **PASSED** (Codex, 3 rounds; 12+3+1 findings all resolved). Plan replaces the legacy `EPUBWebViewBridge` with the Foliate-js engine via a flag-gated, parity-verified, human-gated migration. **Gate 3 BLOCKED on human ratification** (plan §9 G1): high-risk swap of a VERIFIED format with no user-facing feature — a human must ratify go/no-go AND re-open #113 (currently CLOSED) or file a fresh issue before TDD starts. Plan §10 recommends Phase A (Foliate-path consolidation, WI-1–5) be split into its own feature. **WI-14 default-ON flip SHIPPED 2026-06-01 (v3.42.0, human G2 sign-off):** `FeatureFlags.readiumEPUBEngine` default flipped OFF→ON — the Readium navigator is now the default reflowable EPUB engine; legacy `EPUBWebViewBridge` reachable via the persisted override OFF. FeatureFlagsTests updated; FeatureFlags+ReaderEngine+EngineDispatch + 9 core Readium suites green. WI-13 acceptance criteria 4 (highlight CREATE) + 6 (bilingual translation) remain env-gated (gesture / AI provider) but implementation-complete + unit-covered; full VERIFIED awaits those on a real device. |
| 43 | Extract and display cover images from EPUB/AZW3                                | Library/*    | Medium   | VERIFIED  | EPUB OPF + AZW3 MOBI header parsing. 55 cover-related tests across `EPUBCoverParsingTests` (9), `EPUBMetadataExtractorTests` (14), `MetadataExtractorTests` (18), `MOBICoverExtractorTests` (14). Bug #107 for white-edge padding (separate row, out of scope here). **Verification history**: (1) 2026-05-05 result=**fail** (`feature-43-20260505.md`) → split into bug #122 (EPUB redundant-prefix `href`, FIXED in PR #248 v3.13.12) and AZW3 cover gap for "被讨厌的勇气-1" (root cause inconclusive without deterministic re-import). (2) 2026-05-05 result=**partial** (`feature-43-20260505b.md`) — EPUB redundant-prefix path PASSES via synthetic-EPUB integration test. AZW3 BLOCKED on missing fixture. (3) 2026-05-07 result=**partial** (`feature-43-20260507.md`) — AZW3 cover extraction PASSES end-to-end on live iPhone 17 Pro Sim using freshly-bundled `mini-azw3` fixture (PG ebook 1064, Mobipocket v6, codepage 65001): seed → import → real `BookImporter` → `MOBICoverExtractor` → `CustomCoverStore.saveCover` produces a valid 341×512 baseline JPEG at `CustomCovers/azw3_<key>.jpg`. SwiftData record + on-disk JPEG verified. Filed bug GH #340 (since FIXED + CLOSED — AZW3 EXTH title now reads "The Masque of the Red Death" not the filename). DEFERRED: UI cover-grid render. (4) 2026-05-09 result=**pass** (`feature-43-20260509.md`) — round-4 closes the deferred UI render leg. Library grid on iPhone 17 Pro Sim (iOS 26.4) at v3.14.109 (commit 02de2dd) renders the extracted 341×512 JPEG byte-for-byte for `mini-azw3` ("The Masque of the Red Death" with the cyan/red Project Gutenberg artwork). EPUB-without-embedded-cover (`mini-epub3.epub`) correctly falls back to the synthetic blue/EPUB cover (verified by `unzip -l` showing no embedded cover image). TXT renders the generic icon. All acceptance criteria covered; both bugs filed during prior rounds (#122, #340) are FIXED + CLOSED. **VERIFIED**. GH: #121 |
| 44 | DebugBridge — debug-only URL scheme + state dumper for autonomous testing      | DevTools/*   | High     | VERIFIED    | **2026-05-13 round-15** (`dev-docs/verification/feature-44-20260513-round15.md`, result=**pass**): smoke pass after feature #45 WI-1 (PR #581, v3.21.3, commit `6ad6193`). All 7 handlers pass on iPhone 17 Pro Sim (iOS 26.5). `columnCount: "auto"` vs round-14's `"1"` is a WebKit 26.4→26.5 computed-style reporting difference; `scrollWidth === clientWidth === 980` confirms single-column paged mode is correct. Bug #141 SETTLE 100ms placeholder is tracked separately; settle handler writes the sentinel file correctly with documented schema — this is the contract feature #44 tests. **VERIFIED.** **2026-05-09 round-11** (`dev-docs/verification/feature-44-20260509-round11.md`, result=**pass**): closes device-verification of bug #141's eval-Foliate portion against bundled `mini-azw3.azw3` fixture. PR #311 (v3.14.15) shipped the keyed `DebugReaderRegistry.setActiveFoliateWebView/foliateWebView(for:)` binding for AZW3 but device-verify had been blocked on missing AZW3 fixture (bug #143). Bug #143 FIXED by bundling Project Gutenberg #1064. This round runs eval round-trip on merged-main v3.14.121 (commit `a458a69`): seed → open mini-azw3 → `vreader-debug://eval?bridge=foliate&js=<base64>` returns real DOM-state JSON (`docState: "complete"`, `docHTMLLen: 285987`, `relevantWindowKeys: ["FoliateHost","readerAPI"]`) keyed by fingerprintKey + format. No `unsupportedBridge` / `bridgeNotReady` errors. Snapshot post-open shows non-stub `currentBookId`/`format`. **Status stays DONE; VERIFIED still gated on criterion (d') = bug #141 SETTLE portion** (`awaitSettle` 100ms placeholder + per-bridge ready signal — separate work in bug #141 REMAINING). 1 screenshot artifact. DEBUG-only `vreader-debug://` handler with 7 commands (reset/seed/theme/open/settle/snapshot/eval). Active-reader registry, fixture catalog, stable error codes, release-gate script. 97 unit tests. Doc at `docs/subsystems/debug-bridge.md`. **Verification history**: (1) 2026-05-05 result=**fail** (`feature-44-20260505.md`) → bug #121 (URL scheme not registered, FIXED in v3.13.9). (2) 2026-05-05 result=**partial** (`feature-44-20260505b.md`) → bug #123 (`.onOpenURL` blocked by iOS scheme-approval prompt, FIXED in v3.13.11). (3) 2026-05-05 result=**partial** (`feature-44-20260505c.md`) → bugs #124 (FIXED v3.13.14) and #125 (FIXED v3.13.15). (4) 2026-05-05 result=**partial** (`feature-44-20260505d.md`) — all 7 URL handlers + their observable surfaces (logs, sentinel files, sqlite, snapshot JSON) work end-to-end on merged-main v3.13.15. **Implementation complete; row promoted IN PROGRESS → DONE.** VERIFIED still gated on 4 plan criteria not covered by round 4: (a) EPUB/Foliate coverage — both `settle`'s `relocate` happy-path and `eval`'s Foliate happy-path require an EPUB fixture which `DebugFixtureCatalog` doesn't carry today (TXT-only); (b) Release-build absence-gate — `scripts/verify-release-no-debugbridge.sh` exists but wasn't run this round; (c) all unit+integration tests pass — 19 unrelated pre-existing failures (TXTChapterContentLoader/V1toV2Migration/etc.); (d) LOC <300 — `RealDebugBridgeContext.swift` is 491 lines, split deferred per Codex. (5) 2026-05-05 result=**partial** (`feature-44-20260505e.md`) — round 5 closes acceptance criterion (b) Release-build absence-gate: clean Release build of v3.13.18 (commit `1a8968c2`); `scripts/verify-release-no-debugbridge.sh` exits 0 across all six sub-checks (Info.plist, DebugFixtures dir, fixture filenames, DebugBridge-named files, resource refs, binary strings). VERIFIED now gated on 3 remaining: (a) EPUB fixture, (c) test suite cleanup, (d) LOC split. (6) 2026-05-05 result=**partial** (`feature-44-20260505f.md`) — round 6 against the freshly-bundled `mini-epub3` fixture (PR #265, v3.13.20) extends criterion (a) operational coverage to EPUB end-to-end (seed → open → settle → snapshot → eval all produce documented JSON shapes; ZBOOK row inserted with correct title/fingerprintKey). Discovered structural gap that demotes criterion (d) from DEFERRED to **FAIL**: `DebugReaderProbeAdapter` has nil-defaulted `settleStrategy` (100ms placeholder) and `jsEvaluator` (throws unsupported), and no reader host plugs them in — round-4d's "EPUB fixture" framing was incomplete. Filed as **bug #126** (GH #266) for per-format probe-hook wiring at the reader-host level. VERIFIED now gated on: (d) bug #126 fix, (e) test suite cleanup, (g) LOC split. (7) 2026-05-05 — refactor PR #268 (v3.13.22) splits `RealDebugBridgeContext.swift` into per-command extensions (Settle / Snapshot / Eval). Pre-split: 506 lines. Post-split: 247 / 137 / 91 / 106 lines — every file under the 300-line guideline. **Closes acceptance criterion (g)**. Pure structural change; 37/38 DebugBridge tests still pass post-split (the 1 failure is pre-existing — see (e)). VERIFIED now gated on (d) bug #126 fix and (e) test suite cleanup. (8a) 2026-05-06 result=**partial** (`feature-44-20260506.md`) — first verification round on iOS 26.4 since bug #140 (LSD scheme-approval moved to SQLite) was closed in v3.14.10. Patched grant script (PR #302) writes both legacy plist + SQLite Election row. Full end-to-end command sequence verified on iPhone 17 Pro Sim: reset + seed both fixtures (war-and-peace.txt + mini-epub3.epub) + theme + open + settle + snapshot + eval. **First round with screenshot evidence** of both reader formats actually rendering (artifacts in `dev-docs/verification/artifacts/feature-44-r7-{epub,txt}-open-*.png`). Criterion (d) unchanged — bug #126 still open. No regressions across the 25-PR session. (8b) 2026-05-06 — chip away at criterion (e): pre-existing stale-test failures fixed across 3 test files. `SchemaV1Tests.migrationPlanHasSchemas` and `V1toV2MigrationTests.migrationPlanIncludesBothSchemas` switched from hardcoded `count == 2` (drifted to 6 after V3-V6 schemas shipped) to checking `SchemaV1`/`SchemaV2` presence by name — assertion now matches what the test name claims. `TXTChapterContentLoaderTests` helper `makeTestData` now populates `globalStartUTF16`/`textLengthUTF16` (production switched from byte-based to UTF-16-offset reads at some point; tests still passed byte-only init, leaving UTF-16 fields at `-1` default → every load threw `decodeFailed`). 16 tests in that suite + 3 ad-hoc cases all pass post-fix. Total ~19 of the (e) failure count addressed across rounds 6/8. (9) 2026-05-06 — **closes acceptance criterion (e)**. Final 6 stale-test failures fixed: `TXTOffsetTranslator.testToGlobalLocalBeyondLength` (production allows terminal offset == textLengthUTF16 per audit-fix comment; test asserted strict `<`); `TOCChapterProgress.beforeFirstEntry` (production clamps preamble to fraction=0; test wanted fractional preamble — filed as bug #127/GH #271, test updated to match current behavior with forward-link); `PerBookSettings.applyResolvedSettings_doesNotPollutedUserDefaults` (test asserted defaults["readingMode"] == "native" without ever explicitly setting `.native` — added explicit set so the assertion has meaning); `FoliateViewCoordinatorMessageRoutingTests.bridgeReadyTriggersOpenBook` + `bridgeReadyWithoutBase64ReportsError` (production rewrote bridge-ready to no-op since book opens via HTML-embedded base64; rewrote both tests to assert the new no-op contract); `HighlightIntegrationTests.handleRemovalRemovesAndRestores` (real flow has ViewModel call `persistence.removeHighlight` BEFORE `coordinator.handleRemoval` — coordinator only updates visual state; test now mirrors production sequence). **Full suite green: 716/716 tests pass.** VERIFIED now gated only on (d) bug #126 fix. Unblocks feature #45. (10) 2026-05-06 — partial closure of criterion (d). **Bug #126's EPUB-eval portion shipped in PR #305 (v3.14.13)**: `vreader-debug://eval?bridge=...&js=base64(...)` now returns real JS results for native-mode EPUB via the keyed `DebugReaderRegistry.epubWebView(for:)` binding (closure pulls webview at call-time, rejects stale-webview matches across books). 6 readiness probes against mini-epub3 via the new eval surface (artifacts: `feature-11-r2-*`): `__vreader_createHighlight` is function (true), `selectionChanged` handler registered (true), CSS Highlight API supported (true), DOM ready (true), `<p>` count (3), CSS Highlight roundtrip (`set+keys+rangeText`). **Settle's Foliate `relocate` portion** of original criterion (d) split to **bug #141** (GH #305) — settle 100ms placeholder is per-format, untouched by this fix. Same-book reopen race (Codex round-2 narrow Medium) split to **bug #142** (GH #306). Evidence: `feature-11-20260506b.md` (the bridge-readiness slice) + `bug-126-20260506.md` (the eval-portion fix) + `feature-44-20260506b.md` (round 10 — EPUB theme-CSS injection verified end-to-end via `getComputedStyle` against both dark and light modes; surfaced bug #144 — `vreader-debug://theme` doesn't propagate to live `ReaderSettingsStore`, fixed in PR #321). VERIFIED now gated on (d') bug #141 (settle Foliate wiring) — narrower than the original gate. GH: #244 |
| 45 | Verification harness sweep — retire the "Needs device verification" backlog    | DevTools/*   | High     | DONE | XCUITest + DebugBridge recipes for 13 of 15 simulator-automatable backlog items. Adds VERIFIED status. Depends on #44. Plan: `dev-docs/plans/20260513-feature-45-verification-harness-sweep.md` (v3, 2026-05-15). **All WIs shipped** — WI-1 PR #581 v3.21.3; WI-2 PR #586; WI-3 PR #587; WI-4 PR #589; WI-4b PR #590; WI-4c-a PR #591; WI-4c-c PR #599; WI-4d commit `de5a73d`; WI-4e PR #643; WI-5 PR #679 v3.21.64; **WI-6 (named test-plan selector) PR #692 v3.21.69 commit `3753d2a`** — ships `TestPlans/Verification.xctestplan` (25 per-method identifiers across 13 classes) + `TestPlans/All.xctestplan` (no-flag default). Invocation: `xcodebuild test -scheme vreader -testPlan Verification`. **Recovery arc** — Bug #192 (PR #688) renamed `verify_*` → `test_verify_*` after WI-6 Gate 2 audit discovered the 25 methods were XCTest-invisible (vacuous `Executed 0 tests` passes had silently no-opped the harness across all 13 classes). Bug #193 (PR #691) fixed the Feature #36 OPDS XCUITest element-class mismatch surfaced by the post-rename re-run. WI-6's Gate 5 transport-success dispatch confirmed exactly 25 tests run end-to-end (10 passed, 13 XCTSkip-gated, 2 product failures filed as new bugs post-merge for Feature28/29). **2026-05-16 update (verify-cron progressive sampling)**: Feature #23 + Feature #31 confirmed clean in `dev-docs/verification/feature-45-20260516-sampling.md` (full plan run); Feature #31 re-confirmed in this iteration (2/2 PASS solo, 35s). **Feature #37 root-cause identified**: harness gap (XCUITest doesn't swipe-up to bring `perBookSection` into accessibility tree; production feature is fine per `feature-37-20260509.md`). Filed as **Bug #204 / GH #746** (DevTools/* Low). Net status: **7 of 13 classes still unsampled clean** (#11 EPUB load timing, #21 multi-page EPUB fixture, #28/#29 fixture-env-var gaps with 1-of-2 methods clean, #37 awaiting #204 harness fix, #40/#41 TTS control bar timing). VERIFIED status depends on Gate 5 final acceptance evidence file once those unsampled classes are sampled clean. **2026-05-16 round-2 sampling** (`dev-docs/verification/feature-45-20260516-round2.md`, result=partial) at v3.24.6 commit `a6103e5`: identical shape to round-1 (12 PASS, 13 SKIPPED, 0 FAILED in 410s). No regressions across the 40+ commits between v3.22.20 and v3.24.6. Same 7 classes remain gated by harness/fixture/env reasons. No new bugs filed. **2026-05-18 round-3 sampling** (`dev-docs/verification/feature-45-20260518-round3.md`, result=partial) at v3.27.23 commit `6936ccf`: 27 tests, 12 PASS / 15 SKIP / 0 FAIL — GREEN, no regressions across the feature #60 re-skin + Bug #209 repair + Bug #210/#214 changes landed since round-2. Progress: **Feature37 (2 methods) now PASS** (Bug #204 swipe-helper fix landed — previously gated); new `Feature11EPUBBottomChromeVerificationTests` (Bug #214, 2 tests) PASS. 9 method-groups still SKIP (Feature11Highlight, 21, 28-conversion, 29-backup-executes, 31, 35, 36-live, 40, 41 — fixture/harness/env gaps). No new bugs filed. **2026-05-18 round-4 sampling** (`dev-docs/verification/feature-45-20260518-round4.md`, result=partial) at v3.27.25 commit `8cab12a`: 27 tests, 12 PASS / 15 SKIP / 0 FAIL — GREEN, identical shape to round-3, zero regressions across the v3.27.23→v3.27.25 range (Bug #213/#216/#217 — GH #830/#838/#839 — test-infra fixes landed since round-3). Same 9 method-groups SKIP — unchanged fixture/harness/env gaps; no new bugs filed. **2026-05-18 round-5 sampling** (`dev-docs/verification/feature-45-20260518-round5.md`, result=partial) at v3.27.27 commit `9705693`: 27 tests, 12 PASS / 15 SKIP / 0 FAIL — GREEN, byte-identical shape to round-4, zero regressions across the v3.27.25→v3.27.27 range (Bug #219 / GH #846 — Feature11 EPUB-highlight XCUITest seed + readiness probe; Bug #182 / GH #847 — EPUB cross-chapter search highlight). Positive finding: Bug #219 converted `Feature11EPUBHighlightVerificationTests`'s reader-load gates from `XCTSkip` to hard `XCTAssert`; round-5 is the first full-plan run of that — both methods clear the hard gates and skip *deeper* at the WKWebView long-press step (`Feature11EPUBHighlightVerificationTests.swift:147/237`, Bug #220 / GH #845) rather than at the old seed gate. Skip count unchanged (still 9 method-groups). No new bugs filed. **2026-05-19 round-6 sampling** (`dev-docs/verification/feature-45-20260519-round6.md`, result=partial) at v3.34.15 commit `f47e1c5`: true shape **27 tests, 12 PASS / 15 SKIP / 0 FAIL — GREEN, byte-identical to round-5, zero regressions** across the ~7-version delta since round-5 (features #54/#55/#57/#63/#65/#68/#69 + Bug #225/#226 TTSService `rate` `didSet` recursion fix + test-infra fixes). The contended plan run on the shared simulator pool false-failed 6 classes (#28/#29/#34/#36/#40/#41 — testmanagerd wedged 427s on #28, #221-class flaky-parallel-pool cascade); a step-4 isolation re-run of all 6 on a dedicated idle simulator returned `** TEST SUCCEEDED **` (12 tests, 7 skipped, 0 failures), confirming the 6 plan-run failures were contention noise, not regressions. Same 9 method-groups SKIP — unchanged fixture/env/harness gaps; no new bugs filed. GH: #576 |
| 46 | WebDAV backup includes book files — materializing restore reconstructs library | Backup/*     | High     | VERIFIED  | Phase 1 of two-feature split. Adds `library-manifest.json` to the backup ZIP and uploads missing whole-file book blobs to `VReader/books/<format>/<sha256>_<byteCount>.<ext>` (content-addressed, deduped). Restore downloads every missing blob into the sandbox before applying metadata. **Shipped 2026-05-03** — `BackupDataCollector` emits the manifest, `BlobPath.make`+`WebDAVBlobStore.putBlobAtomically` handle the upload, `BookFileMaterializer` runs the download+import on restore. **VERIFIED 2026-05-03** via 4 curl probes against live rclone WebDAV + Swift Testing suite (`WebDAVProviderBackupWithManifestTests`). Evidence: `dev-docs/verification/feature-46-20260503.md`. Phase 2 = feature #47 (also VERIFIED 2026-05-04). GH: #144 (closed). |
| 47 | WebDAV restore — selective book picker + lazy-on-tap downloads                 | Backup/*     | Medium   | VERIFIED  | Phase 2 of feature #46. `BookFileState` (`local / remoteOnly / downloading / failed / missingRemote`), library rows with cloud icons, "Pick…" entry on each backup row → `SelectiveRestorePicker` against the manifest, on-tap blob fetch via background URLSession with `taskDescription` identity, Wi-Fi-only cellular policy via `NWPathMonitor`, "Clean unused remote files" GC carved out as feature #51. Depends on feature #46. **Shipped 2026-05-04** in v3.11.18..v3.12.0 (21 PRs). **VERIFIED 2026-05-04** in v3.12.9 after fixing 5 blocking bugs (#112, #113, #114, #115, #116) found during initial Gate-5 run. Evidence: `dev-docs/verification/feature-47-20260504.md` (initial partial) + `dev-docs/verification/feature-47-20260504b.md` (post-fix pass) + per-bug evidence files. GH: #407 (closed — VERIFIED). |
| 69 | AI Summarize scope selector — make the design's SummaryView scope chips (Section / Chapter / Book so far) functional | AI/* | Low | VERIFIED | **Filed 2026-05-18** — carved out of feature #65 (AI sheet re-skin) at Gate 2 (Codex audit `019e3b84`). The committed design (`vreader-panels.jsx` `SummaryView`) shows three scope chips; #65 omitted them because making them functional is a behavior feature, not a re-skin. **Problem**: the AI Summarize tab summarizes a fixed ~2500-char window around the reading position (`AIContextExtractor`) — there is no way to request a chapter-level or whole-book-so-far summary. **Scope**: a scoped `AIContextExtractor` — Section = the current window; Book-so-far = a token-capped prefix of `ReaderAICoordinator.loadedTextContent` up to the locator; Chapter = a chapter-bounded window (needs TOC boundary data threaded into the AI path) — plus a token-budget strategy for the larger scopes (a long book's prefix exceeds provider token limits; consider map-reduce). **Design**: the scope chips are already designed in `vreader-panels.jsx` `SummaryView`, so this is design-ready and only needs the behavior. **Lineage**: feature #65 Gate-2 carve-out. **2026-05-19 — PLANNED**: Gate-1 plan + Gate-2 independent audit complete. Plan at `dev-docs/plans/20260519-feature-69-ai-summarize-scope-selector.md` (v3 — Codex 3-round Gate-2 audit `019e3e09`, verdict CLEAN; round-1 found 6 findings incl. the Critical "scoped summarize must extract from `loadedTextContent`, not the pre-extracted `currentTextContent` snippet" + the High "MD locator/TOC coordinate-space split → real Chapter/Book-so-far scoped to TXT only", round-2 found 1, round-3 clean). 5 WIs (WI-1 `SummaryScope`+`ChapterBounds` → WI-2 `SummaryScopeResolver` → WI-3 scoped `AIContextExtractor`+`AIContextExtracting` protocol → WI-4 `AIAssistantViewModel` scope wiring → WI-5 `AISummaryTabView` chip strip + acceptance). **2026-05-19 — DONE**: all 5 WIs merged. WI-1 (`SummaryScope` enum + `ChapterBounds` value types, 21 tests) PR #903 v3.33.1 Codex `019e3e34` 2 rounds ship-as-is; WI-2 (`SummaryScopeResolver` TOC→bounds, 17 tests) PR #906 v3.33.3 Codex `019e3e3e` 2 rounds ship-as-is; WI-3 (scoped `AIContextExtractor` + `AIContextExtracting` protocol + `UTF16TextSlicer`, 40 tests) PR #908 v3.33.4 Codex `019e3e48` 2 rounds ship-as-is; WI-4 (`AIAssistantViewModel` scope wiring — `selectedScope`/`setScope` + scoped `summarize`, 14 tests) PR #909 v3.33.6 Codex `019e3e57` 2 rounds ship-as-is; WI-5 (`AISummaryTabView` chip strip + `AISummaryScopeChipStrip` + `aiSheet` threading, 13 tests) final WI Codex `019e3e6f` 2 rounds ship-as-is. **Gate-5 verification**: scope-chip UI rendering + chip-tap selection + the `AIContextExtractor` scoping logic verified via 130 deterministic unit/integration tests across 10 AI/summarize suites; the end-to-end *summarize-output* acceptance criteria are blocked by the verification sim's AI provider returning HTTP 401 (empty `OPENROUTER_API_KEY` — same wall as #65/#68). Evidence: `dev-docs/verification/feature-69-20260519.md` (`result: partial`). Row stays `DONE` (not `VERIFIED`) pending an AI-credentialed verification environment. **2026-05-21 — Gate-5b attempt 2 (`result: partial`)**: `dev-docs/verification/feature-69-20260521.md` (v3.38.29 `5547d390`). Both prior prerequisite blockers from 2026-05-19 are now lifted (Bug #237 + Bug #243 both shipped, valid `OPENROUTER_API_KEY` available). Criteria 1-6 (the 163-test logic suite + chip-strip rendering) all PASS, unchanged. **Criteria 7-8 still DEFERRED** — same wall as feature #65 row 3 / 6 / 11: a hybrid host-driver / XCUITest harness gap. End-to-end summarize-output rendering needs the provider URL configured between `XCUIApplication.launch()` and the scope-chip-tap phase, but `xcrun simctl openurl` from inside an XCUITest sandbox fails (Bug #1054). No code defect observed. Flip → VERIFIED requires ONE of: CU restored, a hybrid host-driver harness, or a DebugBridge AI-stub command. **2026-05-21 — Gate-5b attempt 3 (`result: partial`)**: `dev-docs/verification/feature-69-20260521-VERIFIED.md` (v3.38.42 `48c2b796`). Bug #255 / GH #1112's DebugBridge `ai?action=summarize&scope=<section\|chapter\|book>` command shipped, so the AI action now fires CU-free from the host via the real `runSummarize` path (no XCUITest-sandbox interleave — Bug #1054 sidestepped entirely). **Criterion 7 now PASS** — `ai?action=summarize&scope=chapter` rendered a `.complete` chapter-scoped summary card with a real OpenRouter (`openai/gpt-4o-mini`) response. **Scope-changes-summary intent PROVEN** — Section (scene-level detail) vs Chapter (thematic overview) produce visibly different populated summaries at the same locator. **Criterion 8 (populated Book-so-far ≠ Section) still NOT observed** — at the only reachable reading position (offset 0, title page) `extractBookSoFar` correctly returns `""` (the empty-boundary card), which is correct behavior not a defect; a *populated* Book-so-far needs a non-zero position, and `open?position=N` parses but the host-side TXT seek is a deferred follow-up (snapshot `position` stays `null`) while CU paging is structurally unavailable. Flip → VERIFIED still requires ONE of: CU restored, a host-side TXT seek that moves the reader, or a fixture seeded mid-book. **2026-05-22 — VERIFIED (`result: pass`, 8/8)**: `dev-docs/verification/feature-69-20260522-round3.md` (v3.39.7 `7c2f6930`). Both prior blockers lifted: Bug #257 (open?position seek) is FIXED — `open?bookId=…&position=1000` moved the war-and-peace TXT reader to offset 949 (snapshot `position: "949"`, not null). With a non-zero reading position reachable CU-free, all 8 criteria exercised end-to-end against real OpenRouter (`openai/gpt-4o-mini`). **Criterion 8 now PASS** — Book-so-far at offset 949 rendered a *populated* `.complete` SUMMARY card (Chapter-1 prefix `[0,949)`: Anna Pavlovna / Antichrist / "la grippe"), visibly distinct from the Section (opening-chapters overview) and Chapter (Chapter-2 drawing-room) cards at the same locator. Note: the production `AIService.sendRequest` gate (`FeatureFlags.aiAssistant` + `AIConsentManager.hasConsent`, both UserDefaults-backed, default false) is independent of the `--enable-ai`/`forceAvailable` UI-visibility seam; verification set those two keys via plist (the CU-free equivalent of enabling AI + granting consent in Settings) — no code change, no defect. 77 feature-#69 unit tests re-run green on this build. GH: #897 |
| 68 | Reader chapter-start typography — drop-cap + in-text chapter heading | Reader/* | Low | DONE | **Filed 2026-05-18** — surfaced by an audit of the v2 design renders vs the shipped reader (README screenshot review). **Problem**: the design (`vreader-reader.jsx`) renders, at each chapter start, (a) a centered uppercase serif chapter heading in the text flow (`:333-343`) and (b) a drop-cap — a large oxblood first letter (~2.6× body size, floated left) on the chapter's first paragraph (`:383-390`). Neither was built: no drop-cap code existed in any renderer, and the shipped `ChapterTitleOverlay` (`TXTChapterOverlayViews.swift:19-39`) is a different thing — a top-of-screen caption bar, TXT-only, shown only when chrome is visible. **Design source**: `vreader-reader.jsx:333-343` + `:383-390`. **Lineage**: v2 follow-on of feature #60 (VERIFIED). Cross-renderer scope (TXT/MD attributed strings; EPUB/AZW3 injected CSS; PDF n/a). **2026-05-19 — PLANNED**: Gate-1 plan + Gate-2 independent audit complete. Plan at `dev-docs/plans/20260518-feature-68-chapter-start-typography.md` (v4 — Codex 3-round Gate-2 audit `019e3bee`, all round-1/2/3 findings resolved). **2026-05-19 — DONE**: all 6 WIs merged — WI-1 `ChapterStartTypography` shared constants (PR #880), WI-2 TXT drop-cap + regex-heading restyle (PR #885), WI-3 MD drop-cap + leading-heading restyle + `MDRenderConfig` plumbing (PR #889), WI-4 EPUB drop-cap CSS (PR #890), WI-5 AZW3/MOBI Foliate drop-cap CSS (PR #892), WI-6 acceptance verification (this PR). 128 feature-#68 tests pass; each WI passed a 2-round Gate-4 Codex audit. Gate-5 acceptance pass recorded `result: partial` in `dev-docs/verification/feature-68-20260519.md` — TXT fully device-verified incl. live theme switching; EPUB/MD/AZW3 unit+composition verified + AZW3 confirmed-injecting; full per-format on-device drop-cap visual is the remaining gap (DebugBridge EPUB-open did not navigate + no MD fixture in `DebugFixtureCatalog` + CU outage), so the row stays `DONE` (not `VERIFIED`) pending a follow-up visual pass + `VERIFIED` evidence file. **2026-05-27 — Gate-5 round 2 (`result: partial`, 7/10 pass)**: `dev-docs/verification/feature-68-20260527.md` (v3.39.40 `fd0d6c61`). Re-attempted the deferred visuals CU-free. **#5 EPUB drop-cap + #7 per-theme moved partial → PASS**: live-WKWebView `eval` computed-style on mini-epub3 confirms `body > p:first-of-type::first-letter` = oxblood `#8C2F2F`, 52.52px / body 20.2px = **2.6×**, float-left on the genuine first paragraph; paper→dark theme switch recolors it to `#d6885a` live (sidesteps the old "EPUB-open didn't navigate" blocker via computed-style, no scroll-to-top needed). **#3 MD + #9 paged still partial** — the new `mini-markdown` fixture seeds, but the native paged MD/TXT reader can't be driven to page 1 (`open?position=0` → `snapshot.position=833`, "Page 2 of 2" blank) and a UITextView has no `eval`; **filed Bug #272 / GH #1193** as the concrete unblock. **#6 AZW3** is a feature-correct safe-miss on the only AZW3 fixture. Flip → VERIFIED gated on Bug #272 (then an MD/TXT page-1 screenshot closes #3 + #9). GH: #867 |
| 67 | Settings profile-header card + Stats entry point | App/* | Low | VERIFIED | **VERIFIED 2026-05-26 (Gate-5b, verify cron)** — CU-free XCUITest acceptance pass on iPhone 17 Pro Sim (iOS 26.4) at v3.39.17 build 638 (HEAD `14fe3861`). New suite `vreaderUITests/Verification/Feature67SettingsProfileCardVerificationTests.swift` (3 tests, 0 failures, 41.6s): (1) profile card present — "Your library" header + "… read this month" subline + `settingsProfileStatsButton` pill; (2) Stats pill → feature #58 `ReadingDashboardView` (cross-feature hand-off, #58 VERIFIED); (3) WI-6 AI-group Variant-A collapse — master `aiToggle` always present, `aiProvidersNavLink` + `consentToggle` appear only when AI on, collapse when off. Pixel-level design fidelity carried from Gate-5a screenshots (`feature-67-wi6-*-20260521.png`) + composition/snapshot unit tests. No bug discovered, no product code changed (verification scope). Evidence `dev-docs/verification/feature-67-20260526.md` (`result: pass`). **DONE 2026-05-21 (WI-6 — final WI) — feature complete, awaiting Gate-5b acceptance.** Now that `needs-design` #1068 is resolved (design `vreader-ai-toggles.jsx` + `ai-toggles-artboards.jsx` landed commit `3735529a`, Variant A recommended), WI-6 restyled the two deferred AI toggle rows to the design's colored-tile `SettingsToggleRow` + `PillSwitch`: **Enable AI Assistant** (oxblood `#8c2f2f` sparkle tile, master gate, always visible) and **Allow AI data sharing** (cool-blue `#4a6a8a` `checkmark.shield` tile, consent), merged with the AI Provider row into the design's single "AI" group (provider + consent hidden when AI off). New `PillSwitch` (a `ToggleStyle` over a real `Toggle` for native switch a11y) + `SettingsToggleRow` (peer of `SettingsIconRow`) + `SettingsRowPalette.aiAssistant`/`.aiDataSharing`. The `aiToggle`/`consentToggle`/`aiProvidersNavLink` identifiers + `AIProviderListView` destination preserved (identifier now lands on the actionable `Toggle`, not the row container). Gate-4 Codex audit: 2 rounds, **ship-as-is** (thread `019e4a37`); round-1 found 1 High (a11y identifier on container) + 1 Medium (button vs toggle semantics) + 1 Low (detail spacing), all resolved. Full suite green (7042 tests). Gate-5a slice-verified CU-free on iPhone 17 Pro Sim iOS 26.4 at v3.39.0 via the sim-drive-fallback tap path: AI-ON shows all 3 colored rows incl. the green/gray PillSwitches; toggling AI OFF collapses to the single master row (provider+consent hidden) — `dev-docs/verification/artifacts/feature-67-wi6-{02-ai-on-allrows,03-ai-off-collapsed}-20260521.png`. PR (this) v3.39.0 (minor — final WI completes the feature). This was the last remaining scope → row `IN PROGRESS` → `DONE`. Gate-5b end-to-end acceptance pass (`feature-67-<date>.md` evidence) pending before `VERIFIED`. Plan WI-6 addendum in `dev-docs/plans/20260519-feature-67-settings-profile-card.md`. **WI-5 (2026-05-20)** — `needs-design` #1068 filed for the AI Assistant + Data & Privacy toggle rows. The plan's WI-5 ("definitively restyles `AISettingsSection`") assumed all 3 AI rows would get colored-icon row chrome; Gate-3 audit of `vreader-panels.jsx:868-870` revealed the (then-)committed design depicted only the AI Provider row (`Icons.Sparkle` `#8c2f2f`). Per rule 51, WI-5's PR narrowed scope to restyle the AI Provider row only and added `SettingsRowPalette.aiProvider`; the two toggle rows stayed on plain-`Toggle` chrome pending design — **now unblocked + shipped in WI-6 (#1068 resolved)**. **WI-4 DONE 2026-05-20** — PR #1067 v3.38.15 commit `92cddd2`. Mounts `SettingsProfileCard` as the first `Form` row, restyles Cloud & Sync / Reading / About rows to `SettingsIconRow`, wires the Stats pill through `Notification.Name.openReadingStatsRequested` to a `SettingsStatsPresenter` that presents `ReadingDashboardView` (feature #58's surface shipped in PR #1061). Gate-4 audit: 2 rounds, ship-as-is (Codex `019e457e`); 1 High (stale-VM-on-reopen) + 1 Medium (state-machine test gap) resolved by extracting `SettingsStatsPresenter` (`@MainActor @Observable`) + 7 presenter state-machine tests. Gate-5a slice verified on iPhone 17 Pro Sim iOS 26.4 (5 PNG artifacts in `dev-docs/verification/artifacts/feature-67-wi-4-*.png`). **WI-1 DONE 2026-05-20** — reading-window persistence reads + `MonthBoundary` + `ReadingTimeFormatter.formatCompactHours` (foundational, no UI): PR #1014, 47 tests green, Codex-audited clean (thread `019e4146`). WI-2 (`SettingsRowPalette` + `SettingsIconRow`) PR #1016. WI-3 (`SettingsHeaderViewModel` + `SettingsProfileCard`) PR #1018. **WI-4 + WI-5 are HARD-BLOCKED on feature #58's dashboard-presenter WI (#58 WI-6) reaching `DONE` on `main`** — #58 WI-6 is itself blocked on product decisions D1–D4 (GH #665); per `.claude/rules/48-parallel-execution.md` hard rule 2, WI-4 (the WI that makes the Stats button user-visible) does not enter Gate 3 on an unmet hard dependency. **Depends: #58 (dashboard presenter, #58 WI-6).** **Gate 1+2 complete — plan dev-docs/plans/20260519-feature-67-settings-profile-card.md, Codex-audited clean.** **Design delivered 2026-05-18 — issue-canvas handoff resolves needs-design #862** — Gate-1 triage (2026-05-18): the design's profile-header card shows a user **name** + avatar initial, but the app has no user-account / no user-name source (only OPDS / WebDAV credentials); and the **Stats** button's destination is feature #58's reading dashboard — unbuilt and undesigned. The profile-card identity model + the reading-stats surface need a design decision → `needs-design` #862. The grouped-row restyle (30pt colored-icon rows) IS designed and unaffected. **Filed 2026-05-17** — surfaced by a component-by-component re-skin audit of the design bundle vs shipped state. **Problem**: `SettingsView.swift` re-skinned to v2 chrome with the four design groups, but omits the design's top **profile-header card** (avatar disc, name, "N books · Nh read this month") and the **Stats** button that opens the reading-stats dashboard; grouped rows are native `Form` `NavigationLink`s, not the design's 30pt colored-icon rows. **Design source**: `vreader-panels.jsx` — `SettingsSheet` / `Row`. **Cross-ref**: the Stats button is the entry point to feature #58 (reading-time dashboard, TODO). **Lineage**: v2 follow-on of feature #60 (VERIFIED). GH: #825 |
| 66 | Reader Settings sub-control re-skin — custom accent-track SliderRow + typeface pill toggle | Reader/* | Low | VERIFIED | **VERIFIED 2026-05-19 (Gate-5b device verification)** — `dev-docs/verification/feature-66-20260519.md`, `result: pass`, on iPhone 17 Pro Simulator / iOS 26.4, `main` @ `bbc1444`, v3.33.1. All 8 derived acceptance criteria PASS: (a) Font Size is the custom accent-track `SettingsSliderRow` (Aa-small/Aa-large glyphs); (b) Line Spacing is `SettingsSliderRow` (line-density glyphs); (c) Font is the `TypefacePillToggle` segmented pill — all three are NOT native `Slider`/`Picker`. (d/e) Functional: dragging Font Size enlarged the reader text live, dragging Line Spacing widened line spacing live, tapping "Serif" switched the reader typeface live. (f) Settings persisted across panel close/reopen. (g) Matches the `vreader-fidelity-v1` design (`03-15-reader-settings.png`) and holds contrast on the dark theme. (h) `.accessibilityRepresentation` Slider/Picker confirmed in source + the 27-test suites pass. **Feature #54 interaction**: #54 removed the reading-mode toggle, which was never in #66's scope — produces zero N/A criteria; all #66 criteria applicable and pass. Verified via DebugBridge + the sim-drive-fallback toolkit (computer-use is virtual-display-only on this host). Unit gate: `SettingsSliderRowTests` + `TypefacePillToggleTests` = 27 tests, 2 suites, **TEST SUCCEEDED**. Deferred follow-up: plan §6 catalogued a `Feature66ReaderSettingsControlsVerificationTests` XCUITest suite that was not created in PR #853 — this Gate-5b on-device pass fulfils the same role; the automated suite remains a nice-to-have. **Filed 2026-05-17** — component re-skin audit finding. **Problem**: `ReaderSettingsPanel.swift` has v2 chrome + theme swatches (WI-11), but font-size / line-spacing / brightness are native `Slider`s and font-family is a native segmented `Picker` (`ReaderSettingsPanel.swift:444-575`) — not the design's custom accent-track `SliderRow` (22pt thumb + leading/trailing glyphs) and 2-option typeface-preview pill toggle. **⚠ Gap against feature #60's VERIFIED status**: #60 scope item (7) covers the Reader Settings sheet "matching design" and Gate-5b recorded `result: pass`, but the sub-controls are native — #60's evidence overstated coverage. **Design source**: `vreader-panels.jsx` — `SliderRow`, `ReaderSettingsSheet`. **Lineage**: v2 follow-on of feature #60. **2026-05-18 (feature-cron Gate 1+2)** — implementation plan drafted: `dev-docs/plans/20260518-feature-66-reader-settings-subcontrol-reskin.md` (v3, 2 WIs, both behavioral). Gate-2 independent audit **PASSED** (Codex `019e39f9`, 3 rounds; 2 High + 4 Medium + 1 Low, all resolved). **Scope corrected by the audit**: there is no "brightness" control and no margins control — scope is the two real typography sliders (font-size, line-spacing) + the font-family picker; the design's 2-option typeface pill conflicts with the 3-shown/5-persisted `ReaderFontFamily` model, so the pill re-skins the existing option set (reducing the font set is carved out as a separate `IDEA` row). Status TODO → PLANNED. **2026-05-18 (feature-cron Gate 3-6)** — both WIs implemented, audited, merged (PR #853). Status PLANNED → DONE; Gate-5 end-to-end VERIFIED verification pending. GH: #824 |
| 65 | AI sheet tab-body re-skin — Summarize / Chat / Translate contents | AI/* | Medium | VERIFIED | **Plan**: `dev-docs/plans/20260518-feature-65-ai-sheet-tab-body-reskin.md` (v2 — Gate-2 Codex audit `019e3b84` clean, round 1: 4 High/4 Medium/1 Low all resolved; scope chips omitted → carved to a follow-up feature, 3-WI pure re-skin). **Filed 2026-05-17** — component re-skin audit finding. **Problem**: the AI sheet chrome + header are v2 (`AIReaderPanel.swift` + `AIReaderPanelHeader.swift`), but the three tab **bodies** are not re-skinned (`AIReaderPanel.swift:152-181`) — Summarize is a plain `sparkles` placeholder + native button (no scope chips, no styled summary card with Save/Share/Regenerate, no suggested questions); `AIChatView` lacks the designed accent/sparkle bubbles; Translate routes to a plain `TranslationPanel`/`BilingualView` with no language rail. **⚠ Gap against feature #60's VERIFIED status**: #60 acceptance criterion (e) is literally *"AI sheet Summarize/Chat/Translate tabs match design"* and Gate-5b recorded `result: pass` for all 7 criteria — but the tab bodies do not match. #60's evidence (`dev-docs/verification/feature-60-20260516.md`) overstated coverage of (e). **Design source**: `vreader-panels.jsx` — `SummaryView`, `ChatView`/`ChatBubble`, `TranslateView`. **Lineage**: v2 follow-on of feature #60. **2026-05-19 (Gate 3-6)** — all three WIs implemented, audited, merged: WI-1 Summarize (#870), WI-2 Chat (#871), WI-3 Translate (#873). Status IN PROGRESS → DONE; shipped in v3.31.0. **2026-05-19 (Gate-5b device verification — `result: partial`)**: `dev-docs/verification/feature-65-20260519.md` — Debug build of `main` (`e30f769`, v3.31.7 build 467) on iPhone 17 Pro Sim (iOS 26.4.1), driven via the `sim-drive-fallback` toolkit (computer-use is down — virtual-display-only host). 9/12 surfaces PASS on device: Summarize idle + error states, Chat empty state + v2 pill input bar (conditional accent send button) + accent user bubble (asymmetric corner), Translate language pill rail + idle + error states + pill-tap-fires-translation; unit gate 51 tests/5 suites green. **3 surfaces DEFERRED** — the AI-response cards (Summarize summary card `.complete`, Chat assistant sparkle-avatar bubble, Translate translation result card `.complete`): not device-reachable because the sim's AI provider returns HTTP 401, there is no valid AI credential in this environment (`.secrets/.env` `OPENROUTER_API_KEY` empty), and DebugBridge exposes no AI-stub command. No code defect observed — every rendered surface matched the design; the gap is a tooling/credential limitation, so no bug filed. Status stays **DONE** (not VERIFIED) per `SCHEMA.md` partial semantics. Follow-up to flip → VERIFIED: a Gate-5 run with a valid AI credential OR a new DebugBridge AI-stub command, exercising the 3 deferred cards end-to-end. **2026-05-21 — Gate-5b attempt 2 (`result: partial`)**: `dev-docs/verification/feature-65-20260521.md` (v3.38.29 `5547d390`). Both prior prerequisite blockers are now lifted — Bug #237 (`--enable-ai` consumed via `AITestOverride.forceAvailable`) AND Bug #243 (DebugBridge `provider` URL family) both shipped, and a valid `OPENROUTER_API_KEY` is now in `.secrets/.env`. The new XCUITest `Bug93GeneralChatPersistenceVerificationTests` (added in this same iteration) drives the same `AIChatView` component the AI sheet's Chat tab uses — rows 4 + 5 (Chat empty + v2 pill input bar, accent user bubble) now have XCUITest-grade evidence on v3.38.29. **Rows 3 / 6 / 11 still DEFERRED** — a third structural blocker emerged: the hybrid host-driver / XCUITest harness gap. The AI-response-card rendering needs the provider URL configured between `XCUIApplication.launch()` and the AI-sheet interaction phase, but `xcrun simctl openurl` from inside an XCUITest sandbox fails (Bug #1054). No code defect observed. Flip → VERIFIED requires ONE of: CU restored, a hybrid host-driver harness, or a DebugBridge AI-stub command. **2026-05-21 — Gate-5b attempt 3 (`result: partial`, 11/12 PASS)**: `dev-docs/verification/feature-65-20260521-VERIFIED.md` (v3.38.42 `48c2b796`). Bug #255 / GH #1112's DebugBridge `ai?action=<summarize\|chat\|translate>` command shipped — the missing AI-action trigger — firing each tab's action CU-free from the host via the real chrome-button view-model path (Bug #1054 sidestepped). **Rows 3 + 6 now PASS** — `ai?action=summarize&scope=chapter` rendered the Summarize `.complete` card with a real OpenRouter (`openai/gpt-4o-mini`) summary; `ai?action=chat&text=…` rendered the Chat assistant sparkle-avatar bubble with a real reply. **Row 11 (Translate result card) still not pixel-confirmed** — the translate action fires with no error and the ORIGINAL card + language pill rail render, but `TranslationResultCard` renders *below the fold* of the AI sheet's default `.medium` detent (tall auto-extracted ORIGINAL card stacked above it); reaching `.large` or scrolling needs a gesture, and CU interaction/screenshot is structurally unavailable (`CU display unavailable`) with no DebugBridge scroll/detent command. No code defect. Flip → VERIFIED requires ONE of: CU restored, or a DebugBridge `present?sheet=ai&detent=large` (or scroll) param so the below-fold result card can be captured. **2026-05-26 — Gate-5b attempt 4 (`result: partial`, 11.5/12)** (v3.39.36): with Bug #256's `detent=large` now shipped, re-ran row 11 CU-free (Read-the-`simctl io screenshot` is CU-independent). **New progress**: confirmed the Translate **`.complete` card RENDERS** — observed the loading→result transition (screenshot 1 "Translating…" with the designed accent spinner → screenshot 2 the `.complete` `TranslationResultCard` showing the ORIGINAL portion), and the v2 chrome + language pill rail + ORIGINAL card + loading state all match design (artifacts `feature-65-row11-miniepub-{translate,complete}-20260526.png`). **Residual (the SOLE remaining piece)**: `detent=large` is insufficient — the tall auto-extracted ORIGINAL card alone exceeds the full-height sheet (true on both `war-and-peace` and `mini-epub3`), so the accent-tinted translation card (sibling below ORIGINAL in the `TranslationResultCard` ScrollView) stays below the fold; its pixel design can't be confirmed CU-free without a scroll command. **Filed Bug #271 / GH #1188** (DebugBridge cannot scroll presented sheet content — the concrete unblock). Stays **DONE**; flip → VERIFIED gated on Bug #271 (scroll command) OR CU restored. No code defect. **2026-05-27 — Gate-5b attempt 5 (`result: pass`, 12/12) → VERIFIED**: `dev-docs/verification/feature-65-20260527.md` (v3.39.38 `c513bf7f`). Bug #271 / GH #1188's `vreader-debug://scroll-sheet?to=bottom` shipped in this same build — the final unblock. Row 11 now PASS: `present?sheet=ai&tab=translate&detent=large` + `ai?action=translate&text=Spanish` → `.complete` accent card below the fold (`feature-65-row11-large-before-scroll-20260527.png`); `scroll-sheet?to=bottom` → full accent-tinted SPANISH translation card in view (`feature-65-row11-scrolled-accent-card-20260527.png`) — accent gradient + accent border + accent semibold target-language label, matching `TranslateView` (Speak/Notes intentionally omitted per plan §2.2). Rows 1-10 + 12 carried PASS from attempts 1-3 (unchanged code; full suite 7237 tests/0 failures green on `c513bf7f`). All 12 surfaces PASS → **VERIFIED**. GH: #823 |
| 64 | Unified cross-format highlight-action popover — one styled popover for tap-on-highlight across TXT/MD/PDF/EPUB/AZW3 | Reader/* | Medium | VERIFIED | **VERIFIED 2026-05-21 round-4** — Gate-5b round-4 against v3.38.29 (build 604, commit `d7e20496`, merge of PR #1091 — the third EPUB DebugBridge open chain fix). All 4 highlight-supporting formats PASS end-to-end: TXT (`highlightCount: 0 → 1`), MD (`highlightCount: 0 → 1`), EPUB (`highlightCount: 0 → 1` on 2 independent attempts, with both the Stage-1 early-settle fallback path AND the happy `didFinish` path exercised in the wild), AZW3 Foliate (clean settle, `highlightCount: 0` documented no-op). PR #1088's `[com.vreader.app:epub] loadFileURL` log line — *absent* in round-3's filtered stream and the diagnostic that triggered Bug #252 — is now *present* on every EPUB attempt; the round-3 inference-from-absence is closed by direct observation. Popover-visual criteria (#2–#8) remain DEFERRED due to a continuing 4-session CU outage, but no longer block VERIFIED (unit-test + DebugBridge chain coverage is adequate; a follow-up DebugBridge popover-state command is parked for the future). Evidence: `dev-docs/verification/feature-64-20260521-round4.md`. Bug #1089 (#252) device-verification close-gate also satisfied by this round (same evidence file cited). **2026-05-21 — Gate-5b round-3 acceptance attempt: `partial`.** Against v3.38.27 (build 602, the release that ships BOTH Bug #1085 + Bug #1086 fixes). TXT + MD highlight-create paths PASS (`highlightCount: 0 → 1`, no error). AZW3 Foliate path PASS (clean settle, `highlightCount: 0` as documented no-op). EPUB STILL BLOCKED — `vreader-debug://settle` returns `error: "settle timeout"`, and PR #1088's new `loadFileURL` / `didFinish` / `didFail*` logs do not appear in the filtered `subsystem == "com.vreader.app"` stream — strongest inference from absence is that the run does not reach the `loadFileURL` log site inside `EPUBWebViewBridge.updateUIView`, so the failure is upstream of the bridge at the host / route / representable layer (inference, not direct observation — needs host-layer / `makeUIView` instrumentation). **Filed Bug #252 (GH #1089)** — likely shared root cause with Bug #244 (user-triage EPUB blank content). CU still down for the 3rd consecutive session. Row stays at DONE; GH #822 stays open with `awaiting-device-verification`. Evidence: `dev-docs/verification/feature-64-20260521-round3.md`. **2026-05-20 — DONE: all 10 WIs merged.** WI-1..WI-10 shipped to `main` (PRs #967, #969, #970, #971, #973, #990, #997, #1000, #1002, #1006); the unified cross-format highlight-action popover is live on TXT/MD/PDF/EPUB/AZW3 and the superseded feature-#55/#53 highlight surfaces were torn down in WI-10. WI-10's PR omitted the row status flip — this corrects the drift. Gate-5 final acceptance is pending: GH #822 carries `awaiting-device-verification`; the CU-free XCUITest acceptance path is blocked by the highlight-creation-gesture harness defect (GH #986). **2026-05-19 (Gate 3 — WI-1)** — WI-1 (foundational: `HighlightPopoverContent` / `HighlightPopoverMode` / `HighlightPopoverForm` / `HighlightMutationOutcome` / `HighlightPopoverAction` value types + `HighlightPopoverViewModel`) implemented TDD (37 tests, 4 suites), Gate-4 Codex-audited (`019e4055`, 1 round, ship-as-is), PR opened. Status PLANNED → IN PROGRESS. **Gate 1+2 complete — plan dev-docs/plans/20260519-feature-64-unified-highlight-popover.md, Codex-audited clean.** **2026-05-19 — RE-SCOPED (user decision).** The #949 design landed in commit `84aee57` and #949 is closed — the prior `BLOCKED: needs-design (#949)` clause is resolved. Gate-2 round-3 audit found feature #55 (note-preview-on-tap, shipped) invalidated the original premise — tapping an EPUB/AZW3 highlight now opens the #55 read-only note preview (Share / Open-in-panel only), and the #53 long-press "Delete Highlight" menu survives only on TXT/MD/PDF. Re-scoped from "styled popover replacing the bare Delete UIMenu" to a **unified cross-format highlight-action popover** — one styled card (excerpt, color row, note view/edit, copy/share/delete) for tap-on-highlight on all 5 formats. PR #946 (round-3 audit record) closed unmerged — superseded by the rewrite. The unified card must reconcile two separate designed surfaces (`vreader-note-preview.jsx` #55 callout + `vreader-reader.jsx` `HighlightActionPopover`) into one; carried through claude.ai/design before the Gate-1 plan rewrite. Subsumes needs-design #914. Original plan `dev-docs/plans/20260518-feature-64-highlight-action-popover.md` to be rewritten post-design. **Filed 2026-05-17** — component re-skin audit finding. **Problem**: tapping an existing highlight shows a bare native `UIMenu` with one "Delete Highlight" item (`HighlightActionPresenter.swift:80-94`) — feature #53 (DONE) shipped this minimal version. The design's `HighlightActionPopover` (`vreader-reader.jsx:599-731`) is a styled card: quoted serif excerpt with a colored left border, a 4-circle color-change row, inline note display/edit textarea, and an Edit-note / Copy / Share / Delete action row. Distinct from the new-selection `SelectionPopoverView` (already shipped). **Design source**: `vreader-reader.jsx` — `HighlightActionPopover`. **Lineage**: Refs feature #60 (visual identity v2) + feature #53 (the minimal version this supersedes). GH: #822 |
| 63 | Search results panel v2 re-skin — bring the in-reader `SearchView` onto the visual-identity-v2 chrome | Search/* | Low | VERIFIED | **Filed 2026-05-17** — a feature #60 follow-up gap surfaced when auditing the design bundle against shipped state (Codex audit thread `019e317f` confirmed). **Problem**: `SearchView.swift` still uses pre-v2 chrome (`NavigationStack` + `.searchable` + plain `List`). Feature #60's plan explicitly listed "search results panel" as out-of-scope-for-v1; no row picked it up; #60 reached VERIFIED without it. **Design source**: `dev-docs/designs/vreader-fidelity-v1/project/vreader-search.jsx` (scope toggle This-book / All-books, recent searches + FTS5 hints on empty, results grouped by chapter/book with bolded match terms). **Scope**: behavior-preserving visual re-skin only — search itself (FTS5, result-tap nav) unchanged. **Lineage**: v2 follow-on of feature #60 (VERIFIED, terminal) — a new feature per the close gate, not a #60 reopen. **2026-05-18 (feature-cron Gate 1+2)** — implementation plan drafted: `dev-docs/plans/20260518-feature-63-search-panel-reskin.md` (v3, 2 WIs, both behavioral). Gate-2 independent audit **PASSED** (Codex `019e39f1`, 2 rounds; 2 High + 3 Medium + 1 Low, all resolved). **Scope reconciliation by the audit**: the committed design exceeds this row's "behavior-preserving re-skin only" scope — the design's **"All books" library-wide scope toggle** and **"Recent searches" history** are new capabilities, carved OUT and recommended as separate `IDEA` rows for `/triage`; the design's per-result `p.{page}` badge is dropped (`SearchResult` has no page field — most formats have no page number). The re-skin groups results by the existing `SearchResult.sourceContext` string with no `SearchService` change. Status TODO → PLANNED. **2026-05-18 (feature-cron Gate 3-6)** — both WIs implemented, audited, merged (PR #854). Status PLANNED → DONE; Gate-5 end-to-end VERIFIED verification pending. **2026-05-19 (verify-cron Gate 5b) — result `partial`, stays DONE** (`dev-docs/verification/feature-63-20260519.md`) on iPhone 17 Pro Sim (iOS 26.4.1) at merged `main` `fd96f93` (v3.31.11, build 471). All five user-facing criteria **PASS** via manual device verification (CU down — DebugBridge + `sim-drive-fallback` toolkit): C1 custom in-sheet search bar (glyph + bound `TextField` + clear button + accent "Cancel", no system `.searchable`/`NavigationStack` "Done"); C2 restyled idle prompt (`SearchPromptView` — "SEARCH THIS BOOK" + FTS5 explainer); C3 grouped results list (opened `mini-epub3.epub`, searched "Chapter" → "6 matches in 2 sections", two per-`sourceContext` group cards with serif bolded-match snippet rows + chevrons); C4 restyled no-results state (`SearchNoResultsView` — glyph disc + serif "No matches for …"); C5 behavior preserved (clear button empties query; result-tap dismisses the sheet and navigates the reader to the result's `Locator`). **Blocked from VERIFIED by a UITest regression** the re-skin introduced: all 6 search-sheet UITests (`ReaderSearchSheetTests`, `SearchSheetPlaceholderTests`, `TXTSearchTapHighlightNavigationTests`) fail because PR #854 swapped `SearchView`'s `NavigationStack` root for a `VStack`, so the `searchSheet` accessibility identifier propagates to leaf views instead of resolving as an `app.otherElements` container — filed **bug #223 / GH #891** (same class as bug #209). #63 stays `DONE`; flip to `VERIFIED` once #223 lands and the 6 UITests pass. Verification noted a separate pre-existing TXT search-pipeline gap (`war-and-peace` fixture's `segment_base_offsets` empty → TXT hits don't resolve) — outside #63's scope, not filed against #63. No #63 code changed (verification scope). **2026-05-19 (Gate 5 final — verify, result `pass`) → VERIFIED** (`dev-docs/verification/feature-63-20260519-cu-free.md`) on iPhone 17 Pro Sim (iOS 26.4.1, Debug). The #63 exit condition is met: bug #223 / GH #891 (`searchSheet` identifier propagation) and bug #224 / GH #902 (`SearchBar` 44 pt touch targets) both landed (PRs #905, #920), and the 6 search-sheet UITests (`ReaderSearchSheetTests`, `SearchSheetPlaceholderTests`, `TXTSearchTapHighlightNavigationTests`) re-run green on `origin/main` `c534e0a` (`Executed 6 tests, with 0 failures`). The prior pass verified C1-C5 only by manual inspection + pure-function unit tests; this run closes the C2/C3/C4 XCUITest-coverage gap with a new CU-free suite `vreaderUITests/Verification/Feature63SearchPanelVerificationTests.swift` (5 tests, all green) — C1 custom in-sheet search bar (no `XCUIElementTypeSearchField`), C2 restyled idle prompt (`searchEmptyPromptView`), C3 grouped results list (`searchResultsList` ScrollView + `searchResult_*` rows + "{N} matches in {M} sections" count line), C4 restyled no-results state (`searchNoResultsView` + "No matches for …" headline), C5 clear-button-empties-to-idle (result-tap nav covered by `TXTSearchTapHighlightNavigationTests`, Cancel by the two dismiss tests). Suite queries the re-skinned views by element TYPE (`staticTexts`/`scrollViews`/`buttons`) — bug #223's `.accessibilityElement(children: .contain)` collapses `searchSheet` into one container and propagates child identifiers onto leaves (the bug #214 lesson, EPUB `.epubFixture` seed used so hits resolve via `href`). Continues task #236 (CU-free Gate-5 for the DONE tier), per the #54 pilot. No #63 product code changed. GH: #802 |
| 70 | Cross-format font-size perceptual calibration — unified pt→logical-size mapping so one numeric font-size value renders at a consistent perceived size across TXT/EPUB/PDF/AZW3/MD | Reader/* | High | VERIFIED | **Split from Bug #166 / GH #491 on 2026-05-17.** Bug #166 had two halves: (1) slider ceiling too low — **FIXED**, `TypographySettings.fontSizeRange` raised `12...32`→`12...64` (PR #516); (2) cross-format perceptual inconsistency — the same numeric value renders at visually different sizes because each format pipes font size through a different renderer (TXT/MD = `UITextView` native pt; EPUB = CSS `font-size` injected into WKWebView, compounding with the book's own stylesheet; PDF = `PDFView` scale; AZW3/MOBI = Foliate-js JS/CSS). Half (2) is a never-implemented normalization layer — per-renderer measurement + a unified pt→logical-size mapping — feature-class per rule 47, not a bug fix (so classified independently by `TypographySettings.swift:53-56` and the Bug #166 row, both pre-dating this split). Bug #57 (FIXED) calibrated EPUB-vs-TXT pairwise only; this is the general N-format layer. **Problem**: users switching formats see jarring size jumps even though the setting number is unchanged. **Scope sketch**: a `FontSizeCalibrator` mapping the stored unified value → per-renderer values, threaded through the 5 rendering paths; the slider-ceiling work is explicitly out of scope (already shipped). **Acceptance sketch**: same numeric value → perceptually consistent text across all formats; no regression in bug #57. **2026-05-19 — renumbered #64→#70** (next free number; a separate #64 "Styled highlight-action popover" row, GH #822, kept the #64 slot — collision resolved). **2026-05-19 (feature-cron Gate 1+2)** — implementation plan drafted: `dev-docs/plans/20260519-feature-70-font-size-calibration.md` (v3, 4 WIs: WI-1 `FontSizeCalibration` value types + `FontSizeCalibrator` mapper [foundational], WI-2 route TXT+MD via `ReaderSettingsStore` [behavioral], WI-3 route EPUB [behavioral], WI-4 first-time AZW3/MOBI font-size wiring through the live `FoliateSpikeView` path + final acceptance [behavioral]). Gate-2 independent audit **PASSED** (Codex `019e3ebb`, 3 rounds; round 1 = 1 Critical + 3 High + 2 Medium + 1 Low, round 2 = 1 Medium + 1 Low, round 3 clean — zero open Critical/High/Medium). **Key audit corrections**: (a) the live AZW3/MOBI host is `FoliateSpikeView` (NOT the dormant `FoliateReaderContainerView`) and has **no font-size wiring at all today** — WI-4 is first-time plumbing; (b) PDF has no text-size input (`autoScales`) — excluded from calibration by design; (c) calibration parity is scoped to the default content-size category (TXT's `UIFontMetrics` scaling vs MD/WebView non-scaling diverges under Dynamic Type — documented known limitation). Status TODO → PLANNED. **2026-05-19 (Gate 3 — WI-1)** — WI-1 (foundational: `FontSizeCalibration` value types + `FontSizeCalibrator` mapper) implemented TDD, Gate-4 Codex-audited (`019e3efb`, 2 rounds, ship-as-is), PR #937 opened. Status PLANNED → IN PROGRESS. **2026-05-19 (Gate 3-6 — all 4 WIs shipped)** — WI-1 `FontSizeCalibration`/`FontSizeCalibrator` (PR #937, v3.34.16); WI-2 route TXT+MD via `ReaderSettingsStore` (PR #941, v3.34.18, Codex `019e3f2f` 1 round ship-as-is); WI-3 route EPUB (PR #945, v3.35.2, Codex `019e3f41` 2 rounds ship-as-is); WI-4 first-time AZW3/MOBI font-size wiring through the live `FoliateSpikeView` + a `mini-markdown` DebugBridge fixture for the acceptance pass (v3.36.0, Codex `019e3f56` 3 rounds ship-as-is). **Gate-5 final acceptance PASS** — `dev-docs/verification/feature-70-20260519.md` (`result: pass`) on iPhone 17 Pro Max Simulator, all 8 acceptance criteria verified: at unified 28 TXT/MD render at 28 UIKit pt (anchor), EPUB computed `body` font-size = 31.4px (`.epub` 1.12×), AZW3/MOBI calibrated CSS = 31px; AZW3/MOBI font-size slider — a no-op before this feature — now visibly resizes the reader text (28→12 confirmed via before/after screenshots + the stashed `__vreaderTargetThemeCSS` global); Bug #57 cascade not regressed; PDF unchanged; no schema migration. Status IN PROGRESS → DONE → VERIFIED. GH: #491 |
| 71 | EPUB scroll-mode continuous cross-chapter scroll — reading flows past the end of one chapter into the next without a manual page-turn / chapter-button tap (the deferred §2.3 half of Bug #165) | Reader/EPUB | Medium | VERIFIED | **VERIFIED 2026-05-28** (`dev-docs/verification/feature-71-20260528-verified.md`, result=pass) — all SIX acceptance criteria pass on the merged v3.40.0 build (terminal-WI commit `afd34f81`, PR #1216): (1) continuous cross-chapter reveal via real touch scroll (ALPHA→BRAVO→CHARLIE→DELTA, window `[0,1]`→`[1,2,3]`); (2) no manual tap; (3) no reload flash (one stitched `#vreader-scroll-root` document); (4) position persists + restores across the seam (re-open restored mid-BRAVO `scrollTop:9970`); (5) paged-mode UNREGRESSED (`-readerEPUBLayout paged` → `continuousActive:false`, legacy single-chapter path, "Chapter One" renders); (6) highlight (per-section restore) + search (locator-based) unregressed (full suite 0 failures). **Default-ON activation confirmed**: launched v3.40.0 with NO flag override → `#vreader-scroll-root` present, `sectionsInDOM:[0,1]`. Terminal flag-flip WI shipped in **v3.40.0** (PR #1216): flipped `FeatureFlags.epubContinuousScroll` default OFF→ON + fixed a Codex-caught nested-EPUB cross-directory `<link>` CSS mis-resolution the flip would otherwise have exposed (rewriter resolves the link href against the chapter dir before the loader; flat EPUBs unchanged). Codex Gate-4 3 rounds, ship-as-is. **IN PROGRESS 2026-05-27** — WI-1 `EPUBSpineWindow` (PR #1154, v3.39.11), WI-2 `EPUBChapterBodyRewriter` (PR #1155), WI-3 `EPUBContinuousScrollJS` (PR #1162), **WI-4 `EPUBContinuousScrollCoordinator`** (this PR — `@MainActor` window-transition decision logic + `EPUBScrollBoundarySignal` + async-throwing evaluator + generation token; window mutates only after a successful eval; incremental gen-fenced eviction; `EPUBSpineWindow.reanchored(to:)` + `span` added; Codex Gate-4 thread `019e6449`, 3 rounds, ship-as-is — 1H/3M/1L all fixed; 35 unit tests) all merged. **WI-5 `continuousScroll:` bridge plumbing** (PR #1192, v3.39.40 — config input, mode-branched observer injection replacing `progressTrackingJS`, `continuousScrollHandler` parse + windowed progress, section-scoped selection href w/ cross-section clamp; Codex 3 rounds ship-as-is) merged. **WI-6 (container integration) split into 6a/6b** (plan v4, 2026-05-27, Gate-2 re-audit thread `019e6683` — the original Large WI-6 carried an unresolved `evaluate`-late-binding gap; re-audit surfaced 1 Critical + 3 High/Med WI-6b requirements, all folded into the plan's "WI-6b design requirements"). **WI-6a (container-integration foundations)** (this PR — `EPUBContinuousChapterProvider` spine→`EPUBChapterBody` factory + `EPUBWebViewEvaluatorHandle` late-binding seam resolving the coordinator-evaluate gap; both `@MainActor`, unit-testable, no live-render change; Codex Gate-4 no-findings ship-as-is; 7 tests) merged. **WI-6b (live container wiring) shipped in slices** — **WI-6b-i** core live rendering (v3.39.43: bridge bootstrap-doc load-path, coordinator instantiation gated on `FeatureFlags.epubContinuousScroll && epubLayout == .scroll`, `sectionMaterialized` lifecycle, `EPUBWebViewEvaluatorHandle` bind; Codex 3 rounds ship-as-is — the navigation no-op Critical resolved by flag-gating the mode dark), **WI-6b-ii** per-section highlight restore (v3.39.44: `restoreHighlightsInSectionJS` + `.vreader-chapter-content` wrapper + CSS-scoper root-collapse onto the wrapper; Codex 3 rounds ship-as-is), **WI-6b-iii** saved-position restore (v3.39.46, PR #1199: `restoreFraction` threaded through `materializeInitialWindow`; Bug-#163 webView inset already handles safe-area so the bootstrap inset was reverted; Codex ship-as-is) — all merged. **WI-8 continuous-mode navigation** (v3.39.47, PR #1201: `EPUBContinuousScrollCoordinator.navigate(toSpineIndex:fraction:)` — in-window scroll+reanchor / out-of-window atomic clear+rebuild; container routes `.readerNavigateToLocator` through the coordinator in continuous mode, updates position only on a `true` result; holds `isExtending` single-flight lane for the whole method; Codex Gate-4 thread `019e6873` 3 rounds ship-as-is; 25 coordinator tests) merged. **WI-7 bilingual interlinear in continuous mode IMPLEMENTED + MERGED** (v3.39.54, PR #1210, Refs #1200): ported the FoliateBilingualOrchestrator per-`sectionIndex` pattern to EPUB — `blocksBySection` caching + namespaced bids `s{N}b{seq}` + per-section enumerate/inject driven from `.readerBilingualSectionMaterialized`, reinject-all on `.readerBilingualDidChange`, `.readerBilingualSectionEvicted`→`clearBlocks`, live-evaluator routing for enable/disable/enumerate (continuous bridge skips `pendingHighlightJS`), `onSectionEvicted` gen-guarded. Cross-section `bid` bleed closed (Codex Gate-4 thread `019e6939`, 3 rounds 8→5→1, follow-up-recommended; one accepted non-correctness Medium — in-flight guard for the per-section direct-translation fallback, tracked on #1200). Core unit-verified (full suite 7333); live continuous bilingual rendering is CU-gated → #1200 stays `awaiting-device-verification` (same rAF/CU wall as the flag-flip). **WI-8 navigation DEVICE-VERIFIED 2026-05-27** (`dev-docs/verification/bug-273-20260527.md`, result=pass) on iPhone 17 Pro Sim via the new Bug #273 harness (GH #1203, FIXED): the `vreader-debug://navigate?spine=N[&fraction=F]` driver + the `multi-chapter-epub` 4-tall-chapter fixture. In-window `navigate?spine=1` scrolled scrollTop 0→10047 (section 1 at top, DOM unchanged `[0,1]`); out-of-window `navigate?spine=3` rebuilt the DOM `[0,1]`→`[2,3]` (far-side eviction + target-window materialization) with "Chapter Four" first-visible at the viewport top — both WI-8 branches confirmed on device. Landing-consistency nit **FIXED 2026-05-27 (v3.39.50)**: `fillNeighboursAndScroll` skipped its explicit scroll at fraction 0, so out-of-window jumps relied on the prepend's scroll-anchoring and landed `offsetTop − inset` (77px short); added a navigate-only `force` flag so out-of-window jumps deterministically scroll to the anchor even at fraction 0 (in-window already did). Device-re-verified: out-of-window `navigate?spine=3&fraction=0` now lands scrollTop=10047=sec3Top (gap 77→0), "Chapter Four" fills the viewport — both branches now land at exact `offsetTop`. Codex Gate-4 thread `019e68c7`, no findings. **Remaining (terminal WI-8 step): flip `FeatureFlags.epubContinuousScroll` default ON + full end-to-end acceptance pass.** **Terminal gate is CU-blocked (evidence-backed 2026-05-27)**: the one remaining unverified link is the *scroll-driven* forward/backward extension (the feature's core "scroll past chapter end → next chapter materializes"). Probed on device — the continuous scroll observer IS attached (`#vreader-scroll-root.__vreaderScrollObserver == true`) and the `nearBottom` prefetch threshold IS met (scrolled to 40px from `maxScroll`, ≤ the 800px `PREFETCH_PX`), yet the window did not extend, because `continuousScrollObserverJS` throttles `report()` through `requestAnimationFrame` (`EPUBContinuousScrollJS.swift:208`) and rAF is paused/throttled when the WKWebView isn't actively compositing — the virtual-display cron context (no real display). So a *synthetic* `scrollTop=` + dispatched scroll event cannot drive the observer; a *real* touch scroll on a CU-equipped device can. The extend/evict LOGIC is unit-verified (26 coordinator tests incl. boundary-signal handling + eviction) and its DOM-mutation primitives (append/prepend/remove) are device-verified via the WI-8 navigate rebuild (`[0,1]`→`[2,3]`); only the rAF-gated real-scroll trigger is real-device/CU-only. **Therefore the flag-flip-to-default is a CU-equipped (or human) ship decision, not an autonomous-cron action** — flipping default-ON without confirming real-scroll extension would risk a regression for all EPUB scroll-mode users. **Scroll-driven extend/evict RESPONSE now DEVICE-VERIFIED 2026-05-27** (`dev-docs/verification/feature-71-20260527-scroll-driven.md`, result=partial) via the new `vreader-debug://scroll-boundary?spine=N&near=top\|bottom` driver (v3.39.56, PR #1212 — posts an `EPUBScrollBoundarySignal` directly to `handleBoundarySignal`, bypassing the rAF observer): with `multi-chapter-epub`, `near=bottom`@spine1 extended `[0,1]`→`[0,1,2]` (next chapter materializes), `near=bottom`@spine2 → `[1,2,3]` (forward + far-side eviction), `near=top`@spine1 → `[0,1,2]` (reverse prepend + scrollTop compensated to 9971, no jump). So forward extension + eviction + reverse-scroll prepend are confirmed live. **Real-scroll rAF observer FIRING now DEVICE-VERIFIED 2026-05-28** (`dev-docs/verification/feature-71-20260527-realscroll.md`, result=pass) — computer-use became available this session (real 4K display attached; WKWebView `document.visibilityState === "visible"`, so `requestAnimationFrame` is no longer paused). With real CU touch-drag gestures on iPhone 17 Pro Sim + `multi-chapter-epub`: (1) rAF self-loop probe ticked 175× in ~1.5s (the prior virtual-display blocker is gone); (2) a real swipe-up near the bottom fired the observer's `report()`, which posted `{visibleSpineIndex:1, nearBottomBoundary:true}` to `continuousScrollHandler` (hook-captured); (3) from a pristine re-open, window `[0,1,2]` + ~9 real swipe-up drags across the 800px `PREFETCH_PX` threshold → window `[1,2,3]` (DELTA materialized, spine 0 evicted, `maxScroll` 19087→29057), reaching "Chapter 4 of 4" by pure continuous scroll; (4) continuous flow ALPHA→BRAVO→CHARLIE→DELTA in ONE document, no chapter-stop/reload flash. **The previously-cited regression risk for the flag-flip ("flipping default-ON without confirming real-scroll extension") is RETIRED — real-scroll extension is confirmed.** Other terminal polish from the edge-case list (bottom-clip, seam highlights, very-large-chapter memory) remains pending and is independent of this link. The feature stays `IN PROGRESS` pending the terminal flag-flip ship decision (its own Gate-3→6 cycle: default-assertion test + Codex audit + version bump + PR + full acceptance pass → `DONE`/`VERIFIED`). **WI-8 navigation + landing fix are unit- AND device-verified; the navigation primitive is complete.** **PLANNED 2026-05-25** — Gate-1 plan `dev-docs/plans/20260525-feature-71-epub-continuous-scroll.md` (v3, 8 WIs). **Gate-2 Codex audit (thread `019e5f97`) 3 rounds, final verdict `sound-with-low-notes`, no open Critical/High/Medium.** Codex caught 12 findings; the central one reframed this from "late bridge wiring" to an **early DOM-contract change** (per-chapter base-URL loss, document-relative anchors, per-chapter CSS isolation, mode-specific script injection), restructuring 6→8 WIs. **Chosen architecture**: lazy ±1 chapter window stitched into ONE scrollable `WKWebView` document (append/prepend rewritten chapter DOM on scroll, far-end eviction), NOT a Foliate-engine re-route (rejected — would re-anchor highlights/search/TOC/position onto CFI, far larger). WI-1 `EPUBSpineWindow` (pure window value type) is the foundational first WI. **Filed 2026-05-21 (user triage — "epub cant continue reading while scrolling to the end of the chapter").** Split out of **Bug #165 / GH #489**, whose v3.38.36 fix delivered ONLY design §2.2 (paged-mode side-tap chapter wrap via `EPUBChapterNavigationRouter`); the FIXED note explicitly defers §2.3 (scroll-mode continuous cross-chapter scroll) to a follow-up **feature** — *"architectural rewrite of `EPUBWebViewBridge`'s single-chapter-per-load model."* No feature row was actually filed at that time; this row is it. **Problem**: in EPUB scroll layout, scrolling to the end of a chapter stops at the chapter boundary — the reader does not flow into the next chapter; the user must tap a Next-chapter affordance to continue. This breaks the continuous-reading convention that TXT (Bug #180, FIXED) and AZW3/MOBI (Bug #235, FIXED) already satisfy; EPUB is the remaining format without it. **Why feature-class (not a bug like #180/#235)**: `EPUBWebViewBridge` loads ONE chapter (spine item) per `WKWebView` load. Continuous cross-chapter scroll requires rendering / stitching adjacent chapters within one scroll context (or a virtualized multi-chapter DOM), plus locator/position math across the seam, plus highlight/search/CFI anchoring that currently assumes a single loaded chapter — an architectural change to the bridge, not a contained fix. TXT/AZW3 were contained because their renderers (UITextView/UITableView; Foliate-js section paginator) already had a natural continuous-scroll seam to wire. **Design already exists**: `dev-docs/designs/vreader-fidelity-v1/project/design-notes/reader-navigation.md` §2.3 (continuous cross-chapter scroll in scroll mode) — so the eventual plan is not design-blocked. **Scope sketch**: rework `EPUBWebViewBridge` to render N adjacent chapters in one scroll view (or progressively append/prepend chapter DOM on scroll), preserve CFI/locator across the seam, keep highlight + search anchoring working, sync `ReadingProgressBar` + position persistence to the multi-chapter coordinate. **Edge cases (to expand in the plan)**: first/last chapter bounce; very large chapters + memory; reverse scroll (prepend previous chapter); highlight created across a chapter seam; search-result tap landing mid-stitch; bilingual interlinear injection interacting with multi-chapter DOM; reopen-at-saved-position mid-chapter. **Acceptance sketch**: scrolling past a chapter end continuously reveals the next chapter with no manual tap and no visible reload flash; position persists + restores across the seam; existing EPUB paged-mode (§2.2, Bug #165) + highlight + search unregressed. **Cross-ref**: Bug #165 (GH #489, FIXED — §2.2 paged wrap), Bug #180 (TXT continuous scroll, FIXED), Bug #235 (AZW3 continuous scroll, FIXED). Reported by user 2026-05-21. GH: #1150 |
| 72 | HTTP cloud TTS provider integration — wire the orphaned `HTTPTTSProvider` into the live read-aloud path so a configured cloud-TTS endpoint actually speaks | Reader/* | Medium | VERIFIED | **VERIFIED 2026-05-26** (`dev-docs/verification/feature-72-20260526.md`, result=**pass**, commit `c5f2dd16`, v3.39.29 build 650). All 6 acceptance criteria pass (C1 cloud-config-speaks, C2 state machine + control bar, C3 chunk-level highlight/scroll, C4 unconfigured→on-device fallback, C5 failure→idle, C6 end-to-end). Verified via the close-gate **verification-exception** path — a high-fidelity integration test (`vreaderTests/Integration/Feature72CloudTTSIntegrationTests.swift`, 3 tests) drives the real stack `TTSService → HTTPSpeechSynthesizer → HTTPTTSProvider` with only the network transport (`URLSessionProtocol`) and audio backend (`SpeechAudioPlaying`) stubbed (no live third-party HTTP TTS server available); the test records + asserts the real per-chunk HTTP request contract (method/URL/header/expanded body), so it is not wiring-only. **WI-0..WI-3 shipped** (PRs #1176/#1177/#1178/#1179 — `SpeechSynthesizing.delegateTarget` + generic wiring; `HTTPTTSConfigStore`; `HTTPTTSChunkPlayer`; `HTTPSpeechSynthesizer` adapter + `defaultSynthesizer()` selection). **WI-4/WI-5 (this verification PR)**: the failure-path integration test (C5) uncovered a real end-to-end defect — a cloud synthesis failure fired `didCancel` while `state == .speaking`, and `TTSService`'s old restart guard (`ignore cancel while speaking`) swallowed it, wedging the control bar in `.speaking` with no audio. **WI-5 fix**: replaced the heuristic with a `pendingRestartCancels` counter armed from the `stopSpeaking()` result (not the lagging `@Observable` state — Codex Gate-4 round-2 caught a leak in the state-based first attempt), so a restart-cancel of the outgoing utterance is ignored while a terminal failure-cancel returns to idle; restart-race regressions stay green. Gate-4 audit thread `019e63c5`, 4 rounds, ship-as-is. 61 TTS tests across 6 suites GREEN on iPhone 17 Pro Sim (iOS 26.4). — lifts Bug #270 / GH #1166 into a feature (the cloud-TTS integration was never implemented; `HTTPTTSProvider` is configurable in Settings but never constructed in production — `TTSService` always drives the on-device `SystemSpeechSynthesizer`). Also corrects Feature #26's C6 gating (C6 is unimplemented, not merely unverifiable). **Gate 1 plan**: `dev-docs/plans/20260526-feature-72-http-tts-integration.md`. **Gate 2 Codex audit thread `019e6389`, 2 rounds, CLEAN** (round-1: 5 High + 2 Medium — wrong API names [`validate()` not `isValid`; `synthesize` is `async throws`; `SpeechSynthesizing` has no `delegateTarget`], non-generic delegate wiring in `TTSService`, drifting UTF-16 offset map [`chunkText` trims whitespace], sticky `HTTPTTSProvider.cancel()`, no existing TTS error path, AVAudioSession ownership conflict, WI-4-too-late — all resolved in v2; round-2 clean, zero open Critical/High/Medium). **Architecture**: a `SpeechSynthesizing` adapter (`HTTPSpeechSynthesizer`) wrapping `HTTPTTSProvider` + an `AVAudioPlayer` chunk-playback queue, selected by `TTSService.defaultSynthesizer()` when `HTTPTTSConfig.validate() == .valid`; emulates `AVSpeechSynthesizerDelegate` callbacks (chunk-level `willSpeakRange` against the ORIGINAL utterance, `didFinish`, `didCancel`-on-failure). No new UI (rule 51 N/A). **WIs (v2)**: WI-0 add `delegateTarget` to `SpeechSynthesizing` + generic delegate wiring; WI-1 `HTTPTTSConfig` load/validate accessor; WI-2 `HTTPTTSChunkPlayer` (AVAudioPlayer queue behind a test seam, no session mgmt); WI-3 `HTTPSpeechSynthesizer` adapter (fresh provider per utterance, failure→`didCancel`) + `defaultSynthesizer()` selection; WI-4 verification (real endpoint / high-fidelity integration test) + `architecture.md`. Acceptance: cloud config → speaks; state machine + control bar work; chunk-level highlight/scroll; no/invalid config = unchanged on-device; failure stops cleanly to idle; end-to-end verified. GH: #1174 |
| 73 | Foliate scrolled-mode continuous rendering surface — replace the one-section-at-a-time view-swap substrate in `paginator.js` with a continuous multi-section surface so AZW3/MOBI/KF8 **scroll** mode crosses chapter boundaries smoothly | Reader/Foliate | Medium | VERIFIED | **DONE 2026-05-30 (v3.41.0)** — windowed continuous-scroll surface SHIPPED (`#windowedScroll` ON). Resolves Bug #283. 2-round Codex audit ship-as-is; flag-on device-verified (crossing/slide/overlayers/DOM-order/restore). VERIFIED pending the final on-device visual recheck of the shipped build (CU was unavailable at ship time). Routed from **Bug #283 / GH #1260** (the visible chapter-boundary jump) per user 2026-05-30. #283 is a *partial* — AZW3 scroll mode works + auto-advances (Bug #235, FIXED) but isn't smooth; the **missing capability** is a continuous rendering surface, tracked here as the feature. The jump = 3 stacked discontinuities inherent to the per-section swap (D1 `scrollToAnchor(0)` offset reset, D2 blank flash on `#createView` destroy+async-load, D3 async post-swap reflow); a bounded patch can't clear the "no visible jump" bar (assessed + independent Codex review concurred — see GH #1260). AZW3 analog of **Bug #180** (TXT continuous-surface redesign, FIXED) and same class as **Bug #165** (EPUB continuous, open). Touches **vendored Foliate-js** `vreader/Services/Foliate/JS/paginator.js` — high-risk; needs Gate-1 plan + Gate-2 independent audit before any code. Bug #283 stays open, linked here. GH: #1279 **VERIFIED 2026-05-31** (dev-docs/verification/feature-73-20260531.md, result=pass) — flag-on device + CU-free verification of all acceptance criteria; 2-round Codex audit ship-as-is. |
| 62 | Annotations panel split — replace the unified 4-tab `AnnotationsPanelView` with separate `TOCSheet` (navigation) + `HighlightsSheet` (review) | Reader/* | Medium | VERIFIED | **2026-05-20 — all 5 WIs merged + Gate-5 verified; status VERIFIED.** WI-1 (`AnnotationsSheetRoute` + `AnnotationStreamItem` + `AnnotationStreamBuilder` pure types, PR #974), WI-2 (`AnnotationsEmptyStateView` + the three SVG `Empty*Art` illustrations, PR #980), WI-3 (`TOCSheet` — Contents + Bookmarks, book-titled, count badges, PR #989), WI-4 (`HighlightsSheet` — "Annotations"-titled, 4 filter chips, the unified highlight+standalone-note card stream, designed export button, PRs #996/#998), WI-5 (final — rewire `ReaderContainerView` to route both sheets via `AnnotationsSheetRoute`, delete the 5 legacy view files `AnnotationsPanelView`/`HighlightListView`/`AnnotationListView`/`TOCListView`/`BookmarkListView`, migrate the XCUITest consumers). Each WI Codex-audited `ship-as-is` (WI-5 audit `.claude/codex-audits/feat-feature-62-wi-5-reader-rewire-audit.md`, 3 rounds). **Gate-5 acceptance verification — `dev-docs/verification/feature-62-20260520.md`, result=pass** — all 8 acceptance criteria exercised end-to-end on iPhone 17 Pro Sim (iOS 26.4) at v3.37.0: Contents→`TOCSheet`, Notes→`HighlightsSheet`, the four count-badged filter chips, the export button, the designed `AnnotationsEmptyStateView` empty states, the two sheets mutually exclusive, no import affordance (needs-design #963). 122 feature-62 unit tests + the migrated XCUITest verification suites pass. WI-5 also fixed a pre-existing test-fixture seed bug (the annotations XCUITest suites used the metadata-only `.books` seed which cannot open a reader — switched to `.epubFixture`) and an export-button accessibility tap-target finding. **Status VERIFIED 2026-05-20** — Gate-5 acceptance pass complete (evidence `dev-docs/verification/feature-62-20260520.md`), GH #801 closed with the verification citation; shipped in v3.37.1 (`main` at v3.37.2 after a version-collision-fix). `BLOCKED: needs-design (#963)` scoped ONLY to the `HighlightsSheet` annotation-import affordance — does NOT block the rest of #62 (WI-4 ships the designed export button + retains the `AnnotationImporter` engine for #963's follow-up). **Gate 1+2 complete — plan dev-docs/plans/20260518-feature-62-annotations-panel-split.md, Codex-audited clean.** **Design delivered 2026-05-18 — issue-canvas handoff resolves needs-design #860** — Gate 1 plan drafted (`dev-docs/plans/20260518-feature-62-annotations-panel-split.md`); Gate 2 independent plan audit (Codex `019e3b4f`, 2026-05-18) found the committed `HighlightsSheet` design models its All/Highlights/Notes filters on `HighlightRecord.note` and has **no surface for the app's standalone `AnnotationRecord` notes** — a live feature (own `@Model`, `PersistenceActor+Annotations` CRUD, a `ReaderNotificationHandlers` creation path, the current panel's 4th tab, export/import/backup/CloudKit). Not resolvable without a design/product decision → `needs-design` #860 filed. `TOCSheet` (Contents + Bookmarks) + the empty-state illustrations are fully designed and unaffected. Resume at plan v3 once #860 lands (plan §8 carries the round-1 audit + the five other findings' resolutions). **Filed 2026-05-17** — a feature #60 follow-up gap (Codex audit thread `019e317f` confirmed). **Problem**: `AnnotationsPanelView.swift` is a unified 4-tab sheet (Contents / Bookmarks / Highlights / Notes) — its own header says it covers the design's `TOCSheet` + `HighlightsSheet` "as one 4-tab sheet". The committed design (#793 handoff) decided to split it; feature #60 WI-10 re-skinned the sheets but did NOT perform the split, and #60 verified without it. No `TOCSheet`/`HighlightsSheet` Swift files exist. **Design source**: `feature-60-followups.md` §3 + `vreader-annotations.jsx`. **Scope**: split by job-to-be-done — `TOCSheet` (Contents + Bookmarks, book-titled, "leave the current page") and `HighlightsSheet` (Highlights + Notes, "Annotations"-titled, "revisit reading"); each with filled + empty states; reader bottom-chrome routes Contents → `TOCSheet`, Notes → `HighlightsSheet`. **Scope note 2026-05-17** (re-skin audit): #62's "empty states" explicitly include the design's custom SVG empty-state illustrations (`EmptyTOCArt` / `EmptyBookmarkArt` / `EmptyHighlightsArt`) — the app currently uses plain `ContentUnavailableView` — plus the per-tab/per-filter count badges. The audit's item #3 (annotations empty-state art + count badges) is covered here, not a separate row. **Lineage**: v2 follow-on of feature #60 (VERIFIED). needs-design #963 filed for the narrow HighlightsSheet import-affordance — does NOT block #62. GH: #801 |
| 61 | Book Details sheet — real half-sheet surface for the More-menu "Book details" row (currently routes to a settings interim) | Reader/*, Library/* | Medium | VERIFIED | **Filed 2026-05-17** — a feature #60 follow-up gap (Codex audit thread `019e317f` confirmed). **Problem**: feature #60 WI-6c shipped the More-menu "Book details" row but its handler just sets `showSettings = true` (`ReaderContainerView+Sheets.swift:223`) — a settings interim. No `BookDetailsSheet`/`BookDetailsView` exists. Its design was delivered (the #789 `Design needed:` handoff) but never given an implementation tracker home; #60 verified without it. **Design source**: `feature-60-followups.md` §1 + `vreader-book-details.jsx`. **Scope**: half-sheet, cover-on-top layout — cover + serif title/author + tag chips + key/value metadata list (Format / Size / Pages / Fingerprint-with-copy / Location-with-reveal) + actions block; pencil-overlay cover swap; 4 states (default / long-title / missing-cover / remote-only). **Lineage**: v2 follow-on of feature #60 (VERIFIED). **2026-05-18 (feature-cron Gate 1+2)** — implementation plan drafted: `dev-docs/plans/20260518-feature-61-book-details-sheet.md` (v3, 4 WIs / foundational+foundational+behavioral+behavioral). Gate-2 independent audit **PASSED** (Codex `019e39e3`, 3 rounds; 1 Critical + 2 High + 6 Medium + 1 Low across rounds, all resolved). **Scope refined by the audit**: the design's **remote-only state is descoped** — a reader-side sheet is only reachable for local books (`LibraryBookItem.isReadable == fileState == .local`; non-local rows divert to `BookDownloadSheet`), so remote-only/Download belongs to a hypothetical library-side details sheet, not #61. 3 states ship: default / long-title / missing-cover. Status TODO → PLANNED. **2026-05-18 (feature-cron Gate 3)** — WI-1 (`BookDetailsViewModel`) + WI-2 (`CoverPickCoordinator`) + WI-3 (`BookDetailsSheet` stacked layout + More-menu route) implemented, audited, merged. Status PLANNED → IN PROGRESS. WI-4 (action wiring — cover-swap / share / export / fingerprint-copy / location-reveal) implemented, audited, merged. All 4 WIs shipped; status IN PROGRESS → DONE. **2026-05-19 (verify-cron Gate 5)** — end-to-end device verification (`dev-docs/verification/feature-61-20260519.md`, result=**pass**) on iPhone 17 Pro Sim (iOS 26.4) at v3.31.5 (commit `d25535b7`, build 465). CU was unavailable (`/cu-diagnose`: virtual-only display) → verified via DebugBridge (`vreader-debug://` reset/seed/open) + sim-drive-fallback (`clickat.swift` taps, `dragat.swift` sheet drags) + `simctl io screenshot`. Seeded `war-and-peace`, opened it, tapped ⋯ More → "Book details": the **real `BookDetailsSheet` stacked half-sheet** presented (not the feature-#60 settings interim) — cover-on-top, serif-italic title, author, METADATA card (Format=TXT / Size / Fingerprint+copy / Location+reveal; Pages row correctly omitted for reflowable TXT), 3-row ACTIONS card (Add cover… / Share book… / Export annotations… with sublabel), accent pencil-disc cover-swap affordance. All 3 scoped states confirmed: default + missing-cover (`hasCover==false` → "Add cover…" label) on war-and-peace, and long-title via a separately-imported 79-char-titled TXT (title wraps cleanly across 3 lines of centered serif italic, metadata card stays anchored). Gate-4 unit gate re-run: 35 BookDetails tests across `BookDetailsViewModelTests`/`BookDetailsRouteTests`/`BookDetailsActionsTests` all pass. All 6 acceptance criteria PASS → status DONE → **VERIFIED**. 8 screenshot artifacts (`feature-61-verify-*-20260519.png`). Deferred follow-up notes (not verification failures): the planned `Feature61BookDetailsVerificationTests` XCUITest named in the plan §5 / GH #800 was never committed (this DebugBridge run covers the same surface); `BookDetailsViewModel.isLongTitle` is computed but read by no view (`BookDetailsSheet` header wraps the title natively) — effectively dead code, candidate for cleanup. GH: #800 |
| 60 | VReader visual identity v2 — Source Serif 4 / Inter typography, 5-theme palette (Paper / Sepia / Dark / OLED / Photo), oxblood accent, redesigned reader chrome + sheets (Library / Reader / AI / TOC / Highlights / Reader Settings), new SelectionPopover for new-selection-from-long-press flow (4 colors + Note / Translate / Ask AI / Read), generative typographic covers — per `claude.ai/design` handoff bundle | App/*, Library/*, Reader/*, Theme/* | Medium | VERIFIED | **VERIFIED 2026-05-16 (v3.27.0)** — all 12 WIs merged (WI-1..WI-11 incl. WI-6a/6b/6c + WI-7a..7c5, WI-12). needs-design #760 resolved. Gate 5b full-acceptance pass: result=pass — all 7 acceptance criteria (a-g) exercised end-to-end on iPhone 17 Pro Simulator (build 414). Evidence: `dev-docs/verification/feature-60-20260516.md`. WI-12 (#795) renders the Photo theme background image inside the EPUB WKWebView via a base64 data URL. WI-7a (SelectionPopoverView + action-row contract) shipped in PR #762 as a no-UI-regression slice — view built per design, 8 contract tests pin action order/accent/identifiers. WI-7b (foundational `SelectionPopoverActionRouter` — pure-logic dispatch enum mapping `SelectionPopoverAction` → existing `.readerHighlightRequested` / `.readerAnnotationRequested` / `.readerTranslateRequested` notifications; `userInfo["color"]` carries `NamedHighlightColor.rawValue` additively; `.askAI` / `.read` return `.deferredNotYetWired`) shipping in this iteration — 10 router tests pin enum-case-exhaustive contract, Codex Gate 4 thread `019e2e83` 1 round ship-as-is. WI-7c1 (foundational `SelectionPopoverPresenter` — `.readerSelectionPopoverRequested` notification + typed `SelectionPopoverRequest.post/parse` helpers + SwiftUI `SelectionPopoverPresenterModifier` that presents `SelectionPopoverView` as a sheet; routes taps via `SelectionPopoverActionRouter` + `SelectionPopoverDismissPolicy` keeps the sheet open on `.deferredNotYetWired` results so `.askAI`/`.read` taps don't silently swallow) shipping in this iteration — 10 contract tests pin notification wire format + dismiss policy; `TextSelectionInfo` gained additive `Equatable + Sendable` conformance; Codex Gate 4 thread `019e2ea9` 2 rounds ship-as-is. WI-7c2..7c5 (behavioral, per-bridge swap of `TXTBridgeShared.buildReaderEditMenu` → `SelectionPopoverRequest.post` + attach presenter to the container) are the remaining slices for SelectionPopover. **Progress**: WI-1 (typography registry Swift API) shipped in PR #750; WI-2 (theme tokens, `ReaderThemeV2`) shipped in PR #749; WI-3 (foundational popover types) shipped in PR #745; WI-4 (EPUB theme injection) shipped in PR #753 — EPUB renders now route through `ReaderThemeV2.epubOverrideCSS` via the legacy → V2 projection (`ReaderTheme.asV2`); WI-5 (TXT + MD theme injection) shipped in PR #754 — TXT body bg/ink + MD body ink + MD blockquote sub + MD code-block paper now route through V2. **Slice verify 2026-05-16** (`dev-docs/verification/feature-60-20260516-wi5slice.md`, `result: partial`) closes acceptance criterion (c) for 3 of 5 reachable themes (paper/sepia/dark) on TXT end-to-end via DebugBridge theme switches — OLED + Photo deferred because they're not reachable from the legacy 3-case `ReaderSettingsStore.theme` enum until a later WI migrates the settings type. Remaining WIs 6-10 cover reader chrome, SelectionPopover, library re-skin (cards/rows then container), final sheet re-skin + status-bar tinting + generative covers. **Plan**: `dev-docs/plans/20260515-feature-60-visual-identity-v2.md` (v5, Codex Gate-2 audit clean inline per rule 47; v4 introduces the WI-6a/6b split + needs-design #760; v5 introduces the WI-7a/7b split — WI-7a ships the SelectionPopoverView + action-row contract as a no-regression slice, WI-7b replaces the legacy UIMenu). 10 WIs sequenced from foundational (typography, theme tokens, popover types) → behavioral (EPUB theme injection, TXT/MD theme + typography, reader chrome, SelectionPopover, library cards/rows, library container, sheet re-skins + covers + status bar). Per row Cross-refs: #53/#55 presenters re-skinned post-#60 in separate PRs (not consumed). **Origin**: bundle handed off 2026-05-15 from `claude.ai/design` (share token `tSClWWD84corsOcW52YmNw`). Full prototype committed under `dev-docs/designs/vreader-fidelity-v1/` — README, chat-1 intent log, 9 JSX/HTML source files, 31 PNG screenshots. **Problem**: VReader's current chrome is a mix of SwiftUI system defaults (Library), a TextKit-driven UITextView (TXT/MD), a WKWebView + CSS-injected theme (EPUB), Foliate-js with its own CSS (AZW3/MOBI), and PDFKit (PDF). Each surface drifted its own typography, accent, and theme story; the user wants a single coherent reader identity. The design ("Refined-literary meets native iOS") specifies a unified visual layer over all of these without changing behavior. **Scope (purely visual + UX; no behavioral change to readers, persistence, search, WebDAV, or AI)**: (1) **Typography** — bundle Source Serif 4 (body, default reader font) + Inter (UI chrome). Add a serif↔sans toggle in Reader Settings. Replace ad-hoc font selection across `TXTViewConfig`, EPUB CSS injection, Foliate `setStyles`, and PDFKit. (2) **5-theme palette** — extend `ThemeColor`/`ReaderTheme` to Paper / Sepia / Dark / OLED / Photo. Photo theme uses a user-chosen background image with translucent paper layer (per `vreader-themes.jsx:image`). Tokens: `bg`, `paper`, `ink`, `sub`, `rule`, `accent`, `chrome` + `isDark` flag for status-bar tinting. Replace current theme palette; migrate existing per-book setting. (3) **Oxblood accent** — `#8c2f2f` (light) / `#d6885a` (warm-dark) / `#e8b465` (photo) — single restrained hue across buttons, indicators, selection. (4) **Library redesign** — continue-reading rail + 3-column cover grid, filter chips, search bar, grid↔list toggle. Aligns with #6 view preferences (no behavior change). (5) **Reader chrome** — new top bar (back / title / bookmark / more) + bottom bar (TOC / Display / Highlights / AI) + page indicator + scrubber. Edges-tap-flip / middle-tap-toggle-chrome convention (resolves bug #165 as a side effect; aligns with feature #25 tap zones). (6) **SelectionPopover** — REPLACES the current `HighlightableTextView` 4-item UIMenu (Highlight / Add Note / Define / ▶) for the **new-selection-from-long-press** flow. 4 named highlight colors (yellow / pink / green / blue) + Note / Translate / Ask AI / Read actions. **Cross-ref**: This is distinct from feature #53 (tap-on-existing-highlight Edit/Delete) and feature #55 (tap-on-annotated-text note preview) — those are separate hit-test flows the design does NOT cover. (7) **Sheets** — re-skin TOC, Highlights, AI (Summarize / Chat / Translate tabs), Reader Settings (Brightness + theme picker + Size / Line-spacing / Margin sliders + font toggle), App Settings. (8) **Generative typographic covers** — fallback cover style (classic / modern / editorial / animal / minimal) when no real cover image is available. Coexists with feature #43 cover extraction (decision-policy in plan: real-cover-if-available, generative-fallback). (9) **Status bar tinting** — flips light/dark based on `theme.isDark`. **Out of scope for v1**: PDF chrome (stays on PDFKit defaults until extended), AZW3/MOBI chrome (Foliate-js shell; extension TBD), search results panel, WebDAV / restore picker UI (#52 in flight), AI provider editor (#50 / #185 surface), reading-time dashboard (#58 has no design — needs design extension before implementing), hierarchical TOC tree (#38), bilingual inline mode (#56 — design's Translate is point-in-time only). **Edge cases**: (a) Photo theme image storage / picker / clearing; (b) backward-compat: per-book `epubTheme` value `.warmDark` etc. need migration to the new 5-theme set; (c) cover-policy collision with #43 — choose order at runtime; (d) accent against highlight colors — ensure contrast on each theme; (e) dynamic-island top inset preserved across the new chrome (bug #179 territory); (f) PDF/AZW3 fall-through: if user picks a theme the underlying renderer can't honour, render closest approximation. **Acceptance criteria**: (a) Library matches design's grid + rail on iPhone 17 Pro Sim; (b) Reader (EPUB + TXT + MD) matches design's chrome + page layout pixel-close (within typography metric drift); (c) All 5 themes render correctly including Photo; (d) Long-press text in any reader format produces the new SelectionPopover with 4 colors + 4 actions; (e) AI sheet Summarize/Chat/Translate tabs match design; (f) Source Serif 4 ↔ Inter toggle works in reader; (g) Existing features (highlight persistence, search, AI, backup) unaffected — no regressions in feature #3 / #4 / #11 / #17 / #29 / #44 / #50 verification sets. **Dependencies**: blocks none; informs #53 (presenter could be re-skinned post-#60), #55 (same), #58 (needs design extension first). **Risks**: (a) typography metric drift between Source Serif 4 and current Georgia/system serif breaks scroll-position restore math (bug #179 family) — mitigate with offset re-projection on font change; (b) Photo theme image storage policy + WebDAV backup question (does the image travel?); (c) AZW3/MOBI fall-through — Foliate-js's own typography won't match Source Serif 4 unless we ship a font into the Foliate bundle; (d) re-skinning #53 + #55 presenters post-#60 = double-implementation cost vs gating those features behind #60. **Implementation gating**: per rule 47 follows /feature-workflow Gates 1-6 (Plan → Independent plan audit → TDD → Implementation audit → Device verification → Merge). Likely 8-15 WIs depending on Gate-1 split. Per rule 48 most WIs must serialize (one writer per file per area). Cron-friendly mechanical WIs: font asset bundling, theme-token extension, accent-color sweep. Human-loop WIs: Library redesign, Reader chrome, SelectionPopover swap. **Out-of-design surface decisions** (locked at triage time, plan can adjust): PDF/AZW3/MOBI reader chrome stays on current rendering for v1; #58 reading-time dashboard waits for design extension; #56 bilingual inline waits for design extension; #38 TOC tree waits for design extension; #43 generative covers folds into this feature. Reported by user 2026-05-15. GH: #718 |
| 59 | Register vreader as a system document handler — appear in iOS "Open in" / Share Sheet for `.epub`, `.azw3` (+ `.mobi`/`.prc`/`.azw`), `.md` (+ `.markdown`), `.txt`, `.pdf` and import the tapped file into the library | App/*, Library/* | Medium | VERIFIED | **VERIFIED 2026-05-16 round-3** (`dev-docs/verification/feature-59-20260516-round3.md`, `result: pass`) on iPhone 17 Pro Sim v3.22.23 (commit `cdefc5a5`). All 6/6 criteria PASS — Bug #197 / GH #708 fix (`75fc5409`, v3.22.22) unblocked criterion (b): `simctl openurl booted "file://...epub"` → new book appears in library within ~1s, no cold restart needed (`dev-docs/verification/artifacts/feature-59-verified-library-postfix-20260516.png`). **Round-2 device-verify 2026-05-16** (`dev-docs/verification/feature-59-20260516.md`, `result: partial`). 5/6 criteria PASS, only (b) still BLOCKED by Bug #197 (library not auto-refreshing post-import — still NEW). Round-2 confirmed: (a) Share Sheet shows vreader for PDF — Files-app → tap PDF → Quick Look → Share → Preview/vreader/More listed ✅; (c) `.mobi`/`.prc`/`.azw` dispatch correctly via simctl openurl (FileURLImportRouter logs + DB rows confirm import), `.pdf` dispatches via Share Sheet → tap vreader → DB row appears (lands on prior view due to Bug #197, but the import-to-DB path works) ✅; (e) Files-app context menu wording is "Open With" NOT "Copy to vreader" → `LSSupportsOpeningDocumentsInPlace: true` honored ✅; (f) "Open With" expanded list shows Preview labeled "Default" and vreader as alternative → `LSHandlerRank: Alternate` honored ✅. Combined with round-1 (`feature-59-20260515.md`): all 7 of 8 supported extensions verified to dispatch (only `.markdown` is broken at the Apple system-UTI level, not a vreader bug). Cannot flip to VERIFIED until Bug #197 lands a fix and criterion (b) reverifies. Plan: `dev-docs/plans/20260515-feature-59-share-sheet-open-in-vreader.md` (v1, Gate 2 round-1 audit clean via Codex `019e2a9e`). **2 WIs both shipped** — WI-1 (Info.plist metadata: CFBundleDocumentTypes + UTImportedTypeDeclarations + LSSupportsOpeningDocumentsInPlace) in PR #698 v3.21.73 commit `ba92440`. WI-2 (FileURLImportRouter + production .onOpenURL handler in both Debug & Release Scene branches + `BookFormat.isSupportedExtension`) in this PR. Test suite: 6 InfoPlist regression guards + 6 BookFormat extension cases + 9 router dispatch cases (including 10 supported-extension parameterized + 6 unsupported-extension parameterized). All pass. **Problem**: When the user taps a book file in another app (Files, Mail, Safari downloads, AirDrop receive, third-party file managers) iOS shows an "Open in…" / Share Sheet listing the apps that have declared support for that document type. vreader is not in the list because `Info.plist` declares no `CFBundleDocumentTypes` and no `UTImportedTypeDeclarations`. The app also has no production `.onOpenURL`/scene-delegate handler for `file://` URLs — the existing handler in `VReaderApp.swift:309` is wrapped in `#if DEBUG` and only matches `DebugCommand.scheme` (the `vreader-debug://` URL surface). All five formats (EPUB / PDF / TXT / MD / AZW3-MOBI-PRC-AZW) are otherwise fully supported — `BookImporter` already routes them through format-specific metadata extractors (`AZW3MetadataExtractor` for `.azw3/.azw/.mobi/.prc`, EPUB/PDF/TXT/MD extractors elsewhere) and the reader views all consume `URL` inputs via `open(url:)` ViewModels. So this is purely the missing system-level registration + import dispatch — not a new format-handling capability. **Scope**: (1) **Document-type declarations in `project.yml` Info.plist properties** — add `CFBundleDocumentTypes` array with one entry per supported family, each with `CFBundleTypeName` + `LSHandlerRank: Alternate` (so vreader appears alongside other readers without claiming primacy) + `LSItemContentTypes` listing the relevant UTIs. Standard UTIs: `org.idpf.epub-container` (EPUB), `com.adobe.pdf` (PDF), `public.plain-text` (TXT), `net.daringfireball.markdown` (MD — Apple-recognized public conformance). Custom UTIs declared in `UTImportedTypeDeclarations`: `com.amazon.azw3` / `com.amazon.mobi-pocket` / `com.amazon.kindle` covering the AZW3/MOBI/PRC/AZW extensions (Apple does not provide a system UTI for these). Each imported type spec: `UTTypeIdentifier`, `UTTypeConformsTo: [public.data]`, `UTTypeDescription`, `UTTypeTagSpecification` ({public.filename-extension: [...], public.mime-type: [...]}). (2) **`LSSupportsOpeningDocumentsInPlace: true`** — required so the Files app and document-pickers can hand the URL to vreader without copying the file first; pairs with `UIFileSharingEnabled` if the user wants their library exposed (separate scope decision). (3) **Production `.onOpenURL` handler** in `VReaderApp.swift` (outside the `#if DEBUG` block) that dispatches `file://` URLs to a new `BookImportRouter` (or extension on the existing `BookImporter`) which: securely scopes-the-resource (`url.startAccessingSecurityScopedResource()`), detects format via `BookFormat.from(fileExtension:)`, calls the existing `BookImporter.importBook(at:)` path, releases the scope, and navigates to the new library row (or directly opens the book — UX decision in plan). (4) **Edge cases**: (a) unknown extension reaching the handler (because UTI conformance can be looser than our extension list) — fall back to user-visible alert; (b) file URL points to iCloud / OneDrive lazy file not yet downloaded — surface a "downloading…" state before import; (c) huge AZW3 files imported under memory pressure — same risk surface as existing AZW3 import (covered by `BookImporter`); (d) duplicate import — `fingerprintKey` dedupe already handles this; the handler should still navigate to the existing row, not silently no-op; (e) MD file mistyped as plain text by source app — UTI dispatch picks the highest-conforming registered type; (f) iOS 17+ App Intents / Quick Actions integration is OUT of scope for this row. **Acceptance criteria**: (a) on iOS Share Sheet for an `.epub` file in Files / Mail / Safari, "vreader" appears in the destinations list; (b) tapping the destination launches vreader, imports the file, and lands on either the library or the freshly-opened reader (decide in plan); (c) same flow for `.azw3`, `.mobi`, `.prc`, `.azw`, `.md`, `.markdown`, `.txt`, `.pdf`; (d) duplicate handling: same file shared twice does not create a duplicate library row; (e) Files-app context menu shows "Open in vreader" without a copy step; (f) `LSHandlerRank: Alternate` confirmed by NOT becoming the default handler on a clean device. **Dependencies**: none (all format-handling already implemented). **Risks**: (a) UTI conformance for `.azw3`/`.mobi` is non-standard — Apple doesn't ship a public UTI for these, so we must declare them; if a competing reader app declared a slightly different conforming UTI tree, both apps might show or one might be hidden — confirm against Apple Books, Kindle, Marvin, KyBook on a real device during verification; (b) `LSSupportsOpeningDocumentsInPlace` interactions with the existing book-files-directory model in `BookImporter` — make sure scope-released URLs aren't re-read after `stopAccessingSecurityScopedResource`; (c) `UTImportedTypeDeclarations` declarations could conflict with already-installed system Markdown editors — test on a device with Bear / iA Writer / Drafts installed to confirm we appear in the sheet without breaking their defaults. Reported by user 2026-05-14. GH: #667 |
| 58 | Reading-time + activity dashboard — time-window aggregation (today / week / month / 3mo / 6mo / year / all-time), per-book breakdown with notes and highlights counts, sortable, included in WebDAV backup | Library/*, Stats/* | Medium | VERIFIED | **VERIFIED 2026-05-22 (verify-cron Gate-5b round 2)** — all 6 acceptance criteria PASS on merged `main` `1973b002` (v3.39.8). Round-2 unblocked by Bug #263 / GH #1138 (`vreader-debug://seed-sessions`): seeded a deterministic 6-session spread on-device, confirmed the live per-window ladder today=600<7d=1200<30d=1800<90d=2400<180d=3000<all=3600 + `ReadingStats`=3600s/6, and drove the SAME `ReadingStatsAggregator` over the SAME seeded data via the seam test (criterion b); `sortIsAppliedToPerBookTable` reorders rows + `StatsPerBookTable` exposes all 5 sortable columns + per-book notes/highlights projection (criterion c); VM UserDefaults sort round-trip across construction (criterion d); criterion a reachability carried from round-1 artifact; criteria e/f re-confirmed green by `BackupReadingHistoryTests`. Dashboard's *visual* surface is not CU-free reachable (no `present?sheet=stats` / `vreader-debug://stats`; CU down) so b/c/d verified at the data/behavioral substance layer per the verify-skill authorized path — 333 tests / 0 failures across 9 suites. Evidence: `dev-docs/verification/feature-58-20260522-round2.md` (`result: pass`). GH #665 closed. Round 1 (`feature-58-20260522.md`, `result: partial`) verified a + e/f; b/c/d were blocked on session-seeding (Bug #263, now FIXED). **DONE 2026-05-22 (feature-cron Gate-6)** — all WIs merged on `main` (WI-1–5 PRs #982/#994/#995/#999/#1001; WI-6a/6b/6c) and all 6 acceptance criteria have shipping code: `ReadingDashboardView`+`ReadingDashboardViewModel` (a), `ReadingStatsAggregator` 7-window (b), `StatsPerBookTable` 5-col sort (c) + persisted sort (d), `BackupReadingHistory` reading-history.json (e/f). Dashboard reachable via Feature #67's Stats button (now DONE). Implementation complete; awaiting Gate-5b VERIFIED (verify cron — exercise the acceptance criteria end-to-end incl. a WebDAV backup→wipe→restore round-trip). **D1–D4 RESOLVED 2026-05-20 — A/B/B/A.** D1=A: entry point amended to **"from Settings → profile card → Stats"** (accepts feature #67 dependency — #67 WI-4 owns the SettingsView Stats button, itself hard-blocked on this feature's WI-6 reaching `DONE`, the standard mutual-dependency knot — WI-6a ships a DEBUG-only `vreader-debug://stats` entry for Gate-5 verification while the user-facing hook waits on #67). D2=B: accept the design literally (`Year` calendar-YTD + `Custom` range picker) — **Custom range picker deferred to GH #1058** (filed `needs-design`). D3=B: ship 5 sort fields including `last-read` — **`last-read` column deferred to GH #1059** (filed `needs-design`). D4=A: hero + 7-pill bar (design). **Deferred per D2-B / D3-B: Custom range picker (GH #1058) + last-read sort column (GH #1059); WI-6a's first cut ships 6 windows (Today / 7d / 30d / 90d / Year / All — Custom dropped) + 4 sort fields (title / time / highlights / notes — last-read dropped).** WI-6 split into WI-6a (A-portion: dashboard view + 6-pill bar + 4-col table + DEBUG-only `vreader-debug://stats` entry, ships now), WI-6b (D2-B Custom range picker + Year calendar-YTD bucket swap, blocked on #1058 — **UNBLOCKED 2026-05-20 by PR #1060 Stats Followups design landing**; **WI-6b SHIPPED 2026-05-20** — Custom pill on `StatsTimeWindowBar` + new `StatsCustomRangePicker` sheet (month grid + quick-preset rail + start/end date chips + summary footer) + new `ReadingStatsCustomRange` value type + aggregator widened with optional `customRange` arg (additive; nil = WI-6a behaviour) + VM tracks/persists the applied range across launches; 100 WI-6a/6b tests green), WI-6c (D3-B `last-read` column, blocked on #1059 — **UNBLOCKED 2026-05-20 by PR #1060**; **WI-6c SHIPPED 2026-05-20** as PR #1063 — `.lastRead` case added to `ReadingDashboardSortField` + comparator (nil sinks to bottom regardless of direction); `StatsPerBookTable` gained a 5th `Read` header-tap-driven column (Alt-1 always-5-columns design variant); nil cells render as "—", non-nil reuse `ReadingTimeFormatter.formatRelativeLastRead`). **WI-1–5 MERGED to main 2026-05-19 (WI-1 ReadingStatsModels PR #982; WI-2 ReadingStatsAggregator PR #994; WI-3 ReadingTimeFormatter.formatDuration PR #995; WI-4 ReadingDashboardViewModel PR #999; WI-5 WebDAV reading-history backup section PR #1001 — each Codex-audited ship-as-is, 79 WI-1–5 tests green on main).** **Gate 1+2 complete — plan dev-docs/plans/20260519-feature-58-reading-dashboard.md, Codex-audited clean.** **Design delivered 2026-05-18 — issue-canvas handoff resolves needs-design #862** — triage 2026-05-18: the `ReadingDashboardView` UI (7 time-window cards + sortable per-book table) has no committed design; `needs-design` #862 covers it (bundled with feature #67's profile card — same reading-stats design family). The non-UI parts (the `ReadingStatsAggregator` service + the WebDAV backup-payload extension) are not design-gated and can be planned independently once the dashboard design lands. **Problem**: Per-session reading-time data is already collected (`ReadingSession` SwiftData model: `startedAt`, `endedAt`, `durationSeconds`, `bookFingerprintKey`) and per-book lifetime totals exist (`ReadingStats` model: `totalReadingSeconds`, `sessionCount`, `lastReadAt`), but there is no surface that exposes this data to the user. No view, no time-window aggregation, no per-book breakdown panel, no sorting beyond `LibrarySortOrder.totalReadingTime`. Notes (`AnnotationNote`) and highlights (`Highlight`) counts per book are queryable but never aggregated into a stats surface. WebDAV backup (`BackupDataCollector`) currently omits both `ReadingSession` and `ReadingStats` (CloudKit-only via `SyncReadingSessionRecord` / `VRReadingSession`), so stats are lost on restore-to-fresh-device. **Scope**: (1) **Time-window aggregator** — new `ReadingStatsAggregator` service (likely actor-isolated): query `ReadingSession` records over a `DateInterval`, sum `durationSeconds`, return totals for the standard windows (today / past-7d / past-30d / past-90d / past-180d / past-365d / all-time). Uses `startedAt` as the bucket anchor; sessions spanning a window boundary count toward the window containing `startedAt` (document this decision). (2) **Dashboard view** — new `ReadingDashboardView` (SwiftUI), accessible from **Settings → profile card → Stats** (D1=A resolution 2026-05-20; depends on feature #67's profile card). Top section shows the seven aggregate totals as cards (label + formatted duration via `ReadingTimeFormatter`). Middle section is a per-book table: columns for book title, reading time in the active window, notes count (from `PersistenceActor.fetchAnnotations(forBookWithKey:)`), highlights count (from `PersistenceActor.fetchHighlights(forBookWithKey:)`). Window picker at the top of the per-book table re-runs the aggregator and re-renders. (3) **Sorting** — per-book table sortable by: title (asc/desc), reading time (desc/asc), notes count (desc/asc), highlights count (desc/asc), last-read (desc/asc). Default: reading time desc within the active window. Persist last-used sort in `UserDefaults` mirroring `LibrarySortOrder` pattern. (4) **WebDAV backup inclusion** — extend `BackupDataCollector` with a `collectReadingHistory()` method that emits a new `reading-history.json` (or similar) section containing `ReadingSession` records (sessionId, bookFingerprintKey, bookFingerprint, startedAt, endedAt, durationSeconds, pagesRead, wordsRead) and `ReadingStats` aggregates. `BackupDataRestorer` mirrors with `restoreReadingHistory(...)`. Bumps `kBackupCurrentSchemaVersion`. **Edge cases**: (a) zero sessions for a book — still show row with "0m"; (b) book deleted but historical sessions exist — show row with `(deleted)` placeholder title or omit (decide in plan); (c) session crosses midnight / week boundary — counts to the bucket containing `startedAt`; (d) clock skew (device-time changes) — accept whatever `startedAt` records; (e) very long history (1000+ sessions) — aggregator must be efficient (`@Query`-style predicate over `startedAt`); (f) WebDAV restore from older backup with no `reading-history.json` — section is optional, aggregator falls back to local-only stats; (g) timezone — bucket boundaries computed in user's current timezone (today = local-midnight to local-midnight). **Acceptance criteria**: (a) dashboard surface reachable from Settings → profile card → Stats (D1=A); (b) all 7 windows render correct totals on a fixture with seeded sessions; (c) per-book table shows time/notes/highlights counts, sortable on all 4 columns; (d) sort selection persists across app launches; (e) `BackupDataCollector` emits `reading-history.json`; restore on a fresh device reproduces historical totals; (f) round-trip: backup → wipe → restore preserves `ReadingSession` + `ReadingStats` exactly. **Dependencies**: Feature #29 (WebDAV backup, VERIFIED — for the backup payload extension), `LibrarySortOrder.totalReadingTime` (already exists). **Risks**: (a) aggregator performance over large session histories — may need a denormalized per-day-bucket cache table if N+1 patterns appear; (b) WebDAV backup payload bloat — `ReadingSession` rows accumulate over time; consider compression or window-based truncation; (c) timezone semantics — buckets recomputed if user travels; this is correct but document in UI. **Notes**: builds on existing infrastructure — no new SwiftData @Models required for stats display; read-only over existing schemas plus the new backup payload section. Reported by user 2026-05-14. ~~WI-6 has 4 open product decisions (D1-D4) — blocked at Gate 3 pending user input.~~ (Superseded: D1-D4 were RESOLVED 2026-05-20 as A/B/B/A — see Notes head; WI-6a/6b/6c shipped accordingly and the feature reached DONE 2026-05-22.) GH: #665 |
| 57 | AZW3/MOBI TTS — wire `FoliateTTSAdapter` (or Foliate-webview text extraction) into production so the speaker button works for Foliate-rendered formats | Reader/TTS | Medium | VERIFIED | **VERIFIED 2026-05-26** — criterion-4 pause/resume gap closed CU-free. `Feature26TextToSpeechVerificationTests.test_verify_feature_57_azw3_start_pause_resume_stop_cycle` (seed `.azw3Fixture` — added by Bug #233/#964 — + `--tts-test-mode`) drove the full start → pause → resume → stop lifecycle on the live Foliate AZW3 reader via XCUITest accessibility taps (the play/pause control's "Pause"⇄"Resume" label flips prove `.speaking ⇄ .paused`); 1 test, 0 failures, 20.8 s. All in-scope criteria (1,2,3,4,7) now pass. Evidence: `dev-docs/verification/feature-57-20260526.md`. The 2026-05-19 partial was a framing error (XCUITest taps via accessibility, not CU). **Status**: all 4 WIs merged to `main` — WI-1 (`extractPlainText` JS helper + Swift channel, PR #913, v3.33.8), WI-2 (`startTTS()` AZW3 branch + in-flight extraction gate, PR #916, v3.34.1), WI-3 (re-add `.tts` capability, PR #919, v3.34.3), WI-4 (acceptance verification, this PR). **Verification**: `dev-docs/verification/feature-57-20260519.md`, `result: partial` — criteria 1, 2, 3, 7 verified end-to-end (device + unit); criteria 5 & 6 amended out of scope at the PLANNED flip; criterion 4's **pause/resume** could NOT be exercised end-to-end (the `vreader-debug://tts` grammar has only `start`/`stop`, and CU — the only path to the `TTSControlBar` pause button — is down). Row stays `DONE` (not `VERIFIED`); reaching `VERIFIED` needs a follow-up pause/resume verification (recommend extending `DebugCommand.tts` with `pause`/`resume` actions). **Problem**: AZW3 and MOBI files render through Foliate-js inside a WKWebView. Unlike TXT/MD/PDF/EPUB, the Swift side has no parsed view of the content — there is no `loadBookTextContent` case for these formats. Bug #176 capability-gated `.tts` off `.azw3` to stop the silent failure user-facing (PR forthcoming 2026-05-14, v3.21.42). This feature reverses that gate by implementing a real TTS path. **Plan**: `dev-docs/plans/20260518-feature-57-azw3-mobi-tts.md` (Gate 1; audited clean at Gate 2 round 3, Codex `019e3e3a`). **Scope decision — path (a) chosen** (plan §2): Foliate-webview whole-book plain-text extraction via a new `readerAPI.extractPlainText()` JS helper (walks `view.book.sections[].createDocument()`, the same seam `view.search()` uses) feeding the shared `AVSpeechSynthesizer` pipeline. Path (b) (Foliate in-webview SSML TTS) rejected — `FoliateTTSAdapter` expects a `tts-text` message the bundle never posts, Foliate-js TTS is SSML-based with no `AVSpeechUtterance` bridge, and it is per-section not whole-book. The `v1` path-(a) premise `document.body.innerText` was disproven (host `<body>` is shell-only; book content is two closed-shadow-roots + an iframe deep) — the plan re-bases on the real `createDocument()` seam. **Acceptance criteria**: (1) `.tts` re-added to `FormatCapabilities.capabilities(for: .azw3)`; speaker button visible in AZW3/MOBI reader chrome again. (2) Tapping speaker → TTS starts speaking the AZW3 text; `vreader-debug://snapshot` reports `ttsState: "speaking"` and `ttsOffsetUTF16` advances over time. (3) `vreader-debug://tts?action=start` URL path works for AZW3 (matches TXT/MD/PDF/EPUB behaviour). (4) Pause/Resume/Stop cycle exercised end-to-end on iPhone 17 Pro Sim. (5) **AMENDED at PLANNED flip (plan §7.1, Gate-2 audit-confirmed)**: visual sentence-highlight inside the Foliate WKWebView is **explicitly out of scope** for #57 — `TTSHighlightCoordinator` is TXT/MD-only (`TTSHighlightCoordinator.swift:7-10`) and is never instantiated for Foliate, so the #40 pipeline does not run there at all; a Foliate TTS-highlight overlay is a tracked follow-up feature, not a line item of #57. (6) **AMENDED at PLANNED flip (plan §7.1, Gate-2 audit-confirmed)**: auto-scroll inside the Foliate WKWebView is **explicitly out of scope** for #57, for the same reason — feature #41's own VERIFIED row already declares "EPUB/AZW3/PDF intentionally out of scope (different scroll surfaces)." (7) Removed regression-guard `azw3_doesNotSupportTTS()` test; replaced with positive `azw3_supportsTTS()` assertion. #57 delivers **TTS audio + pause/resume/stop** for AZW3/MOBI; visual highlight/scroll on Foliate is deferred to a separate feature. **Dependencies**: Feature #42 (Foliate unified reader engine) listed as a soft dependency only "if pursuing path a via Foliate's #42-introduced helpers" — **severed**: path (a) uses #57's own `extractPlainText` helper added to `foliate-host.js`, not a #42 helper; #57's Gate 3 does not wait on #42 (merge-order coordination only — plan §3.4). **Risks**: path (a) — `extractPlainText()` must return correct whole-book text and the returned `Promise<string>` must marshal across `evaluateJavaScript` (front-loaded into WI-1's device-verified feasibility slice); whole-book extraction may be slow on large books → handled by deferring to first speaker tap + an in-flight extraction gate (`azw3ExtractionTask`) for rapid repeated taps. Reported by user 2026-05-13 via bug #176 fix's deferral. GH: #904 |
| 56 | Bilingual reading mode — AI-translated chapter displayed inline below original, persistent disk cache, whole-book translation, per-chapter re-translation with provider override | AI/*, Reader/* | Medium | VERIFIED | **2026-05-20 — VERIFIED (Gate 5b round-3)**: all 6 acceptance criteria PASS via host-script + DebugBridge + live OpenRouter provider; criterion (b) re-verified post Bug #245 fix (PR #1076, v3.38.19) — TXT bilingual interleaved render directly observed via `mcp__computer-use__screenshot` (war-and-peace.txt chapter 3, English source + Chinese inline below each paragraph). Evidence: `dev-docs/verification/feature-56-20260520-round3.md` (round-3 result=pass) + `dev-docs/verification/feature-56-20260520-round2.md` (round-2 result=partial, criteria a/c/d/e/f PASS, criterion-b PARTIAL pre-fix). **2026-05-20 — DONE**: all 15 WIs shipped (WI-1..15 incl. WI-2.5 + WI-7a/7b split). WI-15 (final) — `ChapterReTranslateViewModel` + `ReTranslatePickerSheet` + host wiring (`ReaderContainerView+ReTranslate`) deliver acceptance criteria (e) per-chapter re-translate clears old cache and fetches fresh, and (f) provider override does not mutate `ProviderProfileStore`. Awaiting Gate 5b post-merge final-acceptance pass (every AC exercised end-to-end against a real provider) → `VERIFIED`. **Scope note — secondary TOC swipe affordance deferred to a follow-up `needs-design`**: the plan named `ChapterReTranslateSwipeAction` in `TOCListView`, but the existing TOC is `LazyVStack`-in-`ScrollView` and SwiftUI `.swipeActions` requires a `List` host — the designed swipe affordance cannot attach without inventing UI (rule 51). The primary path (More-menu row → picker → progress) fully delivers (e) and (f); the secondary TOC swipe is queued as a follow-up. **Gate 3 complete — WI-1..WI-15 merged (incl. WI-2.5, WI-7a/7b split).** **Gate 1+2 complete — plan dev-docs/plans/20260519-feature-56-bilingual-reading.md, Codex-audited clean.** **Design delivered 2026-05-18 — issue-canvas handoff resolves needs-design #863, #864** — triage 2026-05-18: the committed design (`vreader-bilingual.jsx` + `feature-60-followups.md` §2) covers the interlinear renderer, setup sheet, More-menu row, and `EN ↔ 中` pill — but NOT scope item (3) "Translate entire book" (entry point / confirmation / progress / cancel → `needs-design` #863) nor scope item (4) per-chapter re-translate UI + provider-override picker (→ `needs-design` #864). The core bilingual-reading slices are designed; the global-translate + re-translate slices are blocked. **Problem**: Feature #18 translates selected text within the AI panel (point-in-time, in-memory cache, session-only). Users reading foreign-language books want a full bilingual reading experience: the entire current chapter translated and displayed inline below the original text, cached to disk so translations survive app restarts, with a global translate-entire-book option and per-chapter re-translation control. **Scope**: (1) **Chapter bilingual mode** — a toggle in the reader AA panel (per-book, persisted in `PerBookSettings`) that, when enabled, fetches the full chapter text through `AIService.sendRequest` and renders the translation below the original in each reader format. For TXT/MD: append translation in a styled `UITextView` section below the chapter text. For EPUB: inject translated paragraphs after each `<p>` via a JS bridge call. For Foliate/AZW3: inject via Foliate message. For PDF: overlay translation panel below the page. Target language defaults to Chinese (user-configurable per-book or globally). (2) **Persistent translation cache** — `ChapterTranslationStore` actor (new SwiftData `@Model`): keyed by `(fingerprintKey, chapterHref, targetLanguage, providerProfileID, promptVersion)` → translated text + timestamp. `AIResponseCache` is session-only in-memory; this store persists to disk and survives relaunches. Stale-cache invalidation: user-driven only (re-translate). (3) **Global book translation** — "Translate entire book" action in the Library card or book detail screen. Presents a confirmation alert with estimated chapter count and a warning ("This will send all chapters to your AI provider, which may use a significant number of tokens"). Runs chapter-by-chapter as a `Task.detached` background operation with cancellability; progress bar in a translating status badge. Already-cached chapters are skipped. (4) **Per-chapter re-translation** — "Re-translate Chapter" option in the reader AA panel long-press menu or chapter list context menu. Clears the cached entry for that chapter and re-fetches. Provider override: a picker (using existing `ProviderProfileStore`) lets the user choose a different AI provider for the re-translation without changing the global active provider. **Edge cases**: (a) chapter text exceeds provider max_tokens — split into chunks with overlap, recombine; (b) translation in progress when user navigates away — cancel and discard partial result; (c) offline mode — load from disk cache if available, else show "Translation unavailable offline"; (d) provider changed after cache entry written — treat as stale (different providerProfileID); (e) CJK→CJK translation (e.g. Simplified→Traditional) — same pipeline, no special case; (f) concurrent global translation + single-chapter re-translate racing on the same chapter — last writer wins, or serialise via actor; (g) book deleted while global translation running — cancel the operation and clean up cache entries. **Acceptance criteria**: (a) bilingual mode toggle persists per book; (b) current chapter translation renders inline within 10 seconds for a typical chapter; (c) cached translation loads immediately on next app open without an API call; (d) global translate shows confirmation with chapter count and can be cancelled; (e) per-chapter re-translate clears old cache and fetches fresh; (f) provider override for re-translate does not change the global active provider. **Dependencies**: Feature #18 (VERIFIED — `AIService`, `AITranslationViewModel`), Feature #50 (VERIFIED — `ProviderProfileStore`). Reported by user 2026-05-14. **Design landed 2026-05-16**: `dev-docs/designs/vreader-fidelity-v1/project/design-notes/feature-60-followups.md` §2.1 is feature #56's design source — paragraph-interlinear rendering (each source paragraph followed by its translation at ~0.88× size, `sub` color, slight indent), a first-toggle setup sheet (target language / Paragraph|Sentence granularity / AI-provider chip), and a persistent on/off mode with an `EN ↔ 中` reader-chrome pill. Arrived via the feature #60 follow-up handoff — the #790 `Design needed:` issue proposed this as a new feature before realising #56 already tracked it. Feature #56 can be promoted TODO → PLANNED against this design. **Scope note 2026-05-17**: #56's scope explicitly includes the **More-menu bilingual toggle row** (`feature-60-followups.md §2.3` — off / on / AI-unavailable states) as bilingual mode's entry point. Feature #60 WI-6c deferred that row (`ReaderMoreMenuRow.swift` marks Bilingual deferred) and a "WI-6d" was named but never created; since #60 is VERIFIED the row has no home there — #56 owns it, and #56's eventual plan must implement the row alongside the backing mode. WI-13 (PDF below-page translation panel) + the bilingual-offline-affordance WI are needs-design-blocked. GH: #629 |
| 55 | Tap on annotated text to view note content inline | Reader/* | Low | VERIFIED | **Design delivered 2026-05-18 — issue-canvas handoff resolves needs-design #865** — triage 2026-05-18: #55 needs a tap-on-annotated-text note-preview surface (popover / card / bottom-sheet showing the note body, with dismiss + empty-state). The committed bundle's `HighlightActionPopover` is feature #53's tap-on-highlight flow — a different hit-test; no design depicts the tap-on-annotated-text preview → `needs-design` #865. **Problem**: After a user adds a note to text, tapping that annotated region in the reader does nothing inline — the note content is only accessible via the annotations panel (Notes tab). There is no tap-to-preview affordance. **Scope**: For each reader format — (1) TXT/MD: `HighlightableTextView` / `HighlightableLayoutManager` tracks painted `NSRange` annotation regions; add `UITapGestureRecognizer` that hit-tests against those ranges and presents a popover/bottom-sheet with the note body; (2) EPUB: detect tap on annotated range in JS, post message to Swift with the annotation ID, show inline note preview; (3) Foliate: `handleAnnotationShow(cfi:)` already fires on annotation tap (`FoliateReaderContainerView+Highlights.swift:41`) but only opens the panel — extend to also show inline note content; (4) PDF: `PDFAnnotationBridge` hit-test at tap → show note popover. **No inline display mechanism exists anywhere** — root cause: `ReaderNotificationModifier.onSave` creates `AnnotationRecord` only; no tap handler routes to a note-body display path. Distinct from Feature #53 (inline edit/delete for *highlights*). **Acceptance criteria**: (a) tapping annotated text shows the note body without opening the full panel; (b) dismissible by tapping away or swiping; (c) consistent UX across TXT, MD, EPUB, and Foliate formats. **Cross-ref**: Feature #60 (visual identity v2) — the design's SelectionPopover does NOT cover tap-on-annotated-text; #55 remains a distinct hit-test + presenter flow whose presenter may be re-skinned post-#60. **2026-05-19 — PLANNED**: Gate-1 plan + Gate-2 independent audit complete. Plan at `dev-docs/plans/20260519-feature-55-tap-annotated-text-note-preview.md` (v4 — Codex 3-round Gate-2 audit `019e3e14`, verdict CLEAN; round-1 found 8 incl. 2 High — `AnnotationEditSheet` edits `AnnotationRecord` not highlight notes + the live Foliate path is `FoliateSpikeView+HighlightTap` not the dormant `handleAnnotationShow`; round-2 found 1 High rule-51 issue — no self-designed Delete affordance / `HighlightNoteEditSheet`; round-3 found 1 Medium scope overstatement, resolved). Key facts the plan rests on: `.readerHighlightTapped` (`ReaderHighlightTapEvent`) already fires from all 5 formats and `HighlightRecord.note` already exists — #55 adds a note-preview presenter consuming that event. 7 WIs (WI-1..3 foundational: `NotePreviewContent`/`HighlightLookup`, `PersistenceActor.highlight(withID:)`, `NotePreviewViewModel`; WI-4..5 the callout + sheet views + UIKit rect-anchored presenter; WI-6 native TXT/MD/PDF wiring; WI-7 EPUB+Foliate). **Design note**: the read-only preview surfaces are fully designed; the callout's *Edit* action is a `BLOCKED: needs-design` slice — `needs-design` issue #914 filed 2026-05-19 for the highlight-note edit surface — v1 ships read-only preview, the feature row itself is NOT blocked. **2026-05-19 — IN PROGRESS**: Gate-3 implementation underway. WI-1 (foundational — `NotePreviewContent` value type, `NotePreviewPresenter` pure parse/build + callout-vs-sheet decision enum, `HighlightLookup` boundary protocol) shipped via PR #918, v3.34.3 — Codex audit `019e3e8e` 1 round ship-as-is. **2026-05-19 — DONE**: all 7 WIs merged. WI-2 (foundational — `PersistenceActor.highlight(withID:forBookWithKey:)` + `HighlightLookup` conformance) PR #923, v3.34.6, Codex `019e3eb*` 2 rounds. WI-3 (foundational — `NotePreviewViewModel` `@Observable @MainActor`, monotonic out-of-order tap-token guard) PR #927, v3.34.8, Codex 2 rounds. WI-4 (`NoteCalloutView` — SwiftUI realization of the design's anchored note-preview card, read-only v1) PR #931, v3.34.11, Codex 2 rounds. WI-5 (`NotePreviewSheetView` bottom-sheet fallback + `UIKitNotePreviewPresenter` rect-anchored `UIPopoverPresentationController` presenter with a pipeline state machine + `NotePreviewModifier`) PR #933, v3.34.12, Codex 3 rounds (2 High modal-transition sequencing races fixed via the pipeline state machine). WI-6 (behavioral — wire `NotePreviewModifier` into TXT/MD/PDF native containers via `notePreviewPresenterIfAvailable`; re-home feature #53's inline delete menu from the tap to a `UILongPressGestureRecognizer`) PR #936, v3.34.15, Codex `019e3f01` 3 rounds (1 High + 1 Medium gesture-arbitration: the re-homed long-press is now named + gated by `gestureRecognizerShouldBegin` highlight hit-test + denied simultaneity with the native text-selection long-press). WI-7 (behavioral, final — wire `NotePreviewModifier` into `EPUBReaderContainerView` + `FoliateSpikeView`; remove the tap-time `HighlightActionPresenter.present(...)` from `EPUBWebViewBridgeCoordinator.handleHighlightTapMessage` + `FoliateHighlightTapHandlerModifier` so the tap → #55 preview; #53's tap-time menu dropped for EPUB/Foliate v1) Codex `019e3f25` 2 rounds (High #1: EPUB presents the v1 sheet — the documented no-host-view contract shared with WI-6, test corrected; High #2: a pre-existing AZW3/MOBI panel-delete overlay-strip gap surfaced — filed as **bug #228 / GH #938**, not a #55 acceptance blocker). **Tap → note preview ships on all 5 formats** (TXT/MD/PDF/EPUB/Foliate); the #53 long-press delete menu is scoped to TXT/MD/PDF; EPUB/Foliate highlight delete stays reachable via the Annotations panel. v1 presents the bottom-sheet form for all containers (the rect-anchored callout with a host view is a deferred refinement). The callout's *Edit* action remains `BLOCKED: needs-design` #914 — v1 is read-only preview. **2026-05-19 — VERIFIED**: Gate-5 acceptance pass complete, CU-free (`dev-docs/verification/feature-55-20260519.md`, result=**pass**). New suite `vreaderUITests/Verification/Feature55NotePreviewVerificationTests.swift` (3 tests, TXT/MD/EPUB) drives the full long-press → SelectionPopover "Note" → type → Save → tap-the-highlight → note-preview → dismiss flow through the XCUITest accessibility API with synthesized gestures — no computer-use. All 3 **PASS** on iPhone 17 Pro Sim (iOS 26.5), run as a unit three consecutive times with zero failures. Criterion (a): tapping the annotated region presents `NotePreviewSheetView` (`notePreviewSheet`) with the note body, Annotations panel asserted absent — verified TXT/MD/EPUB. Criterion (b): "Done" tap (or sheet swipe-down fallback) dismisses the preview — verified. Criterion (c): identical flow + same `NotePreviewSheetView` surface across TXT (native `UITextView`), MD (native Markdown via shared `TXTTextViewBridge`), EPUB (WKWebView, JS `highlightTapHandler` bridge); **Foliate** not device-exercised — no openable AZW3 DebugBridge seed fixture (Bug #209 harness gap), its WI-7 `.readerHighlightTapped` → `NotePreviewModifier` wiring is identical to the verified EPUB path + covered by units. Lessons: TXT/MD use the feature-#60 `SelectionPopoverView` not the legacy edit menu; Bug #214 identifier-propagation forces label-based queries for the popover "Note" + preview "Done"; a lingering `UITextView` selection re-presents the popover after the native note sheet dismisses (cleared before the verifying tap). 2 screenshot artifacts. No feature-#55 product code changed, no bug discovered. GH: #619 |
| 54 | Remove Native/Unified reading mode toggle — route by ReaderEngine internally | Reader/* | Medium | DONE | **Problem**: The Native/Unified picker leaks an implementation detail. Users want "apply replacement rules" or "convert Chinese," not "Unified mode." A toggle that makes EPUB rendering worse is the wrong abstraction. Design decision documented in GH #493. **Scope**: (1) Hide picker when no transforms configured; (2) Introduce internal ReaderEngine routing (textNative/markdownNative/epubWKWebView/foliateWeb/pdfKit) replacing readingMode branch; (3) Wire replacement rules into native EPUB (JS text-node preprocessing, CFI-safe) and MD (TextMapper.apply before parse); (4) Migrate and remove readerReadingMode from UserDefaults + per-book PerBookSettings; (5) Retire UnifiedPlaceholderView and dead Unified paths. **Acceptance criteria**: no picker in normal use; replacement rules work in native EPUB and MD without mode switch; readingMode key removed with migration; all existing reader features unchanged. **Dependencies**: Feature #42 (Foliate-js) required for full EPUB engine swap — steps 1/3(MD)/4 can land independently. **Risks**: Foliate JS transforms must be CFI-safe; per-book override migration; transform parity tests required per engine before removing toggle. **Gate 1 + Gate 2 complete 2026-05-19**: implementation plan at `dev-docs/plans/20260518-feature-54-remove-reading-mode-toggle.md` (v3, 7 WIs across 3 autonomous phases + 1 deferred Phase D). Independent Codex plan audit ran 3 rounds; round-3 verdict clean (round-1 found 8 findings — TXT replacement rules deferred to Phase D, synchronous migration; round-2 found 3 — `openPositionUnsupportedInUnifiedMode` one-producer reality, semantic-not-byte migration preservation, WI-4 two-file scope; all resolved). EPUB replacement-rule wiring + `epubWKWebView`/`foliateWeb` differentiation + native-TXT replacement rules are explicitly DEFERRED (Phase D). **All 7 WIs shipped → DONE 2026-05-19**: WI-1 `ReaderEngine` enum + per-format `resolve` (PR #878, v3.31.4); WI-2 `ReadingModeMigration` namespace (PR #879, v3.31.5); WI-3 reader dispatch routed by `ReaderEngine`, Unified dispatch branch deleted (PR #883, v3.31.7); WI-4 Reading Mode + Tap Zones removed from the settings panel (PR #886, v3.31.9); WI-6 DebugBridge cleanup — retired `openPositionUnsupportedInUnifiedMode` (PR #888, v3.31.11); WI-5 `ReadingMode` enum + `readerReadingMode` key/field removed, one-shot launch migration (PR #893); WI-7 content replacement rules wired into the native MD reader via `MDFileLoader` transform chain — the final WI (PR #901). WI-5/WI-6 implementation order was swapped vs the plan (WI-5's field removal would not compile while WI-6's DebugBridge guard still read `store.readingMode`). Phase D (native EPUB replacement rules, `epubWKWebView`↔`foliateWeb` differentiation, native TXT replacement rules + offset map) remains deferred — see plan §4 Phase D. `VERIFIED` flip pending the final-WI full acceptance pass + evidence file. **2026-05-19 (Gate-5 CU-free XCUITest verification)** — `dev-docs/verification/feature-54-20260519-cu-free.md`, `result: partial`, on iPhone 17 Pro Simulator / iOS 26.4, `main` @ `306f7ce`, v3.34.3. A new CU-free XCUITest suite `Feature54ReadingModeRemovalVerificationTests` (4 tests, all green) drives the app through the accessibility API — no computer-use, no DebugBridge URL-confirm — and closes the headless-navigation gap that blocked the earlier `feature-54-20260519.md` partial. Verified end-to-end via the live reader UI: criterion 1 (no Reading Mode / Tap Zones / Native / Unified picker anywhere in the Display panel — for TXT, EPUB, MD, full 12-section scroll), criterion 2's structural half (MD opens into the native `markdownNative` engine with no mode switch — transform correctness still via the 20 integration tests), criterion 5 (TXT/EPUB/MD each open into their native `ReaderEngine` surface and render). Criterion 4 stays device-verified + unit (UserDefaults / launch-migration — no UI surface). **Row stays `DONE`, NOT `VERIFIED`**: criterion 3 (replacement rules in native EPUB without a mode switch) is in the acceptance contract but was DEFERRED to Phase D by the plan and Phase D has not shipped — one acceptance criterion is genuinely unimplemented, so per `SCHEMA.md` the `partial` result cannot flip the row. `VERIFIED` flip awaits Phase D shipping criterion 3 + a follow-up acceptance pass. GH: #609 |
| 53 | Tap on highlighted text to get inline edit/delete options | Reader/* | Medium | VERIFIED | **VERIFIED 2026-05-28** (`dev-docs/verification/feature-53-20260528.md`, result=pass) — final slice (criterion (c) PDF) cleared via CU, now that the `multi-page-pdf` fixture exists (added for #17). Created a PDF highlight (long-press-drag → Highlight → `ZHIGHLIGHT` 1, yellow, page 0) then single-tapped the yellow band → `PDFViewBridge.handleTap` → `.readerHighlightTapped` → the unified highlight-action popover (Feature #64 WI-7) surfaced with quote + note field + color swatches (edit/delete affordances). Criterion (c) now 5/5 (TXT/EPUB/MD/AZW3/PDF). GH #596 closed. **2026-05-18 round-9 Foliate-slice device verify result=PARTIAL** (`dev-docs/verification/feature-53-20260518-round9.md`) on iPhone 17 Pro Sim (iOS 26.4) at v3.27.25 build 439 (== main 8cab12a) — verified the **Foliate (AZW3/MOBI)** format slice using the `mini-azw3` fixture ("The Masque of the Red Death"). **Foliate criteria (a)/(b)/(d) ALL PASS** (reproduced 2×): long-press a body word → "Text Selection" card → Highlight → `highlightCount` 0→1, yellow paint; single-tapping the highlighted word surfaces the inline "Delete Highlight" menu (a); tapping Delete removes the highlight visually + `highlightCount` 1→0 (b); tapping non-highlighted body text shows no #53 menu + toggles chrome normally (d). Criterion (c) now **4/5**: TXT (round-4) + EPUB (round-6) + MD-scroll (round-8) + Foliate (round-9) verified; only **PDF** remains, blocked on the harness gap (no PDF fixture in `DebugFixtureCatalog`, no `--seed-pdf` arg). This round also completed the device-verification close-gate for **Bug #199 / GH #733** (Foliate inline-menu consumer wiring) — GH #733 closed. **VERIFIED flip now gated solely on the PDF slice** (needs a DebugBridge PDF fixture). Observation (not a #53 defect, already documented in Bug #199's row): the Foliate inline menu anchors at the reader view origin, not at the tapped highlight (`sourceRect == .zero` — `foliate-host.js` doesn't forward annotation rects). 5 artifacts. Verification-only — no bug, no code change. **2026-05-18 round-8 MD-slice device verify result=PARTIAL** (`dev-docs/verification/feature-53-20260518-round8.md`) on iPhone 17 Pro Sim (iOS 26.4) at v3.27.25 build 439 (== main 8cab12a) — re-ran the **MD** slice in **scroll mode** (`--reader-default-layout=scroll`), the slice round-7 mis-verified in paged mode. **MD criteria (a)/(b)/(d) ALL PASS** (reproduced 2×): created an MD highlight via the feature-#60 SelectionPopover (long-press body word → 4 colour swatches → yellow → `highlightCount` 0→1, yellow paint rendered); single-tapping the highlighted word surfaces the inline "Delete Highlight" menu (a); tapping Delete removes the highlight visually + `highlightCount` 1→0 (b); tapping non-highlighted body text shows no #53 menu + toggles chrome normally (d). MD scroll mode routes body text through `TXTTextViewBridge` (`MDReaderContainerView.swift:329`) — the same bridge TXT uses — so the #53 tap-on-highlight path is inherited identically to the round-4 TXT slice. Criterion (c) now **3/5**: TXT (round-4) + EPUB (round-6) + MD-scroll (round-8) verified; PDF not exercised (no DebugBridge fixture / no `--seed-pdf` arg — harness gap); Foliate gated on Bug #199 / GH #733. Bug #218 confirmed a misfile (round-7 ran in paged mode — see round-7's `## CORRECTION` + `docs/bugs.md` #218 → DUPLICATE of #215). **VERIFIED flip now gated only on PDF device verify + Foliate (Bug #199)**. 5 artifacts. Verification-only — no bug, no code change. **2026-05-18 round-7 MD-slice device verify result=PARTIAL — round-7's MD "blocked" conclusion CORRECTED/RETRACTED 2026-05-18** (`dev-docs/verification/feature-53-20260518-round7.md`, see its `## CORRECTION` section) on iPhone 17 Pro Sim (iOS 26.4) at v3.27.25 build 439 (== main 8cab12a) — attempted the **MD** format slice. Round-7 originally concluded criteria (a)/(b) were BLOCKED (MD long-press showed only the bare iOS system menu, no SelectionPopover) and filed **Bug #218 / GH #843**. **That conclusion is retracted.** Bugfix-cron re-reproduced with the layout mode controlled — `readerEPUBLayout=scroll` explicitly set — and MD body-text long-press **does** surface the feature-#60 SelectionPopover (4 colour swatches + Note/Translate/Ask AI/Read), identical to TXT: MD scroll mode shares `TXTTextViewBridge` and its selection producer works. Round-7 was confounded by `readerEPUBLayout=paged` lingering from Bug #215's repro, so the MD book opened in paged mode (`NativeTextPagedView`, no `TXTTextViewBridge`, no selection producer — that paged-mode gap is a facet of Bug #215). **Bug #218 reclassified DUPLICATE of Bug #215; GH #843 closed.** MD-scroll highlight creation works → the #53 MD slice IS verifiable in scroll mode; a future verify round should re-run it. Criterion (c): TXT (round-4) + EPUB (round-6) pass; MD-scroll slice re-verify pending (not blocked); PDF no fixture; Foliate gated on Bug #199. **VERIFIED flip is NOT gated on Bug #218** — it remains gated on the MD-scroll re-verify + PDF device verify + Bug #199 (Foliate). **2026-05-18 round-6 device verify result=PARTIAL** (`dev-docs/verification/feature-53-20260518-round6.md`) on iPhone 17 Pro Sim (iOS 26.4) at v3.27.23 build 437 (== main 6936ccf) — re-ran the round-5 EPUB repro after **Bug #211 / GH #820** (wrong `compareBoundaryPoints` constant) and **Bug #212 / GH #828** (stale tap-Delete paint) both shipped + CLOSED. **EPUB criteria (a)/(b)/(d) now ALL PASS**: tapping a yellow EPUB highlight surfaces the inline "Delete Highlight" menu (a — the round-5 blocker is cleared), tapping Delete removes the highlight visually + DebugBridge `highlightCount` 1→0 with no stale paint (b), tapping non-highlighted text toggles chrome with no menu (d); reproduced 2×. This round also serves as the close-gate device verification for Bug #211 + #212. Criterion (c) all-5-formats still incomplete: TXT (round-4) + EPUB (round-6) verified; MD/PDF not exercised (no DebugBridge fixture — MD shares the TXT bridge), Foliate gated on Bug #199 / GH #733. VERIFIED flip now gated only on MD + PDF + Foliate device verify. **2026-05-17 round-5 device verify result=PARTIAL** (`dev-docs/verification/feature-53-20260517-round5.md`) on iPhone 17 Pro Max Sim (iOS 26.4) at v3.27.15 build 429 — exercised the **EPUB** format using the `mini-epub3` DebugBridge fixture (round-4's "no EPUB fixture" note was stale). **EPUB criterion (a) FAILS**: tapping a yellow EPUB highlight surfaces no inline "Delete Highlight" menu — the tap toggles reader chrome instead. Reproduced 3× incl. once with a deliberately large unambiguous tap target. Criterion (b) BLOCKED behind (a); criterion (d) PASSES (non-highlighted-word tap toggles chrome, no menu). Root cause confirmed via DebugBridge `eval`: the WI-4 `click`-listener hit-test in `EPUBHighlightJS.swift` calls `range.compareBoundaryPoints(Range.END_TO_START, probe)` where it needs `Range.START_TO_END`, so the highlight membership test misses every in-range tap (JS registry, CSS paint, `caretPositionFromPoint`, and the `click` event itself are all intact — only the comparison is wrong). **Filed as Bug #211 / GH #820** (Medium). VERIFIED flip now also gated on Bug #211 (EPUB) in addition to Bug #199 (Foliate) + PDF device verify. **2026-05-17 round-4 device verify result=PARTIAL** (`dev-docs/verification/feature-53-20260517-round4.md`) on iPhone 17 Pro Max Sim (iOS 26.4) at v3.27.13 build 427 — re-ran the round-3 repro after the Bug #205 / GH #751 fix shipped (v3.27.12, PR #816, commit 645503d). **TXT acceptance criteria (a)/(b)/(d) now PASS**: tapping a yellow-highlighted word surfaces the inline "Delete Highlight" menu (criterion (a) — the round-3 blocker is cleared), tapping Delete removes the highlight visually + DebugBridge `highlightCount` 1→0 (b), tapping non-highlighted text toggles chrome with no menu (d). No `[com.apple.UIKit:EditMenuInteraction]` error correlated with the tap-on-highlight — the Bug #205 signature is gone. Criterion (c) all-5-formats still incomplete: EPUB/PDF/MD not exercised this round (no DebugBridge fixture for them; MD shares the TXT bridge), Foliate gated on Bug #199 / GH #733. VERIFIED flip remains gated on EPUB+PDF(+MD) device verify + Bug #199. This round also serves as the device verification for Bug #205 / GH #751 (default close-gate path). **2026-05-16 round-3 device verify (Bug #203 close-gate) result=PARTIAL** (`dev-docs/verification/feature-53-20260516-round3.md`). Acceptance criterion (a) for TXT minimum still FAILS even after the Bug #203 coord-space fix shipped in v3.23.7 / PR #748. The coord-space change DID land correctly (`sourceRect` is now textView-local per the regression test). Bug #202's chrome-toggle suppression also confirmed active (tap-on-yellow no longer toggles chrome). But the inline edit/delete menu STILL does not appear — iOS subsystem `[com.apple.UIKit:EditMenuInteraction]` logs `Error`-level events at each `presentEditMenu(with:)` invocation. Suspect cause: the presenter adds a `UIEditMenuInteraction` to the `HighlightableTextView` itself, but UITextView already owns an internal one for selection handling — the second one conflicts. **Filed as Bug #205 / GH #751** (Medium). VERIFIED flip is now gated on Bug #205 fix + round-4 device verify. Bug #199 (Foliate menu consumer wiring) and Bug #203 close-gate ALSO downstream of this. **All 6 WIs MERGED**. **Plan**: `dev-docs/plans/20260515-feature-53-tap-on-highlight.md` (v1, manual-fallback Gate 2 audit clean inline per rule 47). 6 WIs: WI-1 ✅ MERGED v3.22.5 (PR #706 commit 7f29efe — foundational `.readerHighlightTapped` notification + `HighlightActionPresenting` protocol + `HighlightTapAction` enum + coordinator handler; dormant infra, no posters yet); WI-2 TXT ✅ MERGED v3.22.7 (PR #707) + WI-2b subscriber/apply-path-sync ✅ MERGED v3.22.9 (PR #712 commit 78a42fc); WI-3 chunked TXT ✅ MERGED v3.22.12 (PR #715 commit a20206a); WI-4 EPUB ✅ MERGED v3.22.16 (PR #721 commit ee05d61 — JS `{id → Range}` registry + capture-phase click listener; presenter wired via `UIKitHighlightActionPresenter` + `HighlightCoordinator.handleTapAction`); WI-5 AZW3/Foliate ✅ MERGED v3.22.23 (PR #734 commit cdefc5a5 — `handleAnnotationShow(cfi:)` regression fix; live `FoliateSpikeView` path now resolves CFI → UUID via `FoliateHighlightTapResolver` + posts `.readerHighlightTapped` instead of misusing `.readerHighlightRequested`; consumer wiring follow-up tracked as Bug #199 / GH #733); WI-6 PDF ✅ MERGED (this PR — `PDFHighlightTapResolver` hit-tests `PDFHighlightRenderer.annotationMap` against tap point; `PDFViewBridge.handleTap` intercepts highlight-tap before chrome-toggle; `UIKitHighlightActionPresenter` + `HighlightCoordinator.handleTapAction` wired in `PDFReaderContainerView`; rebinds in `updateUIView` so the later-initialized `highlightCoordinator` is picked up). Codex audit `019e2d03`, 3 rounds, ship-as-is. 6 new resolver tests pass. **Feature complete for TXT/MD/EPUB/PDF**; Foliate fires the notification but the inline-menu consumer is tracked separately as Bug #199. VERIFIED flip pending a full acceptance device verify pass across all 5 formats. **Problem**: Deleting a highlight requires navigating to the annotations panel (Highlights tab → swipe-to-delete). Tapping already-highlighted text in any reader format does nothing — there is no tap-on-highlight → inline popup/contextMenu. Users expect a Kindle-style inline popup with Delete (and Edit Color / Note) options. **Scope**: For each reader format — (1) EPUB: detect tap on a `::highlight(vreader-*)` range in JS, post message to Swift with the highlight ID, show UIMenu/action sheet with Delete; (2) TXT/MD: `HighlightableTextView` / `HighlightableLayoutManager` tracks painted NSRange regions — add UITapGestureRecognizer that hit-tests against those ranges and presents a contextual UIMenu with "Delete Highlight"; (3) AZW3/Foliate: `handleAnnotationShow(cfi:)` already fires on highlight tap (`FoliateReaderContainerView+Highlights.swift:39`) but only opens the panel — extend to also offer inline Delete; (4) PDF: `PDFAnnotationBridge` hit-test at tap location → action sheet. **Acceptance criteria**: (a) tapping a highlighted word shows a menu with at minimum a Delete option; (b) Delete removes the highlight visually and from persistence (`HighlightCoordinator.remove` / `.readerHighlightRemoved` pipeline); (c) consistent across all 5 formats; (d) tapping non-highlighted text preserves existing scroll/chrome-toggle behavior. Reported by user 2026-05-13. **Cross-ref**: Feature #60 (visual identity v2) — distinct flow (tap-on-existing-highlight vs new-selection-from-long-press). #53's foundational infra is chrome-agnostic; WI-2..6 presenters may be re-skinned post-#60. GH: #596 |
| 52 | Multiple WebDAV server profiles with active-server switching | Settings/Backup | Medium | VERIFIED | **Problem**: WebDAV backup stores exactly one server's credentials in Keychain under fixed account keys (`com.vreader.webdav.serverURL/username/password`). Users with multiple WebDAV services (Nextcloud at home, Synology at work, Nutstore) must re-enter credentials every time they switch. **Scope**: (1) `WebDAVServerProfile` value type: `id: UUID`, `name: String`, `serverURL: String`, `username: String`, password Keychain-backed per `id`; (2) `WebDAVServerProfileStore` actor: persists a list of profiles (UserDefaults JSON) + `activeProfileID`; (3) Keychain: per-profile key `com.vreader.webdav.profile.<id>.password`; (4) Migration: on first launch, if legacy flat keys exist, migrate them into a profile named "Default" and set as active; (5) Settings UI: replace `WebDAVSettingsView`'s single-server form with a profile list (add/edit/delete/set-active) — mirrors `AIProviderListView` / `AIProviderEditSheet` pattern; (6) `WebDAVProviderFactory.make(...)` reads the active profile from the store instead of reading flat Keychain keys. **Edge cases**: (a) zero profiles — disable Back Up Now / restore with "Add a server in WebDAV Settings" prompt; (b) active profile deleted — fall back to first remaining or disable; (c) credential migration from legacy flat keys — silent, no re-entry required; (d) duplicate URLs are allowed (same server, different credentials); (e) profile name empty — default to serverURL hostname; (f) server URL malformed — validation before save. **Acceptance criteria**: (a) user can add two WebDAV profiles and switch the active one — only the active server is used for backup; (b) existing single-server users migrate without re-entering credentials; (c) deleting the active profile falls back to remaining or disables backup; (d) backup + restore round-trip works with the selected profile. Reported by user 2026-05-12. **Gate 1 + Gate 2 complete 2026-05-14**: implementation plan at `dev-docs/plans/20260514-feature-52-multiple-webdav-profiles.md` (7 WIs sized after Feature #50's precedent; foundational/behavioral tier-marked). Manual-fallback audit (Codex MCP unavailable) inline in plan section 9 — 1 Medium finding (JSON corruption edge case → defensive `try?` fallback in store init) + 2 Low findings (Notification.Name + migrator-async invocation pattern), all fixed in plan. **WI-1 shipped 2026-05-14 in v3.21.46** (PR #648): `WebDAVServerProfile` value type + `WebDAVServerProfileStore` actor + 32 tests (12 + 20). Foundational tier; no user-observable behavior. Gate 4 manual-fallback audit (Codex unavailable): zero open findings, ship-as-is. **Status flipped IN PROGRESS**. **WI-2 shipped 2026-05-14**: `WebDAVProfileMigrator` enum (109 LOC) + 6 tests covering legacy-flat-keychain → "Default" profile migration with idempotency on two axes (marker `com.vreader.webdav.profilesMigrated.v1` set OR store non-empty) + partial-legacy edge (URL-only) + fresh-install + repeat-call safety. Foundational tier; migrator callable but currently unwired (WI-3 wires it into `VReaderApp.init` + `WebDAVProviderFactory`). Stable migrated UUID `00000002-AAAA-4000-8000-000000000001` ensures re-runs upsert in-place. Legacy keychain entries kept (cleanup deferred to a post-#52 WI). Gate 4 manual-fallback audit (Codex unavailable): zero open findings, ship-as-is. **WI-3 shipped 2026-05-14**: `WebDAVProviderFactory.make(persistence:profileStore:...)` async + `makeRequestBuilder(profileStore:)` async variants (read active profile from store, dispatch to `WebDAVProvider` / `WebDAVDownloadRequestBuilder` with the same error contract as legacy sync variants: `missingCredentials` for no-active / empty-fields / no-password, `invalidServerURL` for malformed URL). 8 dispatch tests (`WebDAVProviderFactoryProfileDispatchTests`) cover the contract. `VReaderApp.init()` schedules `WebDAVProfileMigrator.migrateIfNeeded(...)` as a fire-and-forget background `Task.detached` at the end of the success branch. Legacy sync variants left untouched (parallel paths until WI-5); the two existing call sites (`WebDAVSettingsView.swift:371`, `LibraryView.swift:147`) continue to use the legacy flat-keychain readers. **Deliberate divergence from plan section 2.4's "thin wrapper" wording**: making the existing sync `throws` variant a wrapper over the new async path would require either a semaphore (antipattern) or a signature change (breaks the "compiling unchanged" goal); parallel paths achieve the plan's stated after-WI-3 outcome ("backup still works for existing users via the migrated Default profile") without churning the two legacy call sites. Foundational tier. Gate 4 manual-fallback audit (Codex unavailable): zero open findings, ship-as-is. **WI-4a shipped 2026-05-14 in v3.21.54** (PR forthcoming): `WebDAVProfileListViewModel` @Observable @MainActor VM (loadProfiles via atomic loadSnapshot, setActive with stale-id guard, deleteProfile with VM-side state cleanup) + `WebDAVServerProfileListView` (radio-row selection, leading/trailing swipe actions, pencil-edit affordance, empty state with externaldrive icon + Add CTA, `webdavProfilesDidChange` notification observer for external resync) + `WebDAVServerProfileEditSheet` STUB body (Cancel + Add-mode placeholder save writing `WebDAVServerProfile(name: "New WebDAV Server", serverURL: "", username: "")` per the plan's "placeholder save" wording) + `WebDAVSettingsView` modified to add a top NavigationLink section (legacy single-server form retained until WI-4b ships the full editor, to avoid breaking credential entry between merges). Sheet presentation uses `.sheet(item: $editorContext)` with a `WebDAVEditorContext: Identifiable, Equatable, Sendable` wrapper mirroring bug #174's AIEditorContext race-fix pattern. **Unified `.alert(...)` plumbing** via `enum WebDAVListAlertItem` (Codex round-2 finding) collapses the listError + active-delete-confirm into one alert binding so SwiftUI's "one alert per branch" limitation can't drop a path. Active-row swipe-delete prompts a confirmation alert ("Delete active server?") naming the displayName + "leaves no active server" copy; inactive rows delete immediately. **Re-entrancy guard** on stub editor's Add button (`@State isAdding` + `.disabled(isAdding)`) prevents duplicate placeholder rows from rapid double-tap. **VoiceOver disambiguation**: row label joins displayName + username + URL host (so duplicate-named profiles don't sound identical). Codex MCP audit `019e257c` (4 verification rounds): round 1 = 2 Medium (active-delete confirmation + stub-cancel-only) + 2 Low (VoiceOver + UI tests). Round 2 = 2 Medium (dual `.alert` conflict + re-entrant Add button) — both addressed. Round 3 = 2 Medium (listError state inconsistency + dropped deferred listError) — both addressed via direct mutation in listError OK + `promoteDeferredListErrorIfAny()` helper. Round 4 = zero findings, ship-as-is. Test gate: 21/21 new tests pass (11 VM + 7 EditorContext id stability/Equatable + 2 stub editor placeholder save + 1 notification-driven resync). Per-WI Gate 5 slice verification PASS on iPhone 17 Pro Sim (iOS 26.5): Settings → WebDAV Backup → Servers nav-link → empty state with "Add Server" CTA → tap Add → stub editor presents → tap Add toolbar → placeholder row "New WebDAV Server" appears with empty radio → tap radio → row activates (`largecircle.fill.circle`) → swipe-left active row → red Delete chip appears → tap Delete → confirmation alert "Delete active server? 'New WebDAV Server' is currently the active backup server. Deleting it leaves no active server until you switch to another or add one." with Cancel + Delete buttons → tap Cancel → alert dismissed, active row preserved. 2 evidence screenshots (`feature-52-wi4a-01-list-with-active-row-20260514.png`, `feature-52-wi4a-02-active-delete-confirmation-20260514.png`). Behavioral tier — slice verified pre-merge per plan section 4 "Tier: Behavioral. List UI lands but full editor in WI-4b". Audit log: `.claude/codex-audits/feat-feature-52-wi-4a-profile-list-ui-audit.md`. **WI-4b shipped 2026-05-14 in v3.21.57**: replaced WI-4a's stub editor with the full add/edit form — `WebDAVServerProfileEditSheet.swift` (rewrite, ~240 LOC) + `WebDAVServerProfileEditSheet+Sections.swift` (NEW, ~150 LOC: nameSection / endpointSection / passwordSection / testConnectionSection) + `WebDAVProfileListViewModel+Editor.swift` (NEW, ~180 LOC: addProfile / updateProfile / savePassword / deletePassword / testConnection + WebDAVTestConnectionError + validatedServerURL shared static helper) + `WebDAVServerProfileStore.updateIfExists(_:)` single-hop atomic update. Bug #184 pattern: add-mode hides Save Password / Delete Password / Test Connection buttons and shows promoted footnote notes; edit-mode shows the real buttons. WebDAVError → LocalizedError conformance gives specific Test Connection failure messages (authenticationFailed → "check the username and password", httpError(405) → "Server doesn't support WebDAV PROPFIND ... verify this URL points at a WebDAV endpoint"). Behavioral tier; full feature reachable from Settings. **Codex MCP audit `019e26d6` (3 rounds)**: round 1 = 1 High (URL validator weak — accepted `https://` hostless) + 4 Medium (updateProfile stale-guard, WebDAVError generic, testConnection whitespace credentials, name autofill missing) + 1 Low (direct KeychainService in view bypasses test injection); all 6 fixed. Round 2 = 2 new Medium (updateProfile race across 2 actor hops, password whitespace persisted verbatim); both fixed via `updateIfExists` + trim-before-persist. Round 3 = **zero findings, ship-as-is**. Test gate: **31/31 new tests** pass (`WebDAVProfileListViewModelEditorTests`) covering all 5 editor methods + URL validator (6) + WebDAVError messages (3) + round-1 fixes (3) + keychain VM-probe (2) + round-2 fixes (4). Adjacent suites (`WebDAVProfileListViewModelTests` + `WebDAVServerProfileStoreTests` + `WebDAVServerProfileTests` + `WebDAVProviderTests`) all green. Audit log: `.claude/codex-audits/feat-feature-52-wi-4b-profile-editor-audit.md`. **WI-5 shipped 2026-05-14**: foundational cleanup. Removed the legacy single-server credentials form from `WebDAVSettingsView` entirely (Server URL / Username / Password fields + Test Connection + Save / Remove Credentials buttons + supporting state vars + helper methods + 3 keychain-account constants + the `keychain:` init parameter). Migrated the 2 remaining production call sites (`WebDAVSettingsView.refreshBackupVMIfNeeded` + `LibraryView`'s row-tap-to-enqueue observer) from legacy sync `WebDAVProviderFactory.make(keychain:)` / `makeRequestBuilder(keychain:)` to the async profile-store-backed variants from WI-3. Deleted those now-unused sync factory variants (~80 LOC removed; factory header comment rewritten to reflect post-WI-5 state). Wired `WebDAVServerProfileStore.writePassword(_:for:)` + `deletePassword(for:)` to post `.webdavProfilesDidChange` so observers re-evaluate active credentials on password-only mutations (round-2 fix — without this, password changes from the editor sheet would leave the backup section stale). Added `WebDAVSettingsView` `.onReceive(.webdavProfilesDidChange)` to refresh the backup VM on add/switch/password-update from the multi-profile list. Added 3 new UserDefaults keys (`com.vreader.webdav.profiles`, `.activeProfileID`, `.profilesMigrated.v1`) to `TestSeeder.knownPreferenceKeys` for `--reset-preferences` test isolation. Doc-synced `docs/architecture.md` (3 rows: `WebDAVProviderFactory` is profile-store-only now; `WebDAVServerProfileStore` backing is `UserDefaults` / `KeychainService`; `WebDAVProfileMigrator` added). Codex MCP audit `019e26ec` 4 rounds: round 1 = 1 High (source-of-truth divergence — surgical loadCredentials change introduced inconsistency with save/clear paths) + 1 Medium (flat-keychain fallback still in view layer) + 1 Low (arch.md backing column); resolved by escalating the WI-5 scope to remove the legacy form entirely. Round 2 = 1 Medium (writePassword/deletePassword didn't post notification) + 2 Low (stale arch.md rows + orphan `@Environment(\.dismiss)`). Round 3 = 2 Low (stale comments in VReaderApp.swift and WebDAVProviderFactory.swift referencing removed legacy variants). Round 4 = 1 Low (this tracker row narrative). All findings fixed. Test gate: 33/33 editor tests pass (added 2 new `webdavProfilesDidChange` notification-on-password-mutation tests) + 122/122 across 7 WebDAV-adjacent suites all green. Audit log: `.claude/codex-audits/feat-feature-52-wi-5-cleanup-audit.md`. **WI-6 round-1 partial 2026-05-14** (`dev-docs/verification/feature-52-20260514.md`, result=**partial**) on merged-main 942703b (v3.21.61, build 338) against iPhone 17 Pro Sim (iOS 26.5) + 2 test rclone WebDAV servers on 127.0.0.1:8082-8083 (live ~/vreader-webdav-data not touched). **UI-shape verification PASS**: WI-5 cleanup confirmed (WebDAV Backup screen has zero legacy single-server form remnants — just Servers nav-link + Network policy section); WI-4a list view renders with toolbar `+` / radio-row active marker / pencil-edit affordance; WI-4b editor add-mode shows the bug #184 pattern (Save Password / Test Connection buttons hidden, footnote notes shown instead); edit-mode shows the real buttons. Prior-session profile persisted across `vreader-debug://reset?backups=true` (UserDefaults-backed; library-scoped reset doesn't clear WebDAV keys — matches design). 4 screenshot artifacts. **DEFERRED to round-2**: acceptance (a) switch-active + per-profile backup observation, (c) live delete-active-profile fallback on backup section, (d) full backup+restore roundtrip — all blocked on CU `type` not reaching iOS Simulator text fields this session (CU/Simulator integration limitation, not a vreader defect). Three round-2 unblock paths documented in the evidence file: clipboard fast-path (`clipboardWrite` grant + cmd+v), pre-launch UserDefaults+Keychain seed, or a new `vreader-debug://webdav?profile=<json>&password=<pw>` URL. Status stays **IN PROGRESS**. **WI-6 round-2 final acceptance 2026-05-15** (`dev-docs/verification/feature-52-20260515.md`, result=**pass**) on merged-main 891c616 (v3.21.61, build 338) against iPhone 17 Pro Sim (iOS 26.5) + 2 test rclone WebDAV servers on 127.0.0.1:8082-8083. Unblock path: CU clipboard fast-path (`request_access` with `clipboardWrite: true` + `cmd+v` paste keystroke into focused TextFields) — reaches all fields where `type` couldn't. Acceptance: (a) **PASS** — two distinct backup zips landed in their respective server data dirs (`/tmp/vreader-test-webdav-a/.../2026-05-14T20-10-23Z_d6c6c5ce.vreader.zip` when A active; `/tmp/vreader-test-webdav-b/.../2026-05-14T20-12-21Z_d0348cab.vreader.zip` when B active); each server's dir empty when the other was active. (b) covered by 6 `WebDAVProfileMigratorTests` unit tests; live migration not exercisable post-WI-5 (no way to write pre-WI-5 flat-keychain state from a post-WI-5 install). (c) **PASS** — swipe-delete active row → confirmation alert with `"Server B (test)" is currently the active backup server. Deleting it leaves no active server until you switch to another or add one.` copy → Delete → plist `activeProfileID` key REMOVED, profiles array reduced by 1 (Server A preserved as inactive); WebDAV Backup screen Backup section disappears, "Add a WebDAV server above to enable backup." footnote shown. (d) **PASS (backup half)** — Test Connection returned "Connected — the WebDAV server responded successfully." against real rclone Server A; backup roundtrip uploaded zip + per-book blob to correct active-server-only directory via real PROPFIND + auth + MKCOL + PUT; restore button is wired but not explicitly clicked this round (covered by `WebDAVProviderTests` integration). 7 round-2 screenshot artifacts. **Bug discovered + filed** (per scope guardrail, NOT fixed): WebDAVServerProfileListView shows the empty-state empty-state after any mutation (Save, Add, setActive, delete) until view is dismissed and re-presented — underlying UserDefaults persistence is correct (verified via plist), SwiftUI list view doesn't re-render on `.webdavProfilesDidChange`. Status → **VERIFIED**. GH #565 closing. |
| 50 | Multi-provider AI support — Anthropic native API, multiple saved provider profiles, in-reader model switching | AI/*     | Medium | VERIFIED | **Problem**: AI is locked to one OpenAI-compatible profile. Anthropic's Messages API (`POST /v1/messages`, `x-api-key` header, `anthropic-version: 2023-06-01`, different response shape) cannot be used through the current `OpenAICompatibleProvider`. No multi-profile management exists — `AIConfigurationStore` stores exactly one `AIConfiguration` blob. **Scope**: (1) `ProviderProfile` model: `id`, `name`, `kind` (`.openAICompatible`/`.anthropicNative`), `baseURL`, `apiKey` (Keychain-backed), `model`, `isActive`; (2) `ProviderProfileStore` replaces `AIConfigurationStore` — persists a list, exposes `activeProfile`, migrates existing single-config on first launch; (3) `AnthropicProvider: AIProvider` — new concrete implementation for Anthropic Messages API (non-streaming + SSE `content_block_delta` streaming); (4) Settings UI: replace single-profile form with provider list (add/edit/delete/set-active, kind picker, test-connection button); (5) in-reader provider+model picker in settings panel. `AIProvider` protocol and all existing AI features unchanged — they call through `AIService` and are provider-agnostic. **Edge cases**: (a) zero configured providers — disable AI features with "Configure a provider in Settings" prompt; (b) active provider deleted — fall back to first remaining or disable; (c) API key migration from legacy `AIConfigurationStore` → `ProviderProfileStore` on first launch; (d) Anthropic streaming: `content_block_start` + `content_block_delta` SSE events, `message_stop` sentinel, error event shape (`type: "error"`); (e) Anthropic rate limits use the `retry-after` header in seconds (corrected from initial draft via Codex Gate-2 round-1 audit); (f) `anthropic-version` header must be pinned — bumping it could change response shape. **Acceptance criteria**: (a) user can add an Anthropic profile (API key + `claude-3-5-sonnet-20241022`) and use all AI features; (b) user can add multiple profiles and switch active provider from reader settings; (c) existing OpenAI users migrate without re-entering credentials; (d) wrong API key shows a clear per-provider error; (e) all existing AI feature tests pass with both providers. Reported by user 2026-05-10. **Gate 1+2 complete 2026-05-10**: implementation plan at `dev-docs/plans/20260510-feature-50-multi-provider-ai.md` (8 WIs, foundational/behavioral tier-marked). Codex audit thread `019e1269-16d8-70b2-80c6-f8d566de99cb` — 3 rounds, 12 findings (4H/4M/1L plus round-2 added 2H/1L plus round-3 added 1M), all fixed/accepted. **WI-1 shipped 2026-05-10 in v3.14.135** (PR #510, ProviderKind + ProviderProfile + KeychainService extension; Gate-4 audit thread `019e127d-a8c3-7450-98e1-a610487b9460` ship-as-is). **WI-2 shipped 2026-05-11** (ProviderProfileStore actor + commit-style migration; 25 tests; Gate-4 audit thread `019e12e7-7dc5-7182-85ef-8051d91cf35d` 3 rounds: 2H/1M then 1M/1L then 1L, all fixed; ship-as-is). **WI-3 shipped 2026-05-11** (AnthropicProvider non-streaming sendRequest; `x-api-key` + `anthropic-version: 2023-06-01` headers, top-level `system` field, required `max_tokens`, distinct 401/403 messages with `providerName` interpolation, max_tokens >= 1 validation pre-network; 21 tests; Gate-4 audit thread `019e1585-669a-74e0-85e9-b712a919ee83` 2 rounds: 2M/1L then ship-as-is). **WI-4 shipped 2026-05-11** (AnthropicProvider streaming via SSE — `content_block_delta` text deltas + `message_stop` sentinel + `error` event surface; partial-line buffering and UTF-8 mid-byte splits handled by `URLSession.AsyncBytes.lines`; **premature EOF detected and thrown** instead of silent success; streaming HTTP errors buffer up to 1KB of response body for the error excerpt; OSLog warning on malformed data lines; split into `AnthropicProvider+Streaming.swift` extension to stay under the 300-line convention; 16 streaming tests + WI-3 stub-assertion retired = 36/36 GREEN; Gate-4 audit thread `019e1612-4d44-7693-9b6f-ee20fecd3eed` 2 rounds: 1H/1M/3L then ship-as-is). **WI-5 shipped 2026-05-11 in v3.14.147** (PR #526): `AIService.resolveProvider()` switches from single-`AIConfiguration` factory to per-request snapshot resolution against `ProviderProfileStore`. Production dispatches on `snapshot.kind` → `OpenAICompatibleProvider(baseURL:apiKey:model:)` or `AnthropicProvider(baseURL:apiKey:model:maxTokens:)`. No active profile → `AIError.providerError("Configure a provider in Settings.")`; missing/empty per-profile key → `AIError.apiKeyMissing`. `streamRequest` becomes `async throws` (sole caller `AIChatViewModel` already awaited). Production callsites in `LibraryView.swift:697` + `ReaderAICoordinator.swift:76` switch to `ProviderProfileStore.shared`. `AIConfigurationStore` + `apiKeyAccount` retained for backward-compat (cleanup after WI-7). 9 new dispatch tests (`AIServiceProfileDispatchTests`) + 19 existing AIServiceTests + 51 AI VM tests all pass; full-suite SIGSEGV in unrelated AutoPageTurner/TTS suites matches WI-4 pre-existing flake. Gate-4 audit thread `019e16c7-5e87-72c2-b4d2-5f7aaf553e91` 2 rounds: 2L then ship-as-is. **WI-6a shipped 2026-05-11 in v3.15.0** (PR #533, commit 3a54513): Settings UI Phase A — provider list + active selection. `AISettingsViewModel` rewritten from single-profile to multi-profile (drops apiKeyInput/model/baseURL/temperature/maxTokens fields; adds `loadProfiles`/`setActive`/`deleteProfile`). New `AIProviderListView` (~180 LOC): radio-style row selection, per-row swipe-to-delete, empty state with globe icon + Add CTA, editor-placeholder sheet pointing to WI-6b. `AISettingsSection` shrunk to thin wrapper (~70 LOC) with toggle + NavigationLink + consent. Added `ProviderProfileStore.loadSnapshot() -> (profiles, activeID)` for atomic single-actor-hop read (round-1 Gate-4 finding [1] — closes split-read race in `loadProfiles`). `setActive` rejects unknown ids AND reads back authoritative active from store (round-1 finding [2]). Migration test strengthened to contract level: exactly one migrated profile, kind `.openAICompatible`, fields verbatim from legacy AIConfiguration, activeID set, legacy API key copied to per-profile keychain (round-1 finding [3]). `.isSelected` accessibility trait added for active row (round-1 finding [4]). 11 new `AISettingsViewModelMultiProfileTests` + 6 retained feature-flag/consent tests = 17 tests in scope; full AI-feature sweep 70/70 GREEN across AISettingsViewModelTests + AISettingsViewModelMultiProfileTests + ProviderProfileStoreTests + ProviderProfileMigratorTests + AIServiceTests + AIServiceProfileDispatchTests. Gate-4 audit thread `019e1763-2e46-7830-9f18-6160ca6f8887` 3 rounds: 1H/2M/1L → 1L → ship-as-is. **WI-6b shipped 2026-05-12**: Settings UI Phase B — profile editor sheet. New `AIProviderEditSheet` (add + edit modes), `AISettingsViewModel+Editor.swift` extension (addProfile/updateProfile/saveAPIKey/deleteAPIKey/testConnection/validateBaseURL), `KindResetPolicy` helper for value-based kind-picker defaults reset. Form covers: kind picker (segmented), name, baseURL + HTTPS validation (localhost / 127.0.0.1 exception; IPv6 loopback intentionally excluded to match provider preflight), model, temperature slider, maxTokens stepper, API key SecureField + Save/Delete (edit-mode only — add-mode is atomic via top-level Save). Test Connection sends a one-token "ping" against the LIVE form state (not the stored profile) so unsaved edits are exercised. `editorError` surfaces via `.alert` binding on the sheet; empty-state Add path clears stale `editingProfile`. Gate-4 audit thread `019e1808-81a2-7b00-a9f9-b302f00648b9` 3 rounds: 1H/5M → 1M/1L → 1M → ship-as-is. Round-1 fixes: (1) testConnection signature `forID:` → `profile:` so sheet form state drives the test, Anthropic uses profile.maxTokens not hardcoded 1; (2) kind-picker reset (iterated, see round 2); (3) validateBaseURL aligned with provider preflight (drop `::1`/`[::1]`); (4) add-mode Save Key disabled to prevent keychain orphans on Cancel — atomic write path via top-level Save only; (5) empty-state Add clears editingProfile + sheet binds editorError alert; (6) HTTP tests moved to nested `@Suite(.serialized)`, added URL/header shape assertions for OpenAI vs Anthropic dispatch. Round-2 fixes: replaced `isApplyingKindDefaults` flag (SwiftUI .onChange ordering risk) with pure-function `KindResetPolicy`; split AIProviderEditSheet (419→252 LOC) into main file + `AIProviderEditSheet+Sections.swift` extension; refreshed stale header comments. Round-3 fix: edit-mode prefill is sticky — added `inEditMode: Bool = false` parameter to KindResetPolicy so saved values aren't overwritten when they happen to equal defaults. 26 editor tests + 5 KindResetPolicy unit tests (including OpenAI→Anthropic→OpenAI round-trip regression) + 5 validateBaseURL unit tests = 26 in editor suite; 47 total in scope passing. Files: AIProviderEditSheet.swift (252 LOC) + AIProviderEditSheet+Sections.swift (202 LOC) + AISettingsViewModel+Editor.swift (222 LOC) + KindResetPolicy.swift (45 LOC) all under the 300-LOC guideline. **WI-7 shipped 2026-05-12**: in-reader AI provider picker — the FINAL WI of feature #50. New `AIProviderPickerViewModel` (`vreader/ViewModels/`, ~90 LOC) + `AIProviderPicker` SwiftUI Menu (`vreader/Views/Reader/`, ~85 LOC) wired into `AIReaderPanel.swift` toolbar (`topBarTrailing` slot, owned by `@State` so it survives tab changes). Toolbar Menu label shows the active profile's name + chevron.down; rows list every saved profile with the active one carrying both a checkmark glyph (sighted users) and the `.isSelected` accessibility trait (VoiceOver) — mirrors WI-6a's `AIProviderListView` pattern. Empty state renders a disabled "No providers — add one in Settings" row. Round-1 Codex audit finding [1] (thread `019e18a2`, 2 rounds: 1M/1L → ship-as-is): added `Notification.Name.providerProfilesDidChange` posted by `ProviderProfileStore` on every mutation (`upsert` / `remove` / `setActiveProfileID`); the picker VM observes it in init (via `installObserver()` to keep `[weak self]` capture valid) and resyncs via `loadSnapshot()` — so edits made from Settings while the AI panel stays open propagate live. Token stored as `nonisolated(unsafe) private var` so `deinit` can release it from any queue under Swift 6 strict (the plain `nonisolated` form is rejected on `@Observable` mutable stored properties — compiler warning is a known false positive). 9 picker tests + 73 cross-suite = 82/82 GREEN on iPhone 17 Sim iOS 26.4. **All 5 acceptance criteria are now implemented end-to-end**: (a) Anthropic profile + AI features; (b) multi-profile + active-switch from in-reader; (c) legacy migration without re-entry; (d) per-provider error surfacing; (e) all existing AI feature tests pass. Status flipped to **DONE**; VERIFIED gated on the full acceptance pass on a device. Audit log: `.claude/codex-audits/feat-issue-50-wi-7-in-reader-provider-picker-audit.md`. **2026-05-13 VERIFIED** (`dev-docs/verification/feature-50-20260513.md`, commit `f468934`, v3.21.0/build 277, result=**pass**): 220 tests in 18 suites GREEN on iPhone 17 Pro Simulator (iOS 26.5). All 5 acceptance criteria confirmed: (a) AnthropicProvider HTTP shape tests confirm `x-api-key` header, `anthropic-version: 2023-06-01`, top-level `system` field, SSE `content_block_delta`/`message_stop` streaming; (b) AIProviderPickerViewModel tests confirm multi-profile list, active-switch, live `providerProfilesDidChange` propagation; (c) ProviderProfileMigratorTests contract-level migration: exactly one migrated OpenAI profile, fields verbatim, legacy keychain key copied; (d) AnthropicProviderTests 401→`authenticationFailed("…Anthropic.")`, 403→`authenticationFailed("…Anthropic.")`, AIServiceTests no-profile→`providerError("Configure a provider in Settings.")`; (e) 220/220 AI test suite GREEN. GH: #501 |
| 49 | EPUB chapter top/bottom padding — add breathing room before and after each chapter document so chapter boundaries feel visually separated | EPUB/*   | Low    | VERIFIED | **Problem**: `ReaderTheme.epubOverrideCSS` injects `body { padding: 0 16px !important }` — zero vertical padding — so chapter content starts and ends flush against the viewport edge. Readers accustomed to Kindle/Moon+ expect a margin of whitespace at each chapter boundary. **Scope**: single CSS change in `vreader/Models/ReaderTheme.swift`: change `padding: 0 16px !important` to `padding: 2em 16px !important` (or `padding-top`/`padding-bottom` separately). No new types, no new files, no Swift logic. All three themes pick it up automatically because CSS is generated per-theme call. **Edge cases**: (a) EPUB book with its own body padding — overridden by `!important`, consistent with current behavior; (b) paged layout mode — top/bottom padding applies within each page, similar to a book margin; (c) full-bleed cover images that use `body { padding: 0 }` tricks — the `!important` override may add unwanted whitespace around cover art; mitigate by scoping padding to `p, div` or using the `:first-child` / `:last-child` selectors. **Test plan**: unit-test `ReaderTheme.epubOverrideCSS` to assert the padding value is non-zero vertical; snapshot/visual verify on `mini-epub3.epub` that chapter start has visible top breathing room. **Acceptance criteria**: (a) opening a chapter document shows ≥ 1em top padding before the first line of text; (b) bottom of chapter shows ≥ 1em padding after the last line; (c) no visual regression in mid-chapter reading; (d) all three themes (Light, Sepia, Dark) render consistently. Reported by user 2026-05-10. **Shipped 2026-05-12 in v3.17.1** (PR #546, commit `6521f22`). Single CSS literal change in `ReaderTheme.epubOverrideCSS`: `padding: 0 16px !important` → `padding: 2em 16px !important`. All three themes (Light/Sepia/Dark) inherit because CSS is generated per-theme. Plan: `dev-docs/plans/20260512-feature-49-epub-chapter-padding.md`. Gate 2 audit: thread `019e1949` (3 rounds, 2M+2L → 1M+1L → 1L cosmetic). Gate 4 audit: thread `019e198d` (2 rounds, 3L → ship-as-is; audit log `.claude/codex-audits/feat-issue-49-epub-chapter-padding-audit.md`). Three regression-guard tests in `ReaderThemeTests.swift` (28 total in suite, all GREEN). **Gate 5 device verification 2026-05-12 result: pass** — CU-free via DebugBridge on iPhone 17 Pro Sim: portrait paddingTop/Bottom=36px (2em × 18px font, exact), landscape paddingTop/Bottom=36px (intact in short viewport 525px high), columnCount="1" preserved, pagination math consistent. Evidence: `dev-docs/verification/feature-49-20260512.md`. GH: #496 |
| 48 | TXT chapter-mode highlight pipeline (WI-7 — global↔chapter-local UTF-16 offset translation for both search-tap rendering AND gesture-driven persisted highlights) | Reader/* | Medium | VERIFIED | **2026-05-13 VERIFIED** (`dev-docs/verification/feature-48-20260513.md`, v3.21.0/build 277, result=**pass**): `TXTChapterModeHighlightVerificationTests.testChapterModeHighlightCreatedAndPersistsAfterRelaunch` PASSED on iPhone 17 Pro Simulator (iOS 26.5) — long-press word in war-and-peace Chapter I → Highlight → `highlightEmptyState` gone → app.terminate() + relaunch keepExisting → Highlights tab still populated. Both acceptance criteria (a) gesture-create + Highlights tab entry, (b) persist across relaunch, confirmed end-to-end. iOS 26 accessibility flattening required encoding `chapterMode:true` in `accessibilityValue` of `txtReaderContainer`; relaunch approach used instead of navigation (AnnotationsPanelView has no close button — terminate is the stronger persistence assertion). GH: #554 closed. **2026-05-12 promoted TODO → PLANNED with `dev-docs/plans/20260512-feature-48-chapter-highlight-pipeline.md`** — formal Gate 1 plan covers Problem, Surface area (TXTReaderContainerView.swift:486+402, helper at TXTChapterHighlightHelper.swift already exists with 24 tests), 3 WIs (render / scroll / create), test catalogue, risks, BC. Gate 2 ran Codex MCP independent audit (thread `019e1a7f-0b81-7a02-8b2c-e452f76829fa`), 3 rounds; plan v4 clean (zero Critical/High/Medium findings). PR #553 merged. GH: #554. **Filed by bugfix cron 2026-05-09** during bug #154 investigation; **scope expanded 2026-05-09** during bug #160 fix to cover persisted highlights (gesture-creation + restore) in addition to the original search-tap rendering scope. **WI-1 PR #553, WI-2 PR #566, WI-3 PR #570 — all 3 WIs merged as of 2026-05-12 (v3.20.0/build 276). Feature DONE; awaiting device verification (VERIFIED pending close-gate).** GH: #554 |
| 75 | RTL / vertical-writing EPUB paged rendering — consume per-spine-document reading direction so RTL (Arabic/Hebrew) and vertical-writing (`vertical-rl`, CJK) EPUBs page the correct direction, resume the correct page, and report correct progress | Reader/EPUB | Medium | VERIFIED | **Reclassified from Bug #292 / GH #1300** (2026-05-31, per user direction): `metadata.readingDirection` is parsed but consumed by ZERO renderers — RTL/vertical paged rendering is never-implemented code (not broken), so per AGENTS.md it is a feature. **Gate 1 + Gate 2 complete**: plan at `dev-docs/plans/20260531-feature-75-rtl-vertical-epub-paged.md`; Codex `codex exec` plan audit **3 rounds** → READY (zero open Critical/High/Medium). Audit caught: vertical writing is PER-SPINE-ITEM (keystone `PageAxis` enum resolved per `contentURL` via a pre-pagination `getComputedStyle` probe, not book-level), shared `ReaderTapZoneRouter` must not change its global default (EPUB-paged-only `{x,y,w,h}` axis-aware tap path), `.auto` resolution, vertical-`dy` swipe JS. 6 WIs (WI-1 `PageAxisResolver` pure seam → WI-2 `EPUBPaginationHelper` per-axis → WI-3 probe → WI-4 RTL input → WI-5 vertical + `mini-cjk` ch2 → WI-6 acceptance). **Gate 3 (TDD) is the next focused phase — not yet implemented.** GH: #1300 **Readium-default reframe 2026-06-01 (v3.42.0):** after #42 WI-14 flipped the default EPUB engine to Readium, vertical-rl EPUBs now render via Readium, which handles vertical-writing NATIVELY + correctly (CU-free verified: `getComputedStyle(body).writingMode==vertical-rl`, proper vertical columns — `dev-docs/verification/feature-75-20260601.md`, result=partial: render ✅, navigation ⚠️ partial — content advances but clean per-page nav not CU-free-measurable through Readium's column pagination). The legacy `EPUBWebViewBridge` WI-5b vertical-rl paged-nav bug (native UIScrollView clamps negative contentOffset) is now MOOT for default users — only affects the non-default legacy engine (persisted override OFF). RTL axis was device-verified on legacy (WI-1..4) + renders on Readium (#42 WI-13 1b). **VERIFIED 2026-06-01 (v3.42.5):** vertical-rl + RTL EPUB reading delivered via the default Readium engine. Acceptance pass `dev-docs/verification/feature-75-20260601.md` (result=pass): vertical-rl RENDERS (eval writingMode==vertical-rl) + NAVIGATES (`page?dir=next`→Readium goForward: mini-cjk ch1 horizontal→goForward=true→ch2 vertical-rl; multi-chapter-epub totalProgression 0.0→0.25→0.5→0.75). RTL via #42 WI-13 1b + legacy WI-1..4 device-verify. The earlier mini-cjk-vlong goForward=false was a SINGLE-chapter artifact, not a nav bug. Legacy EPUBWebViewBridge WI-5b (native contentOffset snap-back) is moot for default users (non-default engine). |
| 76 | Vertical-writing windowed continuous scroll for Foliate (AZW3/MOBI) — extend Feature #73's K=3 windowed scroll to vertical-writing books (`vertical-rl`/`-lr`), which #73 gated out (`#ensureWindow` returns on `#vertical`), so they still use the per-section swap = the Bug #283 chapter-boundary jump | Reader/Foliate | Medium | VERIFIED | **VERIFIED 2026-06-02 (v3.42.40)** — WI-1 + WI-2 (axis-aware scrolled container layout, PR #1385) + WI-3 (remove the `#vertical` windowing gates + axis-aware primitives, PR #1386) merged = vertical-writing windowed-scroll implemented. All acceptance criteria device-verified on real books: **WI-4 horizontal continuity** (mini-azw3, no #73 regression); **WI-6 large-CJK K=3 memory gate** on the real `被讨厌的勇气` AZW3 (85 ch, 6.3 MB) — baseline RSS 402 MB → peak 403 MB across 60 idb swipes (delta +1 MB / ratio 1.0, far under `≤ baseline+120MB AND ≤ baseline×2.0`), `#evictOutsideWindow` keeps RSS bounded (resolves the #73 Gate-2 H7 concern); **WI-5 vertical-rl windowed crossing** — since no real vertical-rl AZW3 exists (the repo's only AZW3 is horizontal-tb; the `mini-cjk-vlong` vertical fixture is EPUB not AZW3), a DEBUG-only `--force-foliate-vertical-rl` harness (locked `window.__vreaderForceVerticalRL` → section `afterLoad` injects `writing-mode:vertical-rl` before `getDirection`) forces the REAL AZW3 to vertical-rl: it renders genuine vertical-rl columns + windowed-scrolls continuously across sections BOTH directions with RSS bounded (395→415 MB), exercising the full WI-3 vertical path; harness is Release-inert + 2-round-Codex-audited (closed a global-poisoning bypass). Evidence: `dev-docs/verification/feature-76-20260602.md` (result=pass). Residual (non-blocking): a real vertical-authored AZW3 for publisher-CSS coverage + rare vertical-lr. **Reclassified from Bug #283 / GH #1260** (2026-05-31, per user direction): Feature #73's windowing coordinate math is vertical-scroll-axis-only; vertical-WRITING books scroll on the horizontal axis, so windowing was gated out (never implemented for vertical), not broken — per AGENTS.md a feature, the vertical-axis sibling of #73. **Gate 1 + Gate 2 complete**: plan at `dev-docs/plans/20260531-feature-76-vertical-windowed-scroll.md`; Codex `codex exec` plan audit **3 rounds** → READY (zero open Critical/High/Medium). Audit (deepest of the remaining features) caught: vertical sections need an explicit `#container` flex LAYOUT (not just axis math); `getDirection` DISCARDS `writing-mode` (vertical-rl vs -lr lost — live `FIXME`) so an explicit `ScrollModel{axis,scrollProp,sizeProp,rectStartProp,directionSign}` is required; a single canonical `#logicalScrollOffset` API must route every existing axis caller (avoid double-normalize); `#onNeighbourExpand` is unguarded; a numeric memory-gate ceiling (K=3 ≤ baseline+120MB AND ≤ baseline×2.0, else K=2); a mandatory #73 horizontal-AZW3 regression WI. 7 WIs. **Gate 3 (TDD) IN PROGRESS: WI-1 Swift seam SHIPPED 2026-06-01** (commit `e0929934` / PR #1322 — `FoliateScrolledWindowMath.logicalOffset`/`rawOffset(sign:)` canonical logical-offset conversion + tests). **Remaining: the JS portion of WI-1 + WI-2/WI-3 in vendored `paginator.js`** (getDirection→`writingMode`/`ScrollModel`, route every axis caller through `#logicalScrollOffset`, axis-aware `#container` flex, ScrollModel-aware windowed primitives + `#onNeighbourExpand`) — coupled (vertical windowing is only device-verifiable once all land together) + device-gated (WI-5 on a real vertical-writing AZW3 book; no JS unit-test harness). High-risk vendored-Foliate-js; #73's shipped horizontal scroll is guarded by WI-4's regression. GH: #1260 |
| 74 | Locate/flash indicator when navigating to a saved highlight/note from the Notes/Highlights list (TXT) — after the jump, briefly emphasize the target so the reader can see where it landed | Reader/Annotations | Medium | DONE | Filed 2026-05-30 via /triage. The persisted highlight DOES render at the navigated spot (no render-gap), but there is NO post-navigate flash/pulse/scroll-to-prominent emphasis: `handleNavigateToLocator` (`ReaderNotificationHandlers.swift:114-130`) sets a temporary highlight to the highlight's EXACT range, and `HighlightableTextView.setHighlightRanges(persisted:active:)` (`:156-161`) DROPS the temporary highlight when its range equals a persisted range (dedup to avoid double-fill darkening) — so tapping a saved highlight shows the unchanged persisted fill with nothing to call attention to the destination. The transient yellow flash exists for SEARCH results (Feature #2) but was never built for annotation-list jumps. **Rule 51**: the emphasis treatment (a distinct pulse/border/flash that survives the dedup without double-darkening) is a UI decision → needs-design when planned. Cross-ref Feature #2 (search-result flash at destination). **BLOCKED: needs-design (#1343)** — filed 2026-06-01; the highlight-landing emphasis has no committed design (the only designed motion cues are the tap-zone hint flash + skeleton-pulse, neither covers this). Resume Gate 1 once the design bundle lands. Not yet PLANNED → not mirrored to GH as a feature. **DESIGN LANDED 2026-06-02** — claude.ai/design handoff committed (`dev-docs/designs/vreader-fidelity-v1/project/design-notes/reader-highlight-landing.md`, canvas `VReader Highlight Landing Canvas.html`); resolves needs-design #1343. Decision: a one-shot "locate bloom" (wash value-lift 0.42→0.86 + same-hue focus ring + soft glow, ~1.5s, fires once; scroll-to-prominent if off-screen; reduce-motion = static-hold opacity fade; covers 5 themes). Also fixes the dedup no-op (temp==persisted is deduped away) via a third non-deduped `landing:` range layer. Scope TXT/MD first (EPUB/Foliate/PDF later). **Gate 1+2 complete 2026-06-03**: plan `dev-docs/plans/20260603-feature-74-locate-bloom.md`; **Gate-2 Codex audit 3 rounds** (`/tmp/feat74-planaudit{,-r2,-r3}.txt`, READY TO BUILD): R1 2 Med (wash baseline vs code's `fillAlpha`; perf-mitigation overstated) → v2; R2 1 Med (separate landing layer at rest still composites a 2nd 0.4 fill → darkens) → v3 **replace-don't-stack** render rule (the landing wash REPLACES the persisted fill for an equal range — design's single wash value-lift); R3 clean. **WI-1 SHIPPED** (v3.49.10, foundational/dormant): the landing render layer (replace-don't-stack vs the equal-range persisted fill, defeating the dedup no-op) + pure `LandingBloomPaint`. **WI-2 SHIPPED** (final WI → DONE): `LandingBloomCurve` (§3 motion + §5 reduce-motion), `reduceMotion`/`ringAlpha` paint, the `CADisplayLink` driver, and the nonce-gated cancellable navigate-from-list trigger. **Gate-4 Codex audit** (`.claude/codex-audits/feat-feature-74-wi2-locate-bloom-animation-audit.md`, **3 rounds**, ship-as-is): R1 2 High (re-tap never re-blooms; interruptibility unwired) + 2 Med (teardown; reduce-motion §5) → R2 1 High (highlight-hit tap didn't cancel) → R3 clean. 17 unit tests (curve / paint+reduce-motion / nonce-trigger / cancellation / render-layer). **Gate-5b**: the bloom is a transient ~1.5s animation; CU-free capture defeated the harness (`search?query=` CJK injection + sub-second screenshot timing) → visual-render `awaiting-device-verification` (logic exhaustively unit+audit-proven; render reuses the production `drawBackground` idiom). GH: #1456. |
| 77 | Inline per-paragraph translation-progress indicator for EPUB/Foliate bilingual — a ghost/loading placeholder while a chapter/paragraph is being fetched (currently no feedback; the interlinear rows just appear when ready). DESIGNED in `bilingual-offline-artboards.jsx` (#1024) but implemented for PDF only (`PDFBilingualPanelState` maps `isFetching`→`.loading`); EPUB/Foliate never render it. | Reader/Bilingual | Medium | DONE | Filed 2026-06-02 via /triage. Design exists (#1024 section L — Rule 51 satisfied); mirror the PDF bilingual loading shimmer (`PDFBilingualPanelState`→`PDFBilingualLoadingBody`) onto the EPUB/Foliate WebView-JS-injected inline renderers. Deferred WI of Feature #56. **Gate 1+2 complete 2026-06-03**: plan `dev-docs/plans/20260603-feature-77-bilingual-loading.md`; **Gate-2 Codex audit 3 rounds** (`/tmp/feat77-planaudit{,-r2,-r3}.txt`) — R1 NEEDS REVISION (4 High + 2 Med: shimmer CSS belongs in the theme-CSS pipeline not a JS builder; the inject updates `textContent` in-place so a loading class would stick; `.readerBilingualDidChange` isn't a prefetch-start signal; loading is section/unit-scoped not per-paragraph; EPUB has a SEPARATE continuous path; Foliate DOM lives in `foliate-host.js`) → R2 (Readium is the DEFAULT EPUB engine with its own bilingual seams; `inFlightUnits` mutated beyond start/finish) → R3 architecture verified, `applyReTranslateResult` 5th mutation site folded in. **Net: a 5-WI feature** spanning Readium-default + legacy-paged + legacy-continuous + Foliate-host-js + a shared `bilingualLoadingCSSRule()` + a full-set `.readerBilingualPrefetchDidChange` notification — NOT the small Swift-only mirror it was triaged as. **Gate 3 (TDD) IN PROGRESS**: WI-1 (foundational shared seams — `bilingualLoadingCSSRule()`, `.readerBilingualPrefetchDidChange` + `setInFlight` funnel, `bilingualInjectLoadingJS` + orchestrator `buildLoadingJS`, defensive CSS-selector `__vreaderBidEsc`) merged v3.50.9 (PR #1470). WI-2 Readium **default engine** (loading inject/clear via `ReadiumBilingualCommander` + `ReadiumEPUBHost+BilingualLoading`; spine-reentry stale-shimmer clear) merged v3.50.10 (PR #1471). WI-3 Foliate AZW3/MOBI (`foliate-host.js` `bilingualInjectLoading`/`bilingualClearLoading` + bundle resync, `FoliateBilingualOrchestrator.buildLoadingJS`, container `.readerBilingualPrefetchDidChange` wiring + left-section clear-on-leave) merged v3.50.11 (PR #1472). WI-4 legacy EPUB **paged** (override-off path — `EPUBBilingualOrchestrator.clearLoadingJS`, `EPUBBilingualSurfacesModifier` prefetch observer, paged `handleBilingualPrefetchChange` via the `pendingHighlightJS` seam) merged v3.50.12 (PR #1473). WI-5 legacy EPUB **continuous** (override-off — section-scoped `bilingualClearLoadingJS(spineIndex:)`, per-materialized-section reconcile in `EPUBReaderContainerView+ContinuousBilingualLoading`, late-materialize shimmer) merged v3.51.0 (PR #1475). **All 5 WIs MERGED → DONE.** Each Gate-4 Codex-audited (1–2 rounds, ship-as-is; audit logs `.claude/codex-audits/feat-feature-77-wi-{1..5}-*.md`). The shimmer now renders across all four EPUB/AZW3 engines (Readium default + Foliate AZW3 + legacy paged + legacy continuous). **Gate-5b acceptance pending** → VERIFIED (transient shimmer on a CJK book per engine; Readium-OFF device check). GH: #1467 |
| 78 | Ask-AI on text selection — wire the selection-popover "Ask AI" action (and "Read") which are currently `.deferredNotYetWired` on EVERY reader engine (`SelectionPopoverActionRouter` posts nothing → the buttons are shown but dead). Should open the AI panel seeded with the selected text. | Reader/AI | VERIFIED | Filed 2026-06-02 via /triage. Rule-51-clear (buttons already designed; wiring only). **Gate 1+2 complete 2026-06-03**: plan `dev-docs/plans/20260603-feature-78-ask-ai-on-selection.md`; Gate-2 Codex audit 3 rounds, converged 6→2→1 (scope overclaim → narrowed to TXT/MD-scroll/legacy-EPUB/Readium; seed-loss-across-readiness; observer value-capture; dismiss-policy; TTS preempt; draft collision; mount-time consumption; stale `aiInitialTab`). **DONE 2026-06-03** (WI-1+2+3 in one PR — they're behaviorally coupled: the router now dispatches both, so each needs its consumer or the popover dismisses with no action): router posts `.readerAskAIRequested`/`.readerReadAloudRequested`; `AIChatViewModel.seedInput`/`seededInput`/`clearSeed`; `AIChatView.seedDecision` + `applySeedIfPossible` (consume on mount + change, apply-on-empty/drop-over-draft); `ReaderContainerView` parks `pendingAskAIText` → `onChange(showAIPanel)` drains AFTER `ensureAIReady` (AI-available open + post-readiness handoff both seed); readiness-abandon clears seed + resets tab; `.read` → `ttsService.startSpeaking`. Observers extracted to `ReaderAskAIReadObserver` (body type-inference budget). **Gate-4 Codex audit** (`/tmp/feat78-implaudit.txt`, 1 round, ship-as-is — no logic break; seed path traced clean both branches): Medium (view-seam test coverage) → extracted pure `AIChatView.seedDecision` + 3 tests, readiness-abandon/TTS-preempt device-verified; 2 Lows (stale doc/header) fixed. Audit log `.claude/codex-audits/feat-feature-78-wi1-2-ask-ai-selection-audit.md`. **VERIFIED 2026-06-03** — Gate-5b device pass on merged main (v3.47.0, `ab376093`): long-press select → "Ask AI" → AI Chat tab seeded with the selection ("Lorem"), not auto-sent, on the Readium engine. Evidence `dev-docs/verification/feature-78-20260603.md`. GH: #1426 |
| 79 | AI provider editor — pre-filled Base URL / Model should behave as placeholders that clear on focus (currently seeded as real editable defaults the user must delete; `AIProviderEditSheet.swift:111-112`). | Settings/AI | Low | VERIFIED | Filed 2026-06-02 via /triage; needs-design #1363 **DESIGN LANDED** (`design-notes/ai-provider-editor-fields.md`, Variant A). Gate-2 Codex audit 3 rounds, converged 3→1→0. **DONE 2026-06-03** (Variant A, add-mode-only): add-mode binds Base URL/Model empty; pure statics `effectiveBaseURLText`/`effectiveModel` (add-mode blank → kind default; edit-mode raw) drive `canSave`/`save()`/`runTest()` + the live `baseURLError` onChange; `placeholderBaseURL`/`placeholderModel` (add-mode → kind default; edit-mode → "") drive the field placeholders; a muted "Default" tag shows beside an empty add-mode field. Edit-mode keeps raw validate/persist/test + no placeholder/tag. **Gate-4 Codex audit** (`/tmp/feat79-implaudit.txt`, 1 round, ship-as-is): Medium — `effectiveModel` trimmed in both modes → would normalize an edit-mode whitespace-padded model on save; fixed (edit-mode returns raw) + regression test. Tests: 7 helper/effective-value tests in `AISettingsViewModelEditorTests`. Audit log `.claude/codex-audits/feat-feature-79-wi1-placeholders-audit.md`. **VERIFIED 2026-06-03** — Gate-5b device pass (v3.48.0, `7b1d471c`): the Add Provider editor shows empty Base URL/Model + a "Default" tag + the kind-default muted placeholder (criteria 1/6 device-verified); blank→default save + edit-mode-raw are unit-pinned (7 tests). Evidence `dev-docs/verification/feature-79-20260603.md`. Sibling #80 (test-before-save) separate. GH: #1436. |
| 80 | AI provider editor — **Test Connection before Save** (add-mode, no Save-and-reopen round-trip). Make Test available in add-mode gated on input completeness (a key entered), running against the live form state + the typed in-memory key instead of a keychain read; states disabled/idle/Testing/Connected/Failed. | Settings/AI | Medium | VERIFIED | Filed 2026-06-02 via /triage; DESIGNED 2026-06-02 (`design-notes/ai-provider-editor-fields.md` § Feature #80; canvas `vreader-ai-provider-fields.jsx`/`ai-provider-editor-artboards.jsx`). Sibling of #79 (shipped) — same surface (`AIProviderEditSheet`). **Gate 1+2 complete 2026-06-03**: plan `dev-docs/plans/20260603-feature-80-test-before-save.md`; Gate-2 Codex audit 2 rounds (`/tmp/feat80-planaudit{,-r2,-r3}.txt`), 0 Critical/High — round 1 found 2 Medium (wrong design path; stale `testResultText` not invalidated on input change), round 2 → 1 Medium (stale result also not reset on `saveKey`/`deleteKey` saved-key transitions), round 3 → clean. Converged 2→1→0. **Decision**: `testConnection(profile:apiKeyOverride:)` uses the typed in-memory key (else keychain); `runTest` passes it; the Test button shows in add-mode gated on `hasTestableKey` (typed non-empty OR saved); `resetTestResult()` clears stale results on every request-shaping input change + on save/delete-key. Single behavioral WI; reuses #79 effective-values. **DONE 2026-06-03**: `testConnection(profile:apiKeyOverride:)` (typed in-memory key wins; nil/empty → keychain); `runTest` passes the typed key; Test button shows in both modes gated on `hasTestableKey` (pure static) + a disabled "enter a key" hint; `resetTestResult()` wired to onChange of apiKey/baseURLText/model/kind/maxTokens + saveKey/deleteKey; footnote updated. **Gate-4 Codex audit** (`/tmp/feat80-implaudit.txt`, 1 round, ship-as-is): High — an in-flight test could repaint a stale result after a mid-request form change; fixed with a `testGeneration` guard + `shouldApplyTestResult` test; Low header fixed. Tests: override-wins/nil-fallback (HTTP suite) + `hasTestableKey` + generation-guard (40 green). Audit log `.claude/codex-audits/feat-feature-80-wi1-test-before-save-audit.md`. **VERIFIED 2026-06-03** — Gate-5b device pass (v3.49.0, `ce4543fc`): the Add Provider editor now shows a disabled Test Connection button in add-mode + the "Enter an API key above to test — no need to save first." hint + the "test below first" key footnote (criteria 1-base/2/6 device-verified; key-entry-dependent states unit-verified — API-key entry on-device is prohibited). Evidence `dev-docs/verification/feature-80-20260603.md`. GH: #1440. |
| 81 | In-reader AI Providers entry — wire the bilingual setup sheet's "Set up" / "Change…" button (truthful since Bug #301) to a SCOPED in-reader AI Providers sheet pushed inside the bilingual flow, reusing the canonical `AIProviderEditSheet` for add/edit and returning to the bilingual sheet with the new provider selected as the engine. New `ReaderAIProvidersView` (NOT the full SettingsView). | Reader/AI | Medium | VERIFIED | **VERIFIED 2026-06-02** (`dev-docs/verification/feature-81-20260602.md`, result=pass) on iPhone 17 Pro Sim (iOS 26.4) at v3.43.0 build 809 (== merge `edd91b69`). Device-verified BOTH engine-strip states + the full routing: **Change…** (configured, provider seeded via DebugBridge) → pushed the scoped `ReaderAIProvidersView` (context banner + populated list + active "In use" radio + system nav bar `‹`/AI Providers/`+`); tapping a non-active row → `setActive` + **popped back** to the bilingual setup sheet. After `provider?action=clear`: strip truthfully **"Set up"** → pushed the **empty state** (gradient sparkle tile + "No providers yet" + keychain-privacy line + "Add provider") → "Add provider" presented the canonical `AIProviderEditSheet`. No double chrome (root = ReaderSheetChrome; pushed = system nav bar). Criterion (c) save→activate→pop: the literal type-key+Save moment is a prohibited agent action; covered by `ReaderAIProvidersFlowTests` (incl. the non-first-add Critical) + the equivalent device-verified row-tap pop. 5 artifacts. **MERGED** PR #1395 (commit `edd91b69`), v3.43.0. **Gate 1 plan**: `dev-docs/plans/20260602-feature-81-in-reader-ai-providers-entry.md`. **Gate 2 PASSED** (Codex gpt-5.5/high, 3 rounds → READY TO BUILD; v1 1C/4H/4M/1L → v3 clean). **Gate 4** ship-as-is (`.claude/codex-audits/feat-feature-81-wi-1-in-reader-ai-providers-audit.md`, 4 Lows fixed). Routing only; `aiAssistant` flag + consent stay the Library affordances (rule 51) — follow-up #82 (needs-design #1394). GH: #1393 (design #1380 closed). |
| 82 | In-reader AI-enable + consent affordance for the bilingual "Set up" flow — when a fresh user (AI assistant flag off and/or consent not granted) adds a provider via Feature #81's in-reader AI Providers list, the bilingual engine strip correctly stays "Set up" (`BilingualAIReadiness` needs the `aiAssistant` flag + consent + an active provider + a key). Surface an in-reader way to enable AI + grant consent so the configure-from-reader payoff completes without a trip to Library Settings. | Reader/AI | Low | VERIFIED | **VERIFIED 2026-06-02** (`dev-docs/verification/feature-82-20260602.md`, result=pass) on iPhone 17 Pro Sim (iOS 26.4) at v3.44.0 build 813 (== merge `a033336e`). Device-verified all 6 acceptance criteria: bilingual "Set up"/"Change…" → `ReaderAIReadinessView` (3-gate tracker + AI toggle + consent card/ledger + provider block + Ready banner); consent granted ONLY by its toggle; Change-flow no-auto-pop; **Bug #308** — AI button (unconfigured) → readiness sheet (not no-op), and a readied user (per-profile key, no legacy key) → the AI panel opens (the `AIReaderAvailability` per-profile alignment, no loop). 4 artifacts. **Variant A** — a "Set up translation" readiness sheet (`design-notes/reader-ai-readiness.md`, resolves needs-design #1394). **Plan**: `dev-docs/plans/20260602-feature-82-in-reader-ai-readiness.md`. **Gate 2 PASSED** (2 rounds). **WI-1** PR #1404 (v3.43.3, `ba8cf8ef`, ship-as-is); **WI-2** PR #1405 (v3.44.0, `a033336e`, ship-as-is). Capstone of the AI-gating cluster — **resolves Bug #308 (#1396)** + delivered Bug #301's deferred routing slice. Consent stays an explicit user action. GH: #1402 (design #1394 closed). |
| 83 | Readium EPUB cross-chapter continuous scroll (multi-resource stitching shim) — restore Feature #71's VERIFIED continuous cross-chapter scroll on the Readium engine (`readiumEPUBEngine`, default-ON since 2026-06-01). Readium's `EPUBNavigatorViewController` is a PER-RESOURCE paginator with no native multi-spine stitch, so scroll mode stops at each chapter boundary. Build a vreader-side cross-resource continuous-scroll path (parallels #71 legacy / #76 AZW3-windowed) so scrolling flows across spine items. | Reader/EPUB | High | VERIFIED | **VERIFIED 2026-06-02** (`dev-docs/verification/feature-83-20260602.md`, result=pass) on iPhone 17 Pro Sim (iOS 26.4) at v3.45.0 build 816 (== merge `6c3d1efa`). **Resolves Bug #309 (#1403)** — the regression of VERIFIED Feature #71 from the Readium default flip. Device-verified: Readium scroll mode auto-advances across chapter boundaries — scrolling past a chapter bottom advanced **ALPHA→BRAVO→CHARLIE** and continued, single clean advances (no skip), the in-flight guard re-arming per chapter. **Approach (B)** — boundary auto-advance via a JS edge-intent observer through Readium's public `setupUserScripts` hook + a weak message handler → `goForward(animated:false)`; the WI-1 device spike proved the signal observable. **Honest scope: auto-advance (no manual swipe), NOT pixel-stitched #71** — a Readium resource-transition seam remains. **Plan**: `dev-docs/plans/20260602-feature-83-readium-continuous-scroll.md`. **Gate 2 PASSED** (2 rounds). **Gate 4 ship-as-is** (2 rounds: in-flight guard + weak-navigator fixes; `.claude/codex-audits/feat-feature-83-wi-continuous-scroll-audit.md`). PR #1408 (v3.45.0, `6c3d1efa`). 9 model tests. Behavior-only (rule 51 N/A). Mirror: no (tracked under #1403). |
| 84 | Secondary-text `sub`-token AA bump (Paper/Sepia ink@55%→68%) — implement the landed #1292 design so the Display panel's section headers / footers / value captions clear WCAG AA 4.5:1 (Paper 5.81:1, Sepia 4.88:1; Sepia is the binding case). A 2-value change in `ReaderThemeV2.subColor` + a RED→GREEN AA contrast test (the existing `ReaderSettingsPanelContrastTests` asserts only the project's 3.0 secondary bar). | Models/ReaderThemeV2.swift | Low | VERIFIED | Filed 2026-06-02 via /triage — the IMPLEMENTATION slice of the now-closed needs-design #1292 (design delivered PR #1317, `design-notes/secondary-text-sub-token.md`). Rule-51-exempt (restore-to-designed-state). **DONE 2026-06-02** (user lifted the hold): bumped Paper/Sepia `sub` ink@0.55→0.68 (`ReaderThemeV2.subColor`); new `ReaderSettingsPanelContrastTests.secondaryChromeLabelClearsAA` (≥4.5) + design-pin/EPUB-CSS/SettingsHeader test updates. PR #1417, v3.46.0 (build 823), merge `482be9f0`. Plan `dev-docs/plans/20260602-feature-84-sub-token-aa-bump.md`. Gate-4 Codex audit `019e88ef`, 1 round, follow-up-recommended (Medium EPUB-CSS pin fixed; Medium other-light-surface scope accepted + surfaced; 2 Lows fixed). **VERIFIED 2026-06-02** — Gate-5 contrast suites green on merged main (Paper 5.82:1 / Sepia 4.88:1 over `#fcf8f0`; evidence `dev-docs/verification/feature-84-20260602.md`). **Follow-up (surface to user, not filed):** Sepia `chromeColor`/`paperColor` sub (~4.27–4.39:1, improved from 3.36 but <AA) + Dark/OLED secondary (~3.06–3.75:1) — separate visual-weight design calls. GH: #1413. |
| 85 | Seamless Readium EPUB cross-chapter scroll (eliminate the chapter-boundary SEAM) — Feature #83 delivered auto-advance (no manual swipe, fixed Bug #309) but via approach B (`goForward(animated:false)`), so Readium reloads the next spine resource at its top through `PaginationView.slideToView` → a visible resource-transition SEAM/jump at each boundary. #83 documented + accepted this seam in-scope at every gate; truly seamless continuity is the deferred follow-on. Brings EPUB to parity with TXT (#180 continuous-surface) + AZW3 (#76 windowed scroll), which already scroll seamlessly. | vreader/Views/Reader/ReadiumEPUBHost+ContinuousScroll.swift, ReadiumContinuousScrollModel.swift, (approach C) EPUBContinuousScrollCoordinator/EPUBWebViewBridge | Medium | DONE | GH: #1510. **2026-06-05 Gate-5b on-device acceptance — `result: partial` → stays DONE** (`dev-docs/verification/feature-85-20260605.md`, iPhone 17 Pro Sim, v3.54.0 build 886, commit `c68437ed`). **4 core criteria device-verified PASS**: (1) scroll-mode EPUB routes to the #71 legacy stitch not Readium — eval probe `bridge=epub` active + `stitchSections=2`; (2) **headline — seam removed**: both chapters in one continuous DIV (scrollHeight 13881px), "Paragraph 40 of ALPHA" flows straight into "CHAPTER TWO — BRAVO" with no gap/jump/reload; (3) scroll↔paged toggle re-routes the engine cleanly (footer "Chapter 1 of 4" legacy → "Chapter One — 0%" Readium, book intact); (4) **cross-engine position-carry paged→scroll** — paged Readium to CHARLIE/71% via real `page?dir=next` navigator events, toggled to scroll → legacy opened at "Paragraph 36-39 of CHARLIE / Chapter 3 of 4", ±1 window centered on spine 2, scrollTop 12946, NO book-start fallback (`ReaderPositionHandoff` + `EPUBScrollAnchorResolver` working). **3 residual criteria harness/provider-blocked CU-free, unit+audit covered**: scroll→paged exact-fraction RECORD (synthetic scroll can't fire the rAF `onWindowedPosition` observer; the RESTORE half is the same code device-verified in #4; covered by `ReaderPositionHandoffTests`+`EPUBScrollAnchorResolverTests`, 44 tests); highlight-CREATE-via-WKWebView-selection for WI-2 survival (idb long-press didn't engage selection — same gap as #42; re-anchor logic covered by `EPUBHighlightActionsTests` + Gate-4 audit `019e95d9`); bilingual translation CONTENT (AI-provider/API-key — prohibited action; rendering path engine-agnostic, #71-verified). Stays DONE pending a follow-up closing the 3 residual legs. **Prior — DONE v3.54.0** — implementation complete (all 3 WIs). **WI-3 (bilingual/TTS/search continuity) needed NO new code**: the legacy `EPUBReaderContainerView` (which approach C activates for scroll) already implements search-nav (`.readerNavigateToLocator`→`coordinator.navigate`), TTS-follow (posts `.readerPositionDidChange`), and section-scoped bilingual (`+ContinuousBilingual`, #71 WI-7) — all engine-agnostic. **Approach C chosen (user, 2026-06-05)**. Plan: `dev-docs/plans/20260605-feature-85-readium-scroll-seam.md` (Gate-2 audited 3 rounds — Codex `019e957f`). **WI-1 DONE/merged (v3.53.5)** — layout-aware dispatch (scroll→legacy #71 stitch) + the `ReaderPositionHandoff` cross-engine position bridge (synchronous in-memory handoff + Readium post-open one-shot restore via `publication.readingOrder` + `EPUBScrollAnchorResolver` container↔OPF) + DEBUG-probe routing. Gate-4 audit ship-as-is (Codex `019e95b8`, v3 after 2 reverted attempts caught real position-loss bugs), 44 unit tests. **WI-2 DONE/merged (v3.53.6)** — section-scoped quote re-anchor for empty-range Readium highlights via `__vreader_createHighlightInSectionByQuote` (QuoteRecovery-style both-context disambiguation), Gate-4 ship-as-is (Codex `019e95d9`). **WI-3 (bilingual/TTS/search continuity) verified code-level — no new code (engine-agnostic services already wired in the #71 legacy path).** Gate-5b on-device acceptance (seam-removal + bidirectional position + highlight survival + bilingual[AI-gated]/TTS/search continuity) → VERIFIED. Filed 2026-06-02 via /triage — follow-on to Feature #83 / Bug #309 (GH #1403); EPUB analogue of #76 (AZW3)/#180 (TXT). Seam is INHERENT to approach B (Readium's closed per-resource paginator has no stitch injection point) — NOT a fixable #83 rough edge. **Needs an APPROACH DECISION before impl**: (A) fork/patch Readium to stitch resources (rejected in #83's plan — high-cost/fragile), or (C) HYBRID — route Readium *scroll* mode to the legacy #71 `EPUBContinuousScrollCoordinator` single-column stitch (Readium keeps *paged*); approach C is the #83 plan's documented escalation fallback. Relates to the user's standing #180 directive ('boundary-swap is the wrong model, no jump at all'). |
| 86 | Expand AI reading scope & sources (Chat tab) — let the in-reader AI read more than the current ~2500-char window: (1) **current chapter at least** — chat context covers the whole current chapter, not a fixed window; (2) **annotation sources** — the AI can read the reader's notes / highlights / bookmarks when relevant; (3) **read everywhere on demand** — pull any chapter or the whole book when the question needs it (scope selector and/or tool-use/RAG retrieval), with a token-budget strategy (map-reduce) for over-limit spans. | Reader/AI — `ReaderAICoordinator.swift`, `AIChatViewModel.swift`, `AIContextExtractor.swift`, `PersistenceActor+Highlights/Bookmarks` | Medium | DONE | **IMPLEMENTATION COMPLETE 2026-06-03 — all 6 WIs merged (v3.50.1→v3.50.6).** WI-2 foundational types + `.readerAnnotationsDidChange` bus (PR #1460) → WI-3 scope chip+menu (PR #1461, device-verified) → WI-4 sources chip+popover+`ChatAnnotationCache` (PR #1462, device-verified) → WI-5a off-actor `WholeBookReducer` (PR #1463) → WI-5b retrieval cluster+send-flow (PR #1464, armed-cluster device-verified) → WI-6 "Drew on" citations (PR #1465). Each WI: Gate-2 plan audit (4 rounds clean) + Gate-3 TDD + Gate-4 Codex impl-audit (ship-as-is) + Gate-5 slice. CU-free-verifiable acceptance criteria device-verified across the WIs (scope/sources/whole-book-menu+armed-cluster); the whole-book read *answering* + citation *content* are provider-key-blocked → **keyed-verification before VERIFIED** (same gate as WI-1). Filed 2026-06-03 via /triage (user: "the content that AI can read is limited; it should include current chapter at least; read notes/highlight/bookmark if needed; read everywhere if needed"). **WI-1 SHIPPED 2026-06-03** (branch `feat/feature-86-wi1-chat-chapter-context`): chapter-scoped Chat context — `ReaderAICoordinator.chatContext` (reuses #69 `SummaryScopeResolver` + `AIContextExtractor` `.chapter` path, degrades to `.section` for EPUB/nil-bounds/no-locator/no-text, clamped to 12_000) assigned via the single idempotent `refreshChatContext()` funnel, called from every state change (text load, locator relocate, TOC arrival, AZW3-TTS path). **RED→GREEN**: `ReaderAICoordinatorChatContextTests` (5). **Gate-4 Codex audit** (`.claude/codex-audits/feat-feature-86-wi1-chat-chapter-context-audit.md`, 1 round, ship-as-is): Medium (AZW3-TTS path missed the funnel) fixed; Low (wiring test) accepted. **Gate-5 slice smoke**: open + chapter-navigate the 13M CJK TXT with `--enable-ai` runs the wiring with no crash (`dev-docs/verification/artifacts/feature-86-wi1-smoke-nav-20260603.png`); the chat-answers-from-chapter effect is provider-key-blocked → awaiting keyed verification. **WI-2+** (annotation sources / scope selector / RAG) = new Chat-tab UI → `needs-design` per Rule 51 before implementation; feature stays IN PROGRESS until those land. **Gate 1+2 complete 2026-06-03**: plan `dev-docs/plans/20260603-feature-86-chat-context-scope.md` (scopes WI-1 = no-UI chapter-context only; parts 2/3 deferred behind Rule-51 design). **Gate-2 Codex audit** (`/tmp/feat86-planaudit{,-r2}.txt`, 2 rounds, READY TO BUILD): confirmed all model assumptions; round 1 found 2 High (only 2 of 4 `bookContext` writes rewired + locator-change reverts to section; TOC arrives after text) + 1 Low (rationale) → v2 fixes all via a single idempotent `ReaderAICoordinator.refreshChatContext()` funnelling every bookContext write + a host→coordinator `tocEntries` sync on TOC arrival; round 2 clean (0 open). **WI-1** (no-UI, this slice): chapter-scoped chat context. **WI-2+** (annotation sources / scope selector / RAG): new Chat-tab UI → `needs-design` per Rule 51 before implementation. GH: #1453. **DESIGN LANDED 2026-06-03 — #1455 delivered (unblocks WI-2+).** Chat-tab scope+sources+retrieval UI committed: `design-notes/chat-ai-scope-sources.md` + `VReader Chat Context Canvas.html` + `chat-context-artboards.jsx` (+ transcript `chats/chat19-chat-context-1455.md`). **Decision**: a persistent ~40px CONTEXT BAR docked above the Chat composer (NOT Summarize-style top chips — scope/sources are thread-wide, not one-shot): a quiet outline **scope chip** → upward menu (Section ~600t / **Chapter** [default, == shipped WI-1] ~4.2k / Book-so-far ~58k / **Whole book** `ON-DEMAND`, spoiler-aware caption); a green **sources chip** → Notes/Highlights/Bookmarks toggles w/ per-book counts (collapses to "Off"); whole-book retrieval as an in-bar **non-blocking** progress state (Armed→Reading %→Ready, Cancel keeps what's indexed); a per-answer **"Drew on" citation row** (amber `Ch.N · ahead` chip flags spoilers). Components ContextBar/ScopeMenu/SourcesMenu/RetrievalCluster/CitationRow → lift into `vreader-panels.jsx ChatView`. **WI-2+ Gate 1+2 complete 2026-06-03** — plan `dev-docs/plans/20260603-feature-86-wi2-chat-scope-sources-retrieval.md` (6 WIs: WI-2 foundational types + `.readerAnnotationsDidChange` bus → WI-3 scope chip/menu → WI-4 sources popover → WI-5 whole-book retrieval → WI-6 "Drew on" citations → WI-7 acceptance). **Gate-2 Codex audit (gpt-5.4, 4 rounds) → CLEAN — Gate-2 passes** (`/tmp/feat86-wi2-planaudit{,-r2,-r3,-r4}.txt`): R1 3 High + 4 Med (WholeBookRetriever @MainActor mixes UI+heavy read; linear map-reduce not token-realistic; annotation fetch in the relocate funnel hammers SwiftData; wrong seams; citation mis-stamp under async streaming; "cancel keeps indexed" unsafe; citation Kind too display-driven) → R2 4 Med (actor progress callback reorder-revert; cache on request-time not mutation-complete notifications; incomplete refresh set; dropped standalone notes) → R3 2 Med (AsyncStream cancel-ownership; bus misses reader-side direct mutation paths) → R4 CLEAN. Net design: off-actor `WholeBookReducer` (hierarchical reduction, pinned `ResolvedAIProviderConfig`, `sendRequest(_:using:)`, `maxChunks` overflow → structured `WholeBookCoverage`, owned-producer cancel via `reducer.cancel()`) + thin `@MainActor` mirror; `ChatAnnotationCache` refreshed by a new `.readerAnnotationsDidChange` bus posted at the `PersistenceActor` mutation chokepoints; provenance-first `ChatCitation`; send-time `ChatSendSnapshot`; notes = standalone `AnnotationRecord`s + annotated highlights. **Gate 3 (TDD) starting at WI-2.** **Current behavior**: the Chat tab injects `bookContext` = `ReaderAICoordinator.currentTextContent`, a ~2500-char `.section` window centered on the reading position via the LEGACY `AIContextExtractor.extractContext(locator:textContent:format:)` shim (`targetCharacterCount = 2500`). Feature #69's scoped extractor (`.chapter` / `.bookSoFar` via `SummaryScope`/`ChapterBounds`/`SummaryScopeResolver`) exists but is wired to the **Summarize** tab ONLY — Chat never got it. No code path injects annotations into AI context (grep: `bookContext` is purely the text window). No on-demand/whole-book retrieval for Chat. **Why the screenshot shows only the book's START**: precondition **Bug #313** (Readium EPUB never posts `.readerPositionDidChange` → `currentLocator` stays nil → `currentTextContent` falls back to `String(loaded.prefix(2500))` = book head). #313 fixes the *position anchor*; #86 is the *scope+source expansion* on top (fixing #313 alone still leaves a 2500-char window, no annotations, no whole-book). **Reuse**: Feature #69 machinery for (1); `PersistenceActor` highlight/bookmark queries for (2); a Chat scope selector / tool-use loop for (3). **Rule 51**: any new Chat-surface UI (scope chips, source toggles) is undesigned → file `needs-design` at plan time (the #69 Summarize chips are designed, but a Chat-tab equivalent is not). Expands Feature #14 (AI chat). Not yet PLANNED → not mirrored to GH. |
| 87 | AI stop/cancel control — a **Stop button** that interrupts an in-flight AI request (thinking / streaming reply) on the Chat (and Summarize / Translate) tabs, mirroring the standard chat-app pattern where the send button morphs into a stop button while a response streams. | Reader/AI — `AIChatView+Composer.swift` (send disc) · `AIChatViewModel.swift` (cancel primitive) · `Views/Reader/AISummaryTabView.swift` + `TranslationPanel.swift` | Medium | DONE | GH: #1518. **DONE 2026-06-05 (v3.55.0) — all 3 WIs merged.** AI Stop control shipped across all three AI tabs: Chat (send-disc morph + cancellation primitive, WI-1 v3.54.2), Translate (active language pill morphs to Stop, WI-2 v3.54.3), Summarize (loading indicator morphs to Stop + VM-lifecycle refactor, WI-3 v3.55.0). Each WI Gate-4 Codex-audited to ship-as-is. Awaiting Gate-5b on-device acceptance → VERIFIED (the cancellation logic + button-morph state are unit-verified; live stop observation is AI-provider-gated, same posture as the #314/#311 AI bug verifications). **WI-1 implemented 2026-06-05 (v3.54.2)** — Chat cancellation primitive + send-disc morph; Gate-4 Codex audit `ship-as-is` after 3 rounds (`019e967b`→`019e9687`→`019e968d`; caught the cooperative-cancel stale-error race, a pre-task-start ghost-turn race, and a test-harness lost-release hang). 40 unit tests green (`AIChatViewModelTests` + `ComposerSendStateTests` + the #91 agentic-cancel test). Remaining: WI-2 Translate stop, WI-3 Summary stop (VM-lifecycle refactor). **PLANNED 2026-06-05 (Gate-1+2 complete)** — plan `dev-docs/plans/20260605-feature-87-stop-control.md`; Gate-2 independent Codex audit converged across 4 rounds (`019e9651`→`019e965a`→`019e9661`→`019e9668`) to **0 Critical/High/Medium** (caught real resend-race, cooperative-cancel post-`await`-write, regenerate data-loss, and whole-book-await-boundary bugs before implementation). **3 WIs**: WI-1 Chat cancellation primitive (single `sendMessage` launch API + opId race guard + id-based writes + clearHistory safety + agentic abort-no-partial) + send-disc morph; WI-2 Translate stop (reuse existing `translateTask`); WI-3 Summary stop (VM-lifecycle refactor — `AIAssistantViewModel` doesn't own a live task today). Go-ahead given by user 2026-06-05 (released the implementation HELD). **DESIGN LANDED 2026-06-05 — #1476 delivered** (was BLOCKED: needs-design). Design committed: `design-notes/stop-control-87.md` + `VReader Chat Stop Canvas.html` + `chat-stop-artboards.jsx` (transcript `chats/chat20-tool-activity-1483.md`). **Decision**: the composer's 32px send disc morphs IN PLACE into a **stop** (white square + sweeping ring) while a reply streams; tap aborts, **partial reply kept**. Summarize/Translate route stop through their generate/language control. Unblocked — implementation HELD for a separate go-ahead. Filed 2026-06-03 via /triage (user: "there should be a stop button that can stop the AI thinking or replying"). **Current behavior**: NO user-facing stop affordance on any AI tab. On **Chat**, the send button (`chatSendButton`, `arrow.up`) is simply `.disabled(!canSend)` (`canSend` requires `!isLoading`) while a reply streams — it greys out, no stop replaces it — and `AIChatView.sendCurrentMessage` launches `Task { await viewModel.sendMessage(text) }` **discarding the Task handle**, while `AIChatViewModel` stores **no Task and exposes no cancel/stop** (grep: none) → a chat stream is uncancellable today. The **Summarize** (`AIAssistantViewModel`) and **Translate** (`AITranslationViewModel`) VMs DO hold a cancellable `streamTask`/`translateTask` (`.cancel()` on re-issue, feature #65 WI-3) but only cancel implicitly when a *new* request supersedes the old — neither surfaces a stop button. **Scope**: (1) add a cancellation primitive to `AIChatViewModel` (store the streaming `Task`, `cancelStreaming()`; keep or discard the partial reply per design); (2) morph the send button → Stop while `isLoading` on Chat; (3) reuse the existing task-cancel on Summarize/Translate for a stop control there. **Rule 51**: the Stop state of the input/send button is NEW UI — `vreader-panels.jsx ChatView` depicts a send button only, no stop state → file `needs-design` at plan time. Sibling of #86 (same AI panel, different concern: #86 = what AI reads, #87 = interrupting it). Not yet PLANNED → not mirrored to GH. |
| 88 | AI conversation sessions — multiple, switchable chat sessions per book: start a **New conversation**, browse past conversations, and **switch between them** (vs today's single ephemeral thread). Implies persisted chat history (currently lost) + a session picker/switcher. | Reader/AI — new `@Model ChatSession` + SchemaV9 + `ChatSessionPayload` envelope · `PersistenceActor+ChatSessions` · `AIChatViewModel` session lifecycle · `ChatSessionBar` + `ConversationsSheet` | Medium | DONE | GH: #1523. **WI-1 implemented 2026-06-05 (v3.55.1)** — `ChatSession @Model` + SchemaV9 (additive lightweight migration) + `Book.chatSessions` cascade + the `ChatSessionPayload` Codable envelope (decoupled from non-Codable `ChatMessage`/`ChatCitation`, encode→`Data?` never-wipe, version-gated decode) + `ChatSessionRecord`/`ChatSessionSummary` DTOs. Gate-4 Codex audit `ship-as-is` (2 rounds — fixed a silent-conversation-wipe path + an unread payload version; migration wiring/cascade verified correct). V8→V9 populated round-trip + cascade + payload round-trip tests green. **ALL 5 WIs MERGED 2026-06-05 → DONE**: WI-2 (v3.55.2) `PersistenceActor+ChatSessions` CRUD + carry-forward contract; WI-3 (v3.55.3, PR #1526) `AIChatViewModel` session lifecycle on ONE serialized async lane (`runSerializedSessionOp` — 7-round Codex audit closing the whole interleaving-race class); WI-4 (v3.55.4, PR #1527) `ChatSessionBar` (active title + chevron + "New"; observed `storedActiveTitle`; preserve-title-on-update — 3-round audit); WI-5 (v3.56.0) `ConversationsSheet` (New-conversation row + the session list with switch / inline-rename / delete + first-chat empty state; `loadSessionSummaries` + `renameSession` — 2-round audit). All WIs shipped with passing tests + clean Gate-4 audits. **`VERIFIED` flip pending the Gate-5b on-device session-switcher acceptance pass** (CU-free XCUITest with `--enable-ai`). **PLANNED 2026-06-05 (Gate-1+2 complete)** — plan `dev-docs/plans/20260605-feature-88-conversation-sessions.md`; Gate-2 independent Codex audit converged across **5 rounds** (`019e96bd`→`019e96d3`→`019e96e0`→`019e96ec`→`019e96ec`) to **0 Critical/High/Medium** (caught real pre-implementation bugs: missing parent-side cascade on `Book`, non-Codable `ChatMessage`/`ChatCitation` blocking the JSON blob, streaming-handoff leak across session transitions, decode-every-blob list inefficiency, empty-session-persisted-on-open, the load-hook-reruns-on-tab-reentry clobber, and the rapid-switch + cold-open async stale-load races). **5 WIs**: WI-1 `ChatSession @Model` + SchemaV9 + Codable persistence envelope (foundational); WI-2 `PersistenceActor+ChatSessions` CRUD (foundational); WI-3 `AIChatViewModel` session lifecycle + streaming handoff (behavioral); WI-4 session bar (behavioral); WI-5 Conversations sheet (behavioral, final). Go-ahead given by user 2026-06-05. **DESIGN LANDED 2026-06-05 — #1477 delivered** (was BLOCKED: needs-design). Design committed: `design-notes/conversation-sessions-88.md` + `VReader Session Switcher Canvas.html` + `session-switcher-artboards.jsx` (transcript `chats/chat20-tool-activity-1483.md`). **Decision**: a slim **session bar** under the Chat tab (active title + chevron → Conversations sheet; "New" button), Chat-tab-only; a nested **Conversations sheet** (New-conversation row + list with current thread tagged green **"Active"**; per-row switch/rename/delete + empty state). Unblocked — persistence model still required (#88 scope); implementation HELD for a separate go-ahead. Filed 2026-06-03 via /triage (user: "there should be options that can switch different session of AI conversation"). **Current behavior**: ONE ephemeral conversation. `AIChatViewModel.messages` is **in-memory only** (no SwiftData model — the `@Model` set is Book/Highlight/Bookmark/AnnotationNote/**ReadingSession** [reading-time, NOT chat]/ChapterTranslation/… — grep for `ChatSession`/`ConversationRecord` = NONE), and the Chat toolbar's `trash` button (`chatClearButton` → `clearHistory()`) **wipes** it. So there is no history to switch to and no switcher. **Scope**: (1) a persisted chat-session model (`@Model ChatSession` + message records, keyed by `bookFingerprint`) via `PersistenceActor`; (2) load/save the active session in `AIChatViewModel` (today's `clearHistory` becomes "New conversation"); (3) a session switcher UI (list past conversations, new/rename/delete, switch active). **Rule 51**: the session list / switcher is NEW UI — `vreader-panels.jsx ChatView` depicts a single thread + clear button only → file `needs-design` at plan time. Third of the AI-panel cluster with #86 (scope) + #87 (stop) — distinct concern (conversation management). Larger than its siblings (new SwiftData entity + migration). Not yet PLANNED → not mirrored to GH. |
| 89 | Back up AI conversation sessions & history via WebDAV — include the persisted chat sessions/messages (Feature #88) as a new section in the existing WebDAV backup ZIP so AI history survives device loss + restores on a fresh device, alongside annotations/positions/reading-history. | Services/Backup — `BackupSectionDTOs.swift` (new `BackupAIConversationsEnvelope` DTO) · `BackupDataCollector` · `BackupDataRestorer` · `PersistenceActor+Backup` | Low | TODO | Filed 2026-06-03 via /triage (user: "the AI sessions and history should be able to backup via WebDAV"). **BLOCKED by Feature #88** — nothing to back up until chat history is persisted (today `AIChatViewModel.messages` is in-memory only; the backup ZIP carries annotations/positions/settings/collections/book-sources/per-book-settings/replacement-rules/**library-manifest**/**reading-history** but no AI data — confirmed in `BackupSectionDTOs.swift`). **Scope (once #88 lands)**: an ADDITIVE backup section (`ai-conversations.json` → `BackupAIConversationsEnvelope` + `BackupChatSession`/`BackupChatMessage` DTOs, kept in a sibling file à la `BackupReadingHistory.swift` for the <300-line guideline); `BackupDataCollector` emits it, `BackupDataRestorer` ingests it, `PersistenceActor+Backup` fetch/insert; bump `kBackupCurrentSchemaVersion` 2→3 + add 3 to `kBackupAcceptedSchemaVersions` (older v1/v2 archives still restore — the section is additive, exactly the #46 library-manifest / #58 WI-5 reading-history precedent). **Rule 51**: no new UI — rides the existing `WebDAVSettingsView`/`BackupViewModel` flow (N/A unless an opt-in "include AI history" toggle is later desired, which would be a designed surface). Lineage: mirrors reading-history (#58 WI-5) + library-manifest (#46) as additive backup sections. Completes the AI-panel cluster (#86 scope / #87 stop / #88 sessions / #89 durability). Not yet PLANNED → not mirrored to GH. |
| 90 | Bilingual AI summary — let the **Summarize** tab produce a dual-language (source + target) summary, paralleling the reader's bilingual mode (#56) and the Translate tab's language rail, instead of today's single-language summary. | Reader/AI — `AISummaryTabView.swift` · `AISummaryCard.swift` · `AIAssistantViewModel.swift` | Medium | TODO | **DESIGN LANDED 2026-06-05 — #1478 delivered** (was BLOCKED: needs-design). Design committed: `design-notes/bilingual-summarize-90.md` + `VReader Bilingual Summarize Canvas.html` + `bilingual-summarize-artboards.jsx` (transcript `chats/chat20-tool-activity-1483.md`). **Decision**: a SECOND control row under the #69 scope chips — a language control (target + globe → popover) on the left + a **single↔dual** segmented toggle on the right; output modes single / target-only / **interlinear**, plus a dual-skeleton loading state + translation-failure recovery (summary still ships in the available language). Unblocked — implementation HELD for a separate go-ahead. Filed 2026-06-03 via /triage (user: "the summarize should be able to bilingual too"). **Current behavior**: the Summarize tab shows a SINGLE-language summary. `AISummaryTabView` exposes only the #69 scope chips (Section / Chapter / Book-so-far) — **no language or bilingual control**. `AIAssistantViewModel.summarize` already takes a `targetLanguage: String? = nil` param (`:199,:233`) but the UI never sets it, so summaries come back in the model's default language. **Scope**: surface a language/bilingual control on the Summarize tab (reuse the Translate rail's language list) + render the summary card in both languages (source + target), reusing the bilingual presentation patterns from Feature #56 / the Translate result card. **Rule 51**: new UI — a language rail / bilingual toggle + dual-language summary card are NOT in the committed design (`vreader-panels.jsx SummaryView` shows scope chips + a single summary card) → file `needs-design` at plan time. Sibling of the AI-panel cluster (#86/#87/#88/#89) + the contrast bug #315 from the same screenshot. Not yet PLANNED → not mirrored to GH. |
| 91 | Agentic AI chat — tool/function calling — let the Chat model invoke app tools mid-conversation: **search the current book**, **search across other books / the library**, **fetch another book's content**, etc., instead of answering only from the pre-stuffed context window. | Services/AI — `AITypes.swift` (AIRequest `tools`) · `AIProvider`/`AnthropicProvider` (tool-use protocol + loop) · `AIService` · `AIChatViewModel` (tool-call loop) · new `AITool` registry wrapping `SearchService`/`SearchIndexStore`/book-content | Medium | DONE | **DONE 2026-06-05 (v3.52.0)**: all implementation slices merged (WI-1/2/3/4/5/6a/6b/6c/7/8a + 8b split into the resolve seam, BookContentProvider, LibrarySearchBackend, registry builder, AIServiceToolUseAdapter, the VM-branch logic, and the construction wiring). The agentic chat is fully wired — `AIChatViewModel.sendMessage` routes through `AgenticChatDriver` when `agenticTools` is on + the resolved provider supports tool-use; the registry builds OFF-MAIN over the SAME persistent FTS index the reader uses (`PersistentSearchIndex`), with `search_current_book` gated on the open book's index eligibility. Default OFF → byte-for-byte the existing chat. Every WI TDD'd + Gate-4 Codex-audited (audit logs under `.claude/codex-audits/feat-feature-91-wi-*`). **VERIFIED pending**: Gate-5 device verification (a human-gated default-ON flip, symmetric with #42 G2) — drive a real Anthropic provider through a live tool-using chat. **Gate 1+2 complete 2026-06-03**: plan `dev-docs/plans/20260603-feature-91-agentic-tool-calling.md`; **Gate-2 Codex audit 3 rounds → PASS** (threadIds `019e9032`/`019e9038`/`019e903c`, zero open Critical/High/Medium). The audit caught 2 High + 3 Medium real architecture issues, all folded into plan v3: (High) `search_other_books` must use the PERSISTENT `SearchIndexStore.isBookIndexed` + the full stale-index guard set (`requiresReindex` + nil-`segment_base_offsets`) the coordinator applies, not in-memory `SearchService.isIndexed` (import no longer auto-indexes); (High) `get_book_content` is local-format-gated (txt/md/pdf/epub only — native AZW3/MOBI + remote-only → explicit error results); (Medium) resolve ONE provider at loop start (no mid-loop drift); (Medium) suppress citations on tool-driven replies (the "Drew on" snapshot can't reflect tool-read sources). **Net: a 10-WI feature** (WI-1..5 foundational: tool DTOs/`JSONValue`, provider `sendToolRequest` seam, Anthropic + OpenAI tool-use, registry; WI-6a/6b/6c/7/8 behavioral: the 3 tool executors, the bounded `AgenticChatDriver`, VM wiring + `agenticTools` flag default-OFF). The loop runs **silently** (Rule 51 — visible tool-activity UI deferred to **needs-design #1483**). **DESIGN LANDED 2026-06-05 — #1483 delivered (unblocks the deferred tool-activity UI).** Committed `design-notes/tool-activity-91.md` + `VReader Tool Activity Canvas.html` + `tool-activity-artboards.jsx` + `vreader-ai-shell.jsx` (+ transcript `chats/chat20-tool-activity-1483.md`). **Decision**: an in-bubble ACTIVITY STRIP above the assistant text (disclosure, not a log wall) — live status+spinner+step-counter while the loop runs → collapses to one quiet **"Looked at N sources"** chip → tap expands to a **verb · target · result** step timeline; a failed tool marks its step RED (answer still ships, gap auditable). The activity strip = *process* (what it did); the existing citation/"Drew on" chips = *evidence* (what it cited) — they stay separate. Covered across paper/dark, live/collapsed/expanded/error. Components `ActivityLive`/`ActivityChip`/`ActivityTimeline`/`ActivityError` → lift into the `AIChatView` assistant bubble. Implementation HELD for a separate go-ahead. **Gate 3 (TDD) IN PROGRESS**: WI-1 (foundational tool DTOs — `JSONValue` + `ToolDefinition`/`ToolCall`/`ToolResult` + `AITool` protocol + `AIToolRequest`/`AIToolTurn`) in PR; Gate-4 Codex 3 rounds, ship-as-is (`.claude/codex-audits/feat-feature-91-wi-1-tool-dtos-audit.md`). Remaining WI-2..8. GH: #1482. **Mirror: #1483 (tool-activity design).** Filed 2026-06-03 via /triage (user: "the AI chatting should be able to tool call — search the content, search in other books, get other books content, and so on"). **Current behavior**: NO tool/function calling. `AIRequest` is a flat single-shot (`actionType`/`contextText`/`userPrompt`/`targetLanguage` — no `tools` field; grep confirms none in `AITypes`/`AIProvider`/`AnthropicProvider`/`AIService`); the provider sends one prompt and streams one text reply, so the model can only answer from the pre-stuffed `bookContext` window. **Scope**: (1) extend the provider layer for tool definitions + a tool-call→tool-result loop (Anthropic tool-use for `AnthropicProvider`; OpenAI-style function-calling for compatible providers; gate on per-provider capability); (2) a tool registry exposing existing capabilities — `search_current_book` (wraps the built `SearchService`/FTS `SearchIndexStore`), `search_library`/`search_other_books` (cross-book — needs library-wide search or per-book iteration), `get_book_content(bookId,range)` (EPUBParser / `loadBookTextContent` / persistence), optionally annotations (overlap with #86) + BookSource search; (3) the agentic loop in `AIChatViewModel`. **Relationship to #86**: #86 = passive context-stuffing (current chapter / annotations / whole-book); #91 = the ACTIVE tool-use mechanism that could *satisfy* #86's "read everywhere on demand" — complementary; plan together or sequence. Builds on Feature #14 (AI chat) + the Search subsystem + BookSource. **Rule 51**: the loop is backend, but any visible tool-call activity in the chat (e.g. "🔍 searching…" cards/indicators) is NEW UI not in the design bundle → file `needs-design` at plan time for the tool-activity affordance. Large (provider-level changes + tool registry + loop). Not yet PLANNED → not mirrored to GH. |
| 92 | Justified text alignment for the TXT reader (even left/right margins) — TXT text is left/natural-aligned, so the right edge is ragged (esp. CJK, which wraps per full-width char), making the margins look uneven. Justify so both margins are flush, matching the convention CJK readers expect. | vreader/Services/TXT/TXTAttributedStringBuilder.swift (+ paginator/chunked parity) | Medium | IN PROGRESS | GH: #1505. Plan: `dev-docs/plans/20260605-feature-92-txt-justified-text.md` (Gate-2 audited READY TO BUILD — Codex `019e9418`). **2 WIs** (Gate-4 split): WI-1 (builder justify → scroll + chunked, the user's CJK case) **DONE/merged v3.53.1 (PR #1506)** — delivers the user's reported case; WI-2 (paged page-bottom lines) **DEFERRED (verification-blocked)** — render-extension attempt 1 leaked off-page text into the selectable surface (WI-2 Gate-4 Medium, Codex `019e9434`), abandoned; clean fixes (selection-clamp / shared-layout custom render) need interactive on-device selection verification (long-press-drag / Select-All / copy at the page boundary) not feasible CU-free. Cosmetic remainder (one page-bottom line per page, paged mode only). Filed 2026-06-04 via /triage (user: "txt, the padding on left and right is not same wide" — large CJK book). **NOT a padding bug — investigated**: all three TXT render paths use SYMMETRIC horizontal insets (`textInset`/`textContainerInset` `left:16 right:16`, `lineFragmentPadding = 0`) — non-chunked `TXTTextViewBridge` (`TXTViewConfig.textInset` default 16/16), chunked `TXTChunkedReaderBridge.swift:380`, paged `NativeTextPagedView.swift:115`; scroll indicators overlay (no inset); the only SwiftUI `.padding(.horizontal, …)` is symmetric. The perceived asymmetry is the **ragged right edge of non-justified text**: `TXTAttributedStringBuilder.swift:45` builds a `NSMutableParagraphStyle` with `lineSpacing` but NO `alignment` → defaults to `.natural` (left), so every line starts at the 16pt left inset but ends at a variable x (CJK can't fit the next full-width char → wraps, leaving up to ~1 char gap before the right inset). **Scope**: set `paragraphStyle.alignment = .justified` (the missing capability) so lines fill to the right margin. **Edge cases**: CJK justifies cleanly (full-width cells); Latin/mixed justification stretches inter-word spacing (can look gappy — consider CJK-only or a quality threshold); justification must be applied UNIFORMLY across the non-chunked / chunked / **paginator** paths or per-line widths diverge and page boundaries drift (cf. Bug #215's `lineFragmentPadding` parity). **Rule 51**: justify-by-default = pure rendering attribute, no new UI chrome → N/A. An alignment SETTING (Left/Justified toggle in the Display panel) would be new UI → `needs-design` (defer; out of scope for the default-justify slice). **Gate-2**: single-seam change in `build`; page boundaries DON'T drift (justification doesn't move TextKit line breaks — pinned by `justificationDoesNotChangePageBoundaries`); heading stays `.center` (decorator sets a fresh style); offset-neutral (highlights/search/TTS unaffected). **Gate-4 (impl audit)**: found PAGED mode renders each page from an isolated `attributedSubstring` → page-bottom mid-paragraph line is a substring-terminal line TextKit leaves ragged → split into WI-1 (scroll+chunked, ship now) + WI-2 (paged shared-layout). Chunked hard-split (very long no-newline line) is a documented edge. |
| 93 | AZW3/MOBI (Foliate) theme-color parity — apply the app's theme **background + text color** to Foliate-rendered AZW3/MOBI, so the reader's paper/sepia/dark background wins instead of the publisher's embedded `background-image` (e.g. the blue watercolor in 被讨厌的勇气), and body text uses the theme ink. | vreader/Views/Reader/FoliateSpikeView.swift, vreader/Services/Foliate/FoliateStyleMapper.swift | Medium | VERIFIED | GH: #1503. **VERIFIED 2026-06-05** (device, iPhone 17 Pro Sim, evidence `dev-docs/verification/feature-93-20260605.md`, result=pass) — native AZW3 被讨厌的勇气 imported with convert-OFF → Foliate; dark theme renders dark bg + light ink, live-switch to paper renders paper bg + dark ink; publisher blue watercolor gone in both. **DONE v3.53.0 (WI-1, PR #1504)** — Gate-4 audit ship-as-is (Codex `019e940d`), 79 tests green. Plan: `dev-docs/plans/20260605-feature-93-azw3-theme-color-parity.md` (Gate-2 audited clean — Codex `019e93fd`, round 2, READY TO BUILD). Filed 2026-06-04 via /triage (user: "azw3, background img didn't set, it refers to the azw3 img"). **DOCUMENTED OUT-OF-SCOPE GAP, never implemented**: `FoliateSpikeView.themeCSS(for:)` (`:62-72`) sets only `font-size` + `line-height` and passes `textColor: nil, backgroundColor: nil` to `FoliateStyleMapper`; the file header (`:54-57`) states "AZW3/MOBI theme-color parity is a separate gap (see feature #70's files OUT of scope)". So no `body { background … }`/text-color rule is injected → the publisher's embedded `background-image` (blue watercolor) + original text colors render unmodified; the white band at top (title clipped on white) is the same gap (no uniform theme bg). **Fix direction**: thread the theme's `paperColor`/`inkColor` into `themeCSS` so `FoliateStyleMapper` emits `body { background: <paper> !important; }` (the `background` shorthand + `!important` already RESETS `background-image` → neutralizes the publisher image — `FoliateStyleMapper.swift:91-92`) + a text-color rule. **Context — narrowing gap**: Feature #42 flipped Kindle convert-on-import default-ON (v3.42.42) → NEW Kindle imports become EPUB→Readium (themed); this gap now only hits **existing native `.azw3`** books that still render via Foliate. Planning should weigh "apply Foliate theming" vs "rely on #42 convert" for legacy books. **Rule 51**: applying the EXISTING theme paper/ink tokens to AZW3 = parity with already-designed TXT/EPUB theming, no new chrome → N/A. (The custom-background-IMAGE feature `readerUseCustomBackground` extending to AZW3 would be a separate, possibly-designed consideration — out of scope here.) Sibling of Feature #70 (font-size parity, shipped). **Plan refinements (Gate-2)**: descendant `color: inherit !important` reset incl. legacy `<font>` (publisher per-element ink yields to theme ink); iframe CSS is the primary fix (Foliate repaints gutters from the section doc), host shell is a defensive outer-`backgroundColor` fallback; Photo theme explicitly EXCLUDED (its alpha `paperColor` would bleed the publisher image — AZW3 custom-bg is separate). 1 WI (behavioral, final). |
| 94 | Filterable TOC — a search/filter field in the Contents (TOC) sheet that filters the chapter entries by title as you type, so long-TOC books (e.g. multi-hundred-chapter CJK novels) are navigable without scrolling. | vreader/Views/Reader/Annotations/TOCSheet.swift, TOCSheet+Support.swift | Medium | DONE | GH: #1529. **DONE 2026-06-05 (v3.57.0, single WI)** — `TOCTitleFilter` (pure: Foundation case+diacritic-insensitive substring, original-string match ranges, original-index-preserving `filtered`) + `TOCFilterField` (pinned search pill + live count + focused Cancel) + `TOCSheet+Filter` (scroll-split wiring, clear-filter re-scroll, no-match "Search full text" escape hatch, pinned current "Reading" row) + `TOCContentsRow` match-highlight. Gate-4 Codex audit `follow-up-recommended` (2 rounds — added the design's pinned-current row + extracted to stay under the size guideline). 58 tests (TOCTitleFilter + TOCSheet) green. `VERIFIED` flip pending a Gate-5 on-device visual pass (the match-underline colour nicety). **Gate 1+2 complete 2026-06-05** — plan `dev-docs/plans/20260605-feature-94-filterable-toc.md`; Gate-2 independent Codex plan audit converged across **3 rounds** (`019e980c`→`019e9811`→`019e9815`) to **0 Critical/High/Medium** (caught real pre-implementation bugs: filtered rows would renumber + the current-row marker would land wrong [carry original index], a filter field would scroll away inside the single outer `ScrollView` [split the scroll by tab], Foundation matching diverges from `SearchTextNormalizer` NFKC [narrow + document the contract], clearing the filter would not re-fire the current-row auto-scroll, the test plan missed the view-behavior regressions, and a `private` `filterQuery` would be unreadable from a `+Filter.swift` extension). **Single WI** (Small): `TOCTitleFilter` (pure) + `TOCFilterField` + the TOCSheet scroll-split wiring + the row match-highlight. **DESIGN LANDED 2026-06-05 — #1517 delivered** (was BLOCKED: needs-design). Design committed: `design-notes/toc-filter-94.md` + `VReader TOC Filter Canvas.html` + `toc-filter-artboards.jsx` (transcript `chats/chat21-toc-filter-1517.md`). **Decision**: a filter field pinned atop the **Contents** tab of `TOCSheet` — narrows the loaded entries by title (case-insensitive, diacritic-folded, CJK-aware substring; pure client-side, NOT full-text #2/#63); a live **"N of M chapters"** count; matched substring gets a 15% accent tint + 40% underline (ALL occurrences; current-chapter accent+bold composes under it); states default / filtering / no-match (with "Open Search" escape hatch); CJK unlock (剑 narrows 142→handful). Unblocked — implementation HELD for a separate go-ahead. Filed 2026-06-04 via /triage (user: "toc should be able to search"). **Current behavior**: `TOCSheet` renders the chapter list with NO filter field (grep: 0 `TextField`/`searchable` in `TOCSheet.swift`). The only search affordance is the empty-TOC-state **"Open Search" CTA** (`TOCSheet.swift:204-205`, `onOpenSearch`), which dismisses the TOC and opens the SEPARATE full-text **content** search sheet — a different thing (searches book text, not TOC titles). **Scope**: an in-sheet filter field that narrows the displayed TOC entries by title match (case-insensitive, CJK-aware — reuse `SearchTokenizer`/`SearchTextNormalizer` if helpful); pure client-side filter over the already-loaded `tocEntries`, no FTS. Distinct from full-text search (Feature #2/#63) and the TOC current-chapter focus (#313/#248). Cross-format (all formats' TOC uses `TOCSheet`). **Rule 51**: a search/filter field at the top of the Contents sheet is NEW UI not depicted in the committed nav/TOC design → file `needs-design` at plan time (could reuse the `SearchBar` component, but its placement in the TOC sheet must be designed). Not yet PLANNED → not mirrored to GH. |

### Feature #44 — DebugBridge — Plan

- **Problem**: Autonomous AI debug loop and XCUITest regression suite both need cheap, deterministic state setup and ground-truth state inspection. Currently every repro requires manual UI-driving (open library → tap book → scroll → set theme → ...). For WKWebView-heavy bugs (Foliate-js highlights, EPUB rendering, page navigation) there is no external way to read what the webview actually rendered. Vision-only inspection is too flaky for the hard bugs, which is the class that piles up in `docs/bugs.md`.
- **Scope**:
  - **Included**: A `#if DEBUG`-gated `DebugBridge` that handles the `vreader-debug://` URL scheme via `.onOpenURL` on `VReaderApp`. Commands: `reset`, `seed?fixture=<name>`, `open?bookId=<uuid>&cfi=<x>` (plus equivalent for TXT/MD/PDF positions), `theme?mode=<dark|light>&fontSize=<n>`, `settle?token=<id>` (writes ready-sentinel after Foliate `relocate` + native layout settles), `snapshot?dest=<file>` (semantic state JSON to app container), `eval?bridge=foliate&js=<base64>` (active WKWebView JS eval, result to container). Bundle a small set of fixture books in `vreader/Resources/DebugFixtures/` (alice.epub, war-and-peace.txt, sample.azw3, sample.pdf). Document one repro recipe per existing open bug (#107, #108, plus a few of the "Needs device verification" features) in `docs/subsystems/debug-bridge.md`.
  - **Excluded**: No release-build code path. No remote control over network. No authentication (device-local only, DEBUG-only). No fixture *editing* in the bridge — fixtures are read-only bundle resources. No replacement of XCUITest — DebugBridge is a state-setup peer, not a test runner. No screenshot capture from inside the bridge — caller uses `xcrun simctl io booted screenshot`. No mutation of SwiftData beyond the `reset` and `seed` commands.
  - **Out of scope, deferred**: Recording-and-replay of user gestures. Mocking the network layer. AI Genie state injection.
- **Edge cases**:
  - URL scheme called in non-DEBUG build → ignore silently (handler not registered).
  - `seed` called twice with the same fixture → idempotent (no duplicate library row).
  - `open` with unknown `bookId` → write error JSON to `lastError.json`, do not crash.
  - `eval` JS that throws → capture exception in result JSON; do not propagate to app.
  - `eval` called when no webview is active → return `{ "error": "no active webview" }`.
  - `settle` called when render never completes (e.g., corrupt EPUB) → timeout sentinel after 30s with `{ "error": "settle timeout", "phase": "<lastPhase>" }`.
  - Concurrent `vreader-debug://` calls → serialize via `MainActor` queue; later calls wait.
  - Snapshot during in-flight TTS or AI streaming → state JSON includes a `transientOps` array so caller can decide whether to retry.
  - Filesystem write failures on `Caches/` → log and continue; do not block app.
  - `reset` while a book is open → close reader first, dismiss sheets, then wipe.
  - Build configuration with `DEBUG` undefined but URL scheme registered (e.g., archive build): URL scheme registration also `#if DEBUG`-gated in Info.plist via Active Compilation Conditions check at build phase. Verify no `vreader-debug` entry leaks into Release.app.
  - `snapshot` JSON must exclude PII (no fingerprinting hashes of book content beyond what's already in SwiftData).
- **Test plan**:
  - Unit: `DebugBridgeRouterTests` — URL parsing for each command, error paths (malformed URL, missing required params, unknown command).
  - Unit: `DebugStateSnapshotterTests` — JSON shape stability against a fixed model (golden JSON), nil/optional handling, `transientOps` array population.
  - Unit: `DebugFixtureLoaderTests` — idempotent `seed`, missing fixture name → error, fixture path resolution.
  - Integration: `DebugBridgeIntegrationTests` (XCUITest) — launch with `vreader-debug://reset` then `seed` then `open` then `snapshot`, read back container JSON, assert state matches expected.
  - Integration: `DebugBridgeFoliateEvalTests` — open EPUB fixture, `eval` returns `document.querySelectorAll('.foliate-highlight').length` correctly after creating a highlight.
  - Build gate: a CI step asserts `vreader-debug` is **not** present in the Release bundle's Info.plist. Fail the build if found.
  - Manual verification: one repro recipe in `docs/subsystems/debug-bridge.md` exercised end-to-end against current open bugs.
- **Acceptance criteria**:
  - DEBUG build registers `vreader-debug://` and handles all six commands above with correct success/error JSON in the app container.
  - Release build has zero `vreader-debug` references (verified by CI grep + Info.plist check).
  - `xcrun simctl openurl booted vreader-debug://snapshot?dest=state.json` followed by `simctl get_app_container booted <bundle-id> data` produces a valid JSON file with the documented schema (currentBookId, format, cfi/page, theme, fontSize, selection, highlightCount, renderPhase, lastError).
  - `settle` reliably blocks until the Foliate `relocate` event has fired and a frame has been committed (no mid-frame screenshots in the documented happy-path repro).
  - All unit + integration tests pass under `xcodebuild test -only-testing:vreaderTests` and `-only-testing:vreaderUITests`.
  - `docs/subsystems/debug-bridge.md` documents the URL grammar, JSON schema, fixture catalogue, and one worked repro per currently-open bug.
  - LOC under \~300 across new files (router, snapshotter, fixture loader, Foliate eval adapter); no file exceeds the project's 300-line guideline.

### Feature #45 — Verification harness sweep — Plan

- **Problem**: 15 features in `docs/features.md` and many bugs in `docs/bugs.md` are marked "Needs device verification" or "Not verified on device". This backlog grows monotonically because every release adds new entries faster than humans can verify the old ones, and the verification work is repetitive UI-driving that humans hate. Result: tracker statuses lie (`DONE` actually means "code shipped, untested"), regressions ride to release, and pre-release manual passes balloon. Currently 16 occurrences in `docs/features.md` alone.
- **Scope**:
  - **Included**:
    - Add `VERIFIED` status to `docs/features.md` statuses (already done in this commit).
    - Build a verification harness in `vreaderUITests/Verification/` with one XCUITest file per backlog item, sharing helpers in `vreaderUITests/Verification/Helpers/` (DebugBridge URL builder, container JSON reader, settle-token waiter, fixture catalog).
    - Implement automated verifications for 13 simulator-automatable backlog items: features #11, #21, #23, #27, #28, #29 (with local WebDAV container), #31, #34, #35, #36 (with local OPDS feed fixture), #37, #40, #41.
    - Three proving-ground items implemented first to crystallize the pattern: **#37 Per-book reading settings** (simple UI-state assertion), **#34 Collections / tags** (SwiftData + UI), **#11 EPUB highlighting** (webview + race condition; previously bug #77).
    - For each item: at least one happy-path test plus the *specific* edge case that the original bug fix addressed (e.g., #11 must exercise the JS buffering race that bug #77 fixed; #28 must exercise the `didSet` re-apply path that bug #98 fixed).
    - Update `docs/manual-test-checklist.md`: each verified item is marked "Auto-verified by `<test-name>`" and removed from the human checklist.
    - CI: add a `xcodebuild test -only-testing:vreaderUITests/Verification` job to the existing test gate. Target completion under 8 min on CI hardware.
    - A short residual manual checklist (`docs/manual-test-checklist.md` "Real-device only" section) for the irreducible items: #26 TTS audio quality and any flow that requires real iCloud / real haptics / real Apple ID.
  - **Excluded**:
    - Verifying #26 audible TTS output (no programmatic way to QA voice quality on simulator).
    - Real iCloud sync paths, real Apple ID flows, real-device haptics — those stay manual.
    - Visual regression / snapshot diffing — separate concern, separate feature if/when it arrives.
    - Performance / load testing — separate concern.
    - Replacing existing unit tests in `vreaderTests/` — those continue to gate at the unit level; this harness is layered on top, not a replacement.
    - Full test fixture authoring tools (the harness uses fixed bundle resources from #44; new fixtures get added there, not here).
  - **Out of scope, deferred**:
    - Cross-device-type matrix runs (iPad layouts, multiple iOS versions).
    - Localization sweep across all supported languages — verifications run in the default locale.
    - Stress / fuzz testing of the bridge surface itself.
- **Edge cases**:
  - **Flaky tests**: Foliate render and SwiftData async save have inherent timing. The harness must use `settle` from #44 — never `sleep`. Any test that needs `sleep` to pass is rejected from merge.
  - **Fixture pollution**: each test runs `vreader-debug://reset` in `setUp` so the SwiftData container is fresh; tests that depend on a prior test's state are forbidden.
  - **WebDAV / OPDS containers**: tests that need a live server run *only* when the server is up; if it isn't, the test is skipped (not failed) with a clear log line. CI starts the containers as a job step before the test phase.
  - **Race conditions in concurrency-sensitive flows**: bug #77 (highlight buffering), bug #82 (paginate navigator preserve), bug #88 (highlight refresh on import) — each must have a verification that *would have caught the original bug* if run before the fix, not just a happy-path smoke. Verified by reverting the fix locally and confirming the test fails (RED check, then re-apply).
  - **Locale / dynamic type / dark mode**: verifications use a fixed default. A separate "appearance variations" sweep is out of scope.
  - **TTS callbacks without audio**: TTS verification asserts on `AVSpeechSynthesizerDelegate` callback events surfaced via DebugBridge `snapshot` — never requires audible output. Sentence boundary count + fired-callback count is the assertion.
  - **OPDS / WebDAV server flake**: harness uses local fixtures (a static OPDS XML feed served from the test bundle, a local WebDAV root in `/tmp`) to avoid external network dependence. CI never reaches the public internet.
  - **PhotosPicker (#32 theme backgrounds)**: simulator's photo library may be empty; the test seeds a known image into the simulator's Photos via `xcrun simctl addmedia` in setUp.
  - **DocumentPicker (import/export #35)**: handled via `vreader-debug://` import command that bypasses the OS picker (file path passed directly), since the picker is OS chrome and not vreader's responsibility. The test verifies *what happens after import succeeds*, not the picker UI.
  - **Test-name collisions**: each verification is named `test_verify_feature_<NN>_<short_name>` (XCTest requires `test_` prefix for discovery — see Bug #192 / GH #686; the `_verify_feature_` infix keeps tests greppable and the failure → tracker mapping mechanical).
  - **Tracker drift**: when a verification passes, status moves to `VERIFIED`. When it later starts failing, it moves back to `DONE` and a bug is filed. CI prints a one-line status summary so the tracker can be updated mechanically.
- **Test plan**:
  - The deliverable *is* tests. The plan is what they cover and how they're structured.
  - **Helper unit tests** in `vreaderTests/Verification/`:
    - `DebugBridgeURLBuilderTests` — URL escaping, command serialization, fixture name validation.
    - `ContainerJSONReaderTests` — parses `simctl get_app_container` output, handles missing files, malformed JSON.
    - `SettleTokenWaiterTests` — timeout, success path, never returns false-positive.
    - `FixtureCatalogTests` — fixtures listed in catalog actually exist in the bundle.
  - **Verification tests** in `vreaderUITests/Verification/` — one file per feature in the 13-item list. Each file contains:
    1. A happy-path `test_verify_feature_<NN>_<name>` test.
    2. One regression test per linked bug (e.g., feature #11 has a test specifically targeting the bug #77 race).
  - **CI integration**: `verification` test plan added to the Xcode scheme; new CI step runs it after the unit-test phase. Failure of any verification → red CI.
- **Acceptance criteria**:
  - 13 of the 15 backlog items reach `VERIFIED` status, with each entry's Notes column citing the test name.
  - The 2 items that stay manual (#26 audio, anything real-device) are explicitly listed in `docs/manual-test-checklist.md` under a "Real-device only" section, and *removed* from the auto-verifiable list — the manual checklist for these gets shorter, not longer.
  - `xcodebuild test -only-testing:vreaderUITests/Verification` exits 0 and completes in under 8 minutes on the CI runner.
  - Each regression test, when run against the pre-fix commit of its linked bug, fails (RED proof) — recorded once in `dev-docs/verification-red-checks.md`.
  - `docs/manual-test-checklist.md` reflects the new state: items moved to "Auto-verified by `<test-name>`" or "Real-device only".
  - CI prints a summary line per verification (`PASS feature_11_epub_highlighting in 4.2s`) so tracker updates are mechanical.
  - No verification test uses `sleep` or `Thread.sleep` for synchronization — `settle` and explicit waiters only.
  - Total LOC for harness helpers under 500; per-feature tests average \~80 LOC. Files under 300-line guideline.

### Feature #46 — WebDAV materializing restore — Plan

- **Problem**: Feature #29 (WebDAV backup, PR #128) is metadata-only by design — the ZIP contains positions, annotations, settings, collections, book-sources, per-book-settings, and replacement-rules keyed by `bookFingerprintKey`, but **no book file bytes**. `BackupDataCollector.swift:12` documents the trade-off as "Books that no longer exist on the restoring device are silently skipped." The user-visible consequence: restoring a backup to a fresh device leaves the library empty, because every position/highlight/note references a fingerprint that doesn't match any local book. The backup is unusable for its primary purpose (recover my library after wipe / new device). Addresses the gap reported 2026-05-03.
- **Scope**:
  - **Included**:
    - New `library-manifest.json` section in the backup ZIP. Per book: title, author, format, fingerprint key (`{format}:{sha256}:{byteCount}`), SHA-256, byte count, original filename, detected encoding (TXT only), addedAt, lastOpenedAt, blob path. SchemaVersion field for forward compat.
    - Backup uploads missing whole-file book blobs to a content-addressed path on the WebDAV server: `VReader/books/<format>/<sha256>_<byteCount>.<ext>` (NOT `<fingerprintKey>` — colons in the key are awkward across servers/tools; canonical fingerprintKey lives only inside the manifest).
    - Atomic upload pattern: `PUT` to `VReader/uploads/tmp/<uuid>.part` → verify size via PROPFIND `getcontentlength` → `MOVE` into final blob path. Direct `PUT` to the final path can leave a corrupt blob on a partial upload; the temp+MOVE pattern guarantees blobs are either fully present or absent.
    - Existence check before upload: PROPFIND on the final blob path (NOT HEAD — some WebDAV servers don't implement HEAD reliably; PROPFIND is portable and `WebDAVClient` already supports it). If present and `getcontentlength` matches, skip upload (content-addressed dedupe — identical bytes converge across snapshots).
    - Restore order change: download every missing blob from the manifest into the app sandbox at the canonical `Documents/Books/<sanitized-original-filename>` path → run `BookImporter` to register `BookRecord` rows → run the existing metadata restore (positions/annotations/etc.). Metadata restore continues to silently skip un-resolvable fingerprints (defensive — the manifest *should* have provided everything, but a partial blob download must not crash restore).
    - Progress UI: per-blob download progress, total bytes, ETA. Pre-restore confirmation dialog when total missing bytes exceed a threshold (proposal: 100 MB) so the user can cancel before a slow transfer starts.
    - Backup progress UI gains a per-blob upload phase distinct from the metadata-ZIP phase.
    - Backward compat: a backup ZIP without `library-manifest.json` (i.e., a v1-format snapshot from PR #128) restores exactly as it does today (metadata-only, books skipped). The new format is purely additive.
  - **Excluded**:
    - No `BookFileState` enum, no remote-only library rows, no cloud-download icons, no on-tap downloads — those are feature #47. Phase 1's invariant: every `Book` row continues to mean "readable local file at fingerprint-derived sandbox path." This is what keeps the surface area small.
    - No selective restore picker — phase 1 always materializes everything in the chosen backup. Phase 2 adds the picker.
    - No GC of orphaned book blobs when a backup is deleted. Blobs accumulate; a future "Clean unused remote files" action (phase 2) walks every backup manifest and deletes unreferenced blobs.
    - No conditional `If-None-Match: *` create-if-absent — content-addressing makes "last writer wins" safe for identical bytes, and conditional WebDAV semantics aren't universally implemented. Use PROPFIND-then-PUT and tolerate the rare race.
    - No background download (`URLSessionConfiguration.isDiscretionary`) in phase 1 — restore is foreground-driven from the user's tap on "Restore". Phase 2's lazy load benefits from background, but a foreground restore with progress UI is acceptable for v1.
    - No Cellular vs Wi-Fi policy in phase 1 — restore is explicit user action; the user accepts the data charge. Phase 2's lazy-load needs the toggle.
    - No CloudKit / `CKAsset` / FileProvider / Background Assets path — those are different backends, not the same feature. Stay on WebDAV for the user's existing self-hosted setup.
  - **Out of scope, deferred**:
    - Chunked dedup (restic/borg-style) — overbuilt for personal libraries; whole-file SHA-256 is the right 80/20.
    - rsync-style block diffs — EPUB/AZW3/PDF are already compressed/containerized; small metadata changes reshuffle bytes and defeat block-diffs.
    - Cross-backup snapshot diffing (only ship blobs new since the last snapshot) — phase 1 already gets dedup-on-upload via PROPFIND existence checks; explicit diffs are an optimization for later.
    - Encryption at rest — WebDAV server is user-controlled; encryption is a separate cross-cutting feature.
- **Edge cases**:
  - **Empty library at backup time**: `library-manifest.json` is present with `books: []`; restore is a no-op (also empty). No crash.
  - **Backup with 0 books on device that have non-zero annotations** (orphan annotations from before): manifest lists 0 books, annotations restore continues to silently skip un-resolvable fingerprints. No regression vs current behavior.
  - **Partial upload failure mid-backup**: `.part` file remains in `uploads/tmp/`; never moved to final path → next backup's PROPFIND skips this blob (because final path doesn't exist) and re-uploads. No duplicate. No corruption. Tmp files older than 7 days swept by an idempotent housekeeping pass at the start of each backup.
  - **Partial download failure mid-restore**: BookRecord not yet registered for the failed blob; partial sandbox file deleted. Restore continues with the next blob and reports a per-book failure summary at the end (total succeeded / total failed, with retry option). User can re-run restore — already-downloaded blobs are skipped via local-file existence check.
  - **WebDAV server out of space mid-upload**: 507 → surface clear error, leave `.part` for retry, abort backup. Existing `WebDAVError.quotaExceeded` covers it.
  - **Two devices backing up to same WebDAV root with overlapping books**: content-addressed paths converge — both write the same `<sha256>_<byteCount>.<ext>` and PROPFIND skips the second upload. Safe by design.
  - **SHA-256 collision**: not a real risk for SHA-256, but the path also encodes `byteCount` so a hypothetical collision with different sizes would still be distinguishable. A collision with identical size is cryptographically negligible for the threat model (personal backup).
  - **Filename with characters illegal on the WebDAV server**: blob path uses only `[0-9a-f_.<format>]` so it's safe across servers. Original filename from manifest is sanitized for the sandbox at restore time (existing `BookImporter` rules apply).
  - **Same fingerprint, different filenames across devices** (same content imported with different original names): blob path is identical (content-addressed); manifest carries the original filename for the restoring device's sandbox. Each device gets the filename from its source backup.
  - **Books larger than available device storage at restore**: pre-restore confirmation dialog shows total bytes; if the user proceeds and storage runs out, restore aborts with a clear error and a "downloaded so far" summary.
  - **Manifest references blob path that doesn't exist on server** (server pruned manually): restore reports per-book failure, continues with remaining blobs. Manifest schema has version field so a future format change can carry an explicit `blobETag` for stronger integrity.
  - **User restores the same backup twice**: second restore's PROPFIND of local sandbox finds every blob already imported → all blob downloads skipped → metadata re-restore is idempotent (existing behavior). No duplicate library rows.
  - **Concurrent backup and restore on different devices**: blob writes are content-addressed and atomic via temp+MOVE; metadata ZIPs have unique timestamp+UUID names. No conflict.
  - **WebDAV server returns 301 redirect on ********`MOVE`**: existing `WebDAVClient` MKCOL trailing-slash bug (fixed in PR #128) taught us to normalize paths before MOVE. Apply the same normalization.
  - **Manifest > 1 MB** (large library, e.g., 10k books): keep using JSONEncoder — 10k entries with the documented schema is \~3 MB, still trivial. No streaming parser needed at this scale.
- **Test plan**:
  - **Unit — manifest**:
    - `LibraryManifestEncodingTests` — round-trip BookRecord array → JSON → array; schemaVersion present; all fields stable.
    - `LibraryManifestSchemaCompatTests` — v1 ZIP (no manifest) loads with empty book list and triggers metadata-only restore path.
  - **Unit — blob path**:
    - `BlobPathTests` — fingerprintKey → `<format>/<sha256>_<byteCount>.<ext>` mapping, parameterized over all 5 formats. Round-trip: parse blob path back to (sha256, byteCount, format).
  - **Unit — collector**:
    - `BackupDataCollectorBlobTests` — given a fixture library of 3 books, manifest contains 3 entries with correct paths; missing-blob list is computed correctly against a fake WebDAV state.
  - **Unit — restorer**:
    - `BackupDataRestorerMaterializeTests` — manifest with 3 entries → restorer requests 3 downloads via injected `WebDAVClient` mock → registers 3 BookRecords → runs metadata restore.
    - `BackupDataRestorerPartialFailureTests` — 1 of 3 downloads fails → 2 books imported, 1 reported in failure summary, restore overall succeeds with `restorePartiallyFailed` aggregate.
  - **Unit — WebDAVProvider**:
    - `WebDAVProviderBlobUploadTests` — temp+MOVE atomicity with mock client; abort mid-upload leaves `.part` and final path missing; retry succeeds.
    - `WebDAVProviderDedupeTests` — PROPFIND returns existing blob with matching size → skip PUT; PROPFIND 404 → PUT executes.
    - `WebDAVProviderTmpSweepTests` — `.part` files older than 7 days are deleted at backup start.
  - **Integration — Docker WebDAV** (mirroring PR #128's existing integration test):
    - `BackupBlobIntegrationTests` — backup library of 2 books → assert blobs at expected paths via raw curl probe; restore to empty library → assert 2 books present, positions/annotations re-attached.
    - `BackupBlobReuploadTest` — back up the same library twice; second backup uploads zero blobs (PROPFIND dedupe).
  - **Manual verification recipe** (recorded in `docs/manual-test-checklist.md`):
    - On simulator: import 3 books → backup → wipe library via `vreader-debug://reset` → restore → assert 3 books visible with original positions/highlights.
    - Destructive simulation: kill the app mid-upload → relaunch → re-run backup → verify no orphan `.part` files in `uploads/tmp/` after housekeeping sweep.
- **Acceptance criteria**:
  - A backup ZIP from feature #46 contains `library-manifest.json` plus the existing 8 metadata sections.
  - WebDAV server after a backup has `VReader/books/<format>/<sha256>_<byteCount>.<ext>` for every book in the library; second consecutive backup uploads zero new blobs.
  - Restoring a feature #46 backup to an **empty library** populates the library with every book referenced in the manifest, and positions/annotations re-attach correctly.
  - Restoring a feature #29 backup (v1 format, no manifest) continues to work exactly as today — metadata-only, books silently skipped.
  - Mid-upload kill leaves no corrupt blobs at the final path; the next backup attempt re-uploads cleanly.
  - All unit + integration tests pass under `xcodebuild test -only-testing:vreaderTests`.
  - LOC under \~300 per new file (`LibraryManifest.swift`, `BlobPath.swift`, additions to `BackupDataCollector.swift`, `BackupDataRestorer.swift`, `WebDAVProvider.swift`); no file exceeds the 300-line guideline.
  - Manual recipe in `docs/manual-test-checklist.md` passes on iPhone 17 Pro Simulator against `lllyys/vreader-webdav-host`.
  - `docs/architecture.md` updated: add `library-manifest.json` and the blob path layout to the Backup section; note the temp+MOVE upload pattern.

### Feature #47 — WebDAV restore — selective picker + lazy-on-tap — Plan

- **Problem**: Feature #46 ships materializing restore — the user gets their whole library back, but downloading every book up front can be slow or wasteful (large libraries, low-storage devices, secondary devices that only need a few books). Lazy-on-tap downloads and selective restore are the modern reading-app UX (Apple Books, Kindle, Readwise) and unblock multi-device usage. They also let users browse a backup's contents before committing storage.
- **Scope**:
  - **Included**:
    - `BookFileState` enum on `BookRecord`: `local`, `remoteOnly`, `downloading`, `failed`, `missingRemote`.
    - "Restore selectively…" mode in the WebDAV restore UI: lists every book from the chosen backup's `library-manifest.json` with title, author, size, format icon, last-opened date, checkbox. Default selection is none. "Restore all" remains a one-tap option that maps to phase-1 behavior.
    - Library rows for `remoteOnly` books: cloud-download icon, size label, opening attempts the download. Position/annotation sidecars restore alongside the row even before the file is local (so a tap-to-download book lands at the saved position).
    - Per-book download via `URLSessionConfiguration.isDiscretionary` background session — iOS schedules transfers when power/network are favorable.
    - "Wi-Fi only" toggle in WebDAV settings (default ON for cellular-aware behavior). Honored by both lazy downloads and "Restore all".
    - "Clean unused remote files" maintenance action in WebDAV settings: walks every backup manifest on the server, computes the union of referenced blob paths, deletes unreferenced blobs. Requires user confirmation with affected blob count + freed bytes estimate.
    - Reader entry-point checks `BookFileState`: opening a `remoteOnly` book routes to a download flow (progress sheet), not the reader, until the file is `local`.
    - Delete behavior: deleting a `remoteOnly` book removes the row only (no local file to delete); deleting a `local` book removes the file (existing behavior). Both leave the WebDAV blob untouched (GC is a separate explicit action).
  - **Excluded**:
    - No multi-device sync, no CRDT convergence — restore remains a one-shot user action, not continuous sync. Sync is a separate future feature.
    - No partial book downloads (range-requested first chapters) — granularity is whole-book.
    - No automatic retry of failed downloads — user retries via tap on the failed row. Surfacing transient vs permanent failure cleanly is enough; retry policy is out.
    - No CloudKit migration — phase 2 stays on WebDAV.
  - **Out of scope, deferred**:
    - Backup retention policy ("keep last N snapshots, delete older") — independent feature.
    - Per-book encryption — same cross-cutting concern as in feature #46.
    - Read-while-downloading streaming — opens the door to PDF/EPUB streaming complexity that doesn't pay off in the iOS reader context.
- **Edge cases**:
  - **Open ****************************************************************************************************************************************************************`remoteOnly`**************************************************************************************************************************************************************** book offline**: download flow shows a clear "no network" error; row stays `remoteOnly` (does not flip to `failed`).
  - **Cellular with Wi-Fi-only ON**: download is queued (not started); user sees a "Waiting for Wi-Fi" indicator. Background session resumes when Wi-Fi appears.
  - **Cellular with Wi-Fi-only OFF**: download proceeds; cellular byte counter visible.
  - **Selective restore picker on a backup with 10k books**: list is virtualized (`LazyVStack`) and supports filter/sort by title/size/date.
  - **Open a book whose blob no longer exists on server** (manually deleted): download flow returns 404; row flips to `missingRemote`; user can delete the orphan row.
  - **Concurrent downloads of multiple books**: serialized to 2-3 concurrent transfers (avoid saturating WebDAV); progress aggregated in a download tray.
  - **Background download completion when app suspended**: standard `URLSessionDownloadDelegate` background-session callback wakes the app; file moved into sandbox; row flips to `local`.
  - **"Clean unused remote files" run during a concurrent backup on another device**: GC reads manifests at start, deletes only blobs unreferenced at that moment; race window is acceptable because the next backup re-uploads any missing blobs (content-addressed convergence).
  - **Storage pressure during lazy download**: iOS may evict the downloaded file under memory pressure if marked `isExcludedFromBackup` improperly — set the right resource flags.
  - **Selective restore choice changes mid-download**: user can cancel an in-flight download from the row; row reverts to `remoteOnly`.
- **Test plan**:
  - **Unit**:
    - `BookFileStateTests` — every state transition is valid; opening reader for non-`local` routes to download flow.
    - `LazyDownloadCoordinatorTests` — concurrent download cap, retry on tap, Wi-Fi-only gating with mocked reachability.
    - `RemoteBlobGarbageCollectorTests` — given 3 manifests with overlapping book sets, computes correct unreferenced-blob set.
  - **ViewModel**:
    - `SelectiveRestoreViewModelTests` — manifest filtering, selection toggling, "select all" semantics, pre-restore size computation.
  - **Integration — Docker WebDAV**:
    - `LazyDownloadIntegrationTests` — restore selectively (1 of 3 books) → library has 1 `local` and 2 `remoteOnly` rows → tap on one `remoteOnly` → download completes → row flips to `local` and reader opens at saved position.
    - `RemoteGarbageCollectorIntegrationTests` — back up 2 libraries with one shared book → delete one backup's manifest → run GC → only the unique-to-that-backup blob is deleted; shared blob survives.
  - **Manual verification**:
    - On iPhone 17 Pro Simulator with `lllyys/vreader-webdav-host`: backup 5 books → wipe → selectively restore 2 → verify 5 rows visible (2 local, 3 remote) → tap a remote row → verify download + open at correct position. Repeat with Wi-Fi-only toggle and cellular reachability mocked via Network Link Conditioner.
- **Acceptance criteria**:
  - "Restore selectively…" picker correctly lists every book in the chosen backup's manifest and respects user selection.
  - `remoteOnly` rows appear in the library with cloud-download icons and remain visible after app relaunch.
  - Tap-to-download flips a row to `local` and opens at the saved position.
  - "Wi-Fi only" toggle correctly defers cellular downloads to Wi-Fi; visible "waiting" state.
  - "Clean unused remote files" deletes only blobs unreferenced by any backup manifest; concurrent backup on another device does not corrupt state.
  - All unit + integration + ViewModel tests pass.
  - LOC under \~300 per new file; reader entry-point change does not exceed 50 LOC delta in `ReaderContainerView`.
  - `docs/architecture.md` updated: add `BookFileState`, lazy-download coordinator, GC action.

