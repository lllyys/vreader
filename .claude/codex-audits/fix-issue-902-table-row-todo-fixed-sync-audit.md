# Codex audit log — `fix/issue-902-table-row-todo-fixed-sync`

**PR scope**: docs-only — flip `docs/bugs.md` Bug #224 table-row Status column from `TODO` → `FIXED` so the at-a-glance table matches the already-FIXED "Open Bug Details" section header (line 654) for the same bug.

**Date**: 2026-05-21
**Auditor mode**: Manual-fallback per rule 47's "Manual Audit Evidence" section (Codex MCP unavailable / over-engineering for a 1-character table-cell flip).
**Verdict**: ship-as-is.

## Context — why this PR exists

Bug #224 / GH #902 ("Feature #63 `SearchBar` re-skin — `searchTextField` /
`searchCancelButton` touch targets below 44 pt HIG minimum") was fixed
2026-05-19:

- **Code fix shipped** as PR #920 (squash-merged to `main` as commit
  `49588a6d04809b5649532e3546ff9f7d95b7417f`).
- **Version bump** shipped as PR #922 (`v3.34.5`).
- **Post-merge verification evidence** committed as PR #925
  (`dev-docs/verification/bug-902-20260519.md`).
- **GH #902 CLOSED** 2026-05-19 under the **verification-exception** path
  (label: `verification-exception`), with closure comment citing the
  high-fidelity audit + evidence file.

The "Open Bug Details" section header at `docs/bugs.md:654` was correctly
flipped to `(FIXED 2026-05-19)` at that time, and the section body fully
documents the fix shape, audit, and verification.

However, the **at-a-glance table row** at `docs/bugs.md:1359` was left at
`TODO`. This is the standing tracker-row gotcha (precedent: PR #1097 had to
clean up after PR #1096; PR #1099 did the same TODO→FIXED table-row sync
for Bug #181 / GH #616 at v3.38.34). This PR is the same class of cleanup
for Bug #224.

## Files read

- `docs/bugs.md` (rows 654-661 detail section; row 1359 table row)
- `vreader/Views/Search/SearchBar.swift` (production fix already on
  `main` HEAD `d1290386` — lines 76-77 `.frame(minHeight: 44)
  .contentShape(Rectangle())` on the `TextField`, lines 114-115 same on
  the `Cancel` `Button`)
- `dev-docs/verification/bug-902-20260519.md` (post-merge verification
  evidence, result=pass)
- `.claude/codex-audits/fix-issue-902-searchbar-touch-targets-audit.md`
  (the original code-PR audit — 3 rounds, ship-as-is)
- `project.yml` (version `3.38.36` → `3.38.37`, build `611` → `612`)
- `vreader.xcodeproj/project.pbxproj` (regenerated by xcodegen)

## Symbols / signatures verified

- `SearchBar.swift` carries the production fix:
  `grep -c "minHeight: 44" vreader/Views/Search/SearchBar.swift` → 2.
- Audit-exclusion drops confirmed:
  `grep -c "excluding: .hitRegion" vreaderUITests/...` → 0 / 0 in both
  search-sheet audit files.
- GH #902 state confirmed via `gh issue view 902 --json state` → `CLOSED`.
- Verification evidence file exists at the documented path and matches
  the row's narrative.
- PreToolUse hook `.claude/hooks/check_terminal_status_evidence.sh`:
  passes (the `dev-docs/verification/bug-902-*.md` evidence file is
  present).

## Edge cases checked

- **Hook compliance**: the FIXED-status flip on a `docs/bugs.md` row is
  guarded by `check_terminal_status_evidence.sh`. The matching evidence
  file `dev-docs/verification/bug-902-20260519.md` is already committed
  (PR #925, 2026-05-19), so the hook's bug-`N`→`bug-N` filename check
  passes.
- **Idempotency**: the section header (line 654) already says `(FIXED
  2026-05-19)`; flipping the table row is the only outstanding change.
  No double-flip risk — the row's narrative grows by appending the FIXED
  block to the existing TODO-era prose (mirroring the precedent's
  format).
- **Merge-gate "fix-or-implement" compatibility**: `Refs #902` against
  an already-CLOSED GH issue does not gate the PR (the bug row reaches
  `FIXED` in this PR, satisfying the merge gate's "referenced row has
  reached its terminal status" condition).
- **PR-body closing-keyword pitfall** (per user memory): PR body uses
  `Refs #902` only — never `fix/close/resolve #902` (GH would re-open
  to close, generating spurious activity on an already-verified issue).
- **No Swift code touched**: the production code at `SearchBar.swift`
  is unchanged; the fix was already shipped 2026-05-19. This PR is
  pure docs-sync + the mandatory per-PR version bump.

## Risks accepted

- None. The change is a 1-character status flip + a mirrored fix
  narrative copied from the canonical "Open Bug Details" section into
  the table row's Notes cell. No code paths affected.

## Tests added or intentionally deferred

- **No tests added**: rule 10-tdd lists "Documentation, config" as a
  TDD exception. The `docs/bugs.md` table-row update is documentation.
- **No regression-test sanity run**: rule-47 Gate-5 / device verification
  is satisfied by the existing post-merge evidence file
  (`dev-docs/verification/bug-902-20260519.md`). The SearchBar fix
  itself was already device-verified on `main` at v3.34.5; this PR
  does not modify the SearchBar fix.

## Verdict

ship-as-is. Manual-fallback audit per rule 47. The change is a
zero-risk docs-sync that mirrors the long-standing PR-#1099 / PR-#1097
precedent.
