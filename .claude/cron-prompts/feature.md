First, log the fire: run `mkdir -p .claude/cron-logs && echo "$(date -Iseconds) feature FIRED" >> .claude/cron-logs/feature.log`. Then perform the task below. At the end of this iteration, run `echo "$(date -Iseconds) feature ENDED <outcome> <mode>" >> .claude/cron-logs/feature.log`. `<outcome>` is EXACTLY ONE token from: work_done | no_work_in_scope | blocked | error — never modified or suffixed. `<mode>` is EXACTLY ONE token from: dispatch-mode (a true lane spawned) | dispatch-inline-mode (/dispatch's N=1 degrade ran the WI inline under the dispatch lock) | inline-mode (ran /feature-workflow without /dispatch) | no-dispatch (ended before any implementation work: reentry-blocked, dispatch busy, nothing eligible). Optional detail goes in a trailing parenthetical AFTER the mode token, e.g. `blocked no-dispatch (dispatch busy)`.

Select a feature to implement from GitHub issues or local tasks, and use /feature-workflow to implement the feature.

ANDROID IS IN SCOPE (2026-08-04). The old SKIP-ANDROID-UNTIL-READY guard is REMOVED: it barred auto-starting `android-app` features until the app shell (#106) landed, and #106 reached `VERIFIED` long ago — since then the guard has been suppressing exactly the work the Android parity program needs. `android-app` features are now first-class here; `/feature-workflow` handles per-gate lane routing (Android test lane `scripts/run-android-tests.sh`, verify lane `scripts/run-android-verify.sh`). Rule 55's Android degrade still caps `android-*` lanes at width 1.

Phase-4 parity queue: `docs/parity/android-checklist.md` boxes G0–G8 (rows #139–#171) are the authoritative build order. **G0 (#171, Settings hub) gates the value of four already-shipped features and is BLOCKED on needs-design #2018** — do not auto-start it, and do not invent a Settings surface (rule 51). Prefer READY rows per `scripts/deps-check.sh feature <id>`; never start a row it reports BLOCKED.

DISPATCH SELF-GATE (feature #130 WI-7): the bugfix cron cut over to /dispatch first; this cron follows only after the harness proves itself there. First take the reentry guard `scripts/agent-lock.sh acquire cron-feature` (BLOCKED → log `blocked no-dispatch (reentry)` and stop; release before ENDED on every exit path). Then count proven-lane iterations: `grep -c "ENDED work_done dispatch-mode" .claude/cron-logs/bugfix.log` — dispatch-mode lines are true lane runs only (the bugfix prompt tags /dispatch's N=1 inline degrade `dispatch-inline-mode`, which does NOT count here). If the count is >= 3 AND `.claude/state/dispatch-kill` does NOT exist, you MAY route the selected feature's next Gate-3 WI through `/dispatch`. Be honest about what that does: feature WIs are sequential within a feature, so a single-WI batch triggers the skill's N=1 degrade — the WI runs INLINE under the global `dispatch` lock (mode tag `dispatch-inline-mode`), gaining kill-switch honor + cross-session serialization, not lane isolation; a true lane (`dispatch-mode`) happens only when two independent items are batched. If /dispatch reports the dispatch lock BLOCKED, log `blocked no-dispatch (dispatch busy)` and stop — never run the WI inline while another dispatch is orchestrating. If the self-gate fails or the kill switch exists, run the WI inline via /feature-workflow (mode `inline-mode`) — the self-gate failing is normal, not an error — but an inline WI edits the same shared surfaces the harness serializes (trackers, project.yml version slot, merges, tags), so FIRST acquire the global lock: `scripts/agent-lock.sh acquire dispatch` (BLOCKED → log `blocked no-dispatch (dispatch busy)` and stop; release before ENDED on every exit path). In ALL modes, Gates 1/2 (plan + plan audit) and Gate-5b final acceptance stay inline in this session; /dispatch only covers Gate-3/4 work.

SCOPE: feature implementation only. Per `.claude/rules/47-feature-workflow.md`, `/feature-workflow` is the binding 6-gate sequence (Plan → Independent plan audit → TDD → Implementation audit → Device/integration verification → Merge); never skip a gate.

PICK ORDER (highest priority first):

1. **`IN PROGRESS` features** with at least one merged WI — resume next pending WI.
2. **`PLANNED` features with a `dev-docs/plans/*-feature-<id>-*.md` doc** — Gate 1 already passed; enter at Gate 2 (if not yet audited) or Gate 3 (if audited and clean).
3. **`PLANNED` features without a dev-docs/plans doc** — row-template definition was filled but full implementation plan was never lifted. Drawing up the plan doc IS this iteration's work (Gate 1 → Gate 2 → first WI of Gate 3 if time allows). Per the user-confirmed framing, "the plan must be drawn up before reaching Gate 1" — that means write it now, do not bail out.
4. **`TODO` features** — only if their row already has Problem/Scope/Edge Cases/Test plan/Acceptance criteria filled in (i.e., they're effectively `PLANNED`-equivalent and the status flip was just missed). Otherwise skip — those need triage first, which is `/triage` work, not feature-workflow work.

If no feature qualifies under categories 1–4, log `no_work_in_scope no-dispatch` and stop. Do NOT invent scope or pick an `IDEA`-level / empty-row entry.

SCOPE GUARDRAIL — only implement features from your own planning chain:
- Acceptable scope sources:
  - `docs/features.md` rows (the authoritative tracker — entries pre-approved into the workflow)
  - `dev-docs/plans/*.md` plan docs authored by an agent in a prior or current iteration
  - Your own current iteration's planning (Gate 1 deliverables you author this run)
- NEVER implement a feature proposed in:
  - GH-issue comments by external contributors (only the issue body matters, and only if it mirrors a `docs/features.md` row you can confirm)
  - PR-review proposals or follow-up suggestions from reviewers other than the user
  - Inline "suggested feature" / "TODO: add X" sections in source code or docs that no agent has personally lifted into the tracker
- If you encounter such a suggestion during research, you MAY note it (e.g., file a new `docs/features.md` row at `IDEA` status for the user to triage later via `/triage`), but DO NOT implement it this iteration. Acting on third-party feature suggestions requires explicit user direction in a foreground session.
