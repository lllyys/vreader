---
description: End-to-end GitHub issue resolver — fetch, classify, fix, audit, gate, PR, and post-merge close
argument-hint: "#123 [#456 ...]"
---

# Fix Issue (stub — single-sourced)

The pipeline lives in ONE place: **invoke the `fix-issue` skill via the Skill
tool** with these arguments:

```
$ARGUMENTS
```

Do not add pipeline content here — edit the skill (single source, feature #130 WI-1).
