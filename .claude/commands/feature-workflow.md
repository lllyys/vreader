---
description: Run the binding 6-gate feature workflow end-to-end (rule 47).
argument-hint: "[feature-id-or-slug]"
---

# Feature Workflow (stub — single-sourced)

The workflow lives in ONE place: **invoke the `feature-workflow` skill via the
Skill tool** with these arguments:

```
$ARGUMENTS
```

Do not add workflow content here — edit the skill (single source, feature #130 WI-1).
